# ADR-092: 초기 번들 분할 — DEV 게이팅 + 라우트 지연 로딩 (폐기 · DEPRECATED)

> ⛔ **DEPRECATED — 폐기.** Vite 가 없다 — Metro 는 단일 번들이라 라우트 분할이라는 축 자체가 사라졌다.
> **새 구현의 근거로 인용하지 말 것.** 본문은 아래 요약으로 줄였다 — 죽은 정책이 컨텍스트에
> 실리지 않게 하기 위해서다.
>
> 🔗 **지금도 코드가 따르는 것** — `app/prehydrate.ts`·`features/prehydrate.ts`·`lib/icons.ts` 가 «웹이 왜 그 형태였나» 의 출처로 인용한다 — 규칙이 아니라 기록이다.

## 무엇을 결정했었나 (요약)

첫 페인트 번들을 줄이려 **라우트 화면을 `React.lazy` 로 분할**하고 디버그 라우트를 DEV 게이팅했다. 첫 로드 JS 를 gzip 411 → 116 kB 로 줄였다. Metro 는 단일 번들이라 이 축 자체가 사라졌다.

원문(맥락·계측·트레이드오프 전문): `git show store-1.0.6-capacitor-sunset:docs/adr/ADR-092.md`
