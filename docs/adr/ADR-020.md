### ADR-020: 일일퀘스트 카드 도입 — 보스 카드 패턴 재사용, `quest_state` 정규화, 지역 배경 매칭 (구현 완료)

> 🟢 **ACTIVE — 유효.** 현행 RN 앱에 적용되는 결정이다.

**배경**: 컨텐츠 스케줄러 일간 탭은 현재 `daily_contents`의 모든 항목을 동일한 리스트 카드(`{이름} · {now_count}/{max_count}` + 진행률 바)로 표시한다. 이 중 API가 `type: "quest"`로 내려주는 항목(아케인리버·그란디스 지역별로 반복되는 필드 퀘스트)은 `now_count`/`max_count`가 항상 0/0으로 와서 진행률 표기가 의미가 없고, 대신 `quest_state` 필드(0=시작 안 함, 1=진행중, 2=완료)로 실제 상태를 구분해야 한다(사용자 확인, 2026-07-14). `quest_state`는 지금까지 wire 타입(`NexonDailyContentWire`)에만 정의돼 있었고 `normalizeDailyContent`가 이 필드를 파싱하지 않고 버리고 있었다. 사용자는 보스 스케줄러의 보스 카드([[ADR-018]]) 패턴 — 이름 옆 상태 뱃지 + 카드 배경에 bleed로 깔리는 일러스트 — 을 일일퀘스트 항목에도 그대로 적용해달라고 요청했다(2026-07-14).

**실측 확인(2026-07-14, 사용자 제공 실제 API 응답)**: 초안 작성 시 추정했던 접두어·매칭 규칙이 실제 응답과 달라 아래 결정에 반영해 수정했다.
- 접두어는 `"[일일퀘스트] "`가 아니라 `"[일일 퀘스트] "`다(일일과 퀘스트 사이에 공백이 있음).
- 접두어를 제거한 퀘스트명은 지역명과 정확히 같지 않다 — 지역명 뒤에 조사·서술어가 붙은 전체 문장이다(예: `"[일일 퀘스트] 레헬른의 평온한 밤"` → 표시명 "레헬른의 평온한 밤", 지역명은 "레헬른"). 17개 전체 목록과 정확한 매핑은 결정 7 표 참고.

**결정**:
1. **표시 이름 접두어 제거**: `kind: 'quest'` 항목의 `content_name`은 `"[일일 퀘스트] "` 접두어를 포함해 온다. 표시할 땐 이 접두어만 제거하고 나머지 퀘스트명 전체(예: "레헬른의 평온한 밤")를 그대로 보여준다 — 지역 배경 매칭은 이 표시명 전체가 아니라 별도 규칙(결정 6)을 따른다.
2. **`quest_state` 정규화 도입**: `NexonDailyContentWire.type`을 `NexonWeeklyContentWire`와 동일하게 `'contents' | 'quest'`로 좁히고, `quest_state`(`string | null`)를 숫자(`0 | 1 | 2 | null`)로 파싱한다. 도메인 타입 `DailyContent`(`src/types/scheduler.ts`)에 `WeeklyContent`와 대칭으로 `kind: 'contents' | 'quest'`, `questState: 0 | 1 | 2 | null` 필드를 추가한다 — 지금까지 조용히 버려지던 필드를 실제로 쓰기 시작하는 변경.
3. **적용 범위는 `kind === 'quest'` 항목만**: `kind === 'contents'`인 기존 항목(몬스터파크 등)은 지금의 "이름 · now/max + 진행률 바" 표시를 그대로 유지한다. 이번 변경은 일간 탭 전체를 다시 디자인하는 게 아니라 퀘스트 항목에만 신규 카드 스타일을 추가하는 것이다.
4. **일일퀘스트 카드(`DailyQuestCard`) 신설**: `BossCard`와 동일한 카드 골격(80px 높이, `rounded-[14px]`, 레테 고정 배색, 왼쪽 38%까지 선명하고 76%부터 완전 투명해지는 일러스트 bleed·마스크)을 재사용하되 데이터 소스만 다르다 — 왼쪽엔 난이도 뱃지 없이 접두어를 제거한 퀘스트명만, 오른쪽엔 `quest_state` 3단 뱃지(결정 5). `ContentScreen.tsx`에서 `BossScreen.tsx`가 `BossCard`/`DifficultyBadge`를 export하는 것과 동일한 방식으로 `DailyQuestCard`를 export해 아래 디버그 프리뷰가 재사용한다.
5. **`quest_state` 뱃지 3종 (확정, 2026-07-14)**: 보스 카드는 미완료를 "빈 공간"으로 표현했지만, 이번엔 사용자가 "3가지로 구분해서 뱃지를 만들어달라"고 명시했으므로 3가지 모두 뱃지를 그린다.
   - 완료(2): 보스 카드 완료 뱃지와 동일 스타일 재사용 — `rounded-full bg-secondary text-bg text-xs font-bold px-2.5 py-1`, 문구 "완료"
   - 진행 중(1): 파티 배지와 같은 톤의 반투명 pill — `rounded-full bg-white/20 text-[#E8DFEC] text-xs font-semibold px-2.5 py-1`, 문구 "진행 중"
   - 시작 안함(0): 더 옅은 pill — `rounded-full bg-white/10 text-[#E8DFEC]/70 text-xs font-semibold px-2.5 py-1`, 문구 "시작 안함"
6. **지역 배경 매칭 규칙 — 신규 참조 테이블 `src/data/daily-quest-regions.json`**: `weekly-bosses.json`의 `portraitSlug`와 같은 역할로 "지역명 → 배경 이미지 슬러그"만 담는다. `boss-portrait-crops.json`과 마찬가지로 게임 밸런스 수치가 아니라 UI 표시 파라미터라 [[ADR-006]]의 사용자 확인 대상은 아니다. **매칭 규칙**: 접두어를 제거한 퀘스트 표시명이 지역명을 그대로 포함하지 않는 경우가 있어(조사가 붙어 "레헬른의..." 형태), [[ADR-007]]의 보스명 매칭처럼 양쪽 문자열의 공백을 제거한 뒤 **퀘스트 표시명이 지역명으로 시작하는지(`startsWith`)**로 판정한다. 17개 지역 전체가 이 규칙으로 사용자가 제공한 실제 API 응답과 정확히 매칭됨을 확인했다(2026-07-14).
7. **에셋 파일명 영문화 (슬러그 확정, 2026-07-14 사용자 승인)**: 사용자가 `src/assets/maps/`에 넣어둔 한글 파일명 17개를 아래 슬러그로 리네임하고, `src/lib/daily-quest-backgrounds.ts`(`boss-icons.ts`와 동일한 `import.meta.glob('../assets/maps/*.webp', { eager: true, import: 'default' })` 패턴)에서 `{slug}.webp`로 조회한다.

   | 지역명(매칭 키, 실측 확인) | 실제 퀘스트명 예시(API) | 원본 파일명 | 슬러그 |
   |---|---|---|---|
   | 소멸의 여로 | [일일 퀘스트] 소멸의 여로 조사 | 이름없는마을.webp | vanishingJourney |
   | 츄츄 아일랜드 | [일일 퀘스트] 츄츄 아일랜드 최고의 요리 | 츄츄빌리지.webp | chuchuIsland |
   | 레헬른 | [일일 퀘스트] 레헬른의 평온한 밤 | 레헬른중심가.webp | lachelein |
   | 아르카나 | [일일 퀘스트] 아르카나의 평온한 바람 | 정령의나무가보이는곳.webp | arcana |
   | 모라스 | [일일 퀘스트] 모라스의 안정을 위해 | 트뤼에페광장.webp | morass |
   | 에스페라 | [일일 퀘스트] 에스페라 연구 명령 | 베이스캠프.webp | esfera |
   | 문브릿지 | [일일 퀘스트] 문브릿지 조사 | 화이트스피어1.webp | moonbridge |
   | 고통의 미궁 | [일일 퀘스트] 고통의 미궁 조사 | 욕망의제단입구.webp | labyrinthOfSuffering |
   | 리멘 | [일일 퀘스트] 리멘 조사 | 끝에가까운곳앞.webp | limen |
   | 세르니움 | [일일 퀘스트] 세르니움 조사 | 세르니움광장.webp | cernium |
   | 호텔 아르크스 | [일일 퀘스트] 호텔 아르크스 주변 청소 | 몰락한황야의도시.webp | hotelArcus |
   | 오디움 | [일일 퀘스트] 오디움 일대 탐사 | 연구자들의도시.webp | odium |
   | 도원경 | [일일 퀘스트] 도원경 오염 정화 | 죄인들의낙원.webp | dowonkyung |
   | 아르테리아 | [일일 퀘스트] 아르테리아 잔당 처치 | 작전본부.webp | arteria |
   | 카르시온 | [일일 퀘스트] 카르시온 복구 지원 | 이슈피라의눈.webp | carcion |
   | 탈라하트 | [일일 퀘스트] 탈라하트 고대신의 힘 조사 | 신들의무덤.webp | tallahart |
   | 기어드락 | [일일 퀘스트] 기어드락 크로노스의 잔재 수집 | 크로노스가잠든곳.webp | geardrock |

8. **크롭 값 — 신규 `src/data/daily-quest-region-crops.json`**: `boss-portrait-crops.json`과 동일한 `{ size, position }` 구조를 슬러그별로 담는다. 초기값은 전부 `{ size: "cover", position: "center" }`이고, 결정 9의 디버그 프리뷰에서 사용자가 직접 조정한다.
9. **디버그 프리뷰 — `/debug/quest-cards`**: `BossCardPreview.tsx`와 동일한 목적(눈으로 위치를 맞추고 결과 JSON을 복사해 반영)으로 `src/app/content-scheduler/DailyQuestCardPreview.tsx`를 신설하고, `App.tsx`에 온보딩 가드 없이 라우트를 추가한다. 방향키/줌 조정 로직은 `BossCardPreview`의 `BossCropAdjuster`와 동일하지만 공용 컴포넌트로 뽑지 않고 이 파일 안에 그대로 복제한다 — 기존 디버그 프리뷰도 self-contained 방식이고(Storybook 등은 이미 기각, [[project memory: boss-card-crop-debug-tool]]), 임시 디버그 파일 하나를 위해 공용 추상화를 새로 만들 필요는 없다는 판단. 조정이 끝나면 이 파일과 라우트도 보스 카드 프리뷰와 마찬가지로 삭제 가능한 임시 파일로 취급한다.

10. **지역 아이콘 추가 (2026-07-14)**: 결정 4의 카드 좌측 이름 앞에 지역별 작은 아이콘(38×38 원본, 사용자 제공)을 추가한다. 아이콘 파일은 `src/assets/maps/icons/`에 배경과 동일한 슬러그로 저장하고(확장자는 파일마다 png/webp가 섞여 있을 수 있어 `src/lib/daily-quest-icons.ts`가 확장자 무관하게 slug만으로 조회), `getDailyQuestRegionIconUrl(slug)`로 URL을 얻는다. 매칭되는 아이콘이 없으면 렌더링 자체를 생략한다.
11. **몬스터파크 카드 확장 (2026-07-14)**: 일간 탭의 `kind: 'contents'` 항목 중 "몬스터파크"만 결정 4/10과 동일한 배경 bleed + 아이콘 구조를 적용하되, 퀘스트 카드와 달리 진행률(`now_count`/`max_count`)을 유지해야 하므로 다르게 구성한다.
    - 오른쪽 상태 뱃지는 `quest_state` 3단 뱃지 대신 `{nowCount}/{maxCount}` 텍스트를 담은 뱃지로 대체한다. ~~진행 중 뱃지와 동일한 반투명 pill(bg-white/20, 카드 로컬 고정색)~~ → **정정(2026-07-14)**: 진행률 뱃지는 카드 로컬 고정색이 아니라 앱 전역 "카운트 배지"(예: 보스 스케줄러 주간 탭 n/12, `docs/UI_GUIDE.md` 탭 토글 섹션 참고)와 동일한 테마 토큰(`bg-primary/15 text-primary`)으로 변경(사용자 지시, "0/14 뱃지는 테마 컬러에 맞춰").
    - 카드 하단에 기존 진행률 바를 그대로 유지하되, 트랙/채움 색은 레테 고정 배경 위에서 읽히도록 테마 토큰(`bg-surface-2`/`bg-primary`) 대신 흰색 반투명 계열 리터럴로 고정한다(카드 배경/보더/텍스트를 레테로 고정한 것과 같은 이유, [[ADR-018]]).
    - **정정(2026-07-14)**: 카드 높이는 ~~80px(일일퀘스트 카드와 동일)~~ → 진행률 바까지 담으니 답답해 보인다는 사용자 피드백으로 **96px**(`h-24`)로 확대. 일일퀘스트 카드(80px)와 다른 유일한 치수 차이.
    - 에셋 리네임: `src/assets/maps/몬스터파크.webp` → `monsterPark.webp`, `src/assets/maps/icons/몬스터파크_icon.png` → `monsterPark.png`(아이콘 폴더 안에서는 "_icon" 접미사를 생략해 다른 16개 아이콘 파일과 명명 일관성을 맞춤).
    - 슬러그 조회는 결정 4/10과 동일한 `getDailyQuestBackgroundUrl`/`getDailyQuestRegionIconUrl`/`getDailyQuestRegionCrop`를 그대로 재사용한다(신규 lib 불필요) — `daily-quest-region-crops.json`에 `monsterPark` 키를 추가해 크롭값도 같은 파일에서 관리.
    - "몬스터파크" 외 다른 `kind: 'contents'` 항목(현재 실제 API 응답에서 관측된 적 없음)은 기존 plain 스타일(이름 · now/max + 진행률 바)을 그대로 유지한다. 이름 직접 비교(`content.name === '몬스터파크'`)로 분기하며, 별도 매핑 테이블은 아직 만들지 않는다 — 현재 이 처리가 필요한 콘텐츠가 하나뿐이라 과설계를 피한다.

**트레이드오프**: `daily-quest-*` 계열 lib/데이터 이름은 원래 "지역 퀘스트"만 염두에 두고 지어졌는데, 몬스터파크(퀘스트가 아닌 일반 콘텐츠)도 같은 슬러그 조회 함수를 재사용하면서 이름이 실제 쓰임보다 좁게 느껴질 수 있다. 함수 자체는 이미 "slug → URL/crop" 범용 조회라 동작에는 문제가 없고, 이번 변경 범위에 비해 리네임까지 하는 건 과하다고 판단해 이름은 그대로 둔다.

**이유**: 보스 카드에서 이미 검증된 "카드에 일러스트를 bleed로 깔고 오른쪽에 상태 뱃지" 패턴을 그대로 재사용하면 새 시각 언어를 발명하지 않고도 일관된 스케줄러 UI를 유지할 수 있다. `quest_state`를 도메인 타입에 편입시키는 것도 이미 존재하지만 버려지던 데이터를 활용하는 것뿐이라 API 변경이 필요 없다.

**트레이드오프**: `kind === 'contents'`와 `kind === 'quest'`가 같은 일간 탭 리스트 안에서 서로 다른 두 카드 스타일로 섞여 보인다(일부는 진행률 바, 일부는 배경 일러스트+뱃지). 사용자가 "먼저"라고 표현했으므로 의도된 단계적 적용으로 보고, 나머지 `contents` 항목의 카드화는 이번 범위 밖으로 둔다.

