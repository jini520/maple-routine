# ADR-223: 지출은 계정 단위 사용 내역에서 온다. 일 단위로 받고 어제 이전은 굳힌다

> 🟢 유효 · 2026-09-06 · 수집·계산 구현 완료 (화면 표시는 남음)

> 사용자 지정(2026-09-06): "/maplestory/v1/history/starforce /maplestory/v1/history/potential
> /maplestory/v1/history/cube 의 결과를 사용 메소로 치환해서 지출로 잡으려고 하는 작업"

**관련**: [[ADR-222]](창 · 층이 데이터를 소유한다) · [[ADR-086]] 결정 4(조회 원장) ·
[[ADR-219]](기록은 추적 목록이 아니라 자기 자신으로 산다) · [[ADR-006]](게임 수치 임의 추정 금지)

## 맥락

가계부의 지출은 지금 **손입력 둘**뿐이다. 큐브·스타포스·잠재 재설정에 쓴 메소가 가장 큰 지출인데
앱이 그것을 모른다.

넥슨이 그 사용 내역을 준다. 그런데 이 셋은 지금까지 앱이 쓰던 API 와 **모양이 다르다.**

| | 지금까지(스케줄러·캐릭터) | 히스토리 셋 |
|---|---|---|
| 단위 | 캐릭터(ocid) | **계정** |
| 조회 | 날짜 하나 | 날짜 하나 + 커서 |
| 한 번에 | 그 캐릭터 전부 | **1000건**. 넘으면 `next_cursor` |
| 과거 | 창 지나면 못 봄 | **영구 확정** |

## 결정

### 결정 0. 응답 모양 (실측 2026-09-06)

```
GET /maplestory/v1/history/{cube|starforce|potential}?count=N&date=YYYY-MM-DD
GET /maplestory/v1/history/{...}?count=N&cursor=…        ← date 없이 이어받는다

{ count, next_cursor, {종류}_history: [ … ] }
```

| 잰 것 | 결과 |
|---|---|
| 정렬 | **최신 → 과거** 내림차순 |
| 커서 방향 | 과거로 이어진다 |
| 커서에 `date` | 필요 없다. 커서가 날짜 맥락을 든다 |
| 쪽 나눔이 빠뜨리나 | 아니다. 두 쪽(17건)이 전체(187건)의 앞부분과 정확히 일치 |
| `count` | **상한**이다. 10 을 달라고 해도 8·9 가 왔다 |
| 결정성 | 같은 요청 두 번에 커서·id 목록·응답 전체가 **같다** |
| 실사용자 하루 | 큐브 187건. `count=1000` 한 번에 다 왔고 커서 없음 |

줄마다 `id` 가 있고 계정 전체에서 유일하다. 비용 계산에 필요한 것이 응답에 다 있다 — 큐브는
`cube_type` + `item_level`, 스타포스는 `before_starforce_count` · `superior_item_flag` ·
`protect_shield` · 이벤트 플래그.

### 결정 1. 원장은 **`(종류, 날짜)`** 하나다

계정 단위라 ocid 축이 없다. 그래서 하루가 **3콜**(스타포스·잠재·큐브)이고 캐릭터 수를 안 곱한다.

[[ADR-086]] 결정 4 의 조회 원장과 같은 모양이고 같은 규칙을 쓴다.

| 원장이 든 것 | 다시 부르나 |
|---|---|
| 없음 | 부른다 |
| 받음(확정) | **안 부른다. 영구** |
| 받음(잠정 = 오늘) | 날이 바뀌면 부른다 |
| `OPENAPI00009`(집계 전) | **안 적는다.** 다음 방문에 다시 온다 |

**어제 이전은 영원히 안 바뀐다.** 9월 5일에 쓴 큐브는 9월 6일부터 확정이다. 그래서 지난 달을
한 번 열면 그 달은 끝이고, 두 번째 방문부터 조회가 0이다.

**확정 시각을 상수로 안 둔다.** 몇 시에 집계가 끝나는지 몰라도 `OPENAPI00009` 가 오면 안 굳히면
된다. 응답이 말하게 한다([[ADR-222]] 결정 3 과 같은 태도).

### 결정 2. 범위는 **화면이 그리는 날짜**다

가계부는 칸 하나가 하루다. **API 단위와 화면 단위가 정확히 같다.** 그래서 달로 끊지 않는다.

```
주간 보기 진입   그 달 격자 35~42일   105~126콜 (첫 방문) · 이후 3콜
월간으로 전환    이미 받았다          0콜
지난 달로 이동   그 달 격자           105콜 (첫 방문) · 이후 3콜
```

**주간 보기에서도 그 달 격자를 통째로 받는다**(사용자 선택, 정정 5). 화면 아래 열지도가 한 달을
그리기 때문이다. 보이는 이레만 받으면 나머지 날의 열지도가 지출을 빼고 그려져 **실제보다 밝게**
보인다. 격자를 다 받으면 월간으로 옮길 때 조회가 0이다.

굳으면 두 번째부터 3콜(오늘치)이라 이 값을 치르는 것은 그 달을 처음 열 때뿐이다.

### 결정 3. 추적 안 하는 캐릭터의 것도 **담고 보여준다** (사용자 지정)

계정 단위 API 라 추적 목록 밖 캐릭터의 사용 내역도 함께 온다. **거르지 않는다.**

[[ADR-219]] 가 세운 것과 같은 이유다. 추적은 **앞으로 무엇을 받을지**의 축이지 **무엇이 사실인지**의
축이 아니다. 지출은 그보다 더 분명하다. 거를지 말지를 우리가 고를 수 있을 뿐 **돈은 이미 나갔고**,
거르면 합계가 거짓이 된다.

**필터를 두지 않는다.** 숨기면 합계가 거짓이 된다. 추적 캐릭터를 위로 올리거나 표식을 다는 정도로
충분하다.

**초상은 아는 것만 그린다.** 히스토리 응답은 캐릭터 이름을 준다. 초상을 그리려면 ocid 가 필요하고
그것은 캐릭터마다 한 콜이다(계정에 40명이면 40콜). 추적 캐릭터는 `character_profiles` 에 이미
있으니 초상이 붙고, 나머지는 이름만 뜬다. 같은 목록에서 어떤 줄은 초상이 있고 어떤 줄은 없는
모양이 되는데 그것이 정직하다.

### 결정 3.5. 다시 부르는 규칙 (사용자 판단)

**어제 이전은 아예 안 부른다.** 그 날짜의 데이터는 변할 수 없다. 원장의 `settled` 가 1이면
조회 대상에서 빠진다. 그래서 지난 달 두 번째 방문이 **0콜**이다.

**오늘은 매번 1쪽을 받는다.** 저장해 둔 `next_cursor` 와 같으면 새 데이터가 없다는 뜻이라
나머지를 안 부른다. 다르면 **이미 아는 id 를 만날 때까지만** 이어 받는다.

동률 타임스탬프가 실제로 있다(잠재 재설정 3건이 같은 밀리초). 그 순서가 호출 사이에 뒤집히면
id 하나를 만나 멈출 때 나머지를 놓칠 수 있는데, **넥슨이 순서를 보장한다고 본다**(사용자 판단).
이것은 잰 것이 아니라 **가정**이다. 지출이 비는 보고가 오면 여기를 먼저 볼 것.

### 결정 3.7. 장비 레벨 표를 **데이터로 만든다** (사용자 지정)

스타포스 응답에는 `item_level` 이 **없다**(1년치 3,220건 실측). 그런데 비용은 레벨에 크게
좌우된다.

**큐브·잠재 응답이 그 값을 준다.** 그리고 셋 다 `target_item` 을 같은 이름 체계로 준다. 1년치
27,187건에서 **한 이름에 두 레벨이 붙은 적이 없다.**

그래서 표를 코드에 안 넣고 **저장한 것의 투영**으로 만든다.

```
SELECT DISTINCT target_item, item_level FROM enhancement_history WHERE item_level IS NOT NULL
```

별도 표가 없으니 원본과 어긋날 자리도 없다. 실측으로 92종이 서고 스타포스의 **83.7%** 가 덮인다.

**읽을 때 찾는다.** 쓸 때 채우면 그 시점에 표에 없던 장비는 영영 NULL 로 남는다. 레벨은 그
사건의 속성이 아니라 파생값이므로, 표가 자라면 예전에 넣은 행도 함께 값을 얻어야 한다.

**접두사 규칙은 안 넣는다.** `아케인셰이드` 24종이 전부 200 인 것은 맞지만(실측), 그것으로
더 덮이는 것이 1.6%p 뿐이고 그 3종은 큐브를 한 번만 돌리면 저절로 채워진다. 반면 `블랙` 은
계열 안에서 레벨이 갈린다(마법깃펜 100 · 블랙빈 마크 135). 얻는 것에 비해 **틀린 값이 조용히
박힐 자리**가 열린다.

**안 덮이는 14.7%** 는 대개 잠재를 못 붙이는 장비다(블랙·10주년 화이트 계열). 그쪽은 표가
자라도 안 채워지므로 금액 없이 건수만 센다.

### 결정 4. 비용 치환은 **자리만 잡는다** (사용자 지정)

큐브 종류별 가격 · 스타포스 비용 공식 · 잠재 재설정 비용은 [[ADR-006]] 이 금하는 자리다. AI 가
기억으로 쓰면 **틀린 지출이 조용히 쌓인다.**

그래서 이번에는 **받아서 저장하는 데까지**만 한다. 비용 칸은 nullable 로 두고, 표가 들어오는
날 그 칸을 채운다. 화면은 비용을 모르는 줄을 **금액 없이** 센다(`가격 미입력` 과 같은 태도,
[[ADR-124]]).

**그래서 응답 줄을 통째로 남긴다.** 결정 3.5 가 어제 이전을 안 부르기로 했으므로 지금 안 남기면
표가 들어와도 셀 수가 없다. 나중에 `cost_meso` 를 채우는 일이 **조회가 아니라 계산**이어야 한다.

### 결정 5. 수집기는 [[ADR-222]] 의 층이 소유한다

새 소유자를 만들지 않는다. `LedgerDataProvider` 가 창과 히스토리 둘을 든다.

`status` 는 **화면별**이다. 보스 수익에 서 있으면 히스토리가 뒤에서 돌아도 모달이 안 뜬다. 그
화면은 그 데이터를 안 쓴다.

**계획을 먼저 다 세운다**([[ADR-222]] 정정 8). 창이 끝난 뒤 히스토리가 자기 분모를 등록하면 바가
뒤로 간다. 둘 다 콜 없이 원장만 읽어 할 일을 셀 수 있으므로, 계획을 합쳐 분모를 잡은 뒤 실행한다.

진행의 작업 단위는 **`(종류, 날짜)`** 다. 하루가 1000건을 넘으면 커서로 콜이 더 나가는데, 콜을
세면 분모가 도는 중에 늘어난다. 커서는 **작업 안쪽** 일이라 밖에서는 1이다.

## 대가

- **월간 보기 첫 방문이 93콜**이다. 창과 겹치면 더 길다. 모달이 그 시간을 말한다.
- **비용을 모르는 동안 지출이 안 보인다.** 받아 두는 것은 나중에 값을 매길 수 있다는 뜻이지 지금
  화면이 는다는 뜻이 아니다.
- **수익과 지출의 모집단이 다르다.** 수익은 추적 6명, 지출은 계정 전체다. 화면에서 그것을
  **따로 말하지 않는다**(사용자 지정) — 지출은 언제나 계정 전체다.
- **`payload` 를 통째로 남긴다.** 비용을 나중에 셀 수 있게 하는 값이고, 그 대가가 용량이다.

## 정정

### 정정 1. 비용 표가 들어왔다. 셋 다 계산된다 (사용자 제공 2026-09-06)

결정 4 는 값이 없어 자리만 잡았다. 그 값이 왔다. `src/data/enhancement-prices.json` 이 들고
`src/lib/enhancement/cost.ts` 가 판다.

세 종류가 각각 다른 것을 본다.

| 종류 | 무엇으로 매기나 | 값 |
| --- | --- | --- |
| 큐브 | 장비 레벨 하나 | `20 · L²`. 120 이하는 0 |
| 잠재 재설정 | 레벨 구간 4 × 등급 4 | 종류별 표 2장 |
| 스타포스 | 장비 레벨 · 현재 성수 | `round100(1000 + L³(S+1)^exp / divisor)` |

**큐브는 감정비용 하나다.** 종류가 16개나 되지만 값을 안 가른다. 큐브 자체는 캐시·이벤트로
얻으므로 메소가 드는 것은 감정뿐이다. 사용자가 표로 준 여섯 값(130·140·150·160·200·250)이
`20·L²` 와 마지막 자리까지 맞아 식으로 접었다. 그래서 표에 없던 `lv135` 373건도 채워진다.

**통찰력은 100 으로 본다**(사용자 지정). 120 이하는 통찰력 90 이상이면 무료인데, 캐릭터마다
실제 통찰력을 받아 오면 콜이 는다. 모든 캐릭터를 100 으로 놓고 120 이하를 0 으로 센다.

**스타포스는 반올림이 할인보다 앞이다**(사용자 지정). 순서를 바꾸면 값이 어긋난다. 실측 한 건이
`src/lib/enhancement/__tests__/cost.spec.ts` 에 박혀 있다. 160 레벨 18 성은
raw 165,920,194.45 → 반올림 165,920,200 → 30% 할인 116,144,140 이다.

성수마다 나눗수가 다르고 **15·16 성이 같고 22~29 성이 같다**. 표를 손으로 접지 않고
`steps` 배열에 30줄을 그대로 둔 이유다. 규칙이 안 보이는 값이라 접으면 다음 세션이 틀리게 편다.

**노멀 등급은 표에 없어도 구멍이 아니다.** 1년치에서 `노멀` 이 나온 109건은 전부 본 잠재를
재설정한 엠블렘이고, 그 값은 **에디셔널 칸이 비었다**는 뜻이었다. 청구되는 등급은
`potential_option_grade` 쪽이라 노멀이 표에 닿을 자리가 없다.

**장비 레벨을 모르면 지출에 안 세운다**(사용자 지정). 스타포스 응답에는 `item_level` 이 없어
`src/data/equipment-items.json` 이 이름으로 레벨을 준다. 표에 없는 이름은 `cost_meso` 가 NULL 로
남고 **합계에서 빠진다**. 0 으로 세우면 합계가 조용히 거짓이 된다.

1년치 27,187건으로 재 보면 큐브 70.8억 · 잠재 1,799.5억 · 스타포스 1,173.5억이다. 스타포스
3,220건 중 446건이 레벨을 몰라 빠졌다.

### 정정 2. 파괴 복구비용은 **안 센다**. 셀 수가 없다 (사용자 지적 · 데이터 확인)

응답에 `recovery_cost_discount_rate` 가 있어 복구비용을 셀 수 있을 것처럼 보인다. 못 센다.

**복구했다는 줄이 없다.** `item_upgrade_result` 는 성공 · 실패(유지) · 파괴 셋뿐이고, 파괴는 한
줄로 끝난다. 파괴 32건은 전부 `after_starforce_count` 가 12 이고, 그 장비가 몇 초 뒤 12성에서
다시 올라간다.

그 재개를 복구로 읽으려 했는데 **읽을 수가 없다**(사용자 지적). 줄이 장비를 가리키는 것은
`target_item` **이름 하나뿐**이고 개체 식별자가 없다. 이름이 같은 다른 장비인지 복구한 그
장비인지 가릴 값이 없다.

가정이 아니라 사실이다. 1년치 3,220건을 (캐릭터, 장비이름)으로 묶어 성수를 이으면 **10곳이
안 이어진다.**

```
낟낟 / 에테르넬 메이지햇   00:17:23  11 → 12 성공
                          00:17:38  10 → 11 성공   (15초 뒤, 12 가 아니라 10)
풉품 / 슈피겔만의 평범한 목걸이  13:16:57   7 →  8 성공
                                13:17:27   0 →  1 성공   (30초 뒤, 0 성짜리 둘째 개체)
```

이름 하나에 개체가 여럿 붙는다. 「12성 재개 = 복구」로 놓으면 이런 자리에서 조용히 틀린다.
[[ADR-006]] 이 막는 자리다. 지어낸 수치가 아니라 **지어낸 인과**지만 틀린 지출이 쌓이는 것은
같다.

그래서 파괴 줄은 **시도 비용만** 센다. 22성에서 터진 줄은 22성 시도 비용 한 번이 지출이고,
복구비용은 없는 것으로 둔다. 실제로는 덜 잡히지만, 못 재는 것을 지어내는 것보다 낫다.

`recovery_cost_discount_rate` 는 그날 강화한 모든 줄에 딱지처럼 붙는 값이라 파괴와 무관하다
(389건 전부 성공·실패 줄이고 파괴 줄에는 한 번도 안 붙었다).

### 정정 3. 이벤트 월드 기록은 **지출에서 뺀다** (사용자 지정 · 실측)

스페셜은 이벤트 월드다. **재화에 가치가 없다.** 넣으면 지출이 두 배로 부푼다.

1년치 27,187건에서 스페셜이 **3,043.9억 중 1,611.1억(52.9%)** 이었다. 절반이 넘는다.

| | 큐브 | 잠재 | 스타포스 | 합계 |
| --- | --- | --- | --- | --- |
| 전부 | 70.8억 | 1,799.5억 | 1,173.5억 | 3,043.9억 |
| 스페셜 제외 | 65.2억 | 990.0억 | 377.5억 | **1,432.7억** |

**거르는 축이 종류마다 다르다.** 스타포스만 `world_name` 을 준다. 큐브·잠재는 안 준다.

| 종류 | 응답에 있는 것 | 무엇으로 거르나 |
| --- | --- | --- |
| 스타포스 | `character_name` · `world_name` | 줄의 `world_name` |
| 큐브 · 잠재 | `character_name` 만 | `character/list` 의 이름 |

**줄이 월드를 주면 그 값이 이긴다.** 그때의 월드라 지금 목록보다 정확하고, 삭제되어 목록에서
사라진 캐릭터도 막힌다. 실제로 계정 목록에 없는 이름이 다섯 있었다(낟로로 · 살름 ·
asdfonsdf · NRBI · 씨낚).

이름 판정은 **스페셜에서 본섭으로 못 넘어온다**는 데 기댄다(사용자 확인). 스페셜에서 쓰던
이름을 본섭에서 다시 만들면 그 캐릭터 기록까지 빠지는데, **그 경우는 무시한다**(사용자 지정).

세 가지를 사용자가 정했다.

1. **목록에 없는 이름은 일반 캐릭터로 센다.** 월드를 알 길이 없고, 빼면 살름 192.9억이 사라진다.
2. **챌린저스·챌린저스2 는 일반 월드다.** 시즌 월드지만 재화가 본섭으로 넘어온다.
3. **스페셜만 뺀다.**

`character/list` 를 한 번 더 부른다. 계정 단위라 ocid 가 필요 없고 콜이 하나다. 이 계정은
캐릭터 73명에 이름이 73개라 겹치지 않았고, 스페셜은 한 명이었다.

### 정정 4. 월드를 모르면 **그 날짜를 안 굳힌다** (사용자 선택)

정정 3 은 `character/list` 를 받았다고 치고 썼다. 못 받으면 이름 집합이 비고, 스페셜 지출이
그대로 샌다. 스타포스는 줄이 `world_name` 을 들어 안 새지만 큐브·잠재는 막을 근거가 없다.
머리맨들맨둘의 잠재만 809.5억이라 조용히 넘어갈 크기가 아니다.

**못 받으면 그 날짜를 안 굳힌다.** 다음 회차에 다시 부른다. 지출이 늦게 뜨지만 거짓말은 안 한다.

**여기서 둘을 갈라야 한다**(사용자 지적). 목록에 없는 이름을 「월드 모름」으로 묶으면 그 날짜가
**영원히 안 굳는다.** 삭제·이전된 캐릭터는 앞으로도 영영 목록에 안 나오기 때문이다.

가르는 것은 이름이 아니라 **목록 조회가 성공했는가**다.

| 상황 | 뜻 | 처리 |
| --- | --- | --- |
| 목록 조회 실패 | 아무것도 모른다. **회복된다** | 안 굳힌다. 다시 부른다 |
| 목록은 받았는데 이름이 없다 | 답이 난 것이다. **회복 안 된다** | 일반으로 센다. 굳힌다 |

타입으로 강제한다. `isSpendingRecord` 의 `eventNames` 가 `null` 이면 반환도 `null` 이고,
부르는 쪽이 그 값을 다뤄야 컴파일된다. **빈 집합과 `null` 은 뜻이 다르다.** 빈 집합은 스페셜
캐릭터가 없는 계정이라 답이 난 것이다.

구현은 `src/lib/enhancement/world.ts` 다.

### 정정 5. 범위는 **그 달 격자 전체**다 (사용자 선택)

결정 2 는 주간 진입을 21콜로 적었다. 그 주 이레 + 오늘 기준이다. 화면과 안 맞았다.

가계부는 달력 아래에 **한 달짜리 열지도**를 그린다. 화면이 읽는 범위가 처음부터
`coveringRange(보이는 주, 그 달 격자)` 라 35~42일이었다. 이레만 받으면 열지도의 나머지 날이
수익만 그려져 실제보다 밝게 보인다.

**열지도가 맞는 쪽을 골랐다**(사용자 선택). 첫 진입이 105~126콜이고 창 84콜과 겹치면 200콜을
넘는데, 모달이 그 시간을 진행 바로 말한다. 굳으면 두 번째부터 3콜이다.

범위 계산을 화면에서 `src/features/cashbook/range.ts` 로 옮겼다. **화면과 층이 같은 답을 봐야
한다.** 갈리면 열지도가 지출을 빼고 그려지거나, 안 그리는 날을 받느라 콜이 샌다.

**층이 기본 범위를 스스로 안다.** 화면이 `requestDateRange` 로 알려 주기를 기다리면 창이 먼저
분모를 잡고 뒤늦게 히스토리가 자기 몫을 더해 **진행 바가 뒤로 간다**([[ADR-222]] 정정 8).

### 정정 6. 강화 줄은 **펼쳐서 무엇을 강화했는지 보여 준다** (사용자 지정)

정정 5 까지의 강화 줄은 캐릭터 하나에 한 줄이고 누를 데가 없었다. 원천이 넥슨 API 라 갈 화면이
없어서였다. **펼치면 갈 곳이 없어도 할 일이 생긴다.**

결정석 줄이 이미 그 모양이다(그 자리에서 편다). 같은 몸짓을 쓴다.

```
낟낟 · 강화              47회        −12억
   아케인셰이드 클로        32회       −9.8억
   데아 시두스 이어링       15회       −2.2억
```

**큰 금액부터 선다.** 펼쳐서 보는 이유가 「어디에 썼나」라 이름순이면 그 답이 안 보인다. 금액이
같으면 건수로, 그것도 같으면 이름으로 가른다. 순서가 흔들리면 다시 그릴 때마다 줄이 자리를 바꾼다.

**타일을 안 쓴다.** 장비에는 초상이 없고 보려는 것이 금액이라, 이름과 금액이 한 줄에 나란히 서는
쪽이 답을 바로 준다.

**그 장비를 통째로 모르면 금액 자리가 `값 모름` 이다.** `0` 을 적으면 공짜로 강화한 것이 된다.
일부만 모르면 건수 옆에 `값모름 n` 이 선다.

### 정정 7. 강화를 **넷으로 가른다** (사용자 지정)

정정 6 까지는 캐릭터 하나에 `강화` 한 줄이었다. **묶지 않는다**(사용자 지정).

가르는 단위가 `kind` 보다 하나 잘다. API 는 잠재 둘을 한 엔드포인트로 주는데, 본 잠재와
에디셔널은 비용 표가 아예 다르다(에디셔널이 두 배 넘는다). `potential_type` 이 그것을 말한다.

| 갈래 | 어디서 오나 |
| --- | --- |
| 큐브 | `kind === 'cube'` |
| 스타포스 | `kind === 'starforce'` |
| 잠재능력 | `potential_type` 이 `에디셔널 잠재능력 재설정` 이 아닌 것 |
| 에디셔널 잠재능력 | `potential_type === '에디셔널 잠재능력 재설정'` |

1년치 실측이 가르는 값을 보여 준다.

```
잠재능력          1,360건    534.6억
에디셔널 잠재능력   1,050건    455.4억
스타포스          2,449건    377.5억   값모름 441
큐브             18,600건     65.2억
```

**에디셔널이 본 잠재의 80%** 다. 묶었으면 990억 한 덩이로 가려진다. 건수는 큐브가 압도적인데
금액은 꼴찌라는 것도 묶으면 안 보인다.

줄의 신원이 `enhancement:{갈래}:{이름}` 이 된다. `ocid` 가 없어 이름이 그 자리를 든다.

**캐릭터로 먼저 모으고 그 안에서 큰 금액이 위다.** 갈래를 고정 순서로 두면 그날 제일 많이 쓴
것이 목록 가운데에 숨는다.

### 정정 8. 들어오는 대로 화면에 흘린다. 기간 이동은 **모달을 안 띄운다** (사용자 보고)

정정 5 가 격자 전체를 받기로 하면서 지난 달 이동이 105콜이 됐다. 그런데 `revision` 이 **회차
끝에 한 번만** 올라, DB 에 값이 들어가고 있어도 화면은 105콜이 다 끝날 때까지 몰랐다. 그 위를
모달이 덮어 사용자는 빈 달력조차 못 봤다.

둘을 고친다.

1. **수집기가 칸을 끝낼 때마다 알린다**(`onLanded`). 층이 그것을 묶어 `revision` 을 올린다.
2. **기간 이동은 `filling` 을 안 세운다.** 달력을 보려고 옮긴 것인데 모달이 그 위를 덮으면
   아무것도 못 본다. 값이 칸에 붙는 것 자체가 진행 표시다.

**묶어서 올린다.** `revision` 은 컨텍스트 값이라 오를 때마다 층이 통째로 다시 그려진다. 칸마다
올리면 105번이고, 아예 안 올리면 회차가 끝날 때까지 빈 달력이다. 600ms 로 그 사이를 잡는다.

다시 읽는 값이 싸다는 것을 쟀다. 1년치에서 가장 바쁜 달(2025-11, **3,818줄**)을 통째로 파는 데
**10.1ms** 다. 7초 회차에 12번이면 121ms 다.

모달은 **마운트와 당김에만** 남는다. 거기는 창(84콜)이 함께 돌고 화면에 아직 아무것도 없다.

### 정정 9. 확정 전에는 숫자 자리에 **스켈레톤**이 선다 (사용자 지정)

정정 8 이 값을 들어오는 대로 흘리게 하자, 지출이 `−0` 으로 먼저 서고 값이 들어올 때마다 네 번
바뀌었다. 사용자에게는 **앱이 틀렸다가 고쳐지는 것**으로 읽힌다(사용자 보고).

`0` 은 아직 안 받은 값을 안다고 단정한 것이다. 이 저장소가 이미 여러 번 세운 것과 같은 자리다.

**셋을 함께 가린다**(사용자 지정). 수익은 로컬 기록에서 와 이미 참이지만, 한 카드 안에서 한 줄만
숫자가 서면 그 줄만 다르게 읽히고 지출이 들어올 때 두 줄의 몸짓이 갈린다.

스켈레톤은 **`moti/skeleton`** 이다. 직접 안 만든다(사용자 지정).

**네이티브 모듈이 안 늘어야 했다.** `react-native-skeleton-placeholder` 와
`react-native-reanimated-skeleton` 은 둘 다 `react-native-linear-gradient` 를 무조건 부르는데,
그것을 더하면 **Expo Updates 런타임 지문이 바뀐다**. 지문 재료에 네이티브 의존성이 들어 있어,
이 트리에서 만든 OTA 가 지금 스토어 바이너리에 안 붙는다. `moti` 는 JS 전용이고 이미 있는
`expo-linear-gradient` 를 쓴다.

`moti` 는 ESM 으로만 배포해 `jest.config.js` 의 `ESM_ONLY_DEPS` 에 넣었다. 안 넣으면
`Unexpected token 'export'` 로 죽는다.

**`patches/moti+0.30.0.patch` 로 배럴을 끊었다.** `moti/skeleton` 이 `MotiView` 하나 때문에
`../components` 배럴을 부르는데, 그 배럴이 `SafeAreaView` 를 함께 내보내고 그것이 `react-native`
의 **폐기된 `SafeAreaView`** 를 부른다. 앱을 켤 때마다 폐기 경고가 뜨고, RN 이 그것을 지우면
`moti/skeleton` 이 통째로 죽는다. import 를 `../components/view` 로 바꿔 배럴을 안 싣는다.

층이 **`collecting`** 을 새로 든다. `status` 와 갈리는 자리가 기간 이동이다. 거기는 모달을 안
띄우므로(정정 8) 화면이 확정 전을 알 근거가 이것뿐이다.

**자리표시는 눌린 순간 서고 적어도 300ms 는 남는다**(사용자 지정. 1200 → 800 → 300).

굳은 달은 회차가 200ms 에 끝난다. 그 사이 자리표시가 번쩍이고 사라지면 무엇이 지나갔는지 읽을
시간이 없어 화면이 튄 것으로 보인다. 안 굳은 달(2초)과도 몸짓이 갈린다.

**받을 것이 없어도 선다.** 달 안에서 주만 옮기면 범위가 안 바뀌어 조회가 안 나가는데, 그때만
자리표시가 없으면 같은 몸짓이 어떤 때는 번쩍이고 어떤 때는 안 움직인다. 화면이
`markPeriodMoved` 로 **누르는 순간** 알린다. 범위가 바뀌는 이동은 `requestDateRange` 가
이어받는다.

**늦추는 것은 자리표시뿐이다.** 값과 `revision` 은 제때 올린다. 함께 늦추면 화면이 실제로
느려진다.

자리표시의 폭·높이는 **그 자리에 설 글자를 잰 값**이다(순 수익 120×20 · 재료 두 줄 50×12).
넓으면 값이 들어올 때 줄이 줄어들어 카드가 움찔한다.

**달력 칸에는 안 넣는다**(사용자 지정). 칸까지 가리면 달력이 통째로 비어 보인다.

### 정정 10. 다 합산될 때까지 **아무 값도 안 그린다** (사용자 지정)

정정 8 이 값을 들어오는 대로 흘리게 했다. **그 절반을 되돌린다.**

정정 9 가 요약 카드를 자리표시로 가렸지만 **달력 칸은 그대로 바뀌었다.** 수집기의 작업 단위가
`(종류, 날짜)` 라 한 날의 큐브·스타포스·잠재가 따로 도착하고, 그때마다 그 칸의 지출이 커진다.

```
0.6초  9/5 칸 = 큐브분만        −1,600만
1.2초  9/5 칸 = 큐브+스타포스   −1.7억
1.8초  9/5 칸 = 셋 다           −3.3억
```

합산 자체는 **언제나 완전했다**. 그 시점의 DB 를 통째로 다시 접었다. 덜 찬 것은 재료다. 그래도
사용자에게는 숫자가 계속 바뀌는 것으로 보인다.

**중간 `revision` 을 걷었다.** 회차가 끝날 때 한 번만 오른다. 화면은 `collecting` 인 동안 아예
안 읽고 빈 값을 그린다.

**상태를 지우지 않고 그릴 때만 가린다.** 효과에서 지우면 렌더가 한 번 더 돈다.

**칸의 자리는 남는다.** 비는 것은 숫자뿐이다. 자리까지 없애면 칸 높이가 흔들려 격자가 출렁인다.

**읽은 값이 어느 범위의 것인지 함께 든다.** 회차가 끝난 순간부터 새 읽기가 도착하기까지 몇
밀리초가 있는데, 그 사이에 **이전 달의 값이 그려졌다**(사용자 보고). 격자가 앞뒤 달의 날을 함께
그리므로 겹치는 날만 값이 있고 나머지는 비어, 있던 것만 먼저 뜬 것처럼 보인다.

그날 목록도 같다. 고른 날과 읽은 날이 맞아야 그린다.

정정 8 의 나머지 절반(**기간 이동은 모달을 안 띄운다**)은 그대로다. 그 자리를 자리표시가 든다.

## 열린 질문

- **자리표시에 움직임이 안 보인다.** `moti/skeleton` 의 흐르는 빛은 기본이
  `delay 200 · duration 3000` 이라 300ms 수명에서는 시작도 못 한다. 900ms 로 줄이고
  `backgroundSize` 를 6에서 2로 좁혀도 실기기에서 **850ms 떨어진 프레임들이 픽셀 단위로
  똑같았다.** 애니메이션이 아예 안 도는 것인지, 이 테마의 대비(border→surface, L 0.145)에서
  안 보이는 것인지 아직 못 갈랐다. 자리표시가 300ms 만 서 있는 한 어떤 스위프도 못 읽으므로
  급하지 않다.
- **레벨 모르는 장비 441건.** 스타포스에서 빠지는 만큼 지출이 실제보다 적게 나온다. 표에 이름을
  더하면 그만큼 채워진다.
- **용량.** 하루 187건 × 3종 × payload 를 통째로 남기면 한 달이 수 MB 다. 캐시 삭제로 비울 수
  있지만 얼마나 커지는지는 실사용에서 재야 안다.
