# Architecture Decision Records (인덱스)

> 결정 원장. **각 ADR 전문은 [`adr/`](./adr/) 에 파일 하나씩** 있다. 이 파일은 **슬림 인덱스**다 —
> 필요한 결정만 `adr/ADR-NNN.md` 로 열고, 전체를 컨텍스트에 올리지 말 것. `[[ADR-NNN]]` 은 경로가
> 아닌 논리적 참조라 그대로 쓴다. 새 결정은 `adr/` 에 파일을 추가하고 이 표에 **한 줄**만 넣는다 —
> 요약을 이 표에 쌓지 말 것(그렇게 해서 이 파일이 한때 342KB 까지 불었다).

## 이 표를 읽는 법 (먼저 읽을 것)

**ADR 을 인용하기 전에 상태 배지를 먼저 본다.** 배지는 2026-08-21 현행 코드를 기준으로 매겼고,
각 `adr/ADR-NNN.md` 파일 제목 바로 아래에도 같은 배지가 박혀 있다.

| 배지 | 뜻 | 파일에 무엇이 있나 |
|---|---|---|
| 🟢 유효 | 현행 RN 앱이 이 결정대로 돈다 | 전문. 근거로 인용해도 된다 |
| 🟡 부분 폐기 | 일부 결정이 뒤집혔다 | 전문 + 배너의 «살아 있는 것» — 확인한 뒤 인용 |
| ⛔ 폐기 | 더 이상 유효하지 않다 | **요약 몇 줄만**(정책 본문은 git 히스토리로 내렸다) |
| ⚪ 미구현 | 결정만 있고 코드가 없다 | 전문. 착수 전에 다시 읽을 것 |
| 🗑 삭제 | 폐기하고 본문을 지웠다 | 묘비 한 문단. 원문은 git 태그 `store-1.0.6-capacitor-sunset` |

> **죽은 결정은 «읽을 수 있게» 두지 않았다.** ⛔ 28개는 «무엇을 결정했었나» 요약 몇 줄로 줄여
> 두었다(284KB → 29KB) — 파일을 열어도 그 정책을 다시 구현할 만큼의 내용이 나오지 않는다.
> 살아 있는 문서 본문에서 이 번호들로 가는 **`[[…]]` 인용도 끊었다**(157건).

> **이 규약은 테스트가 지킨다** — `src/__tests__/adr-citation-policy.test.ts`. 넷을 본다:
> ① `[[ADR-NNN]]` 이 실재 파일을 가리키는가 ② 소스가 **🔗 도 없는 죽은 ADR** 을 인용하지 않는가
> ③ 「결정 N」 까지 특정한 인용의 번호가 그 문서에 실제로 있는가 ④ 이 표의 배지·집계가 각
> 파일과 맞는가. ③은 `### 결정 1.` 제목을 쓰는 문서에만 건다 — 옛 문서는 결정을 적는 형식이
> 제각각이라 기계가 못 읽는다. 문서를 정리해 형식을 맞추면 자동으로 검사 대상이 되고, 한 번
> 대상이 된 것은 다시 빠질 수 없다.

> **⛔ 라고 전부 죽은 것은 아니다.** 웹뷰 시대 ADR 중 21개는 «결정 몇 개» 가 RN 코드로 그대로
> 넘어왔다(예: ADR-050 의 SQLite 커넥션 자가복구는 `storage/sqlite/db.ts` 가 지금도 건다). 그런
> 파일은 배너에 **🔗 줄**로 못박아 뒀다 — **그 줄에 없는 것은 죽은 것이다.**

## 현행 기준선 (baseline) — 2026-08-21

**이 저장소는 Expo React Native 앱 하나다.** [[ADR-128]] 이 RN 으로 옮겼고 [[ADR-154]]·[[ADR-155]] 가
캐패시터 앱을 끝내고 소스를 지웠다. 그래서 **아래 것들은 저장소에 존재하지 않는다** — 어떤 ADR 이
이것들을 전제로 말하고 있으면 그 문장은 죽은 것이다:

| 없는 것 | 지금은 | 결정 |
|---|---|---|
| Capacitor · WebView · `capacitor.config.ts` | React Native (Expo, Hermes) | [[ADR-128]]·[[ADR-155]] |
| Vite · `index.html` · `main.tsx` · `src/index.css` | Metro · `index.ts` · `global.css`+nativewind | [[ADR-128]]·[[ADR-155]] |
| DOM · CSS 기하 (`sticky`·`fixed`·문서 스크롤·`ResizeObserver`) | RN 뷰 · `ScreenScroll` 셸 · 고정 영역 없음 | [[ADR-131]] |
| @capgo OTA · `publish-live-update.mjs` | `expo-updates` + 자체 매니페스트 Worker | [[ADR-137]]·[[ADR-154]] |
| vaul(Drawer) · React Router · 자체 스택 전환 | `@gorhom/bottom-sheet` · `@react-navigation` | [[ADR-128]]·[[ADR-120]] |
| 자체 구현 당겨서 새로고침 훅 | RN `RefreshControl` | [[ADR-130]] |
| 계정(메이플 ID) 선택 단계 · 예열 | 캐릭터 선택 화면의 계정 드롭다운 | [[ADR-143]]·[[ADR-144]] |
| vitest · 렌더 트리 스냅샷 | jest 하나 | [[ADR-156]]·[[ADR-157]] |

예외 하나: `storage/adapters/capacitor-*` 와 `modules/capacitor-storage`, [`migration/data.md`](./migration/data.md) 는
**지금도 유효하다** — RN 앱이 캐패시터 시절에 저장된 기기 데이터를 계속 읽어야 하기 때문이다([[ADR-155]]).

## 철학

로컬 우선(local-first). 백엔드/로그인 없이 기기 안에서 완결되는 최소 구현을 우선 선택하고,
알림·백그라운드 사운드처럼 JS 만으로 불가능한 기능만 네이티브 어댑터에 의존한다([[ADR-003]]·[[ADR-005]] —
이 경계가 RN 전환을 가능하게 만든 원인이다). Nexon Open API 연동([[ADR-007]])도 사용자 개인이 발급받은
API 키를 기기에 저장해 직접 호출하는 방식이라 이 원칙을 벗어나지 않는다. 게임 밸런스에 따라 자주
바뀌는 수치 데이터는 로직과 분리해 유지보수 비용을 낮춘다([[ADR-006]]).

## 죽은 결정 빠른 목록

회귀를 막는 것이 목적인 목록이다. **이 번호들을 새 구현의 근거로 인용하지 말 것.**
주제별로 «무엇을 하려다 왜 죽었나» 를 보려면 [`adr/README.md`](./adr/README.md) 를 볼 것 —
번호만으로는 같은 실패를 다시 밟는 것을 못 막는다.

- 🗑 **삭제(6)**: ADR-001 · ADR-002 · ADR-028 · ADR-029 · ADR-084 · ADR-095
- ⛔ **폐기(31)**: ADR-005 · ADR-016 · ADR-022 · ADR-024 · ADR-025 · ADR-027 · ADR-039 · ADR-047 · ADR-049 · ADR-050 · ADR-051 · ADR-072 · ADR-073 · ADR-074 · ADR-077 · ADR-078 · ADR-079 · ADR-080 · ADR-081 · ADR-082 · ADR-085 · ADR-090 · ADR-092 · ADR-098 · ADR-102 · ADR-106 · ADR-107 · ADR-112 · ADR-122 · ADR-123 · ADR-192
- 🟡 **부분 폐기(46)**: ADR-004 · ADR-008 · ADR-012 · ADR-013 · ADR-014 · ADR-015 · ADR-018 · ADR-019 · ADR-026 · ADR-035 · ADR-038 · ADR-040 · ADR-045 · ADR-046 · ADR-053 · ADR-054 · ADR-055 · ADR-061 · ADR-062 · ADR-064 · ADR-065 · ADR-086 · ADR-087 · ADR-088 · ADR-089 · ADR-093 · ADR-096 · ADR-097 · ADR-099 · ADR-100 · ADR-105 · ADR-113 · ADR-117 · ADR-119 · ADR-120 · ADR-124 · ADR-127 · ADR-130 · ADR-132 · ADR-142 · ADR-145 · ADR-161 · ADR-170 · ADR-172 · ADR-173 · ADR-178

## 결정 목록

총 205건 — 🟢 120 · 🟡 47 · ⛔ 31 · ⚪ 1 · 🗑 6

| ADR | 상태 | 제목 | 대체 · 비고 |
|---|---|---|---|
| [001](./adr/ADR-001.md) | 🗑 삭제 | Capacitor 기반 하이브리드 앱 채택 | [[ADR-128]] 이 RN 으로 대체했고 [[ADR-155]] 가 캐패시터 소스를 삭제했다. 현행 코드가 이 번호를 인용하는 곳이 없다. |
| [002](./adr/ADR-002.md) | 🗑 삭제 | Vite 기반 React SPA | 같은 전환으로 대체 — 현행 번들러는 Metro(Expo)다. 현행 코드가 이 번호를 인용하는 곳이 없다. |
| [003](./adr/ADR-003.md) | 🟢 유효 | 백엔드 없이 기기 로컬 저장소만 사용 |  |
| [004](./adr/ADR-004.md) | 🟡 부분 폐기 | 서버 푸시 없이 로컬 알림만 사용 | «64개 한도 우선순위 정책» 은 [[ADR-146]] 이 폐기했다. |
| [005](./adr/ADR-005.md) | ⛔ 폐기 🔗 | 사냥 타이머는 상시 알림 + 주기 사운드 | 폐기(2026-08-30) — 사냥 타이머 계획을 접고 포트·어댑터·웹 폴백·기능 문서를 지웠다(3년간 구현 0건 · 소비자 0건). 🔗 **`native/` 어댑터 경계**만 살아 있다 — [[ADR-003]] 과 짝지어 인용한다. |
| [006](./adr/ADR-006.md) | 🟢 유효 | 게임 레퍼런스 데이터는 코드와 분리 |  |
| [007](./adr/ADR-007.md) | 🟢 유효 | 콘텐츠·보스 진행 상태는 Nexon Open API 로 동기화 |  |
| [008](./adr/ADR-008.md) | 🟡 부분 폐기 | 에러 핸들링·복원력 정책 | 병렬 호출 부분은 [[ADR-015]] 정정과 [[ADR-114]] 가 다시 잡았다. |
| [009](./adr/ADR-009.md) | 🟢 유효 | 직업(전직) 기반 테마 시스템 | 토큰 생성 경로만 이사했다 — `src/index.css` 는 없고 `theme/theme-vars.ts` + nativewind 다. |
| [010](./adr/ADR-010.md) | 🟢 유효 | 물욕템 수익 반영·랜덤 컨테이너 결과 기록 |  |
| [011](./adr/ADR-011.md) | 🟢 유효 | 드랍 화면 보스 목록 출처·아이템 노출·에셋 |  |
| [012](./adr/ADR-012.md) | 🟡 부분 폐기 | 스케줄러 캐릭터 추적 정책·동기화 범위 | 목록 통합은 [[ADR-042]], 계정 경계 제거는 [[ADR-143]] 이 대체했다. |
| [013](./adr/ADR-013.md) | 🟡 부분 폐기 | 화면 구조를 컨텐츠/보스 스케줄러로 개편 | 탭 구성은 [[ADR-132]] 가 다시 짰다(첫 화면은 `today`). |
| [014](./adr/ADR-014.md) | 🟡 부분 폐기 | 완료 감지 시 파티원 수 자동 기록 | «가장 최근 완료 기록값 이어받기» 는 [[ADR-019]] 가 완전히 폐기했다. |
| [015](./adr/ADR-015.md) | 🟡 부분 폐기 | 캐릭터 관리 피커 — 정렬·카드 UI·병렬 조회 | UI 는 [[ADR-142]]·[[ADR-144]] 가 다시 짰다. 병렬 조회 규칙만 유효. |
| [016](./adr/ADR-016.md) | ⛔ 폐기 | 온보딩 시 전체 캐릭터 예열 | [[ADR-143]] 이 «계정을 여는 순간의 자격 판정» 으로 대체했다. |
| [017](./adr/ADR-017.md) | 🟢 유효 | 보스 수익 캐시 확장·순서 일관성 |  |
| [018](./adr/ADR-018.md) | 🟡 부분 폐기 | 보스 스케줄러 UI 개편 + 초상화 크롭 | 크롭(`lib/face-crop`)은 유효, 탭 pill·카드 기하는 RN 재설계로 대체. |
| [019](./adr/ADR-019.md) | 🟡 부분 폐기 | 파티 인원 상시 설정 | **결정 6(필터는 활성 탭 안에서만)은 [[ADR-164]] 결정 5 가 폐기했다** — 목록이 하나가 되면서 독립할 상대가 없어졌다. 파티 인원 설정 자체(결정 1~5)는 유효하고, 편집 경로는 [[ADR-121]] 이 넓혔다. |
| [020](./adr/ADR-020.md) | 🟢 유효 | 일일퀘스트 카드 — quest_state 정규화·지역 배경 |  |
| [021](./adr/ADR-021.md) | 🟢 유효 | 주간 컨텐츠 카드 4카테고리 |  |
| [022](./adr/ADR-022.md) | ⛔ 폐기 🔗 | Live Update(OTA) — Capgo + GitHub Releases | [[ADR-137]] 이 `expo-updates` 로 프로토콜을 바꿨다(GitHub Releases 자체 호스팅만 그대로). |
| [023](./adr/ADR-023.md) | 🟢 유효 | 보스 수익 페이지 — 주간/월간 탭·기간 네비·로컬 우선 캐싱 |  |
| [024](./adr/ADR-024.md) | ⛔ 폐기 🔗 | OTA 버전 형식 수정 + 베타 채널 | 베타 채널과 `import.meta.env` 주입은 [[ADR-137]] 이 폐기했다(Metro 에 `import.meta` 가 없다). |
| [025](./adr/ADR-025.md) | ⛔ 폐기 🔗 | 스플래시 — 네이티브 하나로 통일 + MIUI force-dark | [[ADR-138]](Expo prebuild)이 자산·구성을 대체했다. 색 split(`values-night` `#D06100`)·비트맵은 RN 에 없다 — 한 색 `#F58B0F` 다. |
| [026](./adr/ADR-026.md) | 🟡 부분 폐기 | OTA 관찰용 UI — 설정 «앱 업데이트» 섹션 | 섹션은 살아 있고(`app/settings/AppUpdateSection.tsx`) 프로토콜은 [[ADR-137]] 이 바꿨다. |
| [027](./adr/ADR-027.md) | ⛔ 폐기 🔗 | OTA 사용자 동의형 UX + 스토어 업데이트 구분 | [[ADR-137]] 이 «대체» 로 명시했다 — `CapacitorUpdater` API 배선은 전부 죽었다. |
| [028](./adr/ADR-028.md) | 🗑 삭제 | Android 네이티브 스플래시 최소화 (설계 후 폐기) | 설계 후 폐기라 구현된 적이 없고, 캐패시터 스플래시 자체가 사라졌다. 인용처 없음. |
| [029](./adr/ADR-029.md) | 🗑 삭제 | Android 전용 SplashActivity | [[ADR-138]](Expo prebuild)이 대체했다 — MIUI force-dark 를 이기던 비트맵 `SplashActivity` 구조는 Expo 로 넘어오지 않았다. 인용처 없음. |
| [030](./adr/ADR-030.md) | 🟢 유효 | 컨텐츠 스케줄러 다단계 캐싱 전략 |  |
| [031](./adr/ADR-031.md) | 🟢 유효 | 보스 처치 카운트 자체 계산·난이도 보정·시즌 보스 |  |
| [032](./adr/ADR-032.md) | 🟢 유효 | 보스 수익 롤링 조회 윈도우·파티원 스냅샷·선등록 |  |
| [033](./adr/ADR-033.md) | 🟢 유효 | 등록 난이도 ≠ 처치 난이도 (ownComplete) |  |
| [034](./adr/ADR-034.md) | 🟢 유효 | 최초 동기화·캐시 유실 대비 선채움 |  |
| [035](./adr/ADR-035.md) | 🟡 부분 폐기 | 스케줄러 자동/수동 트래킹 모드 | 결정 18 중 보스 관리 진입점 한 줄은 [[ADR-145]] 가 폐기했다. |
| [036](./adr/ADR-036.md) | 🟢 유효 | 보스 표시 순서 결정성 | [[ADR-186]] 이 이 순서를 **앱 전체**(스케줄러 자동 모드 · today · 가계부 타일)로 넓혔다 — 정렬 키는 결정 3 그대로이고 비교자만 `boss-matching` 의 공용 `compareBossOrder` 로 나왔다. 결정 4(`ORDER BY` 안 넣는다)도 유지. |
| [037](./adr/ADR-037.md) | 🟢 유효 | 이전 기간 게이트를 조회 가능성과 일치 |  |
| [038](./adr/ADR-038.md) | 🟡 부분 폐기 | 드롭 아이템 기록 + 고가 드롭 연출 | 시트 구현은 [[ADR-039]]→RN `@gorhom/bottom-sheet`, fps 는 [[ADR-103]] 이 대체. |
| [039](./adr/ADR-039.md) | ⛔ 폐기 🔗 | 바텀시트 자체구현 → vaul(Drawer) | RN 은 `@gorhom/bottom-sheet` 를 쓴다 — vaul 은 웹 라이브러리다. 살아 있던 스킨 중 **위 테두리 1px 은 [[ADR-179]] 결정 4 가 걷었다**. |
| [040](./adr/ADR-040.md) | 🟡 부분 폐기 | 보스 드롭 입력 시트 개편 | 결정 1·2(난이도 무관 통합 표시)는 [[ADR-044]] 가 반전했다. |
| [041](./adr/ADR-041.md) | 🟢 유효 | 보스 반지 상자 결과 선택 |  |
| [042](./adr/ADR-042.md) | 🟢 유효 | 캐릭터 추적 목록 통합 |  |
| [043](./adr/ADR-043.md) | 🟢 유효 | 캐릭터 관리 저장 규칙 |  |
| [044](./adr/ADR-044.md) | 🟢 유효 | 드롭 시트 난이도별 표시 |  |
| [045](./adr/ADR-045.md) | 🟡 부분 폐기 | 고가 드롭 시 카드 강조 효과 | CSS 회전 샤인·글로우는 RN 구현(`valuable-card-glow.ts` 등)으로 대체됐다. |
| [046](./adr/ADR-046.md) | 🟡 부분 폐기 | 총 수익을 노-카드 헤드라인으로 | 결정 5는 [[ADR-054]] 가 부분 폐기했다. |
| [047](./adr/ADR-047.md) | ⛔ 폐기 🔗 | 펼친 캐릭터 카드 헤더를 sticky 로 고정 | [[ADR-131]] «고정 영역 없음» 이 중첩 sticky 를 폐기했다 — sticky 오프셋 실측(결정 3)은 죽었다. |
| [048](./adr/ADR-048.md) | 🟢 유효 | DropEff 프레임별 origin 테이블 | **결정 6(프레임 프리로드)의 RN 판은 [[ADR-173]]** 에 있다 — 포팅에서 빠져 있었다. **정정 1** — 결정 5 의 «ScreenEff 크롭은 버스트 원점 기준 중앙» 은 **가로만 맞다**(세로는 원점이 비트맵 중심보다 ~130px 아래). |
| [049](./adr/ADR-049.md) | ⛔ 폐기 🔗 | 보스 수익 헤더 정렬·유격 CSS 4종 | px 단위 CSS 기하라 RN 에 그대로 옮겨진 수치는 없다. |
| [050](./adr/ADR-050.md) | ⛔ 폐기 🔗 | iOS 탭 동시 탭 → 문서 리로드 → 데이터 유실 | 웹뷰가 없어 «탭 동시 탭 → 문서 전체 리로드» 라는 계기 자체가 사라졌다. |
| [051](./adr/ADR-051.md) | ⛔ 폐기 | 메이플 ID 계정 선택 화면 경유 | [[ADR-143]] 이 계정 선택 단계를 없앴다. |
| [052](./adr/ADR-052.md) | 🟢 유효 | 캐시 삭제 범위 · 테이블 목록 SSOT |  |
| [053](./adr/ADR-053.md) | 🟡 부분 폐기 | access_flag 미확인 캐릭터 비표시 | 결정 1의 판정 기준은 [[ADR-086]] 결정 3이 대체했다. |
| [054](./adr/ADR-054.md) | 🟡 부분 폐기 | 주간 처치 수·결정석 한도 표시 | 결정 4는 [[ADR-059]] 가 폐기했다. |
| [055](./adr/ADR-055.md) | 🟡 부분 폐기 | 수동 선택 가드 — 주간 12개 한도 | 요구 레벨 미달 잠금(#32)은 정정 2로 폐기됐다. |
| [056](./adr/ADR-056.md) | 🟢 유효 | 보스 관리 목록 구성 — 미출시 제외·시즌은 챌린저스 |  |
| [057](./adr/ADR-057.md) | 🟢 유효 | 길드 콘텐츠는 길드 가입 캐릭터만 |  |
| [058](./adr/ADR-058.md) | 🟢 유효 | 캐시 데이터 선택 삭제 2그룹 |  |
| [059](./adr/ADR-059.md) | 🟢 유효 | 결정석 칩·진행 링을 과거 기간·월간까지 |  |
| [060](./adr/ADR-060.md) | 🟢 유효 | 빈 상태 디자인 통일 (`EmptyState`) |  |
| [061](./adr/ADR-061.md) | 🟡 부분 폐기 | 로딩 표현 통일 — 스피너 2종·진행률 | 결정 1은 [[ADR-074]]·[[ADR-113]]·[[ADR-130]] 이 예외를 넣거나 폐기했다. 결정 6의 **예외 없이 h-1.5** 는 정정 4가 두께 축을 열었다. |
| [062](./adr/ADR-062.md) | 🟡 부분 폐기 | 실패 상태 UI 통일 (`ErrorState`·스탈 배너) | 결정 3의 일부는 [[ADR-114]]·[[ADR-115]] 가 폐기했다. |
| [063](./adr/ADR-063.md) | 🟢 유효 | 동기화 실패는 인라인이 아니라 토스트 |  |
| [064](./adr/ADR-064.md) | 🟡 부분 폐기 | 테마 토큰 34종 · 값 파생 생성 | `on-*` 규약과 파생은 유효. 결정 8(모드 수동 목록)은 [[ADR-122]] 가 폐기, `index.css` 생성 경로는 RN 으로 이사. |
| [065](./adr/ADR-065.md) | 🟡 부분 폐기 | 무음 실패 5곳 처리 | 캐시 삭제 실패 토스트 하나는 RN 에 이식되지 않았다([[ADR-128]]). |
| [066](./adr/ADR-066.md) | 🟢 유효 | 수익 아이콘 `ProfitIcon` |  |
| [067](./adr/ADR-067.md) | 🟢 유효 | Nexon API 실패 종류 분리 + 기간 6상태 모델 |  |
| [068](./adr/ADR-068.md) | 🟢 유효 | 기간 6상태의 UI 표현 |  |
| [069](./adr/ADR-069.md) | 🟢 유효 | 보스 수익 기록의 진실성 (world 스냅샷) |  |
| [070](./adr/ADR-070.md) | 🟢 유효 | 주문서 교환권 3종 — scroll→consumable |  |
| [071](./adr/ADR-071.md) | 🟢 유효 | 드롭 획득 히스토리 — period_key 가 '언제' |  |
| [072](./adr/ADR-072.md) | ⛔ 폐기 🔗 | 당겨서 새로고침 — 커스텀 훅 자체구현 | [[ADR-130]] 이 RN `RefreshControl` 로 대체했다 — 제스처 훅 구현은 저장소에 없다. |
| [073](./adr/ADR-073.md) | ⛔ 폐기 🔗 | PTR 인디케이터 — 배너에서 목록 이동으로 | [[ADR-130]] 이 대체했다 — 배너/목록 이동 구현은 없다. |
| [074](./adr/ADR-074.md) | ⛔ 폐기 | 당김 마크 — 단풍잎 외곽선 링 | [[ADR-130]] 이 당김을 `RefreshControl` 로 옮겼고, 마크를 그리던 `PullToRefreshIndicator` 를 2026-09-01 에 지웠다. 코드에 남은 것이 없다. [[ADR-061]] 의 스피너 2종 규칙에 있던 PTR 예외도 함께 사라졌다. |
| [075](./adr/ADR-075.md) | 🟢 유효 | 월간 탭 — 달 경계를 걸친 진행 중 주차 |  |
| [076](./adr/ADR-076.md) | 🟢 유효 | 진행 중 주를 품은 지난 달의 새로고침 게이트 |  |
| [077](./adr/ADR-077.md) | ⛔ 폐기 🔗 | 드롭 히스토리를 중첩 라우트 + 오버레이로 | RN 스택(`@react-navigation/native-stack`)이 중첩 라우트 + `fixed` 오버레이를 대체했다. |
| [078](./adr/ADR-078.md) | ⛔ 폐기 🔗 | 기간 이동의 네이티브 왕복 줄이기 | 웹뷰↔네이티브 브릿지 왕복이 전제였다. 구현 완료로 확인된 적이 없다. |
| [079](./adr/ADR-079.md) | ⛔ 폐기 🔗 | stuck sticky 헤더의 레이어 고정 | 실기기에서 무효로 판명됐다([[ADR-120]]). |
| [080](./adr/ADR-080.md) | ⛔ 폐기 🔗 | 기간 이동 깜빡임 = 스크롤 클램프 프레임 | 문서 스크롤 클램프라는 원인이 RN 에 없다. |
| [081](./adr/ADR-081.md) | ⛔ 폐기 | 아코디언 펼침을 기간별로 기억 | 같은 날 자체 폐기했다(짝인 [[ADR-082]] 가 iOS 에서 불가능). |
| [082](./adr/ADR-082.md) | ⛔ 폐기 | 스크롤 위치도 기간별로 기억 | 실기기 검증으로 자체 폐기했다(sticky 트랜스폼 지연). |
| [083](./adr/ADR-083.md) | 🟢 유효 | 남은 인라인 실패 문구 3자리를 토스트로 |  |
| [084](./adr/ADR-084.md) | 🗑 삭제 | 접기 직후 페인트 전에 스크롤을 자른다 | 실기기 계측으로 반증돼 코드를 되돌렸다([[ADR-085]]). 인용처 없음. |
| [085](./adr/ADR-085.md) | ⛔ 폐기 🔗 | 헤더를 스크롤에서 떼어낸다 (fixed + 실측 spacer) | [[ADR-131]] 이 `fixed` 헤더 + 실측 spacer 구조를 통째로 걷어냈다. |
| [086](./adr/ADR-086.md) | 🟡 부분 폐기 | 계정→캐릭터 리모델링 · (ocid,날짜) 조회 원장 | 조회 원장은 유효. 계정 선택 단계는 [[ADR-143]] 이 없앴다. |
| [087](./adr/ADR-087.md) | 🟡 부분 폐기 | 직전 기간 대비 증감 칩 + 카운트업 | **정정 3 결정 3-2(`AmountFigure` 는 자기 잎을 갖는다)는 [[ADR-202]] 결정 12 가 죽였다** — 그 부품에서 카운트업을 걷어 잎을 가를 이유도 이름표(`identity`)도 사라졌고 `app/cashbook/amount-identity.ts` 가 통째로 없어졌다. 보스 수익 쪽(`atoms/AnimatedNumber` · 정정 1 의 이름표 규칙)은 그대로다. |
| [088](./adr/ADR-088.md) | 🟡 부분 폐기 | 혼테일 테마 — 얼음 동굴 + 배경 이미지 구조 | 결정 5-1은 [[ADR-133]] 이 폐기, 배경 그림은 [[ADR-109]] 가 교체했다. |
| [089](./adr/ADR-089.md) | 🟡 부분 폐기 | 검은마법사 테마 추가 | 색 일부는 [[ADR-105]] 가, 배경은 [[ADR-108]] 이 다시 정했다. |
| [090](./adr/ADR-090.md) | ⛔ 폐기 | 탭 전환 전면광고 + 3중 노출 게이트 | [[ADR-150]] 이 폐기했다 — 현행 앱에 전면광고가 없다. |
| [091](./adr/ADR-091.md) | 🟢 유효 | Android 릴리스 서명 — Play 앱 서명 + 업로드 키 |  |
| [092](./adr/ADR-092.md) | ⛔ 폐기 🔗 | 초기 번들 분할 — DEV 게이팅 + 라우트 지연 로딩 | Vite 가 없다 — Metro 는 단일 번들이라 라우트 분할이라는 축 자체가 사라졌다. |
| [093](./adr/ADR-093.md) | 🟡 부분 폐기 | 자산 최적화 — PNG → WebP(q95) | 에셋 포맷 정책은 유효. dist 크기·Vite 맥락은 죽었다. |
| [094](./adr/ADR-094.md) | 🟢 유효 | 아토믹 4계층 + 합성 패턴 | `components/{atoms,molecules,organisms,templates}` 로 지금도 강제된다. |
| [095](./adr/ADR-095.md) | 🗑 삭제 | NEXON Open API Analytics 스크립트 삽입 | `index.html` 과 함께 사라졌고 RN 에는 `<head>` 가 없다 — 폐기·제거했다. 넥슨 요건이면 새 결정이 필요하다. |
| [096](./adr/ADR-096.md) | 🟡 부분 폐기 | 스케줄러 화면 상태(탭·필터·선택)의 소유 위치 | 보스 쪽 탭·필터는 [[ADR-164]] 가 걷었다(컨텐츠 탭은 그대로). 배너 표 참조. |
| [097](./adr/ADR-097.md) | 🟡 부분 폐기 | 화면 진입 재조회 10분 TTL | 결정 6은 [[ADR-111]] 이 폐기했다. |
| [098](./adr/ADR-098.md) | ⛔ 폐기 🔗 | 탭 이동의 스크롤 클램프 | 문서 스크롤 클램프 처방이라 RN 에 대응물이 없다. |
| [099](./adr/ADR-099.md) | 🟡 부분 폐기 | 스크롤의 소유자를 문서에서 화면으로 | 결론은 RN `ScreenScroll`(36곳)로 이어졌다. 문서 스크롤·sticky 논의는 죽었다. |
| [100](./adr/ADR-100.md) | 🟡 부분 폐기 | 보스 수익도 화면이 스크롤을 소유 | 같음 — 셸은 살아 있고 기하 논의는 죽었다. |
| [101](./adr/ADR-101.md) | 🟢 유효 | 콜드 스타트 — 부팅 때 세 스토어 선하이드레이션 |  |
| [102](./adr/ADR-102.md) | ⛔ 폐기 🔗 | 접기 전 사전 스크롤을 걷어낸다 | 웹 스크롤 워크어라운드를 걷어낸 정리 작업이다. |
| [103](./adr/ADR-103.md) | 🟢 유효 | 고가 드롭 연출을 1.5배 속도로 |  |
| [104](./adr/ADR-104.md) | 🟢 유효 | 테마 선택 모달 — 프리뷰 타일 + 카테고리 + 모드 필터 |  |
| [105](./adr/ADR-105.md) | 🟡 부분 폐기 | 검은마법사 재조정 — 갈색 제거·골드 포인트 | 배경 값은 [[ADR-106]]→[[ADR-108]] 이 다시 정했다. |
| [106](./adr/ADR-106.md) | ⛔ 폐기 | 테마 배경 이미지를 둘 다 뗀다 | [[ADR-108]]·[[ADR-109]] 가 새 배경으로 되돌렸다. |
| [107](./adr/ADR-107.md) | ⛔ 폐기 🔗 | 캐릭터 관리 모달 높이를 안전영역에 묶는다 | [[ADR-144]] 가 모달을 두 층 페이지로 재설계했다. `vh`·`dvh` 식은 RN 에 없다. |
| [108](./adr/ADR-108.md) | 🟢 유효 | 검은마법사 배경을 합성 한 장으로 |  |
| [109](./adr/ADR-109.md) | 🟢 유효 | 혼테일 배경을 얼음 동굴 프레임으로 |  |
| [110](./adr/ADR-110.md) | 🟢 유효 | API 키 발급 가이드는 안내 사이트에 |  |
| [111](./adr/ADR-111.md) | 🟢 유효 | TTL 로 건너뛴 진입도 자동 기록 |  |
| [112](./adr/ADR-112.md) | ⛔ 폐기 🔗 | spacer 실측 — ResizeObserver 한 경로의 한 프레임 어긋남 | `ResizeObserver` 도 실측 spacer 도 RN 에 없다. |
| [113](./adr/ADR-113.md) | 🟡 부분 폐기 | 계정 전수 프로브 settle + character/basic 5분 TTL | TTL 가드는 유효. 계정 선택 화면은 [[ADR-143]] 이 없앴다. |
| [114](./adr/ADR-114.md) | 🟢 유효 | 429 는 문구로만 안내 + 스탈 배너 원인별 분기 |  |
| [115](./adr/ADR-115.md) | 🟢 유효 | API 키 무효화 → 토스트 + 키 화면 자동 이동 |  |
| [116](./adr/ADR-116.md) | 🟢 유효 | 개발 단계 키 429 — 서비스 단계 키 재입력 경로 |  |
| [117](./adr/ADR-117.md) | 🟡 부분 폐기 🔗 | OTA 적용 경로 복구 장치 | OTA 적용 경로 자체는 [[ADR-137]] 이 `expo-updates` 로 바꿨다(`capacitor.config.ts`·`index.html`·`main.tsx` 에 걸던 장치는 없다). |
| [118](./adr/ADR-118.md) | 🟢 유효 | 설정 화면 5행 + 하위 페이지 3 |  |
| [119](./adr/ADR-119.md) | 🟡 부분 폐기 | 개발 노트는 진실 원천 한 벌에서 두 갈래로 | 원천(`src/data/release-notes.ts`)은 유효. `publish-live-update.mjs` 경로는 죽었다([[ADR-137]]·[[ADR-154]]). |
| [120](./adr/ADR-120.md) | 🟡 부분 폐기 | 하위 페이지는 스택으로 연다 | «스택으로 연다» 는 유효(`@react-navigation/native-stack`). 자체 구현 955줄은 삭제됐다. 결정 13·14·15(웹 청크 분할·`lazy`)는 RN 에 안 넘어왔고 결정 15 의 `lib/preloaded-screen.tsx` 는 삭제됐다. |
| [121](./adr/ADR-121.md) | 🟢 유효 | 보스 카드를 탭해 파티 인원·난이도를 고친다 |  |
| [122](./adr/ADR-122.md) | ⛔ 폐기 | 스크림 위 패널 테두리는 모드마다 반대 | CSS `data-mode` 전용 결정이라 RN 에 대응물이 없다. 결정 8 폐기 사실은 [[ADR-064]] 쪽에 남는다. |
| [123](./adr/ADR-123.md) | ⛔ 폐기 🔗 | 경계 페이드에서 backdrop-filter 를 뺀다 | RN 에 `backdrop-filter` 가 없어 되붙일 방법 자체가 없다. |
| [124](./adr/ADR-124.md) | 🟡 부분 폐기 | 드롭 가격은 기록 한 건에 붙는 실판매가 | 결정 5(**앱 내장 키패드 · OS 키보드를 안 부른다**)의 범위가 `DropPricePad` **한 화면**으로 좁혀졌다([[ADR-170]] 정정 4) — 가계부 시트에는 글자 칸(사용처)이 있어 그 전제가 성립하지 않아 **OS 숫자 키보드**를 쓴다. 나머지 결정 여덟은 그대로다. |
| [125](./adr/ADR-125.md) | 🟢 유효 | 기능 사용법 안내는 기능 카탈로그 한 벌 |  |
| [126](./adr/ADR-126.md) | 🟢 유효 | 「자세히 보기」는 받기 전/후가 다르다 |  |
| [127](./adr/ADR-127.md) | 🟡 부분 폐기 | 캐릭터 0명 메이플 ID 는 계정 목록에 안 선다 | 계정 «목록» 자체는 [[ADR-143]] 이 없앴다. 자격 판정 원칙만 남는다. |
| [128](./adr/ADR-128.md) | 🟢 유효 | React Native 로 옮긴다 — 이 저장소의 시대 경계 |  |
| [129](./adr/ADR-129.md) | 🟢 유효 | 에셋 목록을 커밋된 생성물로 (`npm run assets:gen`) |  |
| [130](./adr/ADR-130.md) | 🟡 부분 폐기 | 당겨서 새로고침은 RN RefreshControl | 결정 1 의 배선 한 줄(`refreshing = status === 'loading'`)은 [[ADR-160]] 이 뒤집었다. 나머지는 유효. |
| [131](./adr/ADR-131.md) | 🟢 유효 | 화면에 고정되는 영역을 두지 않는다 |  |
| [132](./adr/ADR-132.md) | 🟡 부분 폐기 | 하단바 캡슐 2층 + 첫 화면을 today 로 | 층을 누가 드는가는 [[ADR-167]] 이 뒤집었다(결정 5·6·10 폐기). 결정 9 는 [[ADR-150]], 결정 12 는 [[ADR-147]]. 배너 표 참조. |
| [133](./adr/ADR-133.md) | 🟢 유효 | 배경은 벽지 한 장뿐 |  |
| [134](./adr/ADR-134.md) | 🟢 유효 | 콘텐츠는 안전영역에서 투명해진다 |  |
| [135](./adr/ADR-135.md) | 🟢 유효 | RN Image 는 두 축을 다 이름 부른다 |  |
| [136](./adr/ADR-136.md) | 🟢 유효 | 앱 루트가 자기 바탕을 칠한다 |  |
| [137](./adr/ADR-137.md) | 🟢 유효 | OTA 프로토콜을 expo-updates 로 |  |
| [138](./adr/ADR-138.md) | 🟢 유효 | 아이콘·스플래시는 Expo prebuild 로 만든다 |  |
| [139](./adr/ADR-139.md) | 🟢 유효 | 헤더가 상단 안전영역에 붙는다 |  |
| [140](./adr/ADR-140.md) | 🟢 유효 | 캐릭터 관리 진입점을 설정 한 곳으로 |  |
| [141](./adr/ADR-141.md) | 🟢 유효 | 동기화 상태를 제목 줄로 올린다 |  |
| [142](./adr/ADR-142.md) | 🟡 부분 폐기 | 캐릭터 선택은 초상화 레일 | 정정 8(링 없는 칸의 치수)은 [[ADR-161]] 이 뒤집었다. 나머지는 유효. |
| [143](./adr/ADR-143.md) | 🟢 유효 | 캐릭터 선택이 메이플 ID 경계를 넘는다 |  |
| [144](./adr/ADR-144.md) | 🟢 유효 | 캐릭터 관리는 «고른 것 / 고르는 곳» 두 층 | **정정 3**(2026-09-03) — 결정 1 이 예고한 대로 `CharacterTrackingPicker/` 를 지웠다. 온보딩이 `CharacterManageBody` 로 옮겨간 2026-08-18 부터 제품 코드에서 안 불리고 있었고, 지우는 조건을 적어 둔 주석만 «아직 쓴다» 로 남아 있었다. 네 파일 1,262줄. 딸려 죽는 것은 없다. |
| [145](./adr/ADR-145.md) | 🟡 부분 폐기 | 보스 관리를 스케줄러 그룹의 세 번째 하위 탭으로 | 결정 5 의 간격·흐림 값은 [[ADR-161]] 이 바꿨다. 나머지는 유효. |
| [146](./adr/ADR-146.md) | ⚪ 미구현 | 알림 시스템 — 네이티브는 능력만, 알림은 JS 로 | 설계 완료·구현 전. 착수 전에 이 문서를 다시 읽을 것. |
| [147](./adr/ADR-147.md) | 🟢 유효 | today 는 위젯 격자다 | **위젯 3(남은 스케줄)의 형태는 [[ADR-181]] 이 다시 짰다** — 정정 12·35·36·37·40 은 폐기다(2×2 격자 · 「줄도 칸도 그 행이 정한다」 · 칸 여백). 격자·배치·데이터·나머지 위젯 여덟은 그대로 유효하다. |
| [148](./adr/ADR-148.md) | 🟢 유효 | 13일 날짜 스윕을 한꺼번에 태운다 |  |
| [149](./adr/ADR-149.md) | 🟢 유효 | 콜드 스타트에서도 확인된 캐릭터부터 흘린다 |  |
| [150](./adr/ADR-150.md) | 🟢 유효 | 전면광고를 걷는다 (인라인으로 가기 위한 첫 걸음) |  |
| [151](./adr/ADR-151.md) | 🟢 유효 | 벨로나 출시 반영 |  |
| [152](./adr/ADR-152.md) | 🟢 유효 | 시스템 글자 크기를 [1.0, 1.235] 로 클램프 | **정정 1**(2026-09-01) — `TextInput` 을 자기 atom 으로 가른다(`atoms/TextInput/TextInput.tsx`). 결정 4 는 한 atom 에서 냈는데 사용처가 여섯으로 늘었고, `TextInput` 만 지는 것이 셋이다(시트 초점 · 조합 입력의 `defaultValue` · 자리표시자 색). 공유하는 클램프 산수와 글꼴은 `Text/font-scaling.ts` · `Text/app-font.ts` 에 남는다. `react-native` 직접 import 금지는 그대로고 예외 목록만 두 파일이 된다. |
| [153](./adr/ADR-153.md) | 🟢 유효 | today 는 «지금 기간» 을 읽는다 |  |
| [154](./adr/ADR-154.md) | 🟢 유효 | 캐패시터 앱의 종료 — 매니페스트 플랫폼 목록 |  |
| [155](./adr/ADR-155.md) | 🟢 유효 | 저장소를 앱 하나로 — 캐패시터 소스 삭제·모노레포 해체 |  |
| [156](./adr/ADR-156.md) | 🟢 유효 | 렌더 트리 스냅샷을 걷는다 |  |
| [157](./adr/ADR-157.md) | 🟢 유효 | 테스트 러너를 jest 하나로 모은다 |  |
| [158](./adr/ADR-158.md) | 🟢 유효 | ADR 원장에 상태 배지를 달고, 죽은 결정의 본문을 요약으로 줄인다 |  |
| [159](./adr/ADR-159.md) | 🟢 유효 | 고른 캐릭터는 스토어 하나가 갖는다 |  |
| [160](./adr/ADR-160.md) | 🟢 유효 | 당김 인디케이터는 «당긴 회차» 에만 돌고, 페이드 아래에서 돈다 |  |
| [161](./adr/ADR-161.md) | 🟡 부분 폐기 | 초상화 칸은 어느 화면에서나 같은 치수다 | **결정 3(얼굴 둘레 테두리)은 [[ADR-188]] 이 뒤집었다** — 테두리가 없어지고 그 신호는 관리 화면의 «빈 링» 으로 옮겨갔다. 결정 1(치수 한 벌)·결정 2(흐림 0.3)는 유효. |
| [162](./adr/ADR-162.md) | 🟢 유효 | 요구 레벨 미달은 '진행 불가' 배지 + 집계 제외 |  |
| [163](./adr/ADR-163.md) | 🟢 유효 | today 는 위젯 이름과 내용을 다른 크기로 다룬다 |  |
| [164](./adr/ADR-164.md) | 🟢 유효 | 보스 스케줄러 주간/월간 탭을 걷고 한 목록으로 | 탭을 걷으며 [[ADR-018]] 탭·[[ADR-019]] 결정 6·[[ADR-060]] 주기축·[[ADR-096]] 보스 축·[[ADR-145]] 결정 2 를 폐기했다. |
| [165](./adr/ADR-165.md) | 🟢 유효 | 「남은 스케줄」 칸 치수는 천장이 아니라 바닥이다 | [[ADR-163]] 의 +1px 이 10.5px 파생 상수를 스탈로 만들어 두 자리 수치가 잘렸다. |
| [166](./adr/ADR-166.md) | 🟢 유효 | 지출은 통화를 갈라 적고, 합계는 사용자 환율로만 합친다 | 구현 완료·실기 검증 완료(#187, 2026-08-25). **참조 데이터는 받았다**(`src/data/spend-catalog.json`, 24항목). **정정 1** — 카테고리 표 교체(컨텐츠·상점·편의·버프·아이템 구매·기타) · 수량 칸 추가 · 고정가라 «고르면 채워진다». **정정 2** — 메포는 메소로 환산·캐시는 안 함(운영정책 위반 경로에 값을 매기지 않는다), 관세는 메소 10% 가산, 메포 항목은 시세 필수(단위 **1억 메소당 메포** — 환산이 나눗셈이라 컬럼 이름을 `point_per_100m_meso` 로 고쳤다). **정정 3** — 결정 4 의 주가 달력 주에서 **목요일 리셋 주**로 바뀌었다([[ADR-170]] 결정 10). **정정 4** — 한도는 적어 두고 **한 건의 수량을 막는다**(`maxQuantity`, 사용자 확인값 6항목 — 몬스터 파크는 축이 셋이라 사용자가 14 로 지정). 문장 파싱도 기간 누계도 안 한다.  **정정 4** — 갈래 「상점·편의」 가 **「이벤트·BM」** 이 되고 묶음이 다시 짜였다(사용자 지정): 이벤트(출석 패스 + 보약 버프 둘) · VIP 사우나 · 기타(닉네임 변경). **보약 버프 둘이 「버프」 에서 옮겨와** 그 갈래는 이제 메소뿐이다. `category` 는 이름 자체가 값이라 **기존 기록을 마이그레이션으로 함께 옮긴다** — 안 옮기면 어느 갈래에도 없는 고아가 된다. **정정 5** — 메이플 포인트 샵은 **기간제**다. «열렸나» 를 카탈로그의 `groups.active` 가 들고(날짜로 판정하지 않는다 — 미뤄지는 날 앱이 거짓말을 한다), 안 열렸으면 **흐리게 두고 «이벤트 기간이 아닙니다»**(사용자 선택 — 숨기지 않는다). 과거 기록은 그대로 남고 수정도 된다. 품목·수량이 바뀌면 사용자가 알려 주고 데이터만 갈아 끼운다. **정정 6** — 농장 둘은 **「입장권」 을 뗀 이름**이다(사용자 지정). 이름이 기록에 글자로 박혀 있으므로 정정 4 가 갈래에 했던 **이관을 항목에도** 한다(`MIGRATE_FARM_TICKET_ITEM_RENAME` — 안 옮기면 수정 시트가 세부를 못 편다). 아이콘 표도 같이 간다 — 안 고치면 에러 없이 그림만 사라진다. **정정 7** — 퀵 패스 셋도 **「퀵패스」 를 뗐다**(묶음 이름이 맥락을 이미 든다). 이관은 정정 6 과 같은 모양이다. |
| [167](./adr/ADR-167.md) | 🟢 유효 | 하단바의 «층» 을 진짜 스택으로 — 제스처·전환은 구조에서 나온다 | 구현 완료(#240) · 실기기 미검증. [[ADR-132]] 결정 5·6·10 을 폐기했다. |
| [168](./adr/ADR-168.md) | 🟢 유효 | 아이템 분배 계산기 — 나누는 것은 수수료이지 몫이 아니다 | 구현 완료(#246) · 실기기 미검증. 유틸리티를 «도구 목록 → 하위 페이지» 구조로 정한다. **정정 5건**(결정 6·8 의 화면 표현 · 도구 이름). |
| [169](./adr/ADR-169.md) | 🟢 유효 | 가계부는 «수익·지출» 의 둘째 하위이고 캘린더가 그 자리다 | 캘린더 구현 완료(#187 첫 조각) · 실기기 미검증. 하위 탭 셋 → 둘([[ADR-132]] 결정 12 부분 폐기 — 껍데기 둘 삭제). **정정 1**(칸은 표식이 아니라 금액 두 줄 + 열지도) · **정정 2**(결정 4·8 의 «달력 주» 를 **월간 격자에 한정** — 주간 보기는 목요일 리셋 주다) · **정정 3**(정정 1 의 «수익 줄은 0 도 적는다» 를 뒤집는다 — **둘 다 0 이면 비운다**. 안 한 날이 대부분이라 격자가 「0」 으로 덮여 실제 숫자가 묻혔다. 자리는 그대로 지킨다) · **정정 4**(열지도 타일이 좌우로만 물러나 있어 칠해진 날이 세로로 붙었다 — 네 방향을 맞춘다. [[ADR-172]] 로 칸이 실제로 칠해지면서 드러났다) · **정정 5**(그날 상세가 «격자가 덮는 범위» 의 칸 금액 표를 보고 서 있어, 고른 날이 그 범위 밖으로 나가면 상세가 통째로 사라졌다 — 8/25 를 고른 채 7월로 옮기면 「기록이 없어요」. 합계를 그날 줄들에서 내고(`dayTotalsOf`) 빈 상태 판정도 그 날 자신의 기록이 낸다. 목이 범위를 안 지켜 테스트가 못 잡았다). 담기는 것은 [[ADR-166]]·[[ADR-170]] 과 #239 가 정한다. |
| [170](./adr/ADR-170.md) | 🟡 부분 폐기 | 가계부는 손으로 적고, 자동으로 흘러든 것과 테이블로 가른다 | **결정 11 의 «합계 카드를 두지 않는다» 는 [[ADR-184]] 가, 정정 3 의 «화살표는 안 죽인다» 는 [[ADR-185]] 가 (앞쪽만) 뒤집었다**(그 두 절만 죽었다). 구현 완료·실기 검증 완료(#187, 2026-08-25) — 결정 3·4 의 보스 쪽만 [[ADR-172]] 가 잇는다. 수입도 손입력(갈래 셋)이고 **자기 테이블**을 가지며, 보스 수익은 **다시 적지 않고 읽어서 표기만** 한다. 진입점은 떠 있는 ＋ 하나 + **펼침판**(갈래는 시트 밖에서 갈린다). 아이콘은 앱이 이미 고른 둘([[ADR-066]] `ProfitIcon` · 삭제된 지출 탭의 `ShoppingCartIcon`) — **새 그림 0개**. **주간 보기**를 더하고 축은 **목요일 리셋 주**다([[ADR-166]] 결정 4·[[ADR-169]] 결정 4·8 을 좁힌다). **정정 1** — `inMonth` 는 뜻만이 아니라 **이름까지** `inPeriod` 로 고친다(달을 걸치는 주에서 옛 이름이 거짓이 된다). **정정 2** — 들어오면 **주간**이다(결정 10 이 안 정해 둔 기본값). 상세가 스크롤 없이 보이고, 축이 보스 수익 탭과 같으며, 날짜를 못 캔 보스 기록도 제자리에 선다. 고른 값은 여전히 기억하지 않는다. **정정 6**(⛔ [[ADR-173]] 이 폐기) — 통화 칩(메소·메포·캐시)을 갈래 칩 밑에서 **금액 칸 아래**(억/만 줄과 같은 줄)로 옮긴다 — 통화는 갈래가 아니라 **금액의 축**이고, 붙어 있던 두 칩 줄이 한 무리로 읽혔다. 「저장」은 폼 리듬에서 한 칸(24px) 벗어난다 — 빠른 칩과 같은 간격이면 오타건이 곧 저장이다. **정정 5** — 시트 안 입력을 `BottomSheetTextInput` 으로(아톰이 판정한다). 키보드 배선은 프롭 셋이 한 벌(입력 부품·`android_keyboardInputMode`·`keyboardBlurBehavior`)이고 하나만 빠져도 «안 올라간다/두 번 올라간다/안 내려온다» 로 갈린다. **정정 8** — 가계부에 **당겨서 새로고침**이 안 붙어 있었다(사용자 지적). 당김은 **동기화 → 날짜 캐기 → 다시 읽기** 차례이고, 보스 수익 탭의 당김과 같은 재조회를 부른다(두 하위 탭이 같은 원천을 본다). 동기화가 먼저인 것이 계약 — 없는 기록은 날짜를 캘 수도 없다. **정정 7** — 접힌 다이얼이 **뒤를 막고 있었다**(사용자 보고): 줄 둘이 마운트된 채 `opacity: 0` 일 뿐이라 그 자리가 터치를 먹어 뒤의 목록이 안 눌렸다. RN 에서 투명도도 `disabled` 도 히트테스트를 안 막는다 — 줄에 `pointerEvents`, 상자에 `box-none`. **정정 4** — 지출·수입 시트의 **금액도 OS 숫자 키보드**다(사용자 지정): 그 시트는 사용처·시세 칸 때문에 어차피 키보드를 부르므로 [[ADR-124]] 결정 5 의 전제가 없다. 앱 키패드만 걷고 **초기화·억/만 줄·빠른 칩은 그대로** — OS 키패드엔 `00` 이 없어 칩이 오히려 더 중요해진다. **정정 3** — 기간 이동을 **보스 수익 탭과 같은 두 줄**로(사용자 지정). 라벨은 `formatBossProfitPeriodLabel` 을 그대로 부르고, 호출부를 잃은 `formatResetWeekLabel`·`formatMonthLabel` 은 지운다. 읽어 주는 이름(「이전 주」)과 «화살표를 안 죽인다» 는 그대로 — 없는 경계를 만들지 않는다.  **정정 9** — 아이템 판매는 **경매장 수수료(없음·3%·5%)를 뗀 값**이 수입이다. 계산은 [[ADR-168]] `netProceedsMeso` 를 그대로 부른다. 「판매 대금」 이 자기 줄을 갖고 큰 숫자는 **못 치는 합계**다(「기타」와 같은 모양). 저장은 **받는 돈 + 뗀 몫** 둘 다 — 내림 때문에 요율만으로는 판매 대금이 되짚어지지 않는다. 「판 것」 → 「판매 아이템」.  **정정 10** — 시트가 원하는 것은 **부품이 아니라 값 하나**였다. `BottomSheetTextInput`(안쪽이 RNGH 입력)이 **안드로이드 한글 조합을 깼다** — 조합 구간이 흐트러져 자모가 따로 확정된다. 아톰이 RN 입력을 그대로 그리고 `animatedKeyboardState.target` 만 직접 채운다(정정 5 의 진단은 유효, **수단은 폐기**). **정정 11** — 매니페스트의 `adjustResize` 는 **죽은 값**이다(이 앱은 edge-to-edge 라 IME 가 인셋으로만 온다). 계측: 키보드 312dp 에 창 높이 변화 0. 그 값을 받은 라이브러리가 «OS 가 했겠지» 라며 보정을 0 으로 두어 **시트가 키보드에 가렸다** — `android_keyboardInputMode="adjustPan"`(= «창은 안 움직인다, 네가 올려라»)이 사실이고 정정 5 의 프롭을 뒤집는다. **정정 12** — 자모를 가른 것은 부품이 아니라 **되쓰기**였다(정정 10 의 진단 폐기, 이번엔 계측했다). 부모가 다시 그리는 나무 안에 `value` 프롭이 있으면 IME 의 조합 구간이 깨진다(키보드·NativeWind·시트 전부 무관, iOS 도 같다). **조합이 도는 칸은 `value` 를 씨앗으로만 받는다**(`defaultValue`) — 숫자 키패드 칸만 종전대로 통제해 서식(`1,234`)을 살린다. 아톰이 키보드 종류로 가른다. **정정 13** — 입력 칸의 **상자**를 우리가 정한다. 치수를 안 주면 플랫폼 기본값이 드러나 «안드로이드 41.14dp vs iOS 20.00pt» 였고(안드로이드 쪽 군살은 EditText 기본 패딩 + 글꼴 패딩), iOS 는 **내용의 글자 종류대로 칸이 자기 키를 재서**(한글 20 · 영문 14) 타건마다 시트가 들썩였다. 아톰이 `padding: 0`·`includeFontPadding: false`·`textAlignVertical: 'center'` 를 기본으로 깔고(호출부 치수가 이긴다), 시트의 폼 줄은 `min-h-7` 로 못 박는다. `leading-*` 는 **안 통한다** — iOS 는 `lineHeight` 를 측정에 안 쓴다. **정정 14** — 시트 손질 넷(사용자 지정): 에픽던전 추가 리워드는 **수량이 없다**(계정당 주 1회라 셀 것이 없다 — «앱이 막는» 설계는 사용자가 취소했고 `maxQuantity: 1` 이면 수량 줄을 안 세운다) · 「사용처」 이름은 갈래가 정한다(아이템 구매 → **「구매 아이템」**) · `FieldRow` 의 값 자리가 **줄을 채운다**(`ml-auto` 라 `flex-1` 이 채울 자리가 없어 자리표시자가 떠 있었다) · 직접 입력 금액 뒤에 **단위**. **정정 15** — **수입에도 통화가 있다**(「기타」만): 이벤트 보상이 메포·캐시로 들어온다. 집계는 지출과 같은 규칙([[ADR-166]] 정정 2 — 메포는 시세로 환산, 캐시는 안 든다)이고 `income_records` 에 지출과 **같은 이름의 칸 셋**을 더한다. **정정 16** — 목록 타일에 **그림**이 붙는다(사용자 지정): 이름 → 파일은 **표**(`lib/spend-icons.ts` — 「솔 에르다」 는 조각 그림, 「몬스터 파크」 는 입장권이라 계산으로 못 만든다), **없는 것은 없는 채로** 두고(비슷한 그림을 갖다 붙이면 틀린 것을 그리는 셈이다), 그림 자리는 **둘**이다 — 기본은 **타일 왼쪽 끝**, **에픽던전 셋만 이름 옆**(사용자 지정 2026-08-28). 위에 얹지 않는 이유는 그림 있는 타일만 커지고 `items-stretch` 라 같은 줄이 통째로 따라 커져서다. 자리는 `spendIconOf` 가 `beside` 로 함께 돌려준다(원천으로 추론하지 않는다). 빈 자리는 안 만든다 — 폭만 먹어 이름이 «교환 / 권» 으로 끊겼다. 파일 이름은 기존 90여 개와 같은 **영문 스네이크**로 맞췄고(png 로 받은 것은 **무손실 webp** 로 — 도트라 손실 압축은 뭉갠다) 아직 안 쓰는 넷도 이름만 먼저 고쳤다. **원천은 둘**이다 — 에픽던전 셋은 아이템이 아니라 **지역 아이콘**을 그대로 쓴다(복사해 두 벌로 두면 한쪽만 갈린다). **정정 17** — 수입 갈래의 **차례**는 「사냥 · 아이템 판매 · 기타」다(사용자 지정 2026-08-29). `INCOME_CATEGORIES` 의 차례가 곧 칩이 서는 차례이자 **기본 갈래**(`[0]`)라, 손이 가장 많이 가는 계산기 갈래([[ADR-175]])가 앞에 서고 안전망 「기타」가 끝에 남는다. 이름·개수·저장 값은 안 바뀐다. |
| [171](./adr/ADR-171.md) | 🟢 유효 | 적은 것은 되돌릴 수 있어야 한다 — 그날의 목록·수정·삭제 | 구현 완료·실기 검증 완료(#187). **결정 5 는 [[ADR-172]] 가 발효시켰다**(#239). 그날 칸이 합계 아래에 **기록을 한 줄씩** 펴고, 줄을 누르면 **적을 때 쓴 그 시트**가 채워져 열린다(수정 모드 — 입력 규칙이 두 벌이 되는 것을 막는다). 삭제는 **시트 안 빨간 글자** 하나(새 제스처도 확인 모달도 안 세운다). 수정은 지우고 다시 넣지 않고 `UPDATE … WHERE id` 라 `recordedAt` 이 안 덮인다. **자동으로 흘러든 것은 여기서 안 고친다** — 보스 수익 줄은 #239 가 얹는 순간 «보스 수익 페이지로 유도»가 되고 삭제가 없다(사용자 지정). |
| [172](./adr/ADR-172.md) | 🟡 부분 폐기 | 보스 수익에 며칟날을 붙인다 — 「뒤집힌 날」을 찾아서 | **정정 1 의 «큰 것부터» 한 줄만 [[ADR-186]] 이 뒤집었다**(타일은 이제 `weekly-bosses.json` 정규 순서다 — 나머지 정정 1 은 전부 유효). 구현 완료·실기기 미검증(#239). 기간 축은 **안 바꾸고**(`daily` 없음, 사용자 지정) `defeated_on` **칸 하나**를 더한다. 날짜는 그 기간의 날짜들을 훑어 «미완료 → 완료» 로 **뒤집힌 날**이고, `date=오늘` 이 400 이라 **오늘은 소거법**으로 확정한다(어제까지 전부 미완료인데 기록이 있다 = 오늘). 못 캐면 NULL 이고 그 자리를 다시 안 판다. 조회 원장은 새로 안 만들고 [[ADR-086]] 결정 4 에 `bosses` 칸을 더한다. 드롭은 컬럼 없이 보스 행에서 **물려받는다**. **정정 5**(사용자 지정 2026-08-29) — **판 것이 하나라도 있어야 판매 줄이 선다**: 미입력만 있어도 0원으로 세우던 것을 걷는다([[ADR-124]] 의 «가격 미입력이 정상» 을 받아 «그 줄이 없으면 먹은 것 자체가 캘린더에서 사라진다» 로 세웠던 자리인데, **가계부는 돈이 오간 기록을 세는 자리**라 값 없는 건이 0원으로 서면 목록이 헐거워진다). 줄이 서면 「미입력 n」 은 그대로 적는다 — «항목» 이 아니라 저쪽에 할 일이 있다는 표시다. 그날 목록은 **캐릭터당 두 줄**(결정석·판매)이고 눌러도 안 고쳐진다 — [[ADR-171]] 결정 5 가 여기서 발효한다. **정정 1** — 결정석 줄은 **탭으로 안 가고 그 자리에서 펼쳐** 그날 잡은 보스를 초상 타일로 보여준다(사용자 지정). 결정 8 의 근거(«고치면 어느 쪽이 참인지 사라진다»)는 그대로다 — 타일은 읽기 전용이고, 사용자가 하려던 것이 고치기가 아니라 «무엇을 잡았지» 였다. 판매 줄은 여전히 탭으로 간다(「미입력」 이 저쪽 할 일을 가리킨다). **정정 2** — 타일은 **네모**이고(`BossPortrait` 에 `shape`, 기본은 원형이라 기존 호출부 무수정) 난이도는 **한 칸**(`E`·`N`·`H`·`C`·`EX`)으로 초상 위 왼쪽 아래에 겹친다 — **색은 한 값도 안 갈린다**([[ADR-147]] 정정 40). **정정 3** — 한 줄에 **여섯**(목록을 여섯씩 끊어 줄을 만든다. 고정 px 면 기기마다 다섯도 일곱도 되고, 퍼센트 `w-1/6`=16.67% 는 여섯이 100.02% 라 하나가 밀린다 — 실측)이고 초상은 44px, **보스 이름은 뺀다**(초상이 대신한다 — 읽어 주는 이름만 「난이도 + 보스」로 넓힌다). **정정 4** — 칸에 상한(`타일 + 8`)을 얹어 **좌우 간격을 어느 기기에서나 8px** 로 못박는다(없으면 넓은 기기일수록 타일 사이만 벌어진다). `flex-1` 은 안 버린다 — 좁은 기기에서 여섯이 들어가려면 칸이 상한보다 좁아져야 한다. |
| [173](./adr/ADR-173.md) | 🟡 부분 폐기 | 지출·수입 시트를 하나의 뼈대로 다시 짠다 | 결정 완료·구현 중(2026-08-26) · 실기기 미검증. 시안 3안을 상태 6개로 검토해 사용자가 상태별로 고른 결과. **큰 숫자는 화면에 하나**이고 언제나 **저장 바로 위**(직접 입력이면 치는 금액, 목록 갈래면 합계) — 같은 숫자가 두 번 나오던 것이 구조에서 사라진다. 합계 카드를 없애고 **힌트 한 줄**이 대신하되 **값이 갈릴 때만** 뜬다(캐시는 아예 없다). 고르는 것은 **세 모양** — 갈래는 칩 · 통화/형태/단계는 **세그먼트** · 빠른 금액 칩은 **없앴다**(결정 4 폐기 — 폼 안·키보드 위 어디에 두어도 안 나아졌다. 대가로 OS 키보드엔 `00` 이 없다). 관세는 큰 숫자 밑이고 켜면 합계가 **굴러 올라간다**(`AnimatedNumber`). 큰 숫자는 **칠 때는 구입가, 손을 떼면 합계**. **제목은 안 바뀐다**(목록 갈래에서 항목을 고른 뒤에만 되돌아가기). 두 단계는 **목록 갈래에만**. 수입 시트도 같은 뼈대. **목록 갈래의 합계는 언제나 메소**다(결정 11) — 메포 항목이어도 그렇고, 실제로 내는 메포는 힌트가 든다([[ADR-166]] 정정 2 ③ 을 좁힌다: 「−0」 을 금지하던 취지는 «힌트가 왜 0 인지 말한다» 로 지킨다). 큰 숫자는 **무엇을 세는지가 바뀌면 안 굴러간다**(결정 12) — 관세·수량·시세는 굴러가고 갈래·대표·단계는 갈아 끼운다(치지도 않은 금액이 줄어드는 애니메이션은 «내가 뭘 지웠나» 로 읽힌다). 정체는 값이 아니라 **세대**로 둔다 — 되돌아왔을 때 옛 기억에서 굴러 내려오지 않게. 갈래를 옮기면 **친 금액을 안 들고 간다**(결정 13). **캐릭터 고르개**를 세운다(결정 14) — [[ADR-166]] 결정 3 이 정해 두고 **컬럼만 있고 화면이 없던** 것이다. 커스텀 드롭다운 한 줄이고 **기본은 「선택 안함」**(`ocid = null` = 계정 단위). **수정 시트는 «무엇인지» 를 못 바꾼다**(결정 15) — 갈래 칩·타일 격자·되돌아가기가 없고 제목이 고른 것을 말한다(「지출 수정」 을 버린다). 다른 것으로 바꾸려면 지우고 다시 적는다. **캐릭터가 붙은 줄은 이름을 앞에 적는다**(결정 16) — `루디 · 몬스터 파크`, 보스 줄이 이미 쓰던 어법 그대로. 수입 줄도 같이(한쪽만 달면 «이 줄엔 캐릭터가 없나» 로 읽힌다). **「기타」는 지출액 × 수량**이다(결정 17) — 통화 밑에 단가를 치고 수량을 붙이면 큰 숫자가 합계가 된다([[ADR-166]] 정정 1 ③ 의 «직접 입력에는 곱할 것이 없다» 를 그 갈래에 한해 좁힌다). 아이템 구매는 관세가 붙는 자리라 그대로 둔다. **스테퍼는 숫자만**(결정 18) — 단위를 `+` 옆에 붙이던 것을 걷는다(알약이 좌우로 어긋났고, 앱에 스테퍼가 두 모양이었다). `PartySizeStepper` 의 「인」 도 함께 걷는다. **둘째 화면에는 갈래 칩이 없다**(결정 8) — 머리의 `‹` 가 되돌아가는 길이라 칩까지 두면 길이 둘이 된다. [[ADR-170]] 정정 6 을 폐기하고 `MesoAmountField`(드롭 판매가)는 안 건드린다. **정정 1** — 「아이템 구매」도 **단가 × 수량**이고 **종류**(장비·소비·기타 — 게임 인벤토리 탭 이름)가 관세를 정한다. 결정 17 이 «안 바뀐다» 로 미뤄 둔 자리인데, 사용자가 그 질문을 없앴다: 관세가 붙는 것은 **장비뿐**이다(소비·기타는 **월드 간 거래가 안 된다** — 있을 수 없는 것을 물을 수 있게 두지 않는다). 장비는 수량이 없어 **지금 모양 그대로**(치는 큰 숫자 + 관세)이고, 소비·기타는 단가 × 수량에 큰 숫자가 **못 치는 합계**이며 관세 체크가 아예 없다. 수량은 **스테퍼가 아니라 치는 칸**이다(주문서 300장을 300번 누를 수 없다 — [[ADR-175]] 결정 8 과 같은 이유). 「기타」 갈래의 스테퍼는 그대로다(거기서 세는 것은 «몇 회» 다). **결정 17 정정**(사용자 지정 2026-08-29) — 「아이템 구매」의 수량에는 **「개」를 적는다**: «단위를 안 적는다» 의 근거(«자유 입력이라 앱이 무엇을 세는지 모른다»)는 **「기타」의 것**이고, 아이템 구매에서 세는 것은 **아이템**이라 성립하지 않는다. 「기타」는 그대로 비어 있다. 종류는 `spend_records.item_kind` 에 남긴다 — 장비는 `quantity IS NULL` 로 되짚이지만 **소비와 기타가 안 갈린다**. `form` 재활용은 기각(그 칸은 카탈로그의 `forms` 뜻이다). `NULL` 은 정정 이전 행이고 **장비로 연다**(그때가 실제로 그 모양이었다). 종류를 바꾸면 **수량 1 · 관세 끔**, 친 금액은 남는다(큰 숫자는 굴리지 않고 갈아 끼운다 — 세는 것이 바뀐다). **관세도 라벨–값 줄이 된다**(결정 6, 사용자 지정) — 큰 숫자 밑의 맨몸 체크박스가 「관세 [없음\|10%]」 세그먼트로, 자리도 큰 숫자 **위**로 올라간다. 짝은 수입 시트의 「수수료 [없음\|3%\|5%]」 이고, 결정 5 의 «굴러 올라간다 · 더해지는 금액을 안 적는다» 는 그대로다(결정 5 는 🟡 — 자리와 모양만 갈렸다). **함께 고친 결함** — 관세 기록을 다시 열면 관세가 **두 번** 붙었고(935,000,000 → 1,028,500,000, 재현함) 「기타」는 합계가 «총액 × 수량» 이 됐다. 되짚는 식 하나로 셋이 맞는다: **단가 = (총액 − 관세분) ÷ 수량**. **결정 1 의 「직접 입력이면 치는 금액」 과 결정 6(칠 때는 구입가, 손을 떼면 합계), 정정 1 결정 2 의 「장비는 여전히 친다」 는 [[ADR-202]] 가 뒤집었다.** 큰 숫자는 이제 일곱 자리 모두에서 못 치고, 금액은 폼마다 라벨–값 줄에서 받는다. 나머지 결정은 그대로다. |
| [174](./adr/ADR-174.md) | 🟢 유효 | 드롭 연출은 프레임을 갈아끼우지 않고 켠다 — 배경은 창 크기를 숫자로 | 구현 완료·실기 검증 완료(2026-08-26, 갤럭시 Z Flip3 릴리스 빌드). [[ADR-048]] **결정 6(프레임 프리로드)이 RN 포팅에서 빠져 있던 것**을 되살린다 — 첫 재생에서 빈 프레임 40장 → 0장. 예열 층은 둘로 갈라 **기둥(pre·loop·end)만 붙들고** 버스트(screen)는 놓아 준다(다 붙들면 캐시 한도를 넘겨 되레 16장). 배경 `<Svg>` 의 퍼센트 크기를 버린다 — 모달이 `880 → 833.67 → 880dp` 로 늦게 커지는데 퍼센트가 가운데 값에서 굳어 화면 아래 46dp 로 시트가 비쳤다. **정정 1** — 예열만으로는 부족했다(캐시 한도를 넘겨 일부가 밀려남). **전 프레임 마운트 + `opacity` 전환**으로 갈아끼우기 자체를 없애고, 게이트를 버스트로 좁히고(대기 0.6→0.13초), 그림이 바뀔 때만 렌더한다. 그리고 **실제 앱·실제 경로에서 재기 전에는 발행하지 않는다**(그 전에 세 번 쏘고 세 번 되돌렸다). |
| [175](./adr/ADR-175.md) | 🟡 부분 폐기 | 「사냥」 수입은 적는 것이 아니라 **계산되는 것**이다 | 구현 완료(2026-08-28) · 실기기 미검증. 가계부 수입 시트의 「사냥」 갈래가 자유 입력에서 **계산기**가 된다 — 지역·사냥터·효율·버프·소재(30분)를 고르면 메소가 나오고 큰 숫자는 **못 치는 합계**다([[ADR-170]] 정정 9 ④ 와 같은 모양). 참조표는 `src/data/hunting-grounds.json`(지역 21 · 사냥터 408, **전부 사용자 제공** — [[ADR-006]]). 계산은 «분당 8젠 × 몬스터 레벨 × 7.5» 에 **레벨 페널티 표 둘**(방향마다 다르고, 낮은 쪽은 감소폭이 고르지 않아 배열이 곧 사실이다)·아이템(유니온의 부 +50% · 소형 재물 획득의 비약)을 곱하고 **끝에서 한 번 내림**한다 — 단 **아이템 둘을 한 통에 넣어 ×1.7 을 내던 것은 [[ADR-177]] 정정 1 이 뒤집었다**(재획비는 통 밖에서 ×1.2 라 ×1.8 이다). **사냥 효율은 맵이 정한다**(결정 3-1) — 고르는 축은 «몇 마리를 놓치나»(0~4)이고 %는 `(마릿수−놓침)/마릿수` 를 반올림한 **라벨**이다(40마리 −1 = 98% · 15마리 −1 = 93%). **반올림은 라벨에만** 하고 셈은 분수 그대로이며, 저장하는 것도 %가 아니라 **놓친 마릿수**라 맵을 바꿔도 고른 조각이 살아남는다. 사냥터 요약 줄은 **감소한 마릿수**를 적는다. 몬스터 레벨이 둘인 맵은 **레벨별로 계산해 평균**낸다(평균 레벨 하나로 접으면 차이가 소수가 되어 정수 표에 안 들어간다). 지역 목록은 **아래로 20 ~ 갈 수 있는 데까지**이고(위아래 대칭이 아니다 — 캐릭터보다 높은 지역엔 못 간다: lv.277 은 도원경까지) 두 끝을 **몬스터 레벨**로 잰다(참조표의 `minLevel`·`maxLevel` 은 **추천 레벨**이라 어긋난다 — 리버스 시티는 추천 205-209 인데 몬스터가 213 까지라 lv.213 이 가장 잘 잡는다). 「±20 · 추천 레벨」 로 잡았던 첫 판을 사용자가 두 번 고쳐 준 결과이고, 그 과정에서 소멸의 여로·리버스 시티의 상한 290 이 **오기**로 밝혀져 200-209·205-209 로 정정됐다. **센 값에는 `≈` 를 붙인다**(사용자 지정 2026-08-29 — 「획득 메소」 줄과 **합계** 둘 다. 미리 세어 둔 값이지 받은 액수가 아니라 표식이 없으면 정산된 금액처럼 읽힌다. 0 에는 안 붙는다). 캐릭터를 안 고르면 페널티 0 이다(그 사실을 말하던 **안내 한 줄은 2026-08-29 걷었다** — 빈 캐릭터 고르개가 이미 그 말을 하고, 문장이 계산기 한가운데서 자리만 차지했다). **사냥터는 레벨 차이가 적은 순 · 같으면 마릿수 많은 순**으로 줄 세운다(결정 6-1) — 참조표 순서면 «지금 갈 만한 곳» 이 한가운데 묻힌다. 레벨이 둘인 맵의 차이는 레벨마다 재서 평균이고(0.5 가 나온다), 레벨을 모르면 마릿수 순으로 접히며, 거르지는 않는다. 솔 에르다 조각은 **직접 입력**(추정하면 틀린 값을 확신 있게 적는 셈)이고 **스테퍼가 아니라 치는 칸**이다(30분에 10개 내외라 8소재면 80개가 넘는다 — 처음에 스테퍼로 냈던 것을 사용자가 지적해 고쳤다. «소재당 개수 × 소재 수» 는 저장되는 것이 실제 개수가 아니라 어림이 되어 버렸다). 계산 입력 여섯 칸을 `income_records` 에 남겨 [[ADR-171]] 결정 2 의 «그 시트가 채워져 열린다» 를 지킨다 — 그전 행은 NULL 이라 옛 모양으로 연다. 포스는 **아이콘 + 숫자 배지**이고 에셋 그룹 `force` 를 새로 등록한다([[ADR-129]]). `SelectField` 는 `renderOption` 만 넓히고 기존 호출부는 무수정([[ADR-173]] 결정 14). **[[ADR-201]] 이 결정 1 과 결정 9 끝절을 넓혔다**(2026-09-02). 계산기가 기본이고 수동 입력이 곁에 서며, 옛 행은 그 수동 입력 폼으로 열린다. |
| [176](./adr/ADR-176.md) | 🟢 유효 | `NOT NULL` 을 떼려면 **테이블을 다시 쓴다** (그리고 목은 그 제약을 못 잡는다) | 구현 완료(2026-08-28) · 실기기 미검증. 이슈 #265 — 「기타」를 메포·캐시로 적으면 저장이 실패했다: `income_records.meso_amount` 가 `NOT NULL` 인데([[ADR-170]] 결정 1, 수입이 메소뿐이던 시절) [[ADR-170]] 정정 15 가 «메소로 번 것이 아니다» 를 `null` 로 적기 시작했다. **`0` 으로 채우지 않는다** — 그러면 «메소를 0 벌었다» 와 구분이 사라지고 수정 시트가 통화를 되짚을 자리를 잃는다. SQLite 는 `ALTER` 로 `NOT NULL` 을 못 떼므로 **테이블을 다시 쓴다**(새 테이블 → 복사 → DROP → RENAME, **한 트랜잭션**). 옮길 칸은 **옛 테이블이 실제로 가진 칸**이다(`PRAGMA table_info`) — `SELECT *` 는 `ensureColumn` 이 뒤에 붙인 칸 때문에 순서가 어긋나 값이 옆 칸으로 옮겨 앉고, 지금 스키마를 박아 두면 칸이 모자란 옛 기기에서 던진다. `ensureColumn` 들보다 **먼저** 돌고, 판정은 «`meso_amount` 가 `NOT NULL` 인가» 하나라 이미 nullable 이면 **한 문장도 안 나간다**. DDL 본문은 **한 벌**을 두 자리가 쓴다. **목은 제약을 못 잡는다** — `node:sqlite` 로 `SqlitePort` 를 구현해 진짜 엔진 위에서 태우는 경로를 둔다(새 의존성 0). 도착점은 **지출과 같은 모양**이다(`spend_records.meso_amount` 는 [[ADR-166]] 정정 2 때부터 nullable). **그 자리에서 하나 더 나왔다** — 통화 칸 셋이 `ensureColumn` 에만 있고 `CREATE TABLE` 문에는 없었다([[ADR-170]] 정정 15). «칸 없이 만들고 곧바로 ALTER» 라 에러 없이 돌던 것이 재작성에서 드러났다: **재작성이 DDL 의 진실성을 처음으로 강제한다** — 이제 칸을 더할 때는 CREATE 문과 `ensureColumn` **두 자리**를 함께 고친다. |
| [177](./adr/ADR-177.md) | 🟢 유효 | 메소 획득량은 **캐릭터에서 읽는다**, 그것도 «최대 세팅» 으로 | 구현 완료(2026-08-28) · 실기기 미검증. [[ADR-175]] 의 계산식이 메소 획득률을 **아이템 둘**(유니온의 부 +50% · 소형 재획비 +20%)로만 잡았는데, 실제 메획의 큰 몫은 **캐릭터에 박혀 있다**(장비 잠재·에디셔널 · 어빌리티 · 심볼 · 유니온 공격대 · 유니온 아티팩트 — 사용자 캐릭터 **149%**). 빼고 세면 사냥 메소가 절반 아래로 나온다. **환산값(`character/stat`)은 안 쓴다** — 이유가 둘이고 서로 **별개**다: ① 그것은 «현재 세팅» 이라 사냥 세팅이 아닐 수 있다(실측 13명 중 **7명이 현재 ≠ 최대**, 최대 격차 **+120%p**) ② `히니` 는 장비·어빌·심볼이 날짜별로 **완전히 동일한데** stat 메획만 **161→273→156→161** 로 출렁였다 — 스냅샷 시점의 **일시 버프가 섞인다**. 그래서 **소스 다섯을 직접 파싱**하고(정규식 하나가 `+20%` 와 `20% 증가` 를 함께 받는다 · 심볼만 `symbol_meso_rate` 전용 숫자 필드) **최대 세팅**을 낸다. 장비는 **잠재+에디셔널 합에 100% 캡**이고(사용자 확정) **프리셋 단위**로 건다 — 합산 뒤 캡은 틀린다. 심볼은 캡 밖. 최대치는 **축별 최댓값의 합**이다(장비·어빌 프리셋이 독립 전환이라 조합을 다 셀 필요가 없다). **함정 셋** — ① `union_raider_preset_1~5` 는 **전 계정 null** 인 죽은 필드(현재값 폴백, 되살아나면 자동으로 잡히게) ② 아티팩트 **크리스탈은 이중 계산**이다(수치 없는 이름이고 `effect` 에 이미 합산됨) ③ 현재 적용본과 프리셋 배열이 같이 오므로 **전체를 훑으면 부풀려진다**. 메획은 **합산 통**에 든다. **정정 1**(사용자 지정) — 셋을 한 통에 넣었던 것을 고친다: **재획비만 통 밖**이라 `(기본 100% + 템메획 + 유니온의 부 + 어빌/유니온/스킬) × 1.2` 이고, 149% 캐릭터가 둘 다 켜면 **×3.588**(결정 6 이 내던 ×3.19 가 아니다). 이것이 [[ADR-175]] 결정 3 의 «둘 다 켜면 ×1.7» 도 **×1.8 로 뒤집는다** — 그때 «아니다» 로 물리친 쪽이 맞았다. `MESO_BOOSTS` 가 `kind`(`additive`/`multiplier`)로 거는 자리를 들고 `boostPercentOf`/`boostMultiplierOf` 가 갈린다. **`id` 는 안 바꿨다**(그전 기록의 `hunt_boosts` 가 안 끊긴다) — 다만 재획비를 켰던 옛 행은 **수정으로 열면 금액이 올라간다**(옛 값이 틀렸던 것이다). 칩 글자도 가산 `+50%` · 곱셈 `×1.2` 로 갈린다. **정정 2**(사용자 지정) — 줄에 적히는 수는 **켠 아이템까지 반영한 «증가량»**이고 **소수점을 버린다**: `floor((100 + 가산)×배율) − 100`. 칩을 켜면 숫자가 오르고(149 → 199 → **258**) 재획비만 켜도 오른다(198). **자른 값이 돈을 세지는 않는다**([[ADR-175]] 결정 3-1 ①과 같은 규칙 — 줄은 258% 여도 곱은 358.8%). 치는 칸일 때는 캐릭터 메획을 치고 총합이 옆에 선다(`[149]% → 199%`). 저장되는 것은 여전히 캐릭터 메획이다. **정정 3**(사용자 보고) — 그 줄이 **시트를 82vh 너머로 밀어 스크롤이 났다**(계산기는 고른 것이 늘수록 줄이 선다 — 사냥터를 고르면 요약 줄 ~34dp, 캐릭터를 고르면 메획 줄 ~41dp, `gap-3` 라 줄 하나가 곧 «높이 + 12dp»). **나중에 들어온 메획 줄을 칩 줄로 접는다**(`메소 획득량 258%  [칩][칩]`) — 요약 줄은 [[ADR-175]] 결정 11 이 세운 자리라 뒤집지 않는다. 숫자와 그것을 움직이는 칩이 붙어 오히려 잘 읽힌다. **폴백(치는 칸)일 때만 자기 줄**이다. ⚠️ dp 는 클래스에서 뽑은 계산이고 **기기 실측이 아니다**. **정정 4**(사용자 지정) — 그래도 스크롤이 남았고, 원인은 높이가 아니라 **줄바꿈**이었다: 칩이 글자라 「소형 재물 획득의 비약 ×1.2」 가 길어 `flex-wrap` 이 칩을 **둘째 줄로 내렸다**(그 줄은 한 줄이 아니라 두 줄이었다). **칩을 그림으로 바꾸고**(`MESO_BOOSTS[].icon` → `assets/items/` · 이름은 `aria-label` 로만) **증가율을 안 적으며**(이미 아는 값 — 정정 1 의 «칩에 적어 보인다» 를 걷는다, 계산 규칙은 그대로) 줄을 **「라벨 왼쪽 · 수와 칩 오른쪽」**으로 세운다(`메소 획득량  258%  (유니온의부)(재획비)`). 폴백일 때 이 줄의 라벨은 「메소 획득률」 로 돌아간다(두 줄이 같은 이름을 달지 않게). 화면에서는 **자동값이고 못 친다**(큰 숫자와 같은 논리) — 못 읽었을 때만 **손입력 칸**이 되고 기본값은 그 캐릭터의 **마지막 성공값**이다. 기록에 **그때의 메획**을 남긴다(`hunt_meso_rate` — 캐릭터 레벨을 박아 둔 것과 같은 이유. `NULL` 은 그전 행이고 **0 으로 읽는다**). 호출은 **캐릭터를 고를 때 다섯 번 병렬**이고 ocid 별 캐시는 **TTL 이 없다**(장비를 바꿀 때만 변하고 낡은 값은 폴백 기본값 말고 안 쓰인다). |
| [178](./adr/ADR-178.md) | 🟡 부분 폐기 | 시트는 **갈래마다 자기 폼**을 갖고, 칸의 상자는 **못 박는다** | 구현 완료(2026-08-29) · 실기기 미검증. 가계부 두 시트가 각 1,000줄이고 갈래별 차이를 **한 함수의 조건문**으로 가르던 것을 **갈래마다 폼 컴포넌트**로 가른다. 사용자가 실기에서 본 넷 중 **①칠 때마다 흔들림 ②초점 시 글자 위 잘림 ③친 글자와 고정 글자 높이 불일치는 뿌리가 하나**다 — 아톰이 두 플랫폼을 맞추려 패딩·글꼴 여백을 지운 탓에([[ADR-170]] 정정 13) 칸의 상자가 **글리프 상자만큼**으로 줄었고, 시트가 `enableDynamicSizing` 이라 **잰 높이가 내용을 타면 흔들림이 시트 전체로 퍼진다**. 그래서 **높이를 내용에서 뗀다** — 줄의 칸은 `FieldTextInput`(20dp), 큰 숫자는 38dp 이고 **치는 칸과 못 치는 글자에 같은 수**를 준다(갈래마다 그 자리가 칸도 글자도 되어서 튀었다). **아톰은 안 고친다**(앱 전역의 칸이다). ④갈래를 옮겨도 값을 들고 다니던 것은 **상태가 컴포넌트에 안 매여** 있어서다 — 폼이 캐릭터부터 저장까지 다 들고, 갈래를 옮기면 **언마운트**되어 «옮길 때 무엇을 지울까»([[ADR-173]] 결정 13)가 규칙이 아니라 **기본값**이 된다. 자리표시자 「비워 둬도 됩니다」는 **무엇을 적는 자리인지**로 바꾼다(아이템 칸은 「아이템 명」). 리팩터는 **옮기는 것이지 바꾸는 것이 아니다** — 모양·글자·`testID` 를 안 바꿔 기존 시트 테스트가 그대로 안전망이 된다(수입 75건이 그대로 통과했고, 고친 하나는 «갈래를 옮겨도 친 금액은 남는다» 를 박아 둔 것이라 그것이 곧 고치려던 동작이었다). 수입 시트는 **1,028줄 → 껍데기 168줄 + 폼 넷**, 지출 시트는 **1,085줄 → 껍데기 110줄 + 폼 셋**으로 갈렸고 `key={category}` 하나가 결정 3 을 실현한다(지출 120건은 **손 안 대고** 통과했다). 지출은 **머리줄과 갈래 칩까지 폼 안**이다 — 목록 갈래는 둘째 화면에서 칩이 걷히고 제목이 되돌아가는 누르개가 되어([[ADR-173]] 결정 8·15) 서는 자리가 갈래마다 다르고, 껍데기가 그리려면 `choice` 를 끌어올려야 해 갈라 놓은 뜻이 반쯤 사라진다. 껍데기로 올린 것은 **스크롤 열쇠 신호 하나**뿐이다. **정정 1**(사용자 보고) — 56억 기록을 닫고 121억 기록을 열면 **가격이 오르는 애니메이션**이 났다. 카운트업 기억이 모듈 수준인데([[ADR-087]] 결정 8) `AmountFigure` 는 이름표를 안 넘기면 **`testID` 를 정체로 쓰고**, 그것이 고정 문자열이라 다른 기록도 같은 칸으로 보였다. **지출은 이미 막고 있었고**([[ADR-173]] 결정 12) **수입이 이름표를 한 번도 안 넘겼다** — 갈래별 폼 이전부터 있던 결함이다. 이제 **폼마다 마운트 때 새 이름표**를 받는다(발급기 이름도 `cashbook-amount-` 로 바로잡음). 갈라내며 지출 「기타」가 이름표를 잃은 것도 되돌렸고, 두 시트에 회귀 테스트를 세웠다(그 회귀를 잡는 테스트가 하나도 없었다). **정정 2**(사용자 보고) — 상자 높이를 못 박았는데도 **친 값과 고정 글자의 높이가 안 맞았다**(단위 「메소」가 숫자 기준선 위로 떴다). 원인은 높이가 아니라 **기준선**이다: 그 줄은 `items-baseline` 인데 **Yoga 는 글자 노드에만 기준선을 주고** `TextInput` 은 **상자 밑변**으로 떨어진다. 두 글자 크기의 기준선 차이를 **픽셀 상수로 적지 않는다**(글꼴 지표에 기대면 글꼴이 바뀌는 날 어긋난다) — 대신 **상자와 기준선을 `Text` 가 만들고 치는 칸을 그 위에 겹친다**(못 치는 자리에서는 그 글자가 곧 보이는 숫자다). 글꼴·크기·줄높이가 같으니 기준선은 **정의상 같다**. `leading-none` → `leading-[38px]`(ascent 가 안 잘리는 진짜 처방)이고 결정 1 의 `height: 38` 은 걷었다. ⚠️ **기기에서 못 재 봤다** — 시뮬레이터를 몰 수단(idb)이 없다. **정정 3**(사용자 보고, 두 번째) — 정정 2 로도 단위가 떠 있었다. 정정 2 도 결국 **Yoga 의 기준선 계산에 기댄** 구조였고(`items-baseline`), 그 계산이 노드 종류마다 어떻게 갈리는지 이 저장소에서 **재 본 적이 없다**(추정으로 두 번 빗나갔다). 그래서 **기준선 정렬 자체를 버린다** — 위에서 맞추고(`items-start`) 숫자 상자와 단위 상자에 **같은 줄높이(38)** 를 주며, 단위 줄에 **숫자와 같은 크기(30)의 폭 0 글자**(`\u200B`)를 심어 그 줄의 지표를 맞춘다. 그러면 기준선은 **정의상 같은 자리**이고, 두 글꼴 크기의 차이를 **픽셀 상수로 적지 않아도 된다**. 클래스 문자열에는 보간을 못 하므로(NativeWind 는 빌드 때 읽는다) 같은 수를 두 곳에 적고 **그 일치를 테스트가 지킨다**. 또 어긋나면 다음은 추정이 아니라 **계측**이다. **정정 4** — 네 번째 화면이 변수를 갈랐다: 같은 줄·같은 정렬·같은 단위인데 **값이 `Text` 인 줄만 맞고**(획득 메소) **`TextInput` 인 줄은 안 맞았다**(조각·조각 가격). 정렬도 단위도 원인이 아니었고(정정 1~3 이 전부 그 둘을 만져서 안 고쳐졌다) 진짜 원인은 **`TextInput` 의 글자가 `Text` 와 다른 자리에 앉는다**는 것이다. 그래서 **그리는 일을 칸에서 뺏는다** — 보이는 글자는 언제나 `Text` 이고 칸은 `color: 'transparent'` 로 그 위에 얹혀 입력과 커서만 맡는다(큰 숫자도 같다). **조합이 도는 칸에는 안 쓴다**(한글 IME 는 부모 상태가 한 글자 늦어 조합 중인 글자가 안 보인다 — 그런 칸엔 단위도 없다). 자리표시자·커서는 그대로 산다(둘 다 글자색과 따로 논다). 결정 1 의 고정 높이는 걷었다. **배운 것: 세로 정렬은 재기 전에 고치지 않는다** — 갈린 것은 사용자 화면에 **대조군이 함께 찍혔을 때**다. **정정 5**(사용자 지정) — 사냥 폼의 줄 배치 셋: **지역·사냥터를 한 줄**로(둘은 «어디서» 하나를 정하는 짝이고 줄을 따로 쓰면 시트가 길어진다 — `SelectField` 는 안 건드리고 각각을 `flex-1` 로 감싼다), **켜고 끄는 것 둘을 값 줄 위**로(켜는 자리와 세어진 값이 위아래로 갈린다), **알약 테두리를 걷고 체크박스**로(원형 테두리는 «고르는 하나» 로 읽히는데 이 둘은 동시에 켜진다 — 읽어 주는 것도 `role="checkbox"`·`aria-checked` 로 갈린다). 그 줄의 라벨은 **「소비 아이템」**이다(「메소 획득률」은 아래 「메소 획득량」과 한 글자만 달라 같은 것을 말하는 줄로 읽혔다 — 이 줄이 묻는 것은 «무엇을 켰나» 다). **「메소 획득량」 줄은 언제나 선다** — 캐릭터를 안 골랐어도 **0%** 로 적힌다(안 세우면 고르는 순간 줄이 생겨 아래가 밀리고, «메획이 안 든다» 는 사실도 화면이 안 말한다). 켠 것은 캐릭터가 없어도 든다(유니온의 부만 켜면 50%). **치는 칸이 되는 것은 캐릭터를 골랐는데 못 읽었을 때뿐**이다. 지역·사냥터의 폭은 **2 : 3** 이다(지역은 길어야 「츄츄 아일랜드」 인데 사냥터는 「풍화된 기쁨과 분노의 땅」 까지 간다). 「캐릭터를 고르면 레벨 차이가 반영돼요」 안내 줄은 **걷었다**([[ADR-175]] 결정 6 정정 — 빈 캐릭터 고르개가 이미 그 말을 한다). **정정 6**(사용자 지정) — **수입 시트는 머리에서 날짜를 바꾼다**: «날짜는 캘린더에서 고르는 것» 이라 시트가 적기만 하던 자리인데, **날을 잘못 골랐다는 것을 아는 자리가 시트**라 닫고 다시 여는 것은 친 것을 버리는 일이다. **하루씩** 옮긴다(멀리 뛰는 것은 캘린더가 이미 한다 — 원래 결정이 막으려던 중복은 그대로 지켜진다). 수정 모드에서도 바꿔 그 기록이 **다른 날로 옮겨 가고**, 날짜를 바꿔도 **친 것은 안 사라진다**(폼의 열쇠는 갈래뿐이다). 셈은 `shiftDateKey`(UTC — 기기 표준시로 세면 자정 언저리에 하루가 밀린다). **정정 7** — 지출 시트도 같게 했다(두 시트가 한 뼈대라 한쪽만 되면 «왜 저기선 안 되나» 가 된다): `DateStepper` 를 `sheet-fields` 로 옮겨 **부품을 한 벌**로 두고 `testID` 뿌리만 각 시트가 준다. 목록 갈래의 **둘째 화면에서도 산다**(되돌아가는 누르개가 선 그 줄이고, 머리줄이 폼 안이라 신호만 껍데기로 올린다 — 스크롤 열쇠와 같은 모양). **정정 2·3 의 치수는 [[ADR-202]] 결정 10 이 바꿨다** — 큰 숫자가 24px 이고 줄 높이는 계단표의 31px 이라 `leading-[38px]` 임의값이 사라졌다. 줄 높이를 글자보다 크게 잡는 규칙과 단위를 같은 줄 상자로 맞추는 처방은 그대로다. |
| [179](./adr/ADR-179.md) | 🟢 유효 | 다크에서 시트는 **자기 표면을 한 칸 올린다**, 경계는 테두리가 아니라 면이다 | 구현 완료(2026-08-29) · 실기기 미검증. 사용자 보고 둘(자리표시자가 안 보인다 · 시트와 백드롭이 안 갈린다) 모두 다크 전용. **스크림 쪽으로는 못 고친다** — 시트 몸통이 `definition.bg` 즉 **자기가 덮는 페이지와 같은 토큰**이라, 스크림을 완전 불투명 검정으로 만들어도 대비 **1.07 이 천장**이다(실측 1.03~1.05, 라이트는 같은 코드가 4.18~4.29 — 거기는 L 0.95→0.55 로 0.40 을 내려갈 수 있고 다크의 `bg` 는 이미 L 0.13~0.15 라 아래에 여유가 없다). 그래서 **시트를 밝게** 한다: `.media-scope`([[ADR-064]] 결정 5)와 **같은 기법**으로 시트가 감싸는 서브트리에 `bg`·`surface`·`surface-2`·`track` **넷을 한 칸 위로 다시 선언**한다(`buildSheetScopeVariables`). **넷을 함께 올리는 것이 핵심** — 몸통만 올리면 `bg-surface` 타일이 몸통과 같은 색이 되고(1.00) 몸통을 더 올리면 타일이 몸통보다 어두워진다. 계열째 올리면 안쪽 위계가 그대로라 **시트 안 코드는 한 줄도 안 고친다**(세 시트와 폼 열몇 개의 `bg-bg`·`bg-surface`·`bg-surface-2` 가 그대로 산다). 한 칸 = **OKLCH L +0.09**(`deriveMediaScope` 가 쓰는 폭과 같은 수 — 새 눈금을 만들지 않는다). 결과 대비 1.19~1.25 · ΔL 0.133~0.139. **대비비가 아니라 ΔL 을 함께 본다** — 새까만 근처에서 WCAG 비는 분모의 +0.05 항이 지배해 눌린다(판단은 시안을 보고 사용자가 했다, [[ADR-064]] 의 «관문이 아니라 참고 수치» 와 같은 자리). 라이트는 안 건드리고 분기는 **반드시 `definition.mode`**([[ADR-064]] 결정 8 — 이름으로 가르면 그것이 폐기한 `DARK_THEMES` 수동 목록이 되살아난다. `resolvePanelBorder` 가 이미 같은 모양이다). **시트 위 테두리 1px 을 지운다**([[ADR-039]] 결정 2 폐기) — 그 선은 면이 경계를 못 만들던 시절의 대타였다(사용자 지정). 대가: 혼테일 `bg` 는 채도 0(`#0B0B0B`)이라 올리면 `#1F1F1F` **중성 회색**이 된다(다른 둘은 색조가 따라온다) — 그 테마의 `bg`·`surface` 가 서로 거의 같은 예전 값이라는 것은 따로 남은 문제다. 자리표시자는 **`placeholderTextColor` 를 앱 전체에서 한 번도 안 주던 것**이 원인 — `app.json` 이 `userInterfaceStyle: "automatic"` 이라 플랫폼 기본값이 **OS 외관**을 따라서, OS 가 라이트인 채 앱만 다크면 iOS 가 `#1A1A1C`(대비 **1.13**)를 그린다. **아톰 하나가** `placeholderTextColor={definition.textDisabled}` 로 건다(화면마다 적으면 빠진 자리가 조용히 플랫폼 기본값으로 남는다). `className`(`placeholder:text-text-disabled`)으로 먼저 썼다가 물렀다 — 그 변형은 **native 프리셋에서만** `placeholderTextColor` 로 컴파일되는데 **jest 의 `globalSetup` 은 `NATIVEWIND_OS` 를 안 세워 web 프리셋으로 돈다**(`::placeholder` 의사요소가 나온다). 앱에서는 되는데 테스트로는 못 보는 자리라 계약을 못 지킨다. ⚠️ **따로 남은 문제**: jest 와 Metro 가 다른 프리셋으로 컴파일한다 — `nativewind.config.js` 가 못박은 «둘이 같은 값으로 컴파일한다» 가 플랫폼 분기 유틸리티(`placeholder:`·`shadow-*`)에서는 안 지켜진다. `text-muted`(6.5~8.1)가 아닌 이유는 **자리표시자가 값이 아니라 힌트**라서다. |
| [180](./adr/ADR-180.md) | 🟢 유효 | 떠 있는 것의 층은 **형제 순서**가 정한다 — 화면이 소유한 오버레이는 «바 위 슬롯» 에 그린다 | 구현 완료(2026-08-30) · 실기기 미검증. 사용자 보고 — 펼침판(＋)을 펴도 하단바만 안 흐려진다. **`zIndex` 로는 못 고친다**: 스크림은 화면 **안**이고 바는 화면들 **뒤**(`Main` 의 `layout`)라 부모가 달라 겨룰 자리가 없고, 화면을 통째로 올리면 바가 흐려지는 대신 **사라진다**(화면은 불투명). 조사에서 나온 실제 층은 «시트 호스팅 컨테이너 < 벽지 < 화면·바 < 안내 < 토스트 < **시트**» — **시트는 이미 바 위다**(`PortalProvider` 가 `{children}` **뒤에** 루트 호스트를 붙인다). 그래서 같은 기구를 한 번 더 쓴다: `Main` 의 `layout` 이 바 **뒤에** 포털 호스트를 두고(`BottomBarOverlayHost`), 펼침판은 **통째로** 그리로 나간다(스크림만 내보내면 FAB 이 그 밑에 깔린다). 열린 채 바를 누르면 이제 이동이 아니라 **닫힘**이다. 포털로 나간 그림은 화면이 숨어도 안 숨으므로(탭은 서로 언마운트되지 않는다) **슬롯 자신이 초점을 판정한다** — `useIsFocused` 는 내비게이터 밖에서 던져서 `NavigationContext` 를 옵션으로 읽는다. **대가**(사용자 선택): 층 전환에서 FAB 이 화면과 함께 미끄러지지 않고 즉시 사라진다(탭 전환·하위 페이지는 종전과 같다). |
| [181](./adr/ADR-181.md) | 🟢 유효 | 「남은 스케줄」은 열이 아니라 **주기 탭**이다 — 수치는 「N개」, 0은 언제나 `CLEAR` | 구현 완료(2026-08-30) · 실기기 미검증. 시안 두 축(열 표 / 주기 탭)을 견줘 사용자가 탭을 골랐다 — **검마가 열 하나를 값하지 못한다**(한 달에 한 번, 그것도 일부 캐릭터만)는 것이 갈림길이었다. 세그먼트 셋이 제목 줄의 **`N개` 자리**에 서고(줄을 새로 안 쓴다 — +6px), 목록은 **그 탭의 수치만** 그린다 — 값이 있는 갈래가 자기 이름과 함께 서므로 「퀘스트 4개」·「보스 1개」이고, 주간만 그 둘이 나란히 선다. 수치는 **「N개」**이고 0 이면 **언제나 `CLEAR`** — 대상이 애초에 없던 캐릭터도 같은 배지라, 이 배지의 뜻은 「완료했다」가 아니라 **「이 주기에 지금 할 게 없다」**다(그래서 분모를 안 센다). 완료해도 목록에서 **안 빠진다**(사람 수가 늘 같아야 «어제와 같은 화면»). **정정 1** — 순서는 «그 탭에 남은 개수 많은 순 → 동수면 관리 순서 → 실패는 맨 아래» 이고 **탭마다 다시 선다**(결정 6 의 «다시 세우지 않는다» 를 뒤집는다). 정렬을 위젯이 하므로 뷰모델은 관리 순서까지만 세우고 `remainingTotal` 은 사라진다. 합계는 그리지 않아 뷰모델의 `scheduleTotal` 이 사라진다. [[ADR-147]] **정정 12·35·36·37·40 을 폐기**하고(2×2 격자와 「줄도 칸도 그 행이 정한다」), 정정 3(「검마」)과 [[ADR-165]](칸 치수는 바닥)는 유효하다. |
| [182](./adr/ADR-182.md) | 🟢 유효 | 공유 컨텐츠는 **두 열**로 서고, 완료를 **체크와 취소선**으로 말한다 | 구현 완료(2026-08-30) · 실기기 미검증. 위젯 9 가 계열 셋을 세로로 쌓아 오른쪽 절반이 통째로 비어 있었다 — **두 열**로 세우고, 열은 **순서를 지키면서 높이가 가장 고른 지점**에서 가른다(지그재그로 나누면 한 줄 더 높아진다). 계열이 하나뿐이면 한 열로 그린다. 완료는 **읽기 전용 체크박스 + 취소선**이 말한다(게임에서 오는 값이라 앱이 못 뒤집는다 — 누를 수 있는 것은 여전히 머리의 `?` 하나). 그래서 오른쪽의 **`CLEAR` 배지를 걷었다** — 같은 말의 세 번째였고 반폭 열에서 긴 이름을 밀어냈다([[ADR-147]] 정정 33 의 «완료 → CLEAR» 가 여기서 죽고, 뷰모델 쪽 «완료면 `count = null`» 은 산다). 오른쪽은 이제 «카운트 있음 → `n/max` · 그 밖 → 빈칸» 둘뿐이고 항목 간격은 2 → 6px. 계열 축·`?` 말풍선·유니온 조건부는 그대로다. 타일은 `4×auto` 라 낮아진 만큼 아래 위젯이 올라온다(바닥은 격자 한 행 82px — 계열이 하나도 없을 때만 닿는다). |
| [183](./adr/ADR-183.md) | 🟢 유효 | 보스 수익 타일은 **높이를 내용이 정한다** — 얼굴을 키우고 바닥 여백을 없앤다 | 구현 완료(2026-08-30) · 실기기 미검증. 고정 4x3(270px)에 캐릭터가 상한 셋까지만 서다 보니 **바닥이 비는 것이 기본 상태**였다(1명이면 87px). 기본 배치를 **`4×auto`** 로 바꿔 높이가 내용을 따르고(그래서 auto 타일이 셋이 됐다 — 초과분 누적 규칙은 [[ADR-147]] 정정 1 그대로), 아래 좌표가 두 칸 올라온다. `variantOf` 가 `'auto'` 를 `full` 로 읽어야 4x2 용 `wide` 로 안 떨어진다. 얼굴은 26 → **32**(「남은 스케줄」과 같은 값), 순위는 **`1st·2nd·3rd`**(칸 폭 20px 고정). 4x3 선언은 남고 스냅샷이 그 분기를 지킨다. |
| [184](./adr/ADR-184.md) | 🟢 유효 | 가계부는 **보고 있는 기간의 합계**를 격자 위에 세운다 (수익 · 지출 · 순 수익) | 구현 완료(2026-08-30) · 실기기 미검증. 기간 전체가 얼마인지는 **칸 마흔둘을 눈으로 더해야** 나왔다 — [[ADR-170]] 결정 11 의 «칸이 곧 합계» 는 하루의 뜻으로만 참이다. 기간 이동과 격자 **사이**에 세 칸(수익 · 지출 · **순 수익**)을 카드 하나로 세우고 **주간·월간이 같은 자리**다. 값은 새 조회 없이 **격자가 그린 칸을 그대로 접는다**(`periodTotals(weeks, amounts)` — `monthIncomeMax` 와 **같은 `inPeriod` 기준**이라 월간의 앞뒤 달 칸이 안 든다. 열지도용 `heatWeeks` 를 넣으면 주간 자리에 달 합계가 선다). 순 수익은 **부호가 색을 정하고**(양수 `rise-ink` · 음수 `fall-ink` · 0 은 `text`) 음수는 `−`+절댓값으로 적는다(`formatMesoCompact` 의 ASCII `-` 는 위 두 칸의 U+2212 와 다른 글자다). 캐시 지출·날짜를 못 캔 보스는 **칸과 같이** 안 든다. **정정 1** — 셋은 **같은 무게가 아니다**(사용자 지적): 수익·지출은 **순 수익을 내기 위한 재료**라 가로 카드 셋을 걷고 **세로 세 줄로 쌓아 오른쪽에 붙인다**(계산이 내려가는 방향). 재료 12px, 답 **18px `font-extrabold`**. 색은 **안 걷고**(크기로만 약해진다) 계산선도 **안 긋는다**(격자 머리선으로 읽힌다). **정정 2** — **범위 이동과 한 줄에** 세운다(사용자 지정), 폭은 **2:1**. 둘은 «어느 기간인가»/«얼마인가» 라는 짝이라 세로로 쌓으면 같은 말을 두 번 한다. 비율은 **flex 로**(`flex-[2]`:`flex-1`) — 고정 px 면 좁은 기기에서 날짜가 접힌다. 블록이 **정해진 폭**을 받으면서 정정 1 의 바닥 폭 상수가 사라졌다(라벨 `w-10` + 금액 `flex-1` 이면 두 열이 저절로 정렬된다). 1/3 칸에 맞춰 재료 11px · 답 16px 로 한 계단씩 내렸고, 넘치는 금액은 말줄임한다. **정정 3**(정정 1·2 의 배치를 걷는다) — 합계가 **자기 줄 전체**를 도로 받고, 그 줄 **안에서 좌우로** 갈린다: 재료 둘은 왼쪽에 쌓이고 답은 **오른쪽 끝**에 혼자 선다(`items-end` — 답이 **지출 줄과 바닥을 맞춘다**; 가운데면 두 재료 사이에 떠 셋이 나란해 보인다). 세로 셋은 답을 재료와 **같은 줄기**에 세워 «넷째 줄» 로 읽혔고, 좌우로 가르면 합계가 먹는 줄이 셋 → **둘**이다. 답은 **라벨과 한 줄**(라벨 열을 따로 세우면 표가 둘로 보인다) · **14px**(18 → 16 → 14, 사용자 지정 — 줄을 혼자 쓰고 재료가 11px 이라 1.27배와 `extrabold` 만으로 답이 답으로 읽힌다). 순서는 결정 1 그대로 **범위 이동 → 합계 → 격자**. **정정 4**(사용자 지정) — 재료 둘을 **가로 한 줄**로, 답을 **아랫줄**로, 블록은 **오른쪽에** 붙인다. 재료를 세로로 쌓으면 «수익 다음이 지출» 이라는 **없는 순서**를 말하는데 그 둘은 같은 층이다. 가로로 서면 답만 내려와 «위 둘에서 아래 하나» 가 된다. 정정 3 의 바닥 폭·라벨 고정 폭이 함께 사라져 **정렬용 상수가 하나도 안 남았다**(라벨이 자기 금액에 붙는다). **정정 5**(사용자 지정) — **바탕만 옅게**: `bg-surface`+`rounded-lg`+패딩, **테두리 없음**(두르면 카드가 되고 정정 1 ④ 가 걷은 것이 그 «묶음 하나» 다). `items-end` → **`self-end`** 여야 바탕 폭이 내용 폭이다 — 안 그러면 빈 바탕이 화면 반을 덮는다. **정정 6 — 「저울」(최종, 사용자 채택)**: 시안 넷을 앱 토큰으로 그려 고른 결과다. 정정 1~5 는 배치를 다섯 번 바꿨어도 **전제가 하나**였다 — 셋을 **한 표**에 줄 세운다. 그러면 답이 «셋 중 큰 하나» 로만 갈려 글자 크기가 혼자 지탱한다. 대신 **답을 카드 헤드라인**(라벨 10px + 20px `extrabold`·`leading-none`)으로, **재료를 그 옆 각주**(11px 두 줄)로 만들어 **위계가 관계를 말하게** 한다. 바닥은 `items-end`(큰 숫자 밑선 = 「지출」 줄 밑선). **비율 막대는 안 그린다**(사용자 지정 — «얼마인가» 에 «어느 비율인가» 를 더하는 일이고 적자 주 규칙이 필요하다). 라벨에 **기간을 안 붙인다**(위 줄이 이미 말한다 — 보스 수익 탭의 「{기간} 총 수익」과 갈리는 지점). 정렬용 상수는 여전히 **0개**. **정정 7**(사용자 지정) — 단위 **「메소」를 큰 숫자에만** 붙인다: 작은 글자로 격하하되 숫자와 사이에 **진짜 공백**을 남긴다([[ADR-046]] 과 같은 처방 — 마진으로만 띄우면 「N메소」로 붙어 스크린리더가 이어 읽는다). 재료 둘에 붙이면 좁은 카드에 「메소」가 셋 서는데 셋이 같은 축이라 한 번이면 된다(테스트가 양쪽으로 못 박는다). |
| [185](./adr/ADR-185.md) | 🟢 유효 | 가계부는 **앞으로 못 간다** — 기간 이동의 천장은 «지금» 이다 | 구현 완료(2026-08-30) · 실기기 미검증. [[ADR-170]] 정정 3 의 «화살표는 안 죽인다» 를 **앞쪽만** 뒤집는다(사용자 지정). 그 근거(«손입력은 언제로든 적는다»)는 **뒤쪽에서만** 참이었다 — 미래 기간엔 볼 것이 구조적으로 없어(칸 전부 빈칸 · 합계 `+0`/`−0`/`0` · 열지도 기준선 0) «아직 안 적었다» 와 «올 수 없는 곳이다» 가 같은 화면으로 말해졌다. 판정은 보스 수익 탭의 **`isLatestPeriod` 그 함수**(두 축의 `periodKey` 가 이미 같은 모양이라 두 하위가 **한 경계**를 갖는다). 죽는 법도 그쪽과 같다 — `disabled`+`aria-disabled` 둘 다, 흐림은 **JS 조건**(NativeWind 의 `disabled:` 는 웹 의사 클래스라 `Pressable disabled` 와 안 이어진다). **결정 2 — 화살표만 막으면 한 번의 탭이 그 벽을 넘는다**: 월간 격자의 꼬리는 다음 달 날짜라(8월 격자는 9/5 까지) 그 칸이 달을 옮겼다. `selectDate` 의 달 동기화에 **이번 달 천장**을 얹는다(`<=` 라 이번 달은 들고, 지난 달 칸은 그대로 옮긴다). 막는 것은 «보는 기간» 이지 **«고른 날» 이 아니다** — 미래 날짜는 여전히 골라지고 시트가 자기 머리에서 날짜를 바꾼다([[ADR-178]] 정정 6). **「이전」 은 안 죽인다.** 대가: 앞으로 옮기며 재던 테스트 여섯이 과거 쪽으로 다시 쓰였다(걸치는 주는 `8/27~9/2` → **`7/30~8/5`**). |
| [186](./adr/ADR-186.md) | 🟢 유효 | 보스 목록의 순서는 **어디서나 `weekly-bosses.json` 정규 순서**다 | 구현 완료(2026-08-30) · 실기기 미검증. 같은 보스 무리가 서는 자리 넷(today 「남은 스케줄」 펼침 · 보스 스케줄러 · 보스 수익 · 가계부 결정석 타일)의 기준이 **넷이었다** — 스케줄러 **자동 모드**는 정렬이 아예 없어 Nexon `boss_contents` **응답 순서**였고, 가계부 타일은 **결정석 금액 내림차순**([[ADR-172]] 정정 1)이었다. 정렬 키는 [[ADR-036]] 결정 3 그대로(**참조 인덱스 → 난이도 인덱스 → 보스명**)이고, 비교자를 `REFERENCE_ENTRIES` 의 소유자인 `lib/boss/boss-matching` 에 **`compareBossOrder` 하나**로 두어 네 소비자가 그것만 부른다([[ADR-036]] 결정 5 가 사설 사본을 흡수한 것과 같은 태도 — 자동 모드에 넷째 사본을 새로 쓰지 않는다). 자동 모드 정렬은 `displayedBosses` **끝**에서 **모드를 안 가르고** 한다(수동은 이미 같은 순서라 멱등이고, 그래야 계약이 한 줄이다) — today 는 그 함수를 부르므로 **한 줄도 안 고친다**([[ADR-147]] 결정 8 이 값을 하는 자리). [[ADR-164]] 결정 1·2(월간→주간 · 완료는 자리를 안 바꾼다)와 [[ADR-181]] 정정 1(today 의 **캐릭터 행** 순서)은 그대로 — 이 결정이 채우는 것은 «무리 안» 뿐이다. **대가**: 가계부에서 «오늘 제일 큰 것» 을 눈으로 못 찾는다(그 질문의 자리는 마리당 금액이 실제로 적힌 보스 수익 탭이다). 주석 둘도 사실로 고쳤다 — `displayed-bosses` 가 자동/수동을 **뒤바꿔** 적고 있었고, `records.ts` 가 동액 타이브레이크의 근거로 [[ADR-036]] 을 댔는데 그 ADR 결정 4 는 정반대(`ORDER BY` 안 넣는다)였다. |
| [187](./adr/ADR-187.md) | 🟢 유효 | 주간 12마리를 채우면 남은 보스는 **«마감»**이다 — 잡지 않은 보스의 드롭은 남지 않는다 | 구현 완료(2026-08-30) · 실기기 미검증. `12` 가 «고르는 수»([[ADR-055]])와 «보여 주는 분모»([[ADR-054]]·[[ADR-059]])로만 있었고 **«이번 주에 더 잡을 수 있는가» 판정은 없었다** — 등록과 실제 처치가 어긋나면 `12/12` 인데 today 「남은 스케줄」에 「보스 2개」가 남고 보스 수익엔 「미완료」 행([[ADR-032]] 결정 4)이 선다. 판정은 참조표의 소유자 `lib/boss/boss-matching`(`isWeeklyClearLimitReached`)에 **하나만** 두고([[ADR-186]] 결정 2 와 같은 태도), `displayedBosses` 가 `isWeeklyLimitClosed` 를 **실어 보낸다**([[ADR-147]] 결정 8 의 등식을 지키려면 화면이 다시 판정하면 안 된다). 스케줄러 카드는 「완료」 자리를 **「마감」**으로 바꾸고(배지가 늘지 않는다 — [[ADR-162]] 결정 3 의 규칙·배색 그대로, 우선순위 「진행 불가」>「마감」>「완료」), today 는 안 세고, **보스 수익은 행을 아예 안 세운다**(정산 페이지라 «벌지 않은 것» 은 줄이 없다). **완료로 칠하지 않는 것이 핵심**이다 — 그러면 안 잡은 보스의 결정석이 금액이 된다. 잡지 않은 보스에 적어 둔 **드롭은 지운다**(사용자 지정): 술어는 «설 자리도 처치 기록도 없다» 하나로 세 경로(한도 마감·추적 해제·기간 종료)를 덮고, 안전 장치 넷(같은 보스의 다른 난이도 행이 있으면 이관의 몫 · 그 기간에 행이 하나라도 있을 것 · 결정석 가격을 아는 조합만 · 동기화가 성공한 캐릭터만)이 «행이 없다≠안 잡았다» 를 막는다. 안 지우면 **보스 수익에서는 증발하고 드롭 히스토리·today 위젯에는 영원히 남는다**. 정리 건수는 토스트로 알린다. **정정 1**(사용자 지정) — 마감 배지의 상자를 **「완료」와 똑같이** 맞추고(자리를 대신하는 배지라 크기가 갈리면 카드 오른쪽 끝이 흔들린다. 색만 다르다), **초상화 레일의 진행 링도 마감을 «다 한 것» 으로 센다**(분자만 — 분모에서 빼는 [[ADR-162]] 의 레벨 미달과 갈린다: 저쪽은 «이 캐릭터의 일이 아니다», 이쪽은 «이번 주 일이 끝났다» 라 열둘을 추적했으면 `12/12` 로 읽혀야 한다). 보스 수익의 아바타 링은 분자가 실제 처치 수라 **이미** `12/12` 였다. |
| [188](./adr/ADR-188.md) | 🟢 유효 | 초상화에서 **회색 판과 얼굴 테두리를 걷고**, 링 자리에 아주 얇은 **빈 링**을 세운다 | 구현 완료(2026-08-30) · 실기기 미검증. 셋 다 사용자 지정. ① 얼굴 원의 `bg-surface-2` 를 걷는다 — 넥슨 캐릭터 이미지가 **투명 배경**이라 «덮여 안 보일» 전제였던 회색이 그림 뒤로 비쳤다(**머리글자 폴백에는 남긴다** — 거기서는 글자가 앉을 바탕이다). ② **얼굴 둘레 테두리**([[ADR-161]] 결정 3, r = 20)를 없앤다 — 진행 링(r = 26)과 6px 뿐이라 링을 그리는 화면에서 얼굴이 선 두 겹에 둘러싸였다. ③ 그 신호가 실제로 필요한 자리(**링이 없는 관리 화면**)는 링 자리에 **두께 1의 빈 링**(`portrait-empty-ring`, r = 26)이 받는다 — [[ADR-161]] 결정 1 이 «레이아웃은 잡되 색은 안 채운다» 로 비워 둔 띠가 채워져 스케줄러와 모양이 맞물리고, **칸마다 항상 서되 고른 칸만 강조색**이라 결정 3 의 «칸 하나만 보고도 읽히는 절대 신호» 도 남는다. 두께가 1인 것이 요점 — 3이면 «0% 진행 링» 으로 읽힌다. 그리는 층은 옛 테두리와 같은 `<Svg>` 다([[ADR-145]] 결정 5 — `borderWidth` 를 주면 그 칸의 얼굴만 작아 보인다). **대가**: 스케줄러·컨텐츠의 선택 표시가 흐림 하나로 준다. |
| [189](./adr/ADR-189.md) | 🟢 유효 | 가계부도 **포커스가 스냅샷을 소유한다** — `boss_profit_records` 에도 리비전을 단다 | 구현 완료(2026-08-30) · 실기기 미검증. 사용자 보고 — *«보스 수익에서 아이템 가격을 입력하고 가계부로 가면 새로고침해야 반영된다»*. 가계부는 탭이라 마운트가 앱 실행당 한 번인데 조회 효과가 `reloadToken` 에만 매여 있었고, 그 수는 **자기 쓰기·당김·마운트 날짜 캐기**에만 올랐다 — 원천 넷 중 둘(`boss_drop_records`·`boss_profit_records`)은 남의 화면이 쓰는데 **물어볼 길이 없었다**. [[ADR-147]] 정정 17 이 today 에서 세운 처방(포커스 + 저장 계층 리비전)을 그대로 받는다: ① 포커스에서 «내가 읽은 판 ≠ 지금 판» 일 때만 다시 읽고(리비전은 **읽기 전**에 찍는다) ② `boss_profit_records` 에도 리비전을 달아(쓰기 **셋 전부**에서 올린다 — 「가계부가 안 읽는 칸」 으로 고르지 않는다) ③ 화면은 `records.ts` 의 `cashbookDataRevision()` **하나**로 묻는다(화면이 `storage/` 를 직접 안 부른다는 규칙을 지키고, 원천이 늘어도 화면이 안 바뀐다 — 두 수 다 단조 증가라 **합**이면 충분하다) ④ 손입력 둘엔 **안 단다**(쓰는 곳이 이 화면 하나뿐이라 지금 달면 안 쓰이는 코드다). |
| [190](./adr/ADR-190.md) | 🟢 유효 | 발행 지문을 **못박는다** — 트리 계산값이 아니라 «스토어 바이너리가 묻는 값» 으로 낸다 | 구현 완료(2026-08-30) · 1.0.7 발행에 적용. RN OTA 는 `runtimeVersion`(네이티브 트리의 fingerprint)으로 매칭되는데 **스토어 1.0.6 의 지문이 트리로 재현되지 않는다**(GUI 아카이브가 낸 값 + [[ADR-155]] 가 네이티브 트리를 루트로 옮긴 것이 겹쳐 영구화 — ios `d304704e…`/android `3df849c0…` vs 트리 `a52ce256…`/`7e2fec5d…`). 그대로 발행하면 매니페스트가 아무도 안 묻는 이름으로 남고 **`latest-*.json`(배달이 아니라 «스토어 업데이트 필요» 판정 파일)이 트리 값으로 덮여 스토어 사용자 전원에게 거짓 모달**이 뜬다. 그래서 [[ADR-137]] 결정 3 의 «지문을 우리가 안 적는다» 를 **트리가 바이너리 지문을 재현 못 할 때만** 뒤집는다: 지문을 `PINNED_RUNTIME_VERSIONS` 에 `binaryAppVersion` 과 함께 **커밋되는 상수**로 두고(2026-08-25 안드로이드 발행은 워크트리 임시 편집이라 기록도 가드도 안 남았다), 발행 **전에** «못박은 값 == 지금 발행된 `/latest` 의 판정값» 을 대조해 다르면 중단한다(export 앞이라 빌드를 안 태운다). 같은 값을 다시 쓰므로 판정은 그대로고 `appVersion` 만 오른다. **대가** — 상수가 사는 동안 트리 계산값은 죽은 값이라 **네이티브 변경은 스토어 빌드로만** 나간다. 1.0.7 이 1.0.6 바이너리에서 도는 근거는 지문이 아니라 사람이 확인한 사실이다(새 네이티브 의존성 0 · 로컬 모듈 JS API diff 0줄 · `app.json` 바이트 동일 · 폰트 0 · RN/expo 버전 동일). 다음 스토어 바이너리가 나오면 **지운다**. |
| [191](./adr/ADR-191.md) | 🟢 유효 | 안드로이드 OTA 는 **에셋 이름까지** 옛 바이너리에 맞춘다 — 지문만 맞추는 것은 부족했다 | 구현 완료(2026-08-30) · **실기기로 원인 확인**. [[ADR-190]] 대로 지문을 못박아 1.0.7 을 내보냈더니 **안드로이드 앱 이미지 273개가 전부 빈칸**이 됐다. 정적 분석은 전부 통과했고(에셋 누락 0 · 해시 손상 0 · 키 집합 상위집합 · 번들이 md5 를 그대로 참조) **원인은 기기 logcat 이 말했다** — 안드로이드는 APK 에 박힌 이미지를 드로어블 **리소스**로 들고 그 이름을 `httpServerLocation` 에서 파생하는데([[ADR-137]] 이 아니라 RN 의 `getAndroidResourceIdentifier`), [[ADR-155]] 가 `packages/core`→`src` 로 옮기며 `_core_src_assets_…` 가 `src_assets_…` 로 바뀌었다(APK 292개 중 273개가 `_core_`). iOS 는 파일 경로로 풀어서 멀쩡했고, 08-25 발행이 통과한 것은 그것이 ADR-155 **이전** 트리에서 구워진 **우연한 안전**이었다. 처방 셋 — ① 못박기를 «발행 허가» 가 아니라 **정확성 신호**로 재해석([[ADR-190]] 정정 1) ② 경로를 추정하지 않고 **APK 이름표(md5→리소스 이름, 기기 logcat 에서 뽑는다)에서 역산**하는 Metro 에셋 플러그인(파생이 `/`→`_` 이므로 목표 이름의 `_` 를 `/` 로 되돌린 것이 답이다. 표에 없는 에셋은 안 건드린다 — 내려받는 것이라 경로와 무관) ③ 발행 스크립트가 못박은 발행에서 이름표를 **강제**하고 export 뒤 **전수 대조**해 하나라도 어긋나면 중단(재발행 실측 290/290 일치). 플러그인과 못박기는 **함께 죽는다** — 새 스토어 바이너리에서 켜져 있으면 같은 사고가 거울처럼 뒤집힌다. **정적 대조가 실기기 확인을 대신하지 않는다**(1.0.7 이 «정적으로는 다 맞는데 화면은 빈칸» 이었다). |
| [192](./adr/ADR-192.md) | ⛔ 폐기 | 테마 배경 실종의 원인은 **밀도 확대가 아니었다** — 실험으로 반증 | 폐기(2026-08-30). «mdpi 한 벌 → xxhdpi 3배 확대 → 4096 초과» 로 보고 픽셀 불변으로 md5 만 바꿔 파일로 내려받게 했으나, 발행·실기기 수신까지 하고도 **화면이 한 픽셀도 안 바뀌었다**(설정 빈 영역 평균 `(11,5,9)`·표준편차 `0` — 수정 전과 동일). 밀도 확대가 사라졌는데도 안 나오므로 크기·텍스처가 원인이 아니다. 4096 경계가 실패/정상을 가른 것은 **우연**이었다. 처방은 되돌렸다(파일 원본 복구 · 관문 예외 제거). 다음 조사의 출발점은 [[ADR-134]] 정정 5 의 «안드로이드는 화면이 벽지를 들고 다닌다» 짝(`ScreenBackdrop`·`screen-backdrop-policy`)이고, 배선과 가드 테스트는 **둘 다 살아 있어** 아직 원인 미상이다. [[ADR-133]] 이 실기기 미검증이라 **회귀가 아니라 처음부터 안 됐을 가능성이 높다**. |
| [193](./adr/ADR-193.md) | 🟢 유효 | 벽지는 **가장 안쪽 불투명 화면**이 든다 — 층 스택이 아니라 탭 내비게이터다 | 구현 완료(2026-08-30) · 실기기 확인 대기. 1.0.7 에서 테마 배경이 **안드로이드에서만** 사라진 회귀(1.0.6 에서는 떴다 — 사용자 확인). [[ADR-134]] 정정 5 의 짝에는 **비대칭**이 있다 — 불투명 화면 배경은 `NavigationContainer` 테마라 **모든** 내비게이터에 걸리는데 벽지는 `screenLayout` 을 준 내비게이터에만 걸린다. [[ADR-167]] 이 탭 위에 층 스택을 끼우며 `screenLayout` 을 그 스택에만 뒀고, 안쪽 탭 셋이 자기 화면을 칠하면서 **층이 깐 벽지를 덮었다**. iOS 는 `ScreenBackdrop` 이 통과 컴포넌트라 멀쩡했다. 처방 — `screenLayout` 을 `TAB_LAYER_PROPS`(셋이 공유) 로 옮기고 **층 스택의 것은 걷는다**(두 겹 마운트 방지). 규칙은 «벽지는 가장 안쪽 불투명 화면이 든다». 상수는 JSX 를 들어 fast refresh 규칙 때문에 `navigation/tab-layer-props.tsx` 로 갈랐다. 기존 가드는 «컴포넌트가 그리는가»·«배경이 불투명한가» 만 보고 **«내비게이터가 두르는가» 를 아무도 안 봐서** 이 회귀가 지나갔다 — 그 자리를 테스트가 메운다. **틀렸던 가설 둘**: «원래부터 안 됐다»(1.0.6 에서 떴다) · «mdpi 3배 확대가 4096 초과»([[ADR-192]] ⛔, 실험으로 반증 — 파일로 내려받게 해도 한 픽셀도 안 바뀌었다). 두 번 다 그럴듯한 대응 관계를 증거로 삼은 것이 잘못이었고, 답은 **코드에 하나뿐인 안드로이드 분기**를 따라가서 나왔다. |
| [194](./adr/ADR-194.md) | 🟢 유효 | 문서와 주석, 앱 문구를 읽히게 쓴다 | 적용 중(2026-08-31). 원인을 두 번 잘못 짚었다(번역투 아님, 어휘 치환도 아님). 문제는 문장 짜는 방식이다. 코드에 있는 이름을 쓰고(결정 1), 결론과 이유를 같이 쓰고(결정 2), 장식 기호 `—`·`«»`·`「」` 를 없앤다(결정 3). 범위는 문서·주석·앱 문구 전부(결정 4). |
| [195](./adr/ADR-195.md) | 🟢 유효 | 배지는 컴포넌트 하나다. 테두리·배경·크기를 프롭으로 받는다 | 배지가 다섯 군데에 흩어져 있었고(`Badge` · `DifficultyBadge` · `QuestStateBadge` · `CategoryBadge` · `CountLabelBadge`) 같은 자리에 서는 배지의 상자가 어긋나 있었다. `components/atoms/Badge/Badge.tsx` 하나로 합치고 색·테두리·그림자를 variant 표 17종이 쥔다(결정 1·2). 두께는 variant 기본값에 `weight` 프롭 덮기 — **NativeWind 가 두께 충돌을 문자열 순서로 안 풀어서** `className="font-bold"` 로는 못 덮는다(측정, 결정 3). 크기는 `default`·`mini` 둘이고 **높이를 안 못박는다**(결정 4를 정정 1·3 이 고쳤다) — 여백이 높이를 만든다. 그라디언트 배지는 **여백에서 테두리 폭을 뺀다**(정정 2). Yoga 가 테두리를 패딩처럼 바깥 크기에 더해, 안 빼면 테두리 있는 배지만 커진다(실측 65 · 63 · 58px). `mini` 만 `fixed` 를 자동으로 켜는데 근거는 자기 높이가 아니라 **놓이는 자리**다(고정칸 타일, [[ADR-152]] 결정 5). 글자 크기는 칩 계단이 쥐고 난이도만 한 단 작다(정정 4). 라벨 표는 스타일이 아니라 도메인 데이터라 상수로 남긴다(결정 5). 스타일 표는 `Badge/variants.ts` 로 갈랐다(정정 5, `SIZE`→`BADGE_SIZE` · `WEIGHT`→`BADGE_WEIGHT`). 트레이드오프는 프롭이 다섯으로 늘어 [[ADR-094]] 결정 1 이 경계한 **호출부마다 예외** 가 컴포넌트 안으로 들어왔다는 것이다. |
| [196](./adr/ADR-196.md) | 🟢 유효 | 글꼴과 글자 계단을 앱이 쥔다 — 두 플랫폼이 같은 값을 본다 | 같은 코드가 두 플랫폼에서 다른 높이로 그려졌다(실측 — 10px 글자의 줄 상자가 iOS 12.0 · 안드로이드 15.2). 원인 둘 — 글꼴이 달랐고(시스템 기본), 임의값 `text-[10px]` 에는 **줄 높이가 안 붙어** 플랫폼 메트릭이 드러났다. Pretendard 를 네이티브에 심되 **파일을 플랫폼마다 다르게** 넣는다(결정 2) — iOS 는 가변 1개, 안드로이드는 정적 5개. 정적은 파일마다 Family 이름이 달라 iOS 가 조용히 시스템 글꼴로 떨어지고, 가변은 `expo-font` 가 만드는 XML 에 `fontVariationSettings` 를 못 넣어 안드로이드 굵기가 붕괴한다. 계단은 `typography.cjs` 한 표가 쥐고 Tailwind 기본을 **교체**한다(결정 3). 칩은 자기 계단을 쓴다(결정 4). 글자 배수 정책([[ADR-152]] 결정 5)은 그대로다(결정 5). 트레이드오프 — 용량 +6.4/12.7MB · OTA 지문 변경 · **`prebuild` 마다 릴리스 서명이 날아간다**([[ADR-091]]). |
| [197](./adr/ADR-197.md) | 🟢 유효 | 써드파티 컴포넌트의 `className` 등록은 `lib/nativewind-interop` 한 곳에서 한다 | NativeWind 가 자동으로 가로채는 것은 RN 기본 컴포넌트뿐이라, `Svg`·`LinearGradient`·`Animated.View`·lucide 아이콘에 준 `className` 은 **에러 없이 무시된다**(실측으로 렌더 트리에 문자열이 그대로 남았다). `cssInterop()` 이 감싼 것을 돌려주는 대신 **등록**을 하므로 어느 모듈을 거쳐 import 했느냐가 결과를 가른다. 그래서 등록을 한 파일에 모으고 쓰는 쪽은 거기서만 가져온다(결정 1). `Svg` 는 `currentColor` 의 출처가 CSS 상속이 아니라 `color` 프롭이라 색·크기를 프롭으로 옮긴다(결정 2). Reanimated 의 CSS 애니메이션은 애니메이션 컴포넌트에만 붙어 `AnimatedView` 가 따로 있다(결정 3). lucide 는 안쪽 `Svg` 의 `createElement` 가 라이브러리 안에 있어 아이콘 컴포넌트 자체를 등록해야 하고, `testID` 는 `data-testid` 로 바뀌어 안 통한다(결정 4). 트레이드오프는 원본 모듈 직접 import 를 기계가 막지 않는다는 것이다. |
| [198](./adr/ADR-198.md) | 🟢 유효 | 버튼 atom 은 외형만 갖고, 스타일 표는 `variants.ts` 로 가른다 | 변형 표에 폭·정렬·간격을 안 넣는다(결정 1). 실측으로 `primary` 9곳의 공통 토큰은 변형 표의 것뿐이었고 `w-full`·`gap-2` 는 5~6곳뿐이라, atom 이 그것까지 쥐면 호출부마다 예외 프롭이 생긴다([[ADR-094]] 결정 1 이 경계한 모양). 라운딩만 예외로 못박는다(디자인 원칙 2). 상자 클래스와 글자 클래스를 **두 벌로 나눈다**(결정 2) — RN 은 글자 스타일이 상자에서 자식 `Text` 로 상속되지 않아서, 한 벌로 두면 라벨이 색도 굵기도 없이 그려지는데 에러가 없다. 그래서 호출부의 글자 유틸은 `className` 이 아니라 `textClassName` 이다. **스타일 표는 `variants.ts` 로 가른다**(결정 3) — 컴포넌트 파일에 넣어 보고 되돌렸다. 코드가 표에 묻히고(배지는 표 90줄 대 코드 60줄), `react-refresh/only-export-components` 로 fast refresh 가 깨진다. 타입은 표에서 파생되므로 함께 옮기고 컴포넌트 파일이 다시 내보내 호출부 경로를 유지한다. `hover:` 와 `type` 은 안 넘어왔고, `primary` 의 `text-base` 는 웹이 상속으로 주던 16px 를 적어 준 것이다(결정 4). |
| [199](./adr/ADR-199.md) | 🟢 유효 | 커스텀 아이콘은 `atoms/Icon` 한 디렉터리에 모으고, lucide 규격은 `IconSvg` 가 쥔다 | 커스텀 아이콘 둘(`ProfitIcon` · `GearIcon`)이 각자 atom 디렉터리에 살면서 lucide 규격 12줄을 **각각 복사**하고 있었다. 새 아이콘을 더할 때 규격이 지켜지는 근거가 복사뿐이고, 어긋나도 그림은 나오므로 화면을 자세히 보기 전에는 모른다. `components/atoms/Icon/` 에 모으고 파일 하나가 아이콘 하나다(결정 1) — 아이콘에는 곁에 둘 짝이 없어 디렉터리를 안 판다. 배럴 `index.ts` 하나로 가져간다 — Metro 가 트리셰이킹을 안 해도 이 디렉터리에는 앱이 직접 그린 아이콘만 살아서 lucide 배럴(1,900개)과 셈이 다르다. `icon-base.tsx` 의 `IconSvg` 가 뿌리를 그리고 아이콘 파일은 자식 도형만 갖는다(결정 2) — 뿌리는 언제나 `fill="none"` 이고 채우는 자리는 아이콘이 도형마다 고른다([[ADR-132]] 정정 25). 프롭 모양(`IconProps` 여섯)은 lucide 와 같게 두는데, 하단바가 `tabBarIcon` 에 색·크기를 값으로 넘겨 lucide 셋과 커스텀 둘을 바꿔 끼우기 때문이다(결정 3). `fill` 의 뜻은 아이콘마다 달라 공통 타입이 아니라 각 아이콘의 JSDoc 이 적는다. 정정 1·2·3 이 구조를 한 단계 더 갈랐다 — `IconSvg` 가 묶고 있던 **격자와 칠**을 나눠 `SvgFrame`(뿌리 배선 + 격자)을 프리미티브로 두고 `IconSvg` 는 그 위의 lucide 칠 프리셋이 된다. 단풍잎이 계기였다: 네 자리에서 아이콘처럼 쓰이는데 격자가 127×130 이고 채운 도형이라, lucide 프리셋을 받으면 뿌리의 `stroke` 가 상속돼 2px 윤곽선이 얹힌다(실측). `MapleLeaf` 아톰이 그 자리를 대신하고, 움직이는 잎 둘은 `atoms/Spinner/` 로 간다 — 가르는 기준은 아이콘이냐가 아니라 **움직이냐**다(반복 모션 배선은 SVG 와 무관하다). 잎 경로는 `Icon/maple-leaf.ts` 로 와서 층 디렉터리 밖 예외가 없어진다. |

| [200](./adr/ADR-200.md) | 🟢 유효 | atoms 는 배럴 하나로 내보내고, 쓰는 쪽은 한 줄로 가져간다 | 98개 파일이 아톰마다 한 줄씩 167줄을 쓰고 있었고 경로가 두 모양(`components/atoms/…` · `../../atoms/…`)으로 섞여 있었다. `atoms/index.ts` 하나로 내보내 98줄이 된다(결정 1). Metro 가 트리셰이킹을 안 해 배럴은 안 쓰는 것까지 그래프에 넣지만, lucide 배럴(1,900개)과 달리 여기엔 아홉이 살고 RN 은 번들이 하나이며 `React.lazy` 가 없어 어차피 함께 들어간다([[ADR-199]] 결정 1 과 같은 셈). 배럴에는 **컴포넌트만** 올리고 곁 파일의 값(`FONT_SCALE_MAX`·`MAPLE_LEAF_PATH`)은 원래 경로로 간다(결정 2) — 층의 얼굴에 내부 사정이 섞이면 무엇이 공개된 것인지 안 읽힌다. atom 끼리는 배럴을 거치지 않는다(결정 3) — 거치면 `index` → `Button` → `index` 순환이 생긴다. 트레이드오프는 깊은 경로 import 를 기계가 안 막는 것과, 무거운 atom 이 생기면 셈을 다시 해야 한다는 것이다. |
| [201](./adr/ADR-201.md) | 🟢 유효 | 사냥 수입은 계산기와 **수동 입력** 둘 중 하나로 적히고, 그 갈림은 기록에 박힌다 | 구현 완료(2026-09-02) · 실기기 미검증. [[ADR-175]] 의 계산기는 사냥터 하나에 머무는 것을 전제하고 그 사냥터가 참조표 408개 안에 있어야 해서, 그 밖의 사냥을 적을 길이 없었다. **획득 메소를 직접 치는 수동 입력**을 곁에 세운다(결정 1). 지역·사냥터·효율·메획·시간 다섯 줄이 안 서고 캐릭터·조각·조각 가격은 양쪽에 다 있다. 큰 숫자는 양쪽 다 **합계이고 못 친다**. 사람이 치는 것은 획득 메소이지 합계가 아니라 [[ADR-175]] 결정 1 이 큰 숫자를 못 치게 한 이유가 그대로 산다. `≈` 는 **계산기에만** 붙는다(결정 2). 그 표식은 미리 세어 둔 값이라는 뜻인데 수동의 메소는 사용자가 실제로 본 값이다. 갈림은 `hunt_typed_meso` **한 칸**이 든다(결정 3). 판정자이면서 사용자가 친 획득 메소다. 합계에서 빼서 되돌리지 않는 것은 이 저장소가 `characterLevel`·`mesoRate` 를 다시 안 재고 그때 값을 박아 두는 것과 같은 이유이고, 되돌리면 판정용 칸이 따로 필요해 결국 두 칸이 된다. **[[ADR-175]] 이전 행은 수동 입력으로 열리고 옛 폼을 안 둔다**(결정 4). 조각이 없어 합계가 곧 획득 메소이고 그것은 정의상 같은 값이다. 그런 행이 있을 수 있는지는 릴리스로 확인했다(iOS 는 OTA 가 `08-14` 다음이 `08-31` 이라 그 빌드를 안 받았고, 안드로이드는 `08-25` 발행본이 그 빌드로 `08-31` 까지 살아 있었다). **모드는 기록에 박히고 수정 중에는 못 바꾼다**(결정 5). 바꾸면 앱이 센 합계가 사람이 친 값으로 둔갑한다([[ADR-173]] 결정 15 와 같은 자리). 폼은 `HuntCalculatorForm`·`HuntManualForm` 으로 갈리고 모드 타입은 `'calculator' | 'manual'` 이며 시트 열쇠가 `${category}:${mode}` 가 된다(결정 6). 수동에는 사냥터 이름 칸이 없지만 **옛 행의 이름은 저장할 때 그대로 들고 간다**(결정 7). 목록이 `item ?? category` 로 적어서, 지우면 화면에 떠 있던 이름이 저장 한 번에 사라진다. |
| [202](./adr/ADR-202.md) | 🟢 유효 | 큰 숫자는 어디서도 못 치고, 금액은 폼마다 자기 칸에서 받는다 | 구현 완료(2026-09-02) · 실기기 미검증. [[ADR-173]] 결정 1 은 큰 숫자를 갈래에 따라 치는 금액이거나 합계로 뒀는데, 부르는 자리가 일곱으로 늘면서 다섯은 이미 못 치는 합계가 됐다. 남은 둘(`income/EtcForm` · `spend/ItemBuyForm` 의 장비)도 못 치게 해 **일곱이 모두 합계**가 된다(결정 1). 같은 자리가 어떤 때는 사람이 친 값이고 어떤 때는 앱이 센 값이면 그 수를 자기가 고칠 수 있는지를 매번 다시 판단해야 하고, 지금은 종류를 장비에서 소비로 바꾸는 것만으로 그 답이 뒤집힌다. `spend/ItemBuyForm` 은 금액 칸을 **늘 세우고** 장비에서만 라벨이 `구매 비용` 이다(결정 2). 하나를 사는 자리에 `단가` 라고 적으면 여러 개 중 한 개 값이라는 뜻이 되어 거짓이다. 줄이 하나이므로 상태와 `testID` 도 하나다. 수입 `기타` 는 **금액 × 수량**이 되고 줄 차례가 `spend/EtcForm` 과 같아진다(결정 3). 큰 숫자가 고른 통화의 금액이라는 것은 안 바꾼다([[ADR-170]] 정정 15). 수량은 `income_records.quantity` 에 박는다(결정 4). 저장 안 하면 수정으로 다시 열 때 수량이 늘 1 로 서고 금액 칸에 총액이 들어가, 사용자가 안 적은 값을 적은 값처럼 보여 준다. 옛 행은 `NULL` 이라 수량 1 로 열리고 총액이 곧 금액이다. `AmountFigure` 에서 `onChangeValue`·`readOnly`·`displayValue` 셋을 없애고 포커스 상태와 `SheetTextInput` 갈림길을 지운다(결정 5). `income/ItemSaleForm` 의 `setGross` 와 `spend/EtcForm` 의 `setTyped` 는 못 치는 자리에 넘어가던 **죽은 배선**이었다. `금액 초기화` 버튼도 사라진다(결정 6). 지울 값이 각자의 칸에 있어서, 큰 숫자 옆에 두면 어느 칸을 지우는지가 화면에 안 적혀 있다. 금액을 받는 줄의 이름도 통일한다(결정 7). 지출 `기타` 의 `지출액` 과 수입 `기타` 의 `수입액` 을 둘 다 **`금액`** 으로 바꾼다(셈은 그대로). 시트 제목이 이미 `지출 추가` · `수입 추가` 라 라벨이 그것을 다시 말하고 있었다. 장비 줄만 `구매 비용` 인 것은 그 밑에 `관세` 가 서기 때문이다. 관세도 사는 데 드는 돈이라 위 줄이 `금액` 이면 어느 것이 합계인지 줄 이름만으로 안 갈린다. 지출 `기타` 의 첫 글자 칸도 `사용처` 에서 **`내용`** 이 된다(결정 8 · [[ADR-170]] 정정 14 ② 를 좁힌다). 수입 `기타` 가 이미 `내용` 이라 두 시트가 같은 것을 같은 이름으로 묻는다. 아이템 구매의 `구매 아이템` 은 그대로다. 이로써 `사용처` 라는 라벨이 앱에서 사라진다. **큰 숫자 밑의 힌트 한 줄을 걷고 큰 숫자가 한국어 단위로 읽힌다**(결정 9 · [[ADR-173]] 결정 2 를 폐기). `850,000,000` 이 아니라 `8억 5천만` 이다. 콤마 세 자리는 만·억·조와 자리가 안 맞아 자릿수를 눈으로 다시 세게 만들고, 힌트 줄은 그 셈을 대신하느라 같은 값을 두 줄에 적고 있었다. 힌트가 하던 셋 중 억/만 환산은 큰 숫자가 직접 하고, 내는 메포와 막힌 이유(`시세를 넣어야…`)는 **없앤다**(사용자 지정). 막힌 이유는 시세 줄의 빨간 `*` 와 꺼진 저장 버튼이 받고 `hintBlocked` 도 함께 사라진다. **값은 하나도 안 깎는다** — 이 자리가 곧 저장될 총액이라 뭉개면 화면과 저장이 갈린다. 서식은 조·억·만 셋에 만 미만은 단위 없이 붙이고(`1억 2345만 6789`), 천 단위로 떨어지면 접고(`5천만`), 콤마는 조 자리에만 넣는다. `formatMesoUnits` 한 벌을 고쳤으므로 `MesoAmountField`·`ItemSplitScreen` 의 보조 줄도 같은 서식이 된다. **큰 숫자를 30px 에서 24px(`text-2xl`)로 줄이고 단위 글자만 20px 로 한 단계 더 내린다**(결정 10). 단위를 들여놓느라 길어진 줄이 30px 로는 시트 폭을 넘고, 단위를 낮추면 숫자가 먼저 읽힌다. 24px 은 계단표([[ADR-196]])가 줄 높이 31px 을 함께 주므로 [[ADR-178]] 정정 2 의 `leading-[38px]` 임의값이 없어진다. **정정 1** — 그 두 크기를 **클래스로** 받는다. 결정 10 은 단위 글자에 줄 높이를 안 준다고 적었지만(안드로이드가 안긴 `Text` 의 줄 높이도 줄 상자에 넣는다는 추정), 두 플랫폼에서 A/B 로 재 보니 **큰 숫자 줄이 픽셀 단위로 같았다**. 바깥 `Text` 가 이미 31px 을 잡고 있어 그보다 작은 줄 높이가 안겨도 상자가 안 줄어든다. 그래서 인라인 크기를 걷고 `text-xl` · `text-2xl` 을 쓴다. `AmountFigure` 에 손으로 적은 글자 크기가 하나도 안 남고, 계단표가 움직이면 세 자리가 함께 움직인다. **단위 왼쪽의 틈(2dp)은 폭만 있는 `View`** 다(결정 11). `marginLeft` 는 안긴 `Text` 에 안 먹고, `letterSpacing` 은 안드로이드가 양옆에 균등하게 넣어 `850 0 만` 이 되며, 공백 글자는 읽어 주는 글에 섞여 `8 억 5 천만` 이 된다(셋 다 실측). 단위끼리 붙은 `5천만` 은 한 낱말이라 안 띄운다. **큰 숫자에서 카운트업을 걷는다**(결정 12 · [[ADR-087]] 정정 3 결정 3-2 를 폐기). 결정 9 로 숫자에 단위가 붙으면서 350ms 동안 그려지는 중간값이 못 읽히게 됐다 — `1234만 5678` 처럼 **길이까지 프레임마다 달라져** 줄이 흔들린다. 딸려서 `identity` 프롭과 `app/cashbook/amount-identity.ts` 가 통째로 사라지고(그 이름표는 카운트업 전용이었다) `AmountValue` 잎도 부품 안으로 접힌다(가를 이유가 프레임마다 도는 `setState` 였다). `hooks/useCountUp` 자체는 남는다 — `atoms/AnimatedNumber` 를 보스 수익 화면 셋과 `WeeklyBossProfitWidget` 이 쓰고, 그쪽 숫자에는 단위가 안 붙는다. |
| [203](./adr/ADR-203.md) | 🟢 유효 | 금액 칸은 글자를 들고, 셈만 숫자로 한다 | 구현 완료(2026-09-02) · 실기기 미검증. 금액 칸이 값을 **숫자로** 들고 화면 글자를 그 숫자에서 다시 만들고 있었다. 그래서 가운데를 못 고친다 — `80000000000` 에서 `8` 을 지우면 `0000000000` 이 0 으로 접히고 글자가 다시 만들어져 **칸이 통째로 빈다**(사용자 보고 2026-09-02). 앞자리 하나를 지웠을 뿐인데 처음부터 다시 쳐야 했다. 폼이 `useState<string>` 을 들고 셈은 `mesoValueOf` 로 뽑는다(결정 1). `acceptMesoText` 가 `parseMesoText` 와 갈리는 자리는 하나다 — **앞자리 0 을 안 걷는다**(그것이 편집 중인 상태다). **칸의 콤마를 뺀다**(사용자 지정) — 콤마를 다시 붙이려면 타건마다 서식을 입혀야 하고 그러면 같은 문제가 돌아온다. 자릿수를 읽어 주는 일은 밑의 큰 숫자가 이미 한다([[ADR-202]] 결정 9). 앞자리 0 은 **커서가 빠질 때** 걷는다(결정 2) — 타건마다 하면 결정 1 이 막으려던 그것이 되살아난다. 필수 칸에 0 이 남는 것이 이것으로 막히고 저장 판정은 그대로다. 키보드는 `number-pad` 그대로다(결정 3) — 값이 글자가 된 것과 무엇으로 치느냐는 다른 이야기다. 여덟 자리를 `sheet-fields` 의 `AmountInput` 한 부품으로 모은다(결정 4) — 커서가 빠질 때 정리하는 일을 칸마다 손으로 달면 한 곳이 빠졌을 때 그 칸만 조용히 안 정리된다. 시세 칸 둘은 원래부터 글자를 들고 있어 이 문제가 없었다. **대안으로 버린 것 둘** — 커서 위치를 직접 계산해 콤마를 지키는 길은 [[ADR-170]] 정정 10·12·13 이 세 번 연달아 고친 자리를 다시 여는 셈이고, `parseMesoText` 한 줄만 고치는 길은 값이 화면 글자의 출처인 구조를 안 건드려 `Number` 로 접히는 순간 다시 빈 칸이 된다. |
| [204](./adr/ADR-204.md) | 🟢 유효 | 얼굴 원은 부품 하나가 그리고, 폴백은 호출부가 고른다 | 구현 완료(2026-09-02) · **실기기 확인할 것 없음**(정정 4 — 크롭이 바뀌는 유일한 자리였던 추적 화면이 이미 앱에서 안 열리고 있었고, 그 디렉터리는 [[ADR-144]] 정정 3 으로 지웠다). 캐릭터 얼굴을 원으로 그리는 자리가 **여덟**인데 여덟이 전부 손으로 다시 그리고 있었다. 크롭 표는 [[ADR-144]] 가 `lib/face-crop` 으로 모았는데도 복사본이 셋 남았고, 그중 `CharacterTrackingGrid` 는 [[ADR-144]] 가 **같은 얼굴이 두 자리에서 다르게 잘린다** 며 버린 값(`{x:115, y:120, size:64}`)을 들고 있었다. [[ADR-144]] 는 남은 복사본을 둘로 적었지만 실제로는 셋이었다. 표를 함수로 뽑는 것만으로는 부족하고 원을 그리는 일 자체를 부품이 가져가야 호출부에 베낄 것이 안 남는다. `molecules/CharacterAvatar` 하나가 원·크롭한 `<Image>`·폴백 자리를 든다(결정 1). molecule 인 것은 얼굴 크롭이 도메인 값이기 때문이다. **폴백은 갈래가 아니라 `ReactNode` 로 받는다**(결정 2). 지금 폴백이 세 모양이고 그중 둘은 각각 [[ADR-144]](주황 원 + 흰 `?`)와 [[ADR-188]] 결정 1(레일의 머리글자)이 정한 것이라 부품이 한 모양을 고르면 그 결정 하나를 코드가 조용히 덮는다. 글자 크기도 자리마다 16·14·13·12 로 갈려 있고 그 값이 상자 크기에서 안 나온다. 딸린 이득으로 폴백 글자가 호출부에 남아 [[ADR-152]] 결정 5 의 `fixed` 계약이 안 움직인다(부품이 글자를 그리면 `today/widgets` 셋이 새는 자리로 걸려 `FIXED_BOX_PATHS` 넷을 늘려야 하는데, 그 배열이 넷인 것이 계약이다). 크롭 표 복사본 셋을 걷어 [[ADR-144]] 의 남은 정리를 끝낸다(결정 3). **추적 화면의 얼굴이 실제로 더 확대된다**(사용자 승인 2026-09-02). 진행 링과 곡선 글자는 계층을 안 올리고 `CharacterRail` 안에서 파일만 가른다(결정 4). ~~소비자가 각각 하나뿐이고, 링을 그리는 다른 자리(`boss-profit/CharacterAvatar`)는 12칸 분절 링이라 반원 둘과 기하가 안 겹친다.~~ **정정 1(2026-09-02 · 사용자 지정)이 결정 4 를 뒤집는다.** 그 근거는 사실이지만 약했다. 부품을 셋으로 가른 이유가 **파일 하나가 세 일을 해서 아무도 못 쓴다** 였는데 `boss-profit/CharacterAvatar` 가 정확히 그 상태였고, 그래서 `DropPriceScreen` 이 또 그 옆에 손으로 얼굴 원을 그리고 있었다(**아홉 번째 복사본**이고 결정 1 을 쓸 때 놓친 것이다). 셋이 바뀐다. ① 디렉터리를 가른다. `molecules/CharacterPortrait/` 가 초상화 넷을 갖고 `CharacterRail/` 에는 레일 하나만 남는다(옛 디렉터리는 565줄 중 497줄이 초상화 것인데 이름이 그중 가장 작은 것을 가리켰다). ② 12칸 분절 링이 `portrait-ring.tsx` 로 와서 이 앱의 링 세 가지가 한 파일에 선다. 그 링만 **자기 `<Svg>` 를 갖는다**(좌표계를 통째로 돌려 12시부터 반시계로 채우는데([[ADR-059]] 정정 2) 그 회전이 같은 `<Svg>` 안의 곡선 글자까지 돌린다. 이 링을 쓰는 규격에는 글자가 없어 나눠도 겹치는 것이 없다). ③ `CharacterPortrait` 이 규격 둘(`rail` 68×70 · `compact` 40×40)을 **판별 유니온**으로 받고 `boss-profit/CharacterAvatar.tsx` 가 사라진다(얼굴 부품이 그 이름을 물려받았다). 치수·링 모양·글자 유무·누를 수 있는지가 전부 짝지어 움직여서 프롭을 한 벌로 합치면 못 쓰는 조합이 타입에 남는다. `compact` 의 40 은 못 바꾼다([[ADR-054]] 정정 6 이 아코디언 헤더 64px 을 지키려고 패딩을 줄인 값이라, 70 을 넣으면 헤더가 94px 이 되고 [[ADR-049]] 가 되돌아온다). 남은 것은 폴백 세 모양을 통일할지다(열린 질문). |
| [205](./adr/ADR-205.md) | 🟢 유효 | molecules 는 잎이고, 다른 molecule 을 조합하면 organism 이다 | 구현 완료(2026-09-03) · 화면 변화 없음. [[ADR-094]] 결정 2 의 계층 정의 표가 두 군데서 사실과 안 맞았다. **molecules 의 「도메인 모름」은 17개 중 17개가 어기고**(ADR-094 가 든 예시 `BossPortrait`·`ValuableDropBadge` 부터 어긴다), **organisms 의 「상태·포털·컨텍스트」는 필요조건이 아니다**(`AboveBar` 는 넷 다 없는데 organism 이었다 — 정정 1 로 `navigation/BottomBarOverlay.tsx` 가 됐다). 그래서 실제로 강제되는 것은 의존 방향 하나뿐이고 배치는 사후 판단이 됐다. [[ADR-204]] 로 `CharacterPortrait` 이 526줄이 되어 molecules 2위의 1.8배가 되자 그 판단을 다시 물어야 했는데 표를 근거로 쓸 수 없었다. **molecules 는 잎이고 organisms 는 조합이다**(결정 1). molecule 은 atoms 를 조합해 프롭을 그리는 데서 끝나고 다른 molecule 을 안 쓴다. organism 은 다른 molecule 을 조합하거나 열고 닫기·선택·애니메이션처럼 시간에 걸친 동작을 소유한다(둘 중 하나만 맞아도 organism 이다). 도메인을 아는지는 기준이 아니다 — 그것으로 가르면 남는 molecule 이 없다. atoms 의 정의는 그대로다. 그 기준으로 넷을 올린다(결정 2): `CharacterPortrait`(526·`CharacterAvatar` 조합) · `MesoPad`(289·`SheetTextInput`) · `CharacterRow`(258·`CharacterAvatar`) · `CharacterRail`(68·`CharacterPortrait`). 남는 molecules 13개가 전부 잎이 된다. **넷을 함께 옮기는 것이 이 결정의 값이다** — 크기가 튀는 둘만 옮기면 규칙이 「큰 것은 올린다」가 되고 다음에 `CharacterRow` 를 보는 사람이 같은 판단을 처음부터 다시 한다. **크기는 기준이 아니다**(결정 3). 526줄인 것은 복잡해서가 아니라 조합이라서이고, 그중 141줄은 SVG 속성 나열이다. 대가는 organisms 가 13개에서 17개가 되어 그 층의 「화면의 구획」 감각이 `CharacterRow`(줄 하나)에서 약해지는 것과, 규칙이 기계로 안 검사되는 것이다(`layer-dependencies.test.ts` 는 여전히 방향만 본다). |

> 🔗 표시가 붙은 행은 **파일 배너에 «지금도 코드가 따르는 결정» 목록이 있다.**
