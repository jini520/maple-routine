# Architecture Decision Records (인덱스)

> 결정 원장. **각 ADR 전문은 [`adr/`](./adr/) 에 파일 하나씩** 있다(표준 ADR 컨벤션). 이 파일은 슬림 인덱스이니, 필요한 결정만 `adr/ADR-NNN.md` 로 열어라 — 전체를 컨텍스트에 올리지 말 것. `[[ADR-NNN]]` 은 경로가 아닌 논리적 참조라 그대로 쓴다. 새 결정은 `adr/` 에 파일을 추가하고 이 표에 한 줄 넣는다.

## 철학
로컬 우선(local-first) MVP. 백엔드/로그인 없이 기기 안에서 완결되는 최소 구현을 우선 선택하고, 알림·백그라운드 사운드처럼 웹만으로 불가능한 기능만 네이티브 브릿지에 의존한다. Nexon Open API 연동([[ADR-007]])도 사용자 개인이 발급받은 API 키를 기기에 저장해 직접 호출하는 방식이라 이 원칙을 벗어나지 않는다. 게임 밸런스에 따라 자주 바뀌는 수치 데이터는 로직과 분리해 유지보수 비용을 낮춘다.

## 결정 목록

| ADR | 제목 · 상태 |
|---|---|
| [ADR-001](./adr/ADR-001.md) | Capacitor 기반 하이브리드 앱 채택 |
| [ADR-002](./adr/ADR-002.md) | Vite 기반 React SPA (Next.js 미사용) |
| [ADR-003](./adr/ADR-003.md) | 백엔드 없이 기기 로컬 저장소만 사용 (MVP) |
| [ADR-004](./adr/ADR-004.md) | 서버 푸시 없이 로컬 알림만 사용 |
| [ADR-005](./adr/ADR-005.md) | 사냥 타이머는 상시 표시 알림 + 주기적 사운드로 구현 |
| [ADR-006](./adr/ADR-006.md) | 게임 레퍼런스 데이터는 코드와 분리된 데이터 소스로 관리 |
| [ADR-007](./adr/ADR-007.md) | 일간/주간 콘텐츠·보스 진행 상태는 Nexon Open API로 동기화 |
| [ADR-008](./adr/ADR-008.md) | 에러 핸들링 및 복원력 정책 |
| [ADR-009](./adr/ADR-009.md) | 직업(전직) 기반 테마 시스템 도입 |
| [ADR-010](./adr/ADR-010.md) | 물욕템 수익 반영 및 랜덤 컨테이너 아이템 결과 기록 |
| [ADR-011](./adr/ADR-011.md) | 물욕 아이템 드랍 화면의 보스 목록 출처, 난이도 무관 아이템 노출, 이미지 에셋 |
| [ADR-012](./adr/ADR-012.md) | 일간/주간 스케줄러 캐릭터 추적(선택) 정책 및 동기화 범위 제한 |
| [ADR-013](./adr/ADR-013.md) | 화면 구조를 "일간/주간 스케줄러"에서 "컨텐츠/보스 스케줄러"로 개편 |
| [ADR-014](./adr/ADR-014.md) | 보스 수익 계산기 — 완료 감지 시 파티원 수 자동 기록 및 캐릭터별 레이아웃 |
| [ADR-015](./adr/ADR-015.md) | 캐릭터 관리 피커 — 즐겨찾기 우선 정렬, 이미지 카드 UI, `character/basic` 병렬 조회 |
| [ADR-016](./adr/ADR-016.md) | 온보딩 시 전체 캐릭터 데이터 예열 + 캐시 우선 표시(Stale-While-Revalidate) |
| [ADR-017](./adr/ADR-017.md) | 보스 수익 계산기 캐시 확장 + 캐릭터 순서/선택 일관성 버그 수정 + 주간 화면 정리 (구현 완료) |
| [ADR-018](./adr/ADR-018.md) | 보스 스케줄러 UI 개편 — 탭 pill 스타일 통일, 보스 리스트 개별 카드화, 보스 초상화 크롭 설정 도입 (구현 완료) |
| [ADR-019](./adr/ADR-019.md) | 파티 관리 — 캐릭터+보스+난이도 단위 상시 파티 인원 설정 도입 (구현 완료) |
| [ADR-020](./adr/ADR-020.md) | 일일퀘스트 카드 도입 — 보스 카드 패턴 재사용, `quest_state` 정규화, 지역 배경 매칭 (구현 완료) |
| [ADR-021](./adr/ADR-021.md) | 주간 컨텐츠 카드 도입 — 4개 카테고리(에픽던전/지역 주간 퀘스트/무릉도장/길드)별 카드화 (구현 완료) |
| [ADR-022](./adr/ADR-022.md) | Live Update(OTA) 도입 — Capgo 플러그인 + GitHub Releases 자체 호스팅 (구현 완료) |
| [ADR-023](./adr/ADR-023.md) | 보스 수익 계산기 → "보스 수익" 페이지 개편 — 주간/월간 탭, 기간 네비게이터, 로컬 우선 캐싱 (구현 완료) |
| [ADR-024](./adr/ADR-024.md) | Live Update 버전 형식 버그 수정 + 베타 채널 도입 — 빌드 시점 분리 (구현 완료) |
| [ADR-025](./adr/ADR-025.md) | 스플래시 스크린 구현 — 네이티브 스플래시 하나로 통일 + MIUI force-dark 대응 (구현 완료) |
| [ADR-026](./adr/ADR-026.md) | Live Update 관찰용 UI — 설정 내 "앱 업데이트" 섹션 + 인앱 즉시 적용 (구현 완료) |
| [ADR-027](./adr/ADR-027.md) | Live Update 사용자 동의형 업데이트 UX + 스토어 업데이트 구분 (구현 완료) |
| [ADR-028](./adr/ADR-028.md) | Android 네이티브 스플래시 최소화 — 무채색 즉시 해제 + JS(HTML) 풀 브랜드 스플래시로 대체 (설계 후 폐기, 구현 없음) |
| [ADR-029](./adr/ADR-029.md) | Android 전용 경량 `SplashActivity` 도입 — Capacitor를 거치지 않는 비트맵 스플래시로 다크모드 진짜 브랜드색 확보 (구현 완료) |
| [ADR-030](./adr/ADR-030.md) | 컨텐츠 스케줄러 캐시 — 미접속·월드/계정 공유·API 오염에 대한 다단계 캐싱 전략 (구현 완료) |
| [ADR-031](./adr/ADR-031.md) | 보스 스케줄러 처치 카운트 자체 계산 · 등록/처치 난이도 불일치 보정 · 챌린저스 시즌 보스 표시 · 파티 관리 모달 필터 (구현 완료) |
| [ADR-032](./adr/ADR-032.md) | 보스 수익 페이지 — 롤링 조회 윈도우 처리, 파티원 수 스냅샷 버그 수정, 미완료 보스 선등록, UI 정리 (구현 완료) |
| [ADR-033](./adr/ADR-033.md) | 보스 수익 — 등록 난이도 ≠ 실제 처치 난이도 오류 수정 (ownComplete 도입), bossDaily/bossWeekly 교차 오염 수정 (구현 완료) |
| [ADR-034](./adr/ADR-034.md) | 스케줄러 최초 동기화·캐시 유실 대비 — 어제(또는 그제) 응답으로 빈 섹션 선채움 (구현 완료, world/account 공유 콘텐츠 선채움은 범위 밖) |
| [ADR-035](./adr/ADR-035.md) | 스케줄러 자동/수동 트래킹 모드 도입 — 앱 자체 체크리스트 지원 (구현 완료) · 결정 22: 모드 선택 화면 재설계 — 카피를 `title`/`description`/`caution` 세 필드로 쪼개고(자동 91자·수동 84자 문단 폐기) 옵션마다 아이콘(`Gamepad2`/`ListChecks`, 배지 없이 단독) + 정보 톤 주의 박스, 설명은 접지 않고 항상 표시(고르기 **전에** 비교하는 화면), 결정 16의 "신규 스타일 금지" 폐기하되 바깥 카드 클래스는 유지, 온보딩·설정 모달 동시 적용 (구현 완료, 2026-08-03, 이슈 #59) · 결정 23: 설정 모드 전환에 **선택 → 확인** 단계 도입 — 탭은 선택일 뿐이고 우측 정렬 `취소`+`적용`(설정 다른 확정 모달과 같은 골격, 파괴적 동작이 아니라 확정 버튼만 `bg-primary`), 현재 모드와 같으면 적용 비활성(같은 모드 재선택 즉시 닫힘은 폐기), 기다림은 규격 밖 18px 인라인 스피너에서 **적용 버튼 안 16px + "적용 중"**으로(진행률 모달·덮개는 기각, [[ADR-061]] S7 중 이 자리만 해소) (구현 완료, 2026-08-03) |
| [ADR-036](./adr/ADR-036.md) | 보스 수익 페이지 — 캐릭터 내부 보스 표시 순서 결정성 확보(weekly-bosses.json 정규 순서) (구현 완료) |
| [ADR-037](./adr/ADR-037.md) | 보스 수익 페이지 — 이전 기간 게이트를 실제 조회 가능성·캐시와 일치, 새로고침 버튼 현재 기간 한정, 탭/기간별 아코디언 독립 (구현 완료) |
| [ADR-038](./adr/ADR-038.md) | 보스 수익(주간) — 드롭 아이템 기록(바텀시트·랜덤 상자 드릴다운), `lib/item-icons` 구현, 고가 아이템 드롭 연출 (구현 완료 2026-07-26) |
| [ADR-039](./adr/ADR-039.md) | 바텀시트 자체구현 → vaul(Drawer) 라이브러리 교체 (구현 완료 2026-07-26) |
| [ADR-040](./adr/ADR-040.md) | 보스 드롭 입력 시트 개편 — 난이도 무관 통합 표시·간소화 난이도 뱃지·고정 드롭 읽기 전용·카테고리 아이콘·고가 리스트 확장(익셉셔널 해머)·드롭 연출 토글 (구현 완료, 2026-07-26 / 토글 라벨·방향 정정 4, 2026-08-08) |
| [ADR-041](./adr/ADR-041.md) | 보스 반지 상자 결과 선택 개편 — 백옥 기준 목록·저가치 반지 '기타' 묶음·반지→레벨 선택 순서·연마석 레벨 없음 (구현 완료, 2026-07-26) |
| [ADR-042](./adr/ADR-042.md) | 캐릭터 추적 목록 통합 — content/boss 분리 폐기, 단일 목록·단일 현재 선택 (구현 완료) |
| [ADR-043](./adr/ADR-043.md) | 캐릭터 관리 저장 — 변경 없으면 저장 비활성·제거만 하면 재동기화 생략·추가분만 fetch (구현 완료, 이슈 #31) |
| [ADR-044](./adr/ADR-044.md) | 보스 드롭 시트 난이도별 표시 — 통합 표시 폐기([[ADR-040]] 결정 1·2 반전)·난이도 필터·미완료 토글/완료 고정 (구현 완료, 2026-07-27) |
| [ADR-045](./adr/ADR-045.md) | 보스 수익 — 고가 아이템 드롭 시 캐릭터/보스 카드 강조 효과(회전 샤인 테두리·글로우·획득 아이템 배지·행 배경) (구현 완료, 2026-07-28) |
| [ADR-046](./adr/ADR-046.md) | 보스 수익 — 총 수익 요약을 카드에서 "노-카드 헤드라인"으로 재설계(기간 전체 고가 드롭 뱃지 포함) (구현 완료, 2026-07-28) |
| [ADR-047](./adr/ADR-047.md) | 보스 수익 — 펼친 캐릭터 카드의 헤더를 sticky로 고정(펼침 셸 `overflow-hidden` 제거·페이지 헤더 높이 실측 오프셋) (구현 완료, 2026-07-28) |
| [ADR-048](./adr/ADR-048.md) | 고가 드롭 연출 — DropEff 프레임 정렬을 프레임별 origin 테이블로 교체 + ScreenEff 배율 고정(유실된 WZ origin을 템플릿 정합으로 복원) (구현 완료, 2026-07-28) |
| [ADR-049](./adr/ADR-049.md) | 보스 수익 — 헤더 정렬·유격 4종 정리(새로고침 30px 탭 줄 인라인화·총 수익 뱃지 absolute·펼침 셸 `overflow: clip`·보스 행 89px 고정) (구현 완료, 2026-07-28) |
| [ADR-050](./adr/ADR-050.md) | iOS 하단 탭 동시 탭 → 문서 전체 리로드 → 보스 수익 데이터 유실 (탭바 클릭 캡처 인터셉터·SQLite 커넥션 자가복구·조회 실패와 기록 없음 구분) (구현 완료, 2026-07-29) |
| [ADR-051](./adr/ADR-051.md) | 메이플 ID 계정 선택 — 단일 계정 자동 확정 폐기, 항상 선택 화면 경유 (구현 완료, 2026-07-29) |
| [ADR-052](./adr/ADR-052.md) | 캐시 데이터 삭제 범위 — 사용자 설정(`trackingMode`·`dropEffect`) 보존, SQLite 테이블 목록을 `db.ts` 정의 배열로 단일 진실 공급원화, 확인 모달 문구 동기화 (구현 완료, 2026-07-29) |
| [ADR-053](./adr/ADR-053.md) | 캐릭터 관리 피커 — `access_flag` 미확인 캐릭터 비표시, 콜드 스타트 로딩 스피너 (캐시가 있을 때의 SWR 동작은 유지) (구현 완료, 2026-07-29) · 결정 1(access_flag 게이트)은 [[ADR-086]] 결정 3의 활동 관측으로 대체(2026-08-03) |
| [ADR-054](./adr/ADR-054.md) | 보스 수익 — 주간 보스 처치 수 표시(캐릭터별 `n/12` · 월드별 결정석 판매 한도 `n/90`) (구현 완료, 이슈 #52·#53). 표시 범위(결정 4)는 [[ADR-059]]로 폐기 |
| [ADR-055](./adr/ADR-055.md) | 수동 선택 가드 — 주간 보스 12개 한도 (구현 완료, 이슈 #62). 요구 레벨 미달 잠금(이슈 #32)은 정정 2로 폐기 |
| [ADR-056](./adr/ADR-056.md) | 보스 관리 목록 구성 — 미출시 보스 제외 · 시즌 보스는 챌린저스 월드 전용 (구현 완료) |
| [ADR-057](./adr/ADR-057.md) | 길드 콘텐츠는 길드 가입 캐릭터만 선택 — `character_guild_name` 배관, "모름"과 "미가입" 분리 (구현 완료) |
| [ADR-058](./adr/ADR-058.md) | 캐시 데이터 선택 삭제 — 2그룹(일반 / 보스 수익·드롭 기록), 그룹 정의를 차집합으로 두어 새 키·새 테이블 자동 편입 (구현 완료, 2026-07-29, 이슈 #57) |
| [ADR-059](./adr/ADR-059.md) | 보스 수익 — 결정석 칩·처치 진행 링을 과거 기간과 월간 탭까지 확장(월간 링은 월간 보스 기준), [[ADR-054]] 결정 4 폐기 (구현 완료, 2026-07-29) |
| [ADR-060](./adr/ADR-060.md) | 빈 상태 디자인 통일 — 공용 `EmptyState`(page/inline), 목록 빈 상태는 컨텍스트 아이콘·페이지 빈 상태는 단풍잎 유지, CTA를 문구가 지시하는 곳으로, "조회 불가"는 별도 `UnavailableNotice`(정보 톤) (구현 완료, 2026-07-29, 이슈 #55) |
| [ADR-061](./adr/ADR-061.md) | 로딩 표현 통일 — 스피너 2종(버튼 16px=트레일 링 / 그 외=신규 스윕), 콜드 스타트·백필은 셸 승계 카드(점선 폐기), 진행률은 얇은 바 하나로(`MapleWaveProgress` 폐기), `~중...`은 새로고침 옆 한 곳만 (구현 완료, 2026-07-30 · S7 모달 차단만 보류) · PTR 인디케이터 예외는 [[ADR-074]] |
| [ADR-062](./adr/ADR-062.md) | 실패 상태 UI 통일 — 공용 `ErrorState`(원인 + 행동, 배지 없는 단독 아이콘), 항목이 있을 땐 스탈 배너, 에러 문구 어미 `~습니다` 통일, [[ADR-053]] 결정 3 "재시도 버튼 없음" 폐기 (설계 확정, 2026-07-30) |
| [ADR-063](./adr/ADR-063.md) | 동기화 실패는 인라인 문단이 아니라 토스트 — 배치 기준("문구가 사라진 자리에 남는 것이 있는가"), 스토어가 버리던 원인을 `toScheduleSyncError`로 보존(401/429가 처음으로 화면에 도달), 파티원 수 실패의 `err.message` 원문 노출 제거 (구현 완료, 2026-07-30) |
| [ADR-064](./adr/ADR-064.md) | 테마 토큰 스케일링 — 17→34토큰, 채움 위 전경색 흰색·`text-bg` 고정 파기(`on-*` 신설), 합성 틴트 67곳을 `*-tint` 값 토큰으로, `*-text`→`*-ink` 개명, 시드 3색+`mode`에서 나머지 30토큰 파생 생성, [[ADR-062]] `error-tint` 금지 폐기 (설계 확정, 2026-07-30) |
| [ADR-065](./adr/ADR-065.md) | 무음 실패 5곳 처리 — 온보딩 키 실패 원인별 토스트 · OTA 다운로드 실패 모달 분기(+`GHOST_BTN` 축소) · 캐시 삭제 실패는 리로드 후 토스트(`sessionStorage`) · 관리 화면 토글 실패 토스트 · 전역 ErrorBoundary(폴백은 "다시 시작" 하나만) (구현 완료, 2026-07-30) |
| [ADR-066](./adr/ADR-066.md) | 수익 아이콘 — lucide `Coins` → 커스텀 입체 SVG `ProfitIcon`(탭바·헤드라인·빈 상태 세 자리 통일), lucide 규격 준수를 조건으로 "새 아이콘은 lucide에서만" 규칙에 도메인 아이덴티티 예외 신설 (구현 완료, 2026-07-31, 이슈 #56) |
| [ADR-067](./adr/ADR-067.md) | Nexon API 실패 종류 분리 + 보스 수익 기간 6상태 모델 — 실측 계측(2026-07-31)으로 "비활성이면 200이 아니다" 전제 반증, `date` 상한(오늘−1일) 발견, 축약 응답 오판·유령 기록·기간 네비게이션 벽 (구현 완료, 2026-07-31, PR #83 — 결정 5는 [[ADR-086]] 결정 4로 구현 완료, 2026-08-03) |
| [ADR-068](./adr/ADR-068.md) | 기간 6상태 UI 표현 — 프리미티브 3종 배분 + `notCollected` pending 톤(넷째 얼굴), 행동 있는 상태만 버튼(`notChecked` 조회 / `failed` 다시 시도), 조회 불가 캐릭터 피커 별도 섹션 + 온보딩 대표 캐릭터 프로브, 이전 게이트만 확장, 월드 이전은 따로 말하지 않음 (구현 완료, 2026-07-31, PR #83) · 결정 4의 "조회 불가는 항상 남긴다"는 [[ADR-086]] 결정 3으로 "추적 중일 때만"으로 좁힘, 계정 선택 차단은 결정 8(2026-08-03) |
| [ADR-069](./adr/ADR-069.md) | 보스 수익 기록의 진실성 — `boss_profit_records` 에 `world` 스냅샷 컬럼(결정석 집계를 캐릭터→행 단위로), 난이도 확정 시 드롭 이관 + 그 난이도에서 획득 불가한 항목 삭제 (구현 완료, 2026-07-31, PR #85, 이슈 #78) |
| [ADR-070](./adr/ADR-070.md) | 주문서 교환권 3종 — 죽은 `scroll` 카테고리를 `consumable` 로 흡수(카테고리 신설 대신), 이름을 "스크롤 교환권"으로 통일, 찬란한 흉성(노멀) 추가·기존 위치는 보존, 파풀라투스 마크 실제 아이콘 반영 (구현 완료, 2026-07-31) |
| [ADR-071](./adr/ADR-071.md) | 드롭 획득 히스토리 — 별도 테이블 없이 `boss_drop_records` 전 기간 조회(삭제 전파가 공짜), 드롭의 '언제'는 `period_key`(`recorded_at`은 replace-all이 덮어 신뢰 불가·감사 필드로 격하), 정렬 `period_key DESC`, 미획득 기간은 고가 전체 하나·달력 주 경과, 획득 불가 기록은 확정 난이도 조합만 표시 필터, 기록 한 건은 게임 로그체 한 줄 문장(조사 계산·word joiner)·꾸밈은 고가만, 미획득 요약은 기간이 길수록 시드는 단풍잎 5단 (구현 완료, 2026-08-01, 이슈 #54) |
| [ADR-072](./adr/ADR-072.md) | 당겨서 새로고침 — 3개 탭 최상위 화면. 외부 PTR 라이브러리 대신 커스텀 훅(문서 전체 스크롤이라 컨테이너 ref가 없다), 최상단 판정은 `window.scrollY <= 0`, store 액션 신설 없이 기존 `refresh(trackedOcids ?? [])` 를 항상 1인자로, 인디케이터는 sticky 헤더 안 `absolute top-full` 배너(목록을 밀어내지 않는다 — 리플로우·수익 헤더 실측 오염 방지)·경계 페이드 다음 형제, 당김은 정적 단풍잎 회전·재조회는 스윕 스피너([[ADR-061]] "스피너 2종" 유지), iOS 러버밴드는 `overscroll-behavior-y: none`, 수익 탭은 `isCurrentPeriod` 에서만, 스크롤 가능한 조상(모달·바텀시트) 안에서 시작한 터치는 페이지 당김에서 제외(결정 14 — 계측으로 확인된 결함의 후속 수정) · 인디케이터 형태(결정 4·5·7)는 [[ADR-073]]으로 폐기 · 문구·마크(결정 6·7)는 [[ADR-074]]로 폐기 · 결정 9 활성 조건은 [[ADR-076]]으로 확장 (구현 완료, 2026-08-01, 이슈 #38 · 결정 14는 후속 수정 · iOS 실기기 검증 보류) |
| [ADR-073](./adr/ADR-073.md) | 당겨서 새로고침 인디케이터 — 불투명 배너에서 목록 이동(RN `RefreshControl` 감각)으로. 헤더는 `sticky top-0` 계약대로 고정하고 목록 블록만 `transform: translateY()` 로 내려간다(레이아웃을 안 건드려 [[ADR-072]] 결정 4의 두 근거 — 리플로우 없음·수익 헤더 실측 높이 불변 — 을 그대로 지킨다), 휴지 시엔 `transform` 속성 자체를 걸지 않아 평상시 DOM 동일(`translateY(0)` 도 containing block·stacking context를 만들어 [[ADR-047]] 중첩 sticky에 영향), 드래그 중 전환 끔·손 뗀 뒤에만 전환, 재조회 중에는 임계 위치(56px)에 머묾, 인디케이터 높이와 목록 오프셋은 같은 순수 함수(`resolveContentOffsetPx`)에서, 인디케이터는 배경·테두리 없이 벌어진 틈 세로 중앙(틈이 곧 페이지 배경), 문구·스피너 2종 규칙은 [[ADR-072]] 결정 6·7 그대로 (구현 완료, 2026-08-01, 이슈 #38 · 실기기 검증 보류) · 마크·문구(결정 7·9)는 [[ADR-074]]로 폐기 |
| [ADR-074](./adr/ADR-074.md) | 당김 인디케이터 마크 — 인디케이터에서 문구(3상태)를 없애고 단풍잎 **외곽선 링** 하나로. 당김 구간은 회전이 아니라 **진행률 드로잉**(`pathLength 300` · `strokeDasharray "300 300"` · `strokeDashoffset = 300 × (1 − 진행률)`, 회전으로는 "얼마나 남았는지"가 안 읽힌다), 임계에서 링이 닫히고 손을 떼면 **같은 링이 그대로 회전**(`MapleSpinner` 트레일 링) — 마크가 안 바뀌어야 한 동작으로 보인다, 두 구간 같은 크기 **28px**(틈 56px의 절반), 문구가 사라져 빈 라이브 리전이 되므로 `role="status"`/`aria-live` 제거하고 `aria-hidden`(재조회는 헤더 `조회 중...` 이 알린다), [[ADR-061]] 결정 1 "24px 이상은 스윕"에 **PTR 한 자리 예외** 신설(크기보다 연속성이 앞선다 — 결정 1 자체는 유효), [[ADR-073]] 결정 7·9 및 [[ADR-072]] 결정 6·7의 문구·마크 부분 폐기 (구현 완료, 2026-08-01, 이슈 #38 · 실기기 검증 보류) |
| [ADR-075](./adr/ADR-075.md) | 보스 수익 월간 탭 — 달 경계를 걸친 진행 중 주차(7/30~8/5)의 합계 원천. 진행 중 주차를 `liveRows` 로만 읽던 것을 "보고 있는 달이 이번 달인가"로 갈라, 지난 달 화면에서는 지난 주와 똑같이 로컬 기록에서 합산([[ADR-067]] 결정 4의 원천 규약을 주(週)가 아니라 화면 기준으로 적용). 상태는 `inProgress` 유지 · 백필·귀속 규칙([[ADR-023]]) 무변경 (구현 완료, 2026-08-02) |
| [ADR-076](./adr/ADR-076.md) | 보스 수익 — "진행 중인 주를 품은 지난 달"은 그 화면에서 새로고침한다. 새로고침 게이트를 `isLatestPeriod` → `isPeriodRefreshable`(= 최신 기간 \|\| `containsInProgressWeek`)로 넓혀 헤더 버튼·동기화 시각·당겨서 새로고침이 계속 한 플래그를 공유하고, `refresh` 는 그 경우 보던 기간을 유지한 채 동기화·자동 기록만 하고 화면 반영은 `loadPeriod` 에 넘긴다(캐시 우선 표시 단계의 화면 반영만 생략 — 스냅샷 갱신은 유지). 백필·기간 네비게이션 게이트 무변경, [[ADR-072]] 결정 9 활성 조건 확장 (구현 완료, 2026-08-02) |
| [ADR-077](./adr/ADR-077.md) | 드롭 히스토리를 `/profit` 의 **중첩 라우트 + 오버레이**로 — 형제 라우트라 이동 때마다 `BossProfitScreen` 이 언마운트돼 아코디언 펼침·보던 기간·스크롤이 전부 소실됐고(히스토리는 독립 페이지가 아니라 스택 화면이다), 같은 언마운트가 iOS WKWebView에서 stuck sticky 헤더 레이어를 다시 안 칠하게 만들어 복귀 시 헤더가 빈 화면으로 그려졌다(실기기 계측: 히트테스트 정상·회전으로 복구 → 페인트 실패 / 스크롤 내린 상태가 필요조건 / `backdrop-filter` 무죄). 언마운트를 없애 상태 보존과 페인트 결함 발화 조건을 동시에 제거하고, 스크롤은 오버레이가 자기 스크롤을 가져 저장·복원 코드 자체를 두지 않는다. `scrollTo(0,0)` + `scrollRestoration:'manual'` 안은 원인 사슬 검증에만 쓰고 폐기(상태 보존과 충돌) (설계 확정·구현 진행 중, 2026-08-02, 이슈 #54) |
| [ADR-078](./adr/ADR-078.md) | 기간 이동 깜빡임 — "페이지 리로드처럼 보인다"는 보고는 **리로드가 아님을 실기기 콘솔로 반증**(828줄 중 `WebView loaded` 1회, 부트스트랩 재실행 없음). 실제 원인은 `loadPeriod` 의 네이티브 브릿지 순차 왕복이 길어 중간 상태가 수백 ms 이상 보이는 것이고, 그동안 sticky 헤더의 조건부 블록이 붙었다 떨어지며 **높이가 바뀌어 레이어가 재생성**돼 정적 `h1` 까지 번쩍인다. `isPeriodChecked` target 루프를 `Promise.all` 로(월간 = 캐릭터×(1+주차) = 36회 직렬 → 병렬, `uncheckedTargets` 순서는 유지), `getSortedCharacterInfo` 가 읽은 프로필을 `buildRowsFromRecords`·`buildWeeklySubtotalsForMonth` 로 흘려보내 캐시 조회를 로드당 캐릭터 1회로(3N → N, 캐시 없는 ocid 제외 규칙은 `characterName: string \| null` 로 보존). 표현·정책 무변경 — 로딩 표시 시점·아코디언 펼침 리셋·`periodKey` 커밋 시점은 범위 밖 (호출 절감은 구현 완료·측정 확인, 2026-08-02 — **깜빡임 대책으로는 실패**했고 원인·처방은 [[ADR-079]]) |
| [ADR-079](./adr/ADR-079.md) | 기간 이동 깜빡임의 진짜 조건은 **스크롤을 내려 sticky 헤더가 stuck 일 때**다([[ADR-077]] 빈 헤더 결함과 같은 필요조건). 계측으로 앞선 후보를 전부 반증 — 언마운트 0회 · `isPeriodLoading` 계속 false · 헤더 높이 불변(`headerH=304`, [[ADR-049]] 덕) · 이동당 렌더 2회 · [[ADR-078]] 호출 절감(97→41)도 체감 무변화. 남는 메커니즘은 stuck sticky 레이어의 **서브트리가 바뀔 때 레이어가 재생성되며 한 프레임이 비는 것**이라, `will-change: transform` 으로 레이어를 상시 고정해 "재생성" 고리를 끊는다. 실제 `transform` 은 쓰지 않는다([[ADR-073]] 결정 3 — containing block 을 만들어 중첩 sticky 후손 기준을 바꾼다). 적용은 증상이 재현되는 보스 수익 헤더 하나로 한정 — **실기기에서 무효로 판명돼 되돌렸다**(페인트가 아니라 레이아웃 문제였다). 배제 계측표는 유효하나 처방은 폐기, 실제 원인·처방은 [[ADR-080]] (실패로 종결, 2026-08-02) |
| [ADR-080](./adr/ADR-080.md) | 기간 이동 깜빡임의 정체 — **스크롤 클램프 프레임**. 프레임 단위 계측에서 모든 이동에 `scrollY=1154 docH=874 headerTop=-1154` 인 1~2프레임이 찍혔다(헤더가 화면 밖). 연쇄: 카드를 펼쳐 문서 1939px·스크롤 1154 → `‹` 로 `periodKey` 가 바뀌며 아코디언 key(#27 펼침 리셋)가 전부 접혀 **문서가 874px로 붕괴** → 최대 스크롤이 0이 되는데 클램프 전 1~2프레임 동안 레이아웃은 짧고 오프셋은 1154라 sticky 헤더가 `-1154` 에 그려짐 → 헤더가 사라졌다 돌아옴. 최상단에서 안 나는 이유는 잘라낼 스크롤이 없어서다. 처방은 `useLayoutEffect` 로 `(tab, periodKey)` 변화 시 **페인트 전에** `scrollTo(0,0)` — 목적지가 0인 것이 중요하다(0은 sticky 헤더의 자연 위치라 컴포지터 지연에도 안전, [[ADR-082]] 폐기로 확인) — 어차피 클램프되는 도착지라 동작 변경이 아니고, 히스토리 왕복은 `periodKey` 를 안 바꿔 [[ADR-077]] 의 스크롤 유지 계약도 그대로. 페인트로 오진했던 [[ADR-079]] 폐기 (설계 확정·구현 진행 중, 2026-08-02) |
| [ADR-081](./adr/ADR-081.md) | 아코디언 펼침을 **기간별로 기억**한다 — #27의 무조건 리셋 폐기. #27이 막으려던 "한 기간의 펼침이 다른 기간으로 새는 것"은 옳지만, remount 로 달성해 **되돌아왔을 때도 잃었다**(사용자: 돌아왔을 때 펼쳐져 있는 건 오히려 바라는 바, 지난 주에서 펼친 적 없는 카드가 펼쳐지는 것만 문제). 펼침을 `BossProfitScreen` 의 `expandedKeys: Set<"tab-periodKey-ocid">` 로 올려 **다른 기간은 키 자체가 달라 누출이 구조적으로 불가능**하게 하고, 아코디언 `key` 는 그대로 둬(팝오버·헤더 실측 같은 다른 로컬 state 가 기간을 건너 딸려가지 않게) 되돌아온 기간의 카드가 처음부터 펼친 채 마운트되게 한다. **폐기(2026-08-02)** — 짝이 되는 스크롤 복원([[ADR-082]])이 iOS에서 불가능해 이것만으론 쓸모가 없었다("보던 곳으로 다시 스크롤해야 하는 건 똑같다"). #27 무조건 리셋으로 복귀. 되살리려면 헤더를 스크롤 맥락 밖으로 빼는 구조가 선행돼야 한다 |
| [ADR-082](./adr/ADR-082.md) | **스크롤 위치도 기간별로 기억**한다 — [[ADR-081]] 로 펼침만 복원되니 "결국 보던 곳으로 다시 스크롤해야 하는 건 똑같다"(사용자 지적). [[ADR-080]] 을 "최상단으로 간다"로 좁게 규정한 것이 원인이었고, 실제 조건은 **페인트 전에 유효한 값이 잡히는 것**이라 목적지는 자유롭다. `(tab, periodKey)` 별 마지막 스크롤을 기억해 같은 `useLayoutEffect` 에서 복원(없으면 0 — 펼친 적 없는 기간은 현행과 동일). 펼침이 복원되면 문서 높이도 같아져 저장값이 유효하므로 클램프도 깨진 프레임도 없다. **폐기(2026-08-02, 실기기)** — 전제가 틀렸다. 목적지는 자유롭지 않고 **0이어야 한다**: 0은 sticky 헤더의 자연 위치라 iOS 스크롤 스레드가 트랜스폼을 못 따라잡아도 헤더가 제자리에 그려지는 반면, 0이 아닌 값은 그 지연 프레임에 **헤더를 화면 밖으로 보낸다**(실물 확인 — 헤더 없이 보스 행이 최상단부터 그려진 프레임). sticky 헤더를 둔 채로는 스크롤 복원 불가 |
| [ADR-083](./adr/ADR-083.md) | 남아 있던 인라인 실패 문구 3자리를 **토스트**로 — [[ADR-063]] 결정 1이 지운 것은 전역 실패 문단 하나뿐이었고, 스케줄러 두 화면의 **캐릭터별** 실패 문단은 남아 있었다. `syncSchedules` 는 캐릭터 단위 실패를 던지지 않고 결과에 실어 반환하므로(401/429는 나머지 캐릭터까지 같은 에러로 채운다) 전역 `error` 가 채워지지 않고, 그래서 두 화면에서는 **토스트가 아예 안 뜨고 액션 없는 빨간 줄만** 남아 결정 3(401 → 설정 열기)이 무력했다. ① 캐릭터별 실패를 `useScheduleSyncErrorToast(selected?.error)` 로 ② 그 경로로 처음 도달하는 `characterUnavailable` 은 **액션 없음**(영구 실패에 "다시 시도"는 눌러도 같은 400) ③ 보스 수익 기간 실패는 **카드가 있을 때만** 토스트(없으면 빈 칸이 남아 `ErrorState` 유지)·중복 키는 `periodKey` 이되 로딩 시작 시 비운다 ④ 온보딩 계정 선택 인라인 제거(3종은 이미 토스트라 중복이었고, `storageWriteFailed` 만 무음이라 `selectAccount` catch에 토스트 추가). [[ADR-063]] "남는 인라인" 표 갱신 (구현 완료, 2026-08-02) |
| [ADR-084](./adr/ADR-084.md) | 아코디언 **접기**도 같은 스크롤 클램프 프레임을 만든다 — [[ADR-080]]의 방어가 `[tab, periodKey]` 에만 걸려 이 경로는 비어 있었다. 접기 = 본문 언마운트(주간 본문 ≈ 보스 행 89pt × 최대 12행)라 문서가 뷰포트보다 큰 폭으로 줄고, 접을 때는 거의 항상 스크롤이 내려가 있어(펼친 카드 헤더가 stuck 이라 그걸 탭해 닫는다) 클램프 전 1~2프레임이 무효 오프셋으로 그려진다. 스크린샷 픽셀 계측(2026-08-02, 402×874pt)으로 두 얼굴을 분리 — 하나만 펼쳤을 때는 첫 카드 상단 324pt(정상 320)로 **레이아웃은 정상이고 헤더 자리만 미도색**([[ADR-077]] 결함의 순간판), 여러 개 펼쳤을 때는 화면 y=0부터 보스 행 + 형제 카드 아바타가 회색 빈 원(**헤더가 화면 밖** + 레이어 재래스터). 처방은 접힘 전이에서 `useLayoutEffect` 로 `scrollTo(0, min(scrollY, scrollHeight - innerHeight))` — 브라우저가 어차피 도달할 도착지를 페인트 전으로 앞당기는 것뿐이고, 유효 범위 안이면 건드리지 않는다. **폐기 — 실기기 계측으로 반증**(2026-08-02): 레이아웃 이펙트 시점엔 이미 잘려 있어 `clamp skip` 만 찍혔고, 깨진 프레임은 그보다 늦게 오는 스크롤 스레드의 옛 오프셋이 만든다. 코드는 되돌렸고 실제 원인·처방은 [[ADR-085]] (폐기, 2026-08-02) |
| [ADR-085](./adr/ADR-085.md) | 접기 깜빡임의 정체는 **iOS 스크롤 스레드가 되돌려 보내는 옛 오프셋**이다 — 프레임 계측(1.0.41 실기기)에서 클램프는 우리 코드보다 먼저 끝나 있었고(`clamp skip`), 깨진 프레임은 **정상 프레임 하나 뒤에**(+35~40ms) 접기 전 오프셋이 되돌아와 페이지 전체가 그 값으로 다시 그려지는 것이었다(① `y1065 h-1065 c-671`, ② `y1533 h-468 c4` — 밀린 거리 = 잘린 양). 그래서 JS로 스크롤을 만지는 처방은 전부 무효([[ADR-084]] 폐기). 두 고리를 함께 끊는다 — ① 페이지 헤더를 `sticky top-0` → **`position: fixed` + 실측 높이 spacer**(sticky 는 화면 위치가 스크롤 오프셋의 함수라 옛 오프셋에 딸려 날아간다; 이 헤더는 문서 최상단 첫 요소라 **보이는 모습은 어떤 스크롤에서도 동일**하고 [[ADR-073]] 결정 1과도 더 정확히 맞는다. 헤더 높이 실측은 `useLayoutEffect` 로 옮겨 첫 프레임 spacer=0 튐 방지) ② **접기 전에** 문서 높이가 아직 그대로일 때 접은 뒤의 최대 스크롤로 먼저 옮기고 다음 프레임에 접어 **클램프 자체를 만들지 않는다**(사라질 높이 = 셸 − 헤더 실측). 목록 전용 스크롤 컨테이너 안([[ADR-072]] 전제 전복)은 같은 효과를 훨씬 큰 변경으로 내므로 보류 (구현 완료·**실기기 검증 완료**, 2026-08-02 — 두 경우 모두 증상 소멸) |
| [ADR-086](./adr/ADR-086.md) | 계정 → 캐릭터 선택 리모델링 — ① 온보딩 진행 단계를 **저장된 값에서 파생**해 끊긴 지점부터 재개(새 진행 상태 키를 두면 진실이 둘이 된다) ② `trackingMode` 미선택을 `'auto'` 로 읽지 않음(동작 기본값은 그대로 자동, 게이트만 "골랐는가"를 구분) ③ 후보 자격을 `access_flag` 게이트에서 **활동 관측**으로(= `access_flag: true` OR 최근 14일 캐릭터 범위 항목 완료 기록, 월드/계정 공유 항목 제외 — 다른 캐릭터가 만든 완료다) ④ **(ocid, 날짜) 조회 원장** 신설 — 같은 날짜 재조회 금지, 성공·00003·00004만 기록하고 00009·네트워크는 미기록, [[ADR-067]] 결정 5(백필 포기 조건)가 이 형태로 구현되어 보스 0건 캐릭터의 매 동기화 14회 영구 반복이 새 날짜 1회로, 만료는 날짜 윈도우가 스스로 굴려 레벨 변화 트리거 불필요 ⑤ 자격 판정을 예열·로스터가 공유(예열이 끊겨도 로스터가 완성) ⑥ 계정 변경은 **캐릭터를 다시 고를 때까지 커밋하지 않음** ⑦ 추적 캐릭터 최소 1명 ⑧ 조회 불가 계정 선택 차단 ⑨ `characterBasicCache` 역인덱스를 **계정별로** 분리(계정 변경 후 이전 계정 캐릭터가 뜨던 직접 원인) (구현 완료, 2026-08-03, 이슈 #87 — 실기기 검증 보류) |
| [ADR-087](./adr/ADR-087.md) | 보스 수익 직전 기간 대비 증감 칩 + 금액 변경 카운트업 — 칩은 헤드라인 **금액행** 옆(라벨행 `h-6` 제약·헤더 높이 예산 무관), 비교 대상은 "보고 있는 기간의 직전 기간"이고 합계 산식은 그 화면 총액과 동일(월간 = 직전 달 monthly + 그 달 weekly), 표기는 퍼센트 우선·같으면 `-`(화살표 없음)·기준이 0이거나 **기록이 없으면 둘 다 절대 증감**(사용자 결정 — 미확인을 0으로 취급해 증감 칩만 기간 상태 기계에서 분리, error-resilience 원칙 2의 의도된 예외), 진행 중 기간도 표식 없이 그대로 비교(`recordedAt` 은 처치 시각이 아니라 기록 시각이라 "같은 시점까지" 비교 불가), 색은 주식 신호(상승 빨강 hue 26 · 하락 파랑 hue 262)를 **`error`·`info` 와 같은 방식으로 파생한 신규 토큰** `rise`/`fall` 2쌍(34→38토큰, 고정 hex 는 라이트·다크 중 한쪽에서 반드시 죽는다)·같으면 방향이 없으므로 테마 색, 애니메이션은 카운트업 하나(`easeOutExpo` · 350ms)를 이 화면 **모든 메소 금액**에, 연타 시 재시작이 아니라 **지금 그려진 값에서 재조준**, **마운트도 값 변경과 동일하게** 다뤄 모듈 수준 `Map` 에 직전 표시값을 기억(키에 탭·기간을 넣어 기간 이동은 자연히 안 굴러간다 — 다른 값을 보게 된 것이지 값이 변한 게 아니다), 세션 한정이라 콜드 스타트 첫 페인트는 안 굴리되 캐시→동기화 구간은 값 변경이라 굴러감 · **정정 1**: 기간 이동은 **총 수익만** 굴린다(사용자 결정) — identity 교체는 재마운트와 동일하게 리셋하고, identity 의 기간은 store 의 목표 기간이 아니라 `loadedTab`·`loadedPeriodKey`(그려지는 데이터의 기간)에서 뽑는다(목표 기간으로 만들면 "새 키 + 옛 금액" 중간 커밋이 기억을 오염시켜 같은 왕복이 회차마다 갈렸다), 헤드라인 키에만 기간이 없다 (구현 완료, 2026-08-05) |
| [ADR-088](./adr/ADR-088.md) | 혼테일 테마 재작업 — 레퍼런스 3장(보스·입장맵·필드맵)에서 샘플링한 색으로, 시드·primary(용암 주황)는 **현행 유지**하고 무채색이던 `secondary`(`#7B777A`)만 **얼음 발광 `#6BB4E8`** 으로(primary 를 얼음으로 돌려 앱 전체를 심야 네이비로 바꾸는 안은 기각), `secondaryTint` 한 토큰만 파생 규칙을 덮음(갈색 표면에 얼음을 15% 섞으면 `#31272B` — 바꾸기 전 회색 틴트와 구분이 안 돼 목적이 배지에서 일어나지 않는다, 차가운 베이스 20% 혼합 `#203343`). **테마 배경 이미지**를 선택 필드로 신설 — 슬러그→URL 은 `import.meta.glob`(일일 퀘스트 지역 배경과 동일 방식), 크기·위치·어둡기·페이드를 JSON 값으로 조절, 배경 없는 테마는 CSS 자체를 안 냄. 벽지 고정은 `background-attachment: fixed`(iOS WKWebView 불안정) 대신 **`position: fixed` 백드롭**이고, 음수 z-index 는 **부모가 스태킹 컨텍스트일 때만** 부모 배경 위에 와서 앱 루트의 `bg-bg` 를 빼야 했다(안 그러면 이미지가 통째로 사라진다 — 브라우저 확인). sticky 헤더는 **반투명으로 열지 않는다**(배경만이 아니라 스크롤된 카드까지 비쳐 사용자 반려) — 헤더가 **뷰포트 크기로 그린 배경 조각을 래퍼가 잘라내** 백드롭과 같은 기하로 이어 그리고(`cover` 는 그리는 상자 기준이라 헤더 상자에 주면 배율이 어긋난다), 그래서 상단 페이드는 0. 값 조정은 `/debug/theme-background` 임시 라우트(실제 커스텀 프로퍼티를 실시간으로 바꿔 화면 자체가 프리뷰). 끝으로 `surface` 를 색조 유지한 채 두 단계 어둡게(`#241110`→`#190707`, `surface2` 동반·틴트 5종 규칙대로 재파생·규칙 파생값이 아니던 `infoTint` 는 같은 폭만 이동) — 카드가 밝기가 아니라 보더로 정의된다 (구현 완료, 2026-08-03) |
| [ADR-089](./adr/ADR-089.md) | 검은마법사 테마 추가(여섯 번째) — 레퍼런스 6장(일러스트·본체·창조의 아아온·파괴의 얄다바오트·초월체·보스 맵)에서 샘플링, 시안 3안을 앱 화면 목업으로 견줘 **A(카민 크림슨 `#E0245E` · 초월체 라벤더 `#A9A2E8` · 창조 핑크 `#E85BC0`, dark)** 채택. **본체의 진홍 `#D8072A` 를 그대로 쓰지 않은 것이 요점** — OKLCH 휴 23.8 이라 브랜드와 무관하게 고정된 `error`(휴 27)·`rise`(휴 26)와 3° 안이라 기록 버튼과 실패 토스트가 같은 색이 된다(고정 휴를 옮기는 건 그 고정의 존재 이유를 지우는 것이라 답이 아님), 맵의 카민(H 10.6)까지 밀어 16° 확보. `secondary` 를 초월체 라벤더로 둔 것은 그 토큰의 자리가 완료 배지·성공 톤이라 붉은 계열이 오면 뜻이 어긋나기 때문([[ADR-088]] 결정 1 과 같은 이유의 차가운 대비). `secondaryTint` 한 토큰만 규칙을 덮음 — 자주색 표면(`#1E1214` H8)에 라벤더(H288) 15% 를 섞으면 `#30252F`(C 0.024 · 휴 41° 이탈)로 `surface2`(`#302224`)와 구분이 안 되는 자두색, 차가운 베이스의 `#2D284A`(휴 이탈 1°)로 덮음([[ADR-088]] 결정 2 와 같은 실패 모드·같은 처방). **진단은 대비가 아니라 색조로** 한다(두 색 대비 1.04:1 은 "구분된다"고 읽히지만 같은 명도대·같은 색조라 안 보인다 — 대비비는 명도만 재므로 이 실패 모드를 원리적으로 못 잡는다), 다만 전역 테스트로는 세우지 않음(같은 척도로 레테 115°·엔젤릭버스터 45° — 낮은 채도 탓에 휴가 잡음이라 임계를 세우면 그 두 테마 값을 다시 정해야 함). 배경 이미지는 보스 맵(`blackmage-throne`, `dim` **0.78** — 처음 짚은 0.86 은 브라우저에서 그림이 거의 안 보였다, 맵이 원래 어둡고 `cover` 가 800×323 을 세로 뷰포트에 맞추며 가운데를 크게 확대해 잘라내기 때문 · 확정은 `/debug/theme-background`), 배경 가진 테마 1 → 2 가 되면서 **디버그 도구의 테마 전환 버튼이 '배경 없는 테마일 때만' 떠 혼테일↔검은마법사를 건너갈 수 없던 결함**을 함께 고침(항상 표시). 기본 다크 테마는 혼테일 유지. 코드 변경 없음 — `it.each(NAMES)` 회귀가 새 테마를 자동으로 검사([[ADR-064]] 결정 10·11) (구현 완료, 2026-08-03 · 배경 값 조정은 사용자 확인 대기) |
| [ADR-090](./adr/ADR-090.md) | 광고 도입 — 이슈 #58이 "테마 유료 판매 → 광고 제거 구독 → 후원 리워드 → **광고만**"으로 네 번 좁혀진 끝의 결론(리워드 붙은 후원은 스토어가 인앱 구매로 분류 — Play가 *"ad-free version of an app"* 을 Billing 필수 예시로 명시, Apple 3.2.1(vii)). 넥슨 약관 제6조⑥("승낙 없이 영리 목적 이용" — 광고만 달아도 해당)과 게임IP 가이드(광고·후원금은 **명시적 예외**, 다만 가이드가 정의한 UGC에 API 유틸리티 앱이 해당하는지 불명)를 검토했고, **동종 서비스 선례**(maplescouter=MAPLESTORY PARTNERS·후원 페이지 / maple.gg / chuchu.gg — 셋 다 게임 애셋을 광범위하게 쓰고 비제휴 고지를 단다)로 실무 리스크가 낮다고 보고 진행. 게임 애셋 사용은 카테고리 공통 전제라(이 앱도 312개) **테마 배경 2장만 교체하는 완화책은 철회**. 결정 — ① 웹 광고(AdSense/GPT) 불가: "Ad placement"가 *"Integrated into a software application **(does not apply to AdMob)**"* 를 금지하고 스크립트가 WebView를 감지해 억제하며 AdSense 정지가 AdMob까지 죽인다(`WebView API for Ads`는 인앱 브라우저용) ② 포맷 **Interstitial**·지점 **탭 전환** — 앱 시작 전면광고는 *"Do not place interstitial ads on app load"* 로 금지고 그 자리의 정답인 **App Open은 배포판에 구현이 없다**(community 8.0.0은 main에 2026-04-10 머지됐으나 미배포 + `prepare`가 husky라 git 설치 시 dist 미생성 / 대안 `capacitor-admob-nextgen`은 주간 333 다운로드라 사용자가 의존성 리스크 반려), 탭 이동은 *"between pages of app content"* 에 해당하고 **위반은 지점이 아니라 빈도**(*"after every user action"*)라 게이트로 해결, 하단 배너는 탭바와 2겹이라 사용자 기각·관리 화면 이탈은 노출 0 수렴이라 기각 ③ **3중 게이트**(마지막 노출 30분 · 앱 시작 후 60초 — 열자마자 누르면 "app load 광고"로 읽힌다 · **사전 로드된 광고가 있을 때만**, 없으면 조용히 건너뛰고 네비게이션은 절대 지연시키지 않는다), 마지막 노출 시각은 `storage/ads.ts`+Preferences 영속(`KEEP_KEYS` 제외 — 지워져도 광고 1회 더일 뿐), **스플래시 시퀀스 무변경** ④ `native/ads.ts` 어댑터 + 웹 no-op 스텁([[ADR-005]]), 플러그인이 로드 여부 조회 API를 안 줘 어댑터가 자체 플래그로 들고 게이트 판정은 `features/ads/policy.ts` 순수 함수로 분리, 광고 단위 ID는 플랫폼별·현재 Google 테스트 ID ⑤ 개인정보 처리방침이 선행 조건(현재 없음)·`AD_ID`·ATT·OTA로 배포 불가. 넥슨 파트너스 신청은 사용자 결정으로 범위 제외 (**구현 완료** — 실 광고 ID·개인정보 처리방침 게시 완료(mapleroutine.store), 스토어 미게시라 노출 제한·실기기 미검증, 2026-08-03~04, 이슈 #58) |
| [ADR-091](./adr/ADR-091.md) | Android 릴리스 서명 — Capacitor 스캐폴드에 `signingConfigs` 가 없어 AAB 업로드가 불가능하던 것을 채운다. **Play 앱 서명**(앱 서명 키는 Google 보관, 우리는 업로드 키만 — 분실해도 재설정 가능하므로 지킬 것은 "잃어버리지 않기"가 아니라 **"새어나가지 않기"**), 키스토어 파일은 **저장소 밖**(`.gitignore` 주석 해제는 하되 그것만으로는 `git add -f`·백업·통째 압축 경로가 남는다), 비밀번호는 `android/keystore.properties` 분리, 파일이 없으면 서명 없이 빌드(키스토어 없는 환경에서 빌드가 깨지지 않게 — 서명 없는 AAB는 Play가 거부하므로 새어나갈 위험이 없다), `versionCode` 단조 증가·소진된 번호 재사용 불가 (구현 완료·배선 양방향 검증, **실제 업로드 키 생성은 사용자 작업**, 2026-08-04) |
| [ADR-092](./adr/ADR-092.md) | 초기 번들 분할 — 전체 품질 점검(2026-08-05) 실측에서 메인 청크가 **1,019 kB(gzip 411 kB) 단일 덩어리**였고 `React.lazy` 는 0건이라 8개 라우트 화면·모든 store·`src/data/*.json` 이 첫 페인트에 함께 평가됐다. ① `/debug/*` 라우트·컴포넌트 5개(2,033 LOC)를 **삭제** — 각 파일 주석의 원래 지시("조정이 끝나면 삭제")를 실행한 것으로, 2026-07-13에 "일러스트가 추가될 때마다 다시 쓰이니 못 지운다"고 보고 DEV 게이팅을 제안했다 보류됐던 판단을 사용자가 재작성 비용을 감수하고 뒤집었다("나중에 필요하면 다시 만들자"). 고아가 되는 것은 `BossCard` 의 `export` 키워드 하나뿐(`BossCardPreview` 재사용 전용이었고 제품·테스트 어디서도 import 안 함)이고 나머지 심볼·데이터 파일은 제품이 계속 쓴다. 게이팅안은 [[ADR-090]] 이 밟은 함정도 안고 있었다 — `vite build` 가 `DEV` 를 항상 `false` 로 치환하고 Capacitor 는 늘 빌드된 번들로 돌아 실기기에선 못 연다. 대가로 [[ADR-089]] 검은마법사 배경 값 조정 도구가 사라진다(다시 만질 일이 생기면 복원 선행) ② 라우트 화면 8개를 `lazy()` 로 분할(네이티브에선 청크가 WebView 로컬 파일이라 지연이 없다) ③ **Suspense 경계는 라우트별로** — `<Routes>` 전체를 감싸면 `/profit/drops` 로드 중 `BossProfitScreen` 까지 폴백으로 대체돼 [[ADR-077]] 이 막은 증상(펼침·기간·스크롤 상실, iOS stuck sticky 헤더)이 그대로 되살아난다. 중첩 자식은 `<Outlet />` 자리, 즉 부모 서브트리 안쪽에서 자기 element 만 감싼다 ④ 폴백은 [[ADR-061]] `LoadingState` 재사용·스플래시 `MIN_SPLASH_MS` 와 독립 ⑤ 참조 0건인 `arcanus1.png`(2.65 MB, eager glob 이 끌어온 고아) 삭제 — 참조 있는 대용량 파일은 미변경, eager glob → lazy 와 PNG→WebP 는 2단계. 경계는 "라우트마다 하나"가 아니라 **최상위 1 + 중첩 자식 1 = 2개**로 족하다(React 가 가장 가까운 경계를 쓰므로 자식 서스펜드가 바깥까지 안 올라간다). 이 결정은 자식을 **영원히 서스펜드**시켜 부모 헤딩이 남는지 보는 테스트로 고정했다 — 로드가 끝난 뒤를 보는 테스트로는 못 잡고(끝나면 부모가 돌아온다), 안쪽 경계를 일부러 지워 실제로 판별함을 확인했다. (**구현 완료**, 2026-08-05 — 첫 로드 JS **gzip 411.3→115.8 kB(-72%)**, 메인 청크 1,019→338.5 kB, dist 12→9.1 MB, 디버그 라우트 문자열 0건, 테스트 2,250개 통과·lint clean, **실기기 미검증**) |
| [ADR-093](./adr/ADR-093.md) | 자산 최적화([[ADR-092]] 2단계) — dist 9.1 MB 중 이미지가 8 MB 였다. ① **PNG→WebP(q95)를 슬러그 키 디렉터리에만** — `import.meta.glob` 조회 키가 디렉터리마다 달라서, `bosses/` `maps/` `maps/icons/` `themes/` 는 **확장자를 뗀 슬러그**라 파일만 바꾸면 코드 변경이 0인 반면(glob 패턴이 [[ADR-021]] 때부터 `*.{webp,png}` 로 둘 다 받는다) `items/`(+`rings/`)는 [[ADR-011]] 결정 6의 `iconFile` 이 **확장자까지 박아** 둬 게임 데이터 3곳([[ADR-006]] 대상)을 함께 고쳐야 하는데 얻는 것이 43 KB뿐이라 제외. 품질은 사용자가 비교 도구(엣지 밀도 최대 구간을 잘라 차이를 8~40배 증폭)에서 직접 보고 q95 선택 ② `drop-effect/` JPG 55개(0.58 MB)는 **이미 손실이라 재인코딩이 2세대 손실**이고 초당 11.5~15프레임으로 지나가 검증도 어려워(그 검증 도구가 [[ADR-092]] 에서 지운 `/debug/*` 였다) 보류 ③ **`eager: true` glob 을 lazy 로 바꾸는 계획 철회 — 측정으로 반증**: glob 에 걸린 에셋은 참조 여부·`eager` 여부와 무관하게 **전부 emit**되고(참조 0건 `mugong.png` 도 emit 확인) `eager` 는 URL 문자열의 인라인 여부만 정한다. 이미지 바이트는 애초에 JS 에 안 들어가고 `<img>` 렌더 때 받으므로 dist·런타임 어느 쪽도 안 바뀐다 — 자산의 지렛대는 **안 쓰는 파일 삭제와 쓰는 파일 축소** 둘뿐 ④ 참조 0건 5개 삭제(드래곤 4종+무공 235 KB) — `ContentScreen` 이 `armorDragon` 하나만 고정으로 쓰는 이유는 주간마다 드래곤이 바뀌는데 **API 가 어느 것인지 안 알려주기** 때문이고, 이들이 직전 조사에서 "참조 1건"으로 잡혔던 건 [[ADR-092]] 가 지운 `BossCardPreview` 의 후보 목록이었다(**디버그 도구가 자산을 살려두고 있었다**). 자산 최적화는 코드를 안 바꿔 **조용히 깨지므로**(슬러그 미해석 시 폴백만 뜨고 에러 없음) 선언된 슬러그 전수 해석 테스트를 신설 (**구현 완료**, 2026-08-05 — dist **9.1→7.3 MB**, ADR-092 전 기준 12→7.3 MB(-39%), 변환 30개 전부 해상도 일치·필요한 투명도 보존 확인, 테스트 2,253개 통과, **실기기 미검증**) |
| [ADR-094](./adr/ADR-094.md) | 3단계 구조 리팩터링 기준 — 아토믹 디자인 + compound/render prop 을 **취약 구조를 가두는 수단**으로 쓴다. 착수 전 실측에서 점검의 전제 둘이 흔들렸다: ① 프리미티브 근거였던 "아이콘 버튼 26회 중복"이 **[[ADR-092]] 가 지운 디버그 화면 안에 전부 있어 현재 0회**(남은 최대는 카드 셸 8회) ② LOC 는 위험도가 아니라 **결정 밀도**의 대리 지표다(`BossProfitScreen` 1,775 LOC 에 ADR 언급 117개 = 15줄당 1회, 그 값들은 [[ADR-080]]~[[ADR-085]] 에서 실기기로 여러 번 틀린 끝에 얻었다). 반면 방향을 뒷받침하는 사실도 나왔다 — `design-system.md` 가 카드·버튼·인풋을 **이미 클래스 문자열로 규정**하는데 코드는 637곳에 흩어 반복(중복 49%)하고, `Modal` 은 프롭 7개 중 4개가 스타일 탈출구(`card` `maxWidth` `align` `tightBottom`)이며 `CharacterAccordion` 은 프롭 11개다. 결정 — 합성 패턴은 **호출부 2곳 이상이거나 취약 구조(sticky·측정·스태킹)를 감쌀 때만** 쓰고(사용자 전역 규칙의 "단일 사용 추상 금지"와의 충돌 방지), 계층은 **의존 방향 규칙**이며 디렉터리 물리 이동은 **한다 — 마지막에 단독 커밋으로**(경로 변경 diff 에 내용 변경이 섞이면 실수가 숨는다), atoms 는 `design-system.md` 가 규정한 것만·라운딩 차등은 서로 다른 atom(원칙 2), 유틸리티 조합(`text-sm text-text-muted` 22회 등)은 감싸지 않는다(목표는 "className 제거"가 아니라 "디자인 결정이 박힌 문자열의 단일화"), `useMemo` 는 넣지 않는다(Lighthouse TBT **10ms** — 증거 없음). **안전장치는 "렌더 결과 DOM 1:1 동일"** — jsdom 이 레이아웃을 계산하지 않아 기존 291케이스가 정확히 그 영역만 못 잡으므로(테스트 코드에도 명시), 리팩터링 전에 DOM 스냅샷을 떠 커밋마다 대조한다 (기준 승인 · **1단계 구현 완료**, 2026-08-05) |
| [ADR-095](./adr/ADR-095.md) | NEXON Open API Analytics 스크립트 삽입 — 넥슨이 개발자에게 제공하는 웹 애널리틱스(`openapi.nexon.com/analytics.js?app_id=`)를 `index.html` `<head>` 에 가이드 원문 그대로(`async` 포함) 한 줄 넣는다. 이 앱엔 지금 애널리틱스가 하나도 없다. `app_id` 는 비밀값이 아닌 공개 식별자라 하드코딩(사용자 개인 API 키와 층이 다름 — [[ADR-007]]), `.env` 체계를 값 하나 때문에 도입하지 않는다. **웹뷰 origin(`https://localhost`·`capacitor://localhost`)에서 연동이 성립하는지는 전제가 아니라 확인 대상** — 가이드가 "웹 서비스" 기준으로 쓰였고 origin 언급이 없어, 삽입 후 최대 24시간 뒤 애플리케이션 상세 하단이 "연동 완료"로 바뀌는지 보고 판정한다(추정으로 우회책을 미리 만들지 않는다). 앱 코드는 이 기능을 **모른다** — `analytics.js` 전역을 아무도 참조하지 않으므로 오프라인·넥슨 장애 시 조용히 실패하고 끝난다([[ADR-003]] 의 기기 완결성 유지). 앱 코드가 참조하지 않아 지워져도 안 깨지는 한 줄이라 `index.html` 전문 검사 테스트로 고정([[ADR-093]] 자산 테스트와 같은 이유). 개인정보 처리방침([[ADR-090]] 에서 게시)·스토어 데이터 안전성 신고에 제3자 수집 주체 추가는 앱 코드 밖 후속 작업. 넥슨 약관은 해당 없음(자사 제공 도구를 안내대로 사용 — [[ADR-090]] 의 수익화 조항과 층이 다름) (**구현 완료**, 2026-08-05 — 실제 경로는 가이드 예시와 달리 `/js/analytics.js`, app_id 322698. **연동 성립은 최대 24시간 뒤 확인 필요**, 개인정보 처리방침 반영 미착수) |
| [ADR-096](./adr/ADR-096.md) | 스케줄러 화면 상태(탭·필터·선택 캐릭터)의 소유 위치 — 컨텐츠·보스 스케줄러의 탭이 화면 로컬 `useState` 라 ① 다른 탭에 다녀오면 초기화되고 ② 스케줄러와 관리 화면이 서로의 탭을 몰라 "주간에서 컨텐츠 관리 → 일간 목록"이 됐다. 같은 타입 선언이 네 파일에 두 벌씩 복제된 것이 그 구조의 흔적. 최악 사례는 빈 상태 CTA — `contentEmptyProps(tab)` 가 탭을 받아 "주간 항목을 골라주세요"라 안내([[ADR-060]])하고는 이동할 때 그 탭을 버렸다. 같은 앱의 보스 수익 탭은 이미 스토어 소유라([[ADR-023]]) 정상 동작했다. 결정 — ① 탭과 **보스 솔로/파티 필터**를 기능 스토어로 승격(필터를 빼면 "탭은 기억하는데 필터만 초기화"되는 반쪽 상태가 된다), 타입 선언도 스토어 한 벌로. 보스 수익 `setTab` 과 달리 **동기 세터**(탭 전환에 네트워크가 없다) ② 관리 화면은 **진입 시점에** 스케줄러 탭을 이어받는다(로컬 `useState` 초기값으로 — 초기값은 첫 렌더에만 읽히므로 "진입 시점"이 정확히 표현된다). **한 방향이다** — 처음엔 스토어를 그대로 공유해 양방향으로 묶었는데 과했다(사용자 지적): 관리 화면에서 다른 탭을 잠깐 뒤져보기만 해도 돌아갔을 때 스케줄러가 바뀌어 있어, 이 ADR이 고치려던 문제를 반대 방향으로 다시 만들었다. 관리 화면은 하위 작업 화면이라 맥락을 물려받되 부모를 건드리지 않는다. 원래 결함과의 차이는 초기값이 **하드코딩이 아니라 스케줄러의 현재 탭**이라는 것 ③ **영속화하지 않는다**(요청받은 문제는 세션 안에서 자리를 잃는 것이고, 보스 수익도 안 한다) ④ 관리 화면 우상단 읽기 전용 pill → `CharacterSelectDropdown`, `onSelect` 는 스케줄러와 **같은 `selectCharacter`** 라 바꾼 캐릭터가 양쪽에 함께 반영. **탭과 달리 이쪽은 양방향** — 탭은 "무엇을 보고 있는가"라 잠시 갈라져도 되지만 선택 캐릭터는 **편집 대상**이라, 관리 화면에서 다른 캐릭터를 고쳐 놓고 돌아왔을 때 스케줄러가 이전 캐릭터를 보여주면 편집이 어디 적용됐는지 알 수 없다 ⑤ 컴팩트 변형 추가 — 스케줄러용 `min-w-[160px] py-3` 를 제목 줄에 넣으면 헤더가 두꺼워지고 좁은 화면에서 제목과 폭을 다툰다(호출부 2곳이라 [[ADR-094]] 기준 충족). URL 쿼리 파라미터 안(`/content?tab=weekly`)은 동등하게 동작하지만 같은 종류의 상태를 앱 안에서 두 방식으로 다루게 돼 기각, `navigate` state 전달 안은 진입 경로 3곳·복귀 경로까지 전부 실어 보내야 하고 하나만 빠뜨려도 조용히 되돌아가 기각. 언마운트 후 재마운트로 고정 — 한 번 렌더해 보는 테스트로는 안 잡힌다 (설계 · 구현 전, 이슈 #143) |
| [ADR-097](./adr/ADR-097.md) | 화면 진입 재조회에 **10분 TTL** — 세 탭 화면이 마운트마다 `loadTrackedOcids()` → `syncSchedules` 를 돌리는데 라우트라 탭을 오갈 때마다 재마운트되고, **세 화면의 네트워크가 동일**하다(`character/list` 1 + 추적 캐릭터당 `scheduler/character-state` + 백필, 결과도 같은 `scheduler-cache`) — 앱을 켜고 한 바퀴 돌면 같은 응답을 3번 받았다. 판정 기준은 **저장된 데이터의 나이**(`syncedAt`, 성공한 동기화에서만 갱신되므로 새 상태를 만들지 않는다)라 세 화면이 사실상 게이트를 공유한다(페이지별 메모리 타임스탬프 안은 첫 한 바퀴 3배를 못 막아 기각). 단 ① **앱 재시작 후 첫 동기화**는 TTL 무시(실행당 1회·전역, 화면당이면 시작 직후 3배가 돌아온다. 실패해도 "시도"로 표시해 오프라인에서 탭마다 재시도하지 않게 하고, 예열도 이번 실행의 동기화로 친다) ② **명시적 재조회**(헤더 버튼·당겨서 새로고침·재시도)는 TTL 무시 — 게이트를 `refresh()` 가 아니라 자동 경로인 `loadTrackedOcids()` 에만 둬서 `force` 인자·시그니처 변경 없이 모든 사용자 트리거가 강제 경로가 된다. 건너뛴 진입은 캐시를 **신선한 값**으로 취급하고(`isStale: false` — 아니면 탭 이동마다 "오래된 데이터" 토스트가 뜬다) "n분 전"은 실제 `syncedAt` 을 그대로 보여준다. 보스 수익 자동 기록은 실제 동기화에서만([[ADR-017]] 규칙 유지 — 최대 10분 지연). 여기에 **`character/basic` 갱신을 편승**시켜 이슈 #139(레벨·외형이 피커를 열기 전까지 안 바뀜)를 해소한다 — 별도 TTL 없이 같은 정책, 프리플라이트 이후 병렬·best-effort·자격 스윕 없음, 수익 화면만 프로필을 동기화 전에 읽으므로 완료 분기에서 다시 읽는다. 피커는 정책 밖(현행 유지 — 자격 판정의 유일 경로). 구현하며 셋을 정정 — 순수 판정 함수는 `isWithinSyncTtl(oldest, now)` 가 아니라 **`isSyncFresh(syncedAts, trackedCount, now)`**(호출부가 "가장 오래된 값"을 먼저 접으면 캐시가 **없는** 캐릭터가 배열에서 빠져 흔적 없이 사라진다 — 새로 추가한 캐릭터가 조회 없이 빈 채 남는다), 건너뛰는 진입도 **`set` 을 한 번은 한다**("아무것도 하지 않는" 분기는 구현할 자리가 없다 — 지키는 것은 `loading` 을 경유하지 않아 한 프레임 번쩍이지 않는 것), 보스 스케줄러만 `loadPartySizes` 를 **게이트 앞으로** 옮김(로컬 SQLite 조회라 네트워크 TTL 대상이 아닌데 함께 건너뛰면 파티원 수 배지·필터가 옛 값으로 남는다) (**구현 완료**, 2026-08-06, 이슈 #139 — 테스트 2,404개 통과·lint clean, **실기기 미검증**·10분 값은 사용자가 쓰며 조정할 잠정값) |
| [ADR-098](./adr/ADR-098.md) | **탭 이동도 같은 스크롤 클램프를 만든다** — 스케줄러 복귀 시 한 프레임 튐(사용자 보고 2026-08-06, iOS 전용, 스크린샷은 [[ADR-085]] "헤더가 화면 밖" 얼굴 그대로). 브라우저 프레임 계측으로 사슬 확인 — 네 탭이 문서 전체 스크롤 하나를 공유하는데(`ADR-072` 결정 1) **라우트 변경 시 스크롤을 되돌리는 코드가 앱 전체에 없어**(`scrollTo` 는 `BossProfitScreen` 뿐) 새 화면이 비-0 오프셋으로 마운트되고, 문서 높이가 다르면 클램프까지 생긴다(`보스 y=649 doc 1432 → 컨텐츠 y=289 doc 1072`, 설정·수익행은 `y→0`). 거기에 iOS 스크롤 스레드의 뒤늦은 옛 오프셋([[ADR-085]] 계측)이 겹치면 `sticky top-0` 헤더가 화면 밖으로 날아간다. **보스 수익이 멀쩡한 이유도 같은 사슬로 설명된다** — 마운트 `scrollTo(0,0)`([[ADR-080]], deps 가 마운트에도 발화) + `fixed` 헤더([[ADR-085]])로 두 겹. 처방은 [[ADR-085]] 를 그대로 넓힌 것 — ① 화면을 통째로 바꾸는 이동은 **이동 전에**(옛 문서가 아직 높을 때) `scrollTo(0,0)` 하고 **같은 태스크에서** `navigate`(`useScreenNavigate`; 마운트 후 처방은 [[ADR-084]]가 이미 반증, `rAF` 로 한 프레임 미룬 첫 구현은 **실기기 연속 프레임에 떠나는 화면이 최상단으로 올라간 모습이 찍혀 반려** — 스크롤과 DOM 교체 사이에 페인트를 두지 말 것), `/profit/drops` 오버레이만 예외([[ADR-077]] 스크롤 유지 계약) ② 공용 `PageHeader`([[ADR-094]])를 `fixed` + 실측 spacer 로 — [[ADR-085]] 결정 1의 조건("헤더가 문서 최상단 첫 요소라 보이는 모습이 동일")이 스케줄러 4화면에서도 성립함을 확인한 것이라 그 ADR 을 어기는 게 아니다. 호출부는 안 바뀐다(래퍼 `<div>` 하나로 감싸 `space-y-4` 형제 마진 배분을 보존 — 프래그먼트로 반환하면 목록이 16px 내려간다) ③ **설정은 바꾸지 않는다** — 페이지 헤더 자체가 없어 고리가 구조적으로 없고, 필요 높이 835pt(390px 폭+안전영역 59/34)라 대부분 기기에서 스크롤조차 없다 ④ 관리 화면 2개도 같은 구조라 함께, 뒤로가기 복원도 ①이 덮는다(떠날 때 0으로 옮겨두므로 저장값이 0) (**구현 완료 · 실기기 1차 검증**, 2026-08-06 — 헤더는 두 프레임 모두 제자리로 결정 2는 확인됐고, `rAF` 지연은 반려돼 같은 태스크로 정정. **남은 관측**: 헤더만 새 화면이고 목록은 옛 화면인 프레임 = 콘텐츠 레이어 재래스터 지연([[ADR-084]] 얼굴)이라 결정 1·2가 끊는 고리가 아니다 — 남으면 다음 후보는 [[ADR-085]] 결정 4의 목록 전용 스크롤 컨테이너) |
| [ADR-099](./adr/ADR-099.md) | **스크롤의 소유자를 문서에서 화면으로 옮긴다** — [[ADR-098]] 이 원인을 두 층(① 오프셋이 화면을 넘어 살아남음 = SPA 는 문서가 하나 · ② WKWebView 재래스터/컴포지터 지연)으로 가른 뒤, ①을 JS 로 보정하는 처방은 **보정 동작 자체가 ②에 걸려 눈에 보였다**(rAF 로 미루면 떠나는 화면이 솟구치고, 같은 태스크로 붙여도 그대로 — 실기기 연속 프레임). 문서 스크롤을 쓰는 한 이동 근처에서 오프셋을 만지면 ②가 그것을 그리므로, 남는 길은 오프셋을 안 건드리거나 **소유자를 바꾸는 것**뿐. 후자를 택해 화면이 `overflow-y: auto` 컨테이너를 갖게 했다 — 스크롤 상태가 그 DOM 요소에 붙어 **화면과 함께 태어나고 죽으므로 계승할 오프셋이 존재하지 않는다**(네이티브의 화면별 `UIScrollView`·Ionic `ion-content` 와 같은 의미론). **컨테이너는 뷰포트 크기**여야 한다(사용자 요구 — 헤더 아래 박스만 스크롤시키면 인디케이터가 그 박스에 갇힌다). 멀티 웹뷰 하이브리드 안은 기각(JS 컨텍스트가 N 개면 zustand·[[ADR-097]] TTL 게이트·SQLite 커넥션이 쪼개져 스크롤 하나 때문에 상태 공유 설계를 다시 짜게 된다). **컨텐츠 1화면 프로토타입 → 실기기 통과(이동 프레임 소멸·모멘텀 동등·당김 생존) → 스케줄러 4화면 확산**, 마크업은 공용 셸 `ScreenScroll` 하나([[ADR-094]] 와 같은 이유). 곁가지 셋은 전부 같은 뿌리 — **문서 스크롤에만 브라우저가 공짜로 해주던 처리**를 컨테이너에선 우리가 명시해야 한다: 흰 인디케이터 → `color-scheme` + `scrollbar-color` 를 테마 `mode` 에서 직접(색은 `color-scheme` 만으로는 라이트에서 안 잡혔다), 노치 침범·탭바 뒤 가림 → 스크롤포트를 "보이는 영역"에 맞춤(`top-[var(--sa-top)]` + `bottom-[var(--tab-bar-h)]`, 안쪽 `-mt` 로 상쇄해 스크롤 범위 불변), 탭바 높이는 `4rem` 가정이 아니라 **`BottomTabBar` 실측**(가정과 실제가 어긋나 띠가 생겼다). 포기한 것: 상태바 탭 → 최상단(웹뷰 메인 스크롤뷰 전용), 뒤로가기 스크롤 자동 복원. 보스 수익은 중첩 sticky([[ADR-047]])·접기 사전 스크롤([[ADR-085]])이 얽혀 범위 밖 (**구현 완료 · 실기기 검증 통과**, 2026-08-06) |
| [ADR-100](./adr/ADR-100.md) | **보스 수익도 화면이 스크롤을 소유한다** — [[ADR-099]] 결정 4(범위 밖)를 폐기한다. 스케줄러 4화면이 실기기 검증을 통과한 뒤로는 미루는 비용이 더 크다: 스크롤 모델이 둘로 남아 이 화면만 [[ADR-098]] 원인 ①에 계속 노출되고, 애초에 그 증상의 진원지가 여기다([[ADR-080]]·[[ADR-084]]·[[ADR-085]] 전부 이 화면에서 나왔다). ① 루트를 공용 `ScreenScroll` 로 ② **`fixed` 페이지 헤더는 spacer 와 함께 래퍼로 묶어 셸 *안*에**(첫 구현은 셸 바깥 형제로 뒀다가 **실기기 반려** — 인디케이터는 스크롤포트 위에 겹쳐 그려지는데 셸 바깥 `z-10` 헤더가 그것을 가렸다. 벌거벗겨 넣으면 셸 안쪽 `space-y-4` 마진이 spacer 에 얹혀 목록이 16px 내려가므로 래퍼가 짝이다 — 공용 `PageHeader` 와 같은 모양이 됐다. 히스토리 오버레이만 셸 바깥, [[ADR-077]]) ③ **중첩 sticky([[ADR-047]]) 오프셋에서 상단 안전영역을 뺀다** — sticky 의 `top` 은 스크롤포트 기준인데 셸은 `top-[var(--sa-top)]` 에서 시작하므로 뷰포트 실측값을 그대로 쓰면 카드 헤더가 그만큼 아래에 멈춘다(카드 헤더·배지 레일·본문 페이드 세 자리를 `calc(<실측>px - var(--sa-top))` 로) ④ 문서를 읽던 넷을 전부 컨테이너로 — 기간·탭 최상단([[ADR-080]]), 접기 전 사전 스크롤([[ADR-085]] 결정 2, `scrollHeight`·`clientHeight`·`scrollTop`), **캐릭터 이슈 팝오버 닫기**(문서 스크롤이 아니면 `window` 에 `scroll` 이 안 와 조용히 죽는다 — 회귀 가드 테스트 추가), 당김 판정 ⑤ 스크롤 루트는 `BossProfitContext` 로 내린다(4단계 아래 `CharacterAccordion` 이 쓴다 — 프롭으로 내리면 [[ADR-094]] 3단계가 없앤 통과 전용 프롭이 다시 생긴다). [[ADR-085]] 결정 2는 요소 스크롤러에서도 같은 옛 오프셋이 오는지 모르므로 **남겨둔다**(떼는 것은 별도 관측이 필요한 결정) (**구현 완료**, 2026-08-06 — 테스트 2,434개 통과·lint clean, **실기기 검증 대기**: 중첩 sticky stuck 위치와 접기가 확인 대상) |
| [ADR-101](./adr/ADR-101.md) | 콜드 스타트 진입 프레임 — 실기기에서 보스 스케줄러·보스 수익 첫 진입에 [빈 상태 → 로딩 → 목록] 세 장이 스쳐 지나갔다(사용자 관측 2026-08-06, 사진 4장). 재현 테스트로 시퀀스를 실측하니 `empty → loading → list` 이고 **네트워크는 한 번도 안 나간다**([[ADR-097]] 게이트 안) — API 지연이 아니라 로컬 저장소 읽기 위에 그려진 프레임이다. 원인 둘: ① 세 화면이 `isEmpty = trackedOcids === null \|\| length === 0` 로 **"아직 안 읽음"(null)과 "0명"을 같은 것으로** 읽어 첫 페인트가 모르는 사실을 단정했다([[ADR-060]]·[[ADR-061]] 이 이미 금지한 "빈 상태로 위장"의 마지막 3곳 — 그 조사는 관리 화면 2곳만 잡았다) ② `loadTrackedOcids` 가 `set({trackedOcids})` 와 `refresh()` 로 커밋을 둘로 나눠, 그 사이 브리지 왕복(Preferences N회 + SQLite 오픈 + `getCachedCharacterBasic` N회) 동안 화면이 "추적 중인데 0명"을 본다. 결정: ① `isEmpty = trackedOcids !== null && length === 0` — 빈 상태는 읽고 확인한 뒤에만 ② 앱 셸이 부팅 때 세 스토어를 **순차** 선하이드레이션(스플래시 1초 안에 끝나 탭 진입이 곧 목록) ③ 순차인 이유는 [[ADR-097]] 신선도 조건이 앞 회차가 캐시를 쓴 **뒤에야** 참이 되기 때문 — 병렬이면 같은 응답을 3번 받는다 ④ `loadTrackedOcids()` 는 동시 호출을 한 회차로 합친다(single-flight — "평생 한 번"이 아니라 "동시에 하나만", 영구 메모면 [[ADR-097]] 10분 TTL 이 죽는다) ⑤ 스토어는 **동적 `import()`** 로 — 정적이면 [[ADR-092]] 번들 스플리팅이 통째로 무효가 된다(덤으로 라우트 청크가 미리 받아져 탭 전환 폴백도 줄어든다) ⑥ 온보딩 미완료면 안 돈다 (**구현 완료 · 실기기 검증 완료**, 2026-08-06 — iPhone 17 사이드로딩으로 네 프레임이 모두 사라진 것을 확인) |
| [ADR-102](./adr/ADR-102.md) | **접기 전 사전 스크롤([[ADR-085]] 결정 2)을 걷어낸다** — 카드를 펼친 채 스크롤을 내리고 접으면 접히기 직전에 페이지가 한 번 튀었다(사용자 연속 프레임 2세트, 2026-08-06). 원인은 결정 2의 두 단계가 **두 프레임으로 그려지는 것**이다: `setIsExpanded` 가 rAF 콜백 안이라 React 18 이 Scheduler(MessageChannel)로 넘겨 다음 매크로태스크에 렌더하고, 그 사이 브라우저가 "스크롤은 옮겨졌는데 본문은 아직 붙어 있는" 상태를 반드시 한 번 페인트한다(기존 테스트가 그 중간 상태를 계약으로 단언하고 있었다). 튀는 거리는 클램프될 양 전부라 접은 목록이 뷰포트에 들어가면 **최상단까지** 간다. [[ADR-085]] 는 iOS **문서** 스크롤 스레드를 상대로 고리를 두 군데 끊으면서 결정 4("목록 전용 스크롤 컨테이너가 근본")를 미뤘고, 그 근본은 [[ADR-099]]·[[ADR-100]] 이 채택했다 — [[ADR-100]] 이 "**무해하고** 검증된 코드라 유지, 떼는 것은 별도 관측이 필요한 결정"으로 남겨둔 그 관측이 이 보고다. 결정: ① 접기는 상태만 바꾼다(`collapse()`·`shellRef`·실측 3개 삭제) — 사라지는 본문은 **탭한 헤더보다 항상 아래**라 스크롤을 안 건드리면 헤더와 위쪽 내용이 제자리에 남는다 ② [[ADR-085]] 결정 1(`fixed` 페이지 헤더)은 다른 고리를 끊으므로 **유지** ③ 되살릴 근거는 "실기기에서 옛 오프셋이 되돌아오는가" 하나이고, 그때는 사전 스크롤을 **같은 커밋에** 태우는 형태(`flushSync`)를 먼저 검토한다 — 정체는 사전 스크롤이 아니라 그것이 별도 프레임인 것이다. 곁가지로 `removedHeight` 의 2px 과다 계산(접힘 헤더에만 있는 보더)도 함께 사라진다 (**구현 완료 · 실기기 검증 완료**, 2026-08-06 — 중간 프레임 소멸 확인, [[ADR-085]] 옛 증상 재발 없음 = 요소 스크롤러가 원천을 없앴음이 사후 확인됐다) |
| [ADR-103](./adr/ADR-103.md) | **고가 드롭 연출을 1.5배 속도로 재생한다** — 이슈 #136 이 물은 셋(A 시작 지연 / B 재생 속도 / C 종료 구조) 중 사용자 판정은 **B**, 최초 처방은 "fps 2배"였으나 화면으로 보니 너무 빨라 **1.5배로 확정**(같은 날 왕복 — 배율은 계측이 아니라 눈으로 정하는 값임이 확인됐다). ① 단계별 고정 fps([[ADR-038]] 결정 8)라는 **방식은 유지**하고 값만 전부 1.5배 — screen 15→22.5(1067→711ms) · pre 14→21(571→381ms) · loop 11.5→17.25(2087→1391ms/회) · end 12→18(583→389ms). 네 단계에 **같은 배율**을 걸어둔 덕에 2배→1.5배 정정이 값 다섯 개 재계산으로 끝났다 ② **중앙 아이템 팝인도 같은 배율**(`.35s/.5s` → `.233s/.333s`) — 팝인은 fps 가 아니라 CSS transition 이라 fps 만 올리면 혼자 옛 속도로 남아, 버스트가 사라진 뒤에도 아이템만 계속 커진다(원래는 팝인 1033ms·버스트 1067ms 로 함께 끝나는 타임라인이었다) ③ `DROP_START_FRAME=8` 은 **그대로** — 등장 트리거는 시간이 아니라 버스트가 최대로 벌어지는 그 그림에 묶여 있고, 등장 시점(533→356ms)은 fps 를 따라 자동으로 온다 ④ 이슈의 (A) 가설(`screen` 16프레임 1.0MB 만 프리로드에서 빠져 있다)은 **다른 축이라 손대지 않는다** — fps 상향은 그 지연을 완화하지 않으므로, 실기기에서 시작 지연이 남으면 그때 별도로 판다. 테스트는 상수가 아니라 실제 타임라인을 단언하고 각 시점을 **1배·2배 양쪽에서 깨지도록** 골랐다 (**구현 완료**, 2026-08-06 — 실기기 육안 확인 미verify) |
| [ADR-104](./adr/ADR-104.md) | **테마 선택 모달을 프리뷰 타일 + 카테고리 섹션 + 모드 필터로 재설계** — 이슈 #140. 분류 없는 평평한 목록 + 3점 도트(`ThemeSwatchDots`, 모달과 설정 행이 공유)를 바꾼다. 시안 4종을 실제 색값·실제 모달 폭으로 그려 "테마 하나가 먹는 세로"로 재니 A 54 / B 51 / C 82 / D 30px 였고, **"2열이니 길이가 절반"이 틀렸음**이 드러났다(타일이 행의 두 배 높이라 B≈A). ① 카테고리 셋 **기본**(머쉬맘·혼테일)·**직업**(렌·엔젤릭버스터·레테)·**보스**(검은마법사), **겹침 없음**(`category` 는 문자열 한 개) — 소속은 사용자 확인([[ADR-006]]), 머쉬맘·혼테일은 보스이기도 하나 "앱 기본 라이트/다크" 역할이 우선 ② 목록은 **2열 프리뷰 타일** — 타일이 그 테마 `bg`/`surface`/`primary`/`text` 로 미니 화면을 그려 "고르면 화면이 이렇게 된다"를 보여준다(이득은 길이가 아니라 판단 속도) ③ 카테고리와 모드는 **직교하는 축**이라 카테고리는 섹션, 라이트·다크는 **필터 칩**(전체·라이트·다크, 기본 전체, **저장 안 함**, 결과 0인 섹션은 헤더째 감춤, 선택 테마가 필터에서 빠지는 것은 정상) — 칩 모양은 [[ADR-018]] 탭 토글 재사용 ④ **배경 이미지는 목록에 노출하지 않는다** — 앱 `dim`(0.82/0.78)은 전체 화면용이라 92px 타일에 덮으면 검은 띠고, 노출하면 **썸네일용 dim 값이 새로 생긴다**(시안 C 탈락 근거) ⑤ 설정 행에서 색 표식을 없앤다 → `ThemeSwatchDots` 는 사용처 0이 되어 **컴포넌트·테스트째 삭제** ⑥ "**JSON 키 순서 = 표시 순서**" 규약을 "카테고리 순서 → 그 안 JSON 키 순서"로 교체(카테고리 순서는 프로덕트 결정이라 JSON 이 아니라 레지스트리 상수). 테스트는 값이 아니라 스키마·그룹핑·필터 규칙을 단언한다([[ADR-064]] 결정 11 방식). 채택은 D → B → B+모드 필터로 두 번 바뀌었다 — 마지막 변경은 "라이트·다크를 갈라 보여주는 건 어떤가"라는 물음에서 왔고, 카테고리(섹션)와 모드(필터)가 **직교하는 두 축**임을 그때 갈랐다 (**구현 완료**, 2026-08-06 — 테스트 2,471개·lint·빌드 통과, 브라우저 실물 확인, 실기기 미verify) |
| [ADR-105](./adr/ADR-105.md) | **검은마법사 테마 재조정 — 갈색 제거 · 포인트를 골드로 · 배경 대칭축 보정**([[ADR-089]] 일부 폐기). 사용자 지적 셋이 전부 값 문제였고, 셋 다 원인이 규칙에 적혀 있다. ① **갈색은 파생 규칙의 정상 출력이다** — `NEUTRAL_RAMP`([[ADR-064]] 결정 9)이 중립을 `primary` 휴 쪽으로 기울이는데, 카민 `#E0245E` 는 휴 10.6 이라 채도를 0.02~0.03 까지 낮추면 나오는 색이 정확히 갈색·토프다(붉은 시드 테마의 구조적 귀결). 채도를 죽이면(×0.35) 무채색이 되어 램프의 존재 이유가 사라지므로 **휴만 340(자두)로 옮겨** 이 테마의 중립 13토큰을 `overrides` 로 덮는다 — 명도·채도 상수는 램프 그대로라 톤 계단은 다른 테마와 같은 자리에 남는다. 휴 340 은 추정이 아니라 **그림 측정치**(채도 가중 지배 대역이 0~20 52% / 330~360 43%, 어두운 픽셀 평균 휴 359)에서 왔고 붉은 쪽은 이미 `primary` 가 맡고 있다. **규칙도 시드 인자도 안 바꿨다** — 바꾸면 기존 5테마가 전부 움직인다, 같은 요구가 또 나오면 그때 `neutralHue` 를 붙인다 ② `secondary` 초월체 라벤더 `#A9A2E8` → **불티 골드 `#E7B758`**([[ADR-089]] 레퍼런스 표에 이미 샘플링돼 있던 값, 탈락 시안 B 가 들고 있던 방향). 고정 휴(`error` 27·`rise` 26·`fall` 262)와 안 겹치고 자주 중립과 휴차 105° 를 번다. 딸려 오는 값 둘 — `onSecondary` 가 규칙대로 아이보리→브론즈 `#573D00`(아이보리가 밝은 금 위에서 가시성 하한 2:1 아래로 사라지는 경우, 대비 **2.06→5.46** 으로 [[ADR-089]] 미달 표 첫 줄이 사라진다), `secondaryTint` 는 덮개를 유지하되 값 교체 — **[[ADR-089]] 결정 2 의 실패가 색만 바꿔 재발**한다(자주 표면 + 골드 15% = `#362824` 휴 37, 골드에서 46° 이탈한 **흙색** — 갈색을 걷어내는 작업에서 갈색을 다시 만드는 꼴), 처방도 같아 골드 휴에서 직접 잡은 `#3B2B07`(이탈 1°)로 덮는다 ③ 배경 `position: center` → **`52% center`** — 그림이 오른쪽으로 치우쳐 보이는 이유는 **그림의 대칭축이 그림 한가운데가 아니기 때문**이다(미러 상관으로 재면 x=413/800 = 51.6%). `cover` 가 세로 뷰포트에서 2.6배로 확대해 가운데 150px 만 보여주므로 13px 가 화면에서 **35px(390 폭의 9%)** 로 커진다. 52% 는 폭 360~430 에서 ±2px·태블릿에서도 1.1% 안. **그림을 다시 자르지 않는다** — [[ADR-088]] 결정 3 의 "그림이 아니라 값을 고친다"가 이 값이 있는 이유이고, 자르면 같은 에셋을 쓰는 헤더 조각과 어긋날 위험도 생긴다. 기준선 아래는 7→6건. `job-themes.json` 한 블록만 바뀌고 제품 코드·파생 규칙·테스트는 그대로다 (구현 완료, 2026-08-06 · 색 확정은 사용자 눈 확인 대기 · 배경 `position` 값은 [[ADR-106]] 에서 그림째 폐기) |
| [ADR-106](./adr/ADR-106.md) | **테마 배경 이미지를 혼테일·검은마법사 둘 다 뗀다 — 데이터 0건, 기계장치 유지**. 배경 그림을 둘 다 다른 것으로 바꾸기로 해 새 그림이 정해질 때까지 `background` 블록을 지운다(2개 → **0개**). [[ADR-088]] 구조상 앱은 그대로 돌지만(여섯 테마 전부 `bg` 단색) **테스트 10건이 깨진다** — 정체는 하나로, `NAMES.filter(background !== undefined)` 나 `theme: '혼테일'` 처럼 **데이터에서 "배경 있는 사례"를 골라 쓰고 있었다**. ① 데이터만 떼고 `theme-backgrounds.ts`·`buildThemeCss` 분기·`.theme-backdrop`·`ThemeHeaderBackdrop`·`hasThemeBackground` 는 한 줄도 안 건드린다 — 새 그림이 JSON 블록 하나로 다시 붙는 것이 [[ADR-088]] 결정 3 의 요점이고 지금이 그 시험대다 ② **에셋 webp 2개는 남긴다(임시)** — 곧 교체될 자리이고, 배경 경로 테스트가 실재하는 슬러그 하나를 필요로 한다(파일이 0개면 `getThemeBackgroundUrl` 이 아무것도 해석 못 해 검사 방법 자체가 사라진다). 대가는 적어 둔다: glob 이 `eager` 라 참조 없이도 번들에 실린다(**약 170KB 죽은 에셋**), 새 그림 커밋에서 같이 지우는 것이 이 상태의 정상 종료 ③ **데이터가 비었다고 테스트를 지우지 않는다** — 지우면 새 그림이 올 때까지 배경 경로(`--theme-bg-*` 방출·슬러그 미해석 폴백·백드롭 렌더 분기)가 아무도 안 보는 채로 썩고 새 그림을 붙이는 순간에야 안다. 검사 대상은 데이터가 아니라 **기계장치**이므로 사례를 데이터에서 고르지 말고 **테스트가 만든다**(픽스처 + `getThemeDefinition` 부분 모킹). 모킹은 "있음" 쪽에만 걸고 "없음" 쪽은 진짜 테마 여섯으로 계속 실물 검사한다 ④ 0건이면 루프가 안 돌아 **저절로 초록**이 되는 전수 검사(슬러그 실재 확인, [[ADR-093]] 계열)는 원래 있던 `length > 0` 가르개를 떼는 대신 **`it.skipIf` 로 skip 표시**를 남긴다 — "검사했고 괜찮다"와 "검사할 것이 없다"는 리포트에서 달라야 하고, 새 그림이 들어오면 가르개 없이 자동으로 되살아난다. [[ADR-105]] 결정 3(`52% center`)의 **값**은 이 그림 전용이라 함께 사라지지만 그 값을 잰 **방법**(미러 상관으로 대칭축을 찾고 `cover` 배율 증폭을 감안해 퍼센트를 잡는다)은 새 그림에도 그대로 쓴다 (구현 완료, 2026-08-06 · 새 배경 그림 대기) |
| [ADR-107](./adr/ADR-107.md) | **캐릭터 관리 모달 높이를 안전영역 기준으로 묶고, 스크롤포트를 모달 가장자리까지 넓힌다** — 사용자 보고 2건(2026-08-06). ① 카드에 상한이 없었다 — 유일한 상한이 본문 **안쪽** 그리드의 `max-h-[70vh]` 라 카드는 거기에 크롬 194px(패딩 48·테두리 2·헤더 72·간격 32·버튼 40)이 더 붙은 높이가 되고, `vh` 는 시스템 바를 포함한 화면 전체라 **안전영역이 큰 기기일수록 더 많이 침범**한다. 오버레이가 `px-4 pt-[calc(1rem+var(--sa-top))] pb-[calc(1rem+var(--sa-bottom))]` 로 여백을 만들고 카드가 `flex max-h-full flex-col` 로 그 안에 갇힌다(헤더·푸터 `shrink-0` — 줄어드는 건 본문뿐), 그리드의 `max-h-[70vh]` 는 제거 ② **`min-height` 가 `max-height` 를 이기므로** 3줄 고정(`min-h-[385px]`, [[ADR-053]]·[[ADR-062]])을 그대로 두면 상한이 짧은 기기에서 무효가 된다 → 모달에서만 `min(385px,calc(100dvh-var(--sa-top)-var(--sa-bottom)-15rem))` 로 클램프(15rem = 크롬 194 + 여백 32 올림, **385px 가 애초에 안 들어가는 기기에서만 발동**하므로 상수 오차가 무해), 온보딩은 페이지라 클램프 없이 그대로 ③ **스크롤포트를 그리드에서 호스트로 옮긴다**([[ADR-099]] 와 같은 규칙) — 인디케이터는 콘텐츠가 아니라 스크롤포트 위에 그려지므로 카드 `p-6` 안쪽 그리드가 스크롤포트면 인디케이터도 24px 안쪽에 뜬다. 모달은 `-mr-6 pr-6 min-h-0 overflow-y-auto`(음수 마진이 스크롤포트를 테두리까지 넓혀 인디케이터를 끝에 붙이고 같은 크기 패딩이 콘텐츠 여백을 되돌린다), 온보딩은 종전 `max-h-[70vh] overflow-y-auto` 를 자기 자리에. 스탈 배너는 스크롤포트 밖에 남긴다. 반려: 그리드에 `className` 프롭(상쇄 클래스가 생긴다) · 그리드 자체에 음수 마진(온보딩이 삐져나온다) · 카드 전체 스크롤(CTA 가 숨는다) · `70vh`→`60vh`(침범 폭만 줄이는 완화책) (**구현 완료**, 2026-08-06 — 테스트·lint·빌드 통과, 브라우저 하네스 실측(카드 크롬 194px 로 추정치 일치 · 여백 = 인셋 + 16px · 스크롤포트 우측 간격 1px = 테두리), 실기기 미verify) |
| [ADR-108](./adr/ADR-108.md) | **검은마법사 배경을 합성 한 장으로 되돌린다 — 알파 하늘 + `cover` + `dim` 0.65**. [[ADR-106]] 이 "새 그림은 JSON 블록 하나로 다시 붙는다"고 적어 둔 그 자리다. 사용자가 바닥·사슬·블레이드를 한 장으로 합성해(`blackmage-background`, 1980×800) 넣었고, 중간에 오브젝트를 레이어 목록(`layers[]`)으로 쌓는 스키마를 만들었다가 **되돌렸다**(사용자 지시) — 합성 한 장이면 [[ADR-088]] 스키마가 그대로 맞고 손댈 이유가 없다. ① **제품 코드·타입·CSS·테스트 변경 0** — [[ADR-064]] 결정 10 의 "테마 추가는 JSON 한 블록"이 배경에도 성립함이 확인됐다. 배경 선언이 0 → 1 이 되면서 [[ADR-106]] 결정 4 의 `it.skipIf` 두 건이 **손대지 않아도 되살아났다**(skip 2 → 0) ② **하늘은 알파로 비운다** — 처음 받은 불투명 그림은 맨 윗줄이 `#221D28` 이라 `bg`(`#0B0509`)와 붙는 자리에 가로 띠가 생겼고, **`fadeTop` 은 이걸 못 고친다**(그 값은 뷰포트 상단을 페이드하는 것이지 그림의 윗변을 다루지 않는다). 투명 하늘이면 `bg` 가 비쳐 이음매가 구조적으로 없다 → **webp 변환에서 알파 필수**(RGB 변환하면 되살아남, 1,198→251KB) ③ **원본 `.png` 를 에셋 폴더에 남기지 않는다** — glob 이 `*.{webp,jpg,png}` 라 같은 이름의 png/webp 가 **같은 슬러그로 충돌**해 한쪽이 조용히 덮이고 1.2MB 가 번들에 실린다 ④ `size: cover`(사용자 "높이를 꽉 채워") — 대가를 실측해 남긴다: 아트워크 2.475 vs 폰 뷰포트 0.462, **5.4배 차이라 세로를 채우면 가로가 19%만 보인다**(양쪽 사슬·블레이드가 잘리고 바닥은 2.6배 확대로 뭉갠다). `position` 으로 고를 수 있는 건 "어느 19%"뿐이고(사용자가 `25%` 로 왼쪽 블레이드를 잡음), **값으로 풀 수 있는 문제가 아니라 캔버스 비율 문제** — 종횡비 ≤ 0.46 으로 다시 뽑으면 `100% auto` 로 자르지 않고 채워진다 ⑤ `dim` **0.65** — 0 은 "너무 도드라진다"(사용자), 0.35·0.6·0.72 를 브라우저에서 견줘 확정. 기준선(혼테일 0.82·옛 검은마법사 0.78)보다 밝은 이유는 **하늘이 투명이라 눌리는 면적이 이미 작기** 때문 ⑥ `blackmage-throne.webp` 삭제 — [[ADR-106]] 결정 2 가 정한 정상 종료(조건 충족), `hontail-cave.webp` 는 혼테일 새 그림이 없어 유지 (구현 완료, 2026-08-07 · 배치 값은 사용자가 눈으로 확정) |
| [ADR-109](./adr/ADR-109.md) | **혼테일 배경을 얼음 동굴 프레임으로 교체하고, 죽은 에셋을 전부 정리한다**. [[ADR-106]] 에서 뗐던 두 배경이 이로써 둘 다 채워져 그 임시 상태가 완전히 닫힌다. ① 이 그림은 **프레임/비네트**(고드름이 네 변을 두르고 가운데가 통째로 투명, 알파 50%)라 네 변에 붙이는 `100% 100%`(늘리기)가 후보였고 실제로 그려 보니 고드름이 세로 형태라 왜곡이 잘 안 읽혀 그림도 좋았지만 **채택하지 않았다** — 사용자 지시가 "왜곡시키지 마, height만 꽉 채워". 결과적으로 두 테마 모두 `cover` 이고, **그림 종류는 `size` 를 가르지 않는다** — 가르는 것은 크롭이 얼마나 아픈지와 `position` 을 고르는 방식이다 ② **프레임은 크롭이 장면보다 더 아프다** — 폰(0.462)에서 가로 노출이 검은마법사 19% / 혼테일 26% 인데, 장면은 중심부가 남으면 인상이 얼마간 살지만 **프레임은 양 끝이 정의상 서로 반대편이라 좁은 슬라이스에 둘 다 담을 방법이 원리적으로 없다**(좌우 고드름 기둥이 잘림). `position` 으로 고를 수 있는 건 "어느 26%"뿐 (사용자가 `45% bottom`) → **캔버스를 폰 비율(≤0.46)로 다시 뽑는 것이 답**([[ADR-108]] 결정 4 와 같은 결론). `auto 100%` 와 `cover` 는 세로로 긴 화면에서 결과가 같지만 **뜻이 다르다**(넓은 화면에서 전자는 좌우에 `bg` 를 남긴다) ③ `dim` **혼테일 0.8 · 검은마법사 0.65** — 혼테일이 높은 이유는 **상단 고드름이 헤더 텍스트와 겹치기 때문**이고 검은마법사는 내용이 아래에 모여 안 겹친다. 옛 값(0.82/0.78)과 비슷해 보여도 **물려받은 게 아니라 새로 잡은 값** ④ **죽은 에셋 전부 삭제**(`hontail-cave.webp`) → 에셋 폴더에 안 쓰는 파일 0개. 지우자 **테스트 2건이 깨졌는데** 배경 경로 테스트가 그 죽은 슬러그를 "실재하는 슬러그"로 쓰고 있었다 — [[ADR-106]] 결정 2 가 예고한 바로 그 결합이라 픽스처를 진짜 슬러그로 옮겨 끊었다(앞으로 "안 쓰는데 테스트 때문에 못 지운다"가 안 생긴다) ⑤ 받은 파일명 `hantail-` 오타를 `hontail-` 로 정규화. 제품 코드·타입·CSS 변경은 여전히 0 (구현 완료, 2026-08-07 · 배치 값은 사용자가 눈으로 확정) |
| [ADR-110](./adr/ADR-110.md) | **API 키 발급 가이드를 앱이 아니라 안내 사이트에 둔다**(`mapleroutine.store/api-key`, 스크린샷 7장). 온보딩의 첫 관문이 앱 밖 7단계 절차인데(로그인→애플리케이션 등록→약관·게임·단계→서비스 정보→목록→키 복사) 앱이 주는 안내는 `ApiKeyForm` 의 링크 한 줄뿐이었다. ① **앱 번들이 아니라 웹** — 1.2MB 스크린샷을 모든 사용자가 받지만 보는 건 최초 1회고, 넥슨이 화면을 바꾸면 사이트는 push 한 번인데 앱은 OTA 배포가 필요하며, 사용자는 발급 중 어차피 브라우저에 있다 ② **스크린샷은 커밋한다 — [[ADR-038]] 의 반대쪽 사례**: 그 기준은 크기가 아니라 "다시 만들 수 있는가"였고, 이건 사용자 개인 넥슨 계정 화면 + 손으로 얹은 빨간 주석이라 재생성 불가 ③ **"서비스 단계"로 안내** — `nexon-api.md` 의 "서비스 단계 키 전제"(개발 단계는 초당 5건·일 1,000건)가 깨지면 병렬 동기화가 바로 걸린다 ④ `build-site.mjs` 의 정적 자산 복사를 `cp({recursive})` 로 — `readFile`/`writeFile` 은 디렉터리에서 EISDIR 로 죽는다. **후속 완료(2026-08-08, 이슈 #61)**: `ApiKeyForm` 을 이 페이지에 연결 — 교체가 아니라 1차 `API 키 발급 방법`(가이드) + 보조 `openapi.nexon.com` 바로 가기 둘 다, 설정 화면엔 두지 않음(재입력 경로가 없어 갈 곳 없는 안내가 된다). 구현 완료 |
| [ADR-111](./adr/ADR-111.md) | **TTL 로 건너뛴 진입도 자동 기록한다 — 게이트를 "네트워크"와 "기록" 두 축으로 가른다**([[ADR-097]] 결정 6 폐기). 앱을 켜고 `/content` → `/profit` 으로 가면(`/` 가 `/content` 로 리다이렉트되므로 **거의 모든 콜드 스타트**) 이번 주 처치 보스가 목록엔 있는데 **금액이 0** 으로 뜬다(이슈 #160). 원인은 `skipSync` 하나가 네트워크 재조회와 자동 기록(`upsert`)을 **함께** 건너뛰는 것 — `hasSyncAttemptedThisRun()` 은 실행당 1회라 `/content` 의 동기화가 플래그를 세우고 그 동기화가 방금 캐시를 썼으니 `isSyncFresh` 도 참이라, 수익 첫 진입은 **항상** 건너뛴다. 그런데 수익의 "계산"은 캐시 우선 표시가 아니라 **동기화 뒤의 자동 기록 루프**가 한다(`partySize` → `payoutMeso` → SQLite). [[ADR-097]] 결정 6 이 "자동 기록만 최대 10분 미뤄진다"고 적을 때 상정한 **낡은 캐시**가 이 경로엔 없다 — 여기 캐시는 **같은 실행에서 방금 성공한 동기화가 쓴 값**이라 게이트가 방어할 이유 없는 자리를 막았다. ① 게이트를 두 축으로 갈라 **네트워크 재조회는 그대로 0**([[ADR-097]] 결정 1~4 무변경), 자동 기록·드롭 이관([[ADR-069]] 결정 4)만 캐시 단계에서 수행 ② 안전 가드는 나이가 아니라 **기간 동일성**(`getCurrentBossProfitPeriod(row.cycle, syncedAt).periodKey === row.periodKey`) — `buildBossProfitRow` 가 `periodKey` 를 `now` 로 계산하므로 **리셋 경계를 넘은 캐시**만 위험하고, [[ADR-017]] 이 걱정한 "낡은 캐시발 잘못된 파티원 수"는 **캐시가 출처가 아니다**(`partySize` 는 기록 시점에 `boss_party_settings` 를 그 자리에서 읽는다 — 캐시가 정하는 건 "어떤 조합을 기록할지"뿐이라 기간 동일성이 정확히 그 위험만 겨눈다) ③ 판정 기준은 `syncedAt`(서버의 `asOf` 가 아니다 — `row.periodKey` 와 **같은 기기 시계**여야 하고, 게이트가 이미 쓰는 값이라 기준이 하나로 남으며 추가 조회 0회) ④ 기록은 화면 `set` **앞**에서 끝낸다(`set` 은 계속 1회 — [[ADR-097]] 결정 5 정정 3 유지, 뒤로 미루면 **0 → 실제값 점프**), 자리는 `refreshInPlace`([[ADR-076]] 결정 2)보다 앞이라 두 분기가 함께 덮인다 ⑤ 유지되는 방어 넷 — 건너뛴 진입의 `syncSchedules` 0회 · **비-건너뛴 경로의 캐시 단계는 여전히 기록 안 함**([[ADR-017]] 의 방어가 설 자리는 정확히 거기) · 폴백 캐시 진입 제외([[ADR-067]] 결정 7) · **기록 조회 실패(`null`)면 기록 안 함**([[ADR-050]] 결정 3 — 캐시 단계 폴백을 `[]`→`null` 로 바꾸는 것이 선행 조건, 안 그러면 사용자가 저장한 파티원 수가 1 로 덮인다) ⑥ **캐시 단계도 기록만 있는 조합을 행으로 되살린다**([[ADR-067]] 결정 4 누락 보완 — 같은 증상의 두 번째 경로): 기간 키에 현재 주·달을 항상 포함하고, 프로필 맵을 캐시 *행* 이 아니라 **캐시 엔트리**에서 만든다(행이 0인 캐릭터는 프로필이 없어 `appendRecordOnlyRows` 가 통째로 건너뛴다 — 고치려는 시나리오가 프로필 부재로 다시 막힌다), 건너뛴 진입 전용이 아니라 **캐시 단계 일반**에 적용(두 경로가 다른 화면을 그리면 그게 다음 결함이다). 버린 대안 — **화면별 실행 플래그**([[ADR-097]] 결정 3 이 명시적으로 기각: 콜드 스타트 직후 같은 응답을 화면 수만큼 받는다, 게다가 결합을 남겨 TTL 안 두 번째 진입에서 재발) · **자동 기록을 `loadPeriod` 로 이동**(가장 근본적이나 [[ADR-014]]·[[ADR-019]]·[[ADR-050]]·[[ADR-067]] 이 얹힌 자리라 범위가 크다 — 이 결정이 그 길을 막지 않고 오히려 선행 작업이다). 구현하며 셋을 정정 — 자동 기록 루프를 `features/boss-profit/auto-record.ts` 로 **분리**(두 경로가 같은 루프를 불러야 하는데 인라인이라 복사하면 [[ADR-050]]·[[ADR-067]]·[[ADR-069]] 가 한쪽에만 적용되는 사고가 열린다, 다른 것은 "출처가 지금의 사실인가" 술어 하나뿐), 결정 6은 딸고 온 것이 둘이 아니라 **넷**(기록 조회의 `cachedRows.length > 0` 게이트 제거 — 행 0인 진입이 곧 복원 대상이라 그 게이트가 재료를 지웠다 · 정렬을 복원 **뒤** 한 번으로), 뒤집힌 옛 테스트는 갱신이 아니라 **삭제**(계약이 정반대라 같은 이름을 남기면 오독한다). (**구현 완료**, 2026-08-08, 이슈 #160 — 테스트 2,503개 통과·build·lint 에러 0, **실기기 미검증**: 콜드 스타트 → `/content` → 수익 탭에서 새로고침 없이 총 수익이 맞게 나오는지는 실물 확인이 남았다) |
| [ADR-112](./adr/ADR-112.md) | **spacer 실측의 갱신 경로가 `ResizeObserver` 하나면 헤더 높이를 바꾸는 상태 전환마다 한 프레임 어긋난다 — 측정 전용 layout effect 를 deps 없이 따로 둔다**. 보스 수익에서 기록 없는 과거 기간으로 이동하면 `지난 주 기록을 불러오고 있어요` 카드가 **한 프레임 약 90px 아래에 그려졌다 제자리로 올라온다**(이슈 #168). 사슬은 다섯 고리 — `isPeriodLoading: true` → 총 수익 헤드라인이 `!isPeriodLoading` 게이트에 걸려 사라져 **헤더 DOM 이 91px 짧아짐**(라벨행 `h-6` 24 + `mt-1.5` 6 + 금액행 `h-8` 32 + `mt-3` 12 + 헤어라인 1 + 부모 `space-y-4` 16) → 그런데 spacer(`stickyHeaderHeight`)는 측정 `useLayoutEffect` 의 deps 가 `[isEmpty]` 하나뿐이라 **재실행되지 않아 옛 값** → 그 상태로 한 프레임이 그려짐 → `ResizeObserver` 가 뒤늦게 발화해 제자리로. **핵심은 갱신 경로가 RO 하나뿐이라는 것** — RO 콜백은 스펙상 페인트 *전*에 배달되지만 그 안의 `setState` 는 React 이벤트 밖이라 Scheduler(MessageChannel) 태스크로 넘어가 **다음 프레임**에 렌더된다([[ADR-102]] 가 접기의 `rAF` 콜백에서 관측한 그 성질), 반면 `useLayoutEffect` 안의 `setState` 는 같은 커밋에서 페인트 전에 동기 반영된다. [[ADR-085]] 트레이드오프 셋째 줄("헤더 높이가 바뀌면 RO 가 따라가고 첫 프레임은 `useLayoutEffect` 가 막는다")이 이 자리를 이미 가리키고 있었으나 **마운트 첫 프레임만** 막고 있었다. 이슈의 '미확인' 2건은 **코드 확인으로 답이 나왔다** — ① 기록이 있는 기간에도 **난다**(게이트가 `!isPeriodLoading` 이라 기록 유무와 무관, 다만 캐릭터 카드가 들어차 콘텐츠 변화에 섞여 덜 띌 뿐이고 기록 없는 기간은 셸 승계 로딩 카드([[ADR-061]] 결정 2) 하나뿐이라 이동이 그대로 읽힌다) ② 공용 `PageHeader` 도 **구조적으로 같다**(deps `[]`, 네 화면 전부 헤더 `children` 에 조건부 블록 — 로딩 카드·탭 줄·클리어 카운트·시즌 보스 줄·`selected !== null`) — **단 사용자 보고도 브라우저·실기기 관측도 없다**(구조가 같아 함께 고치는 것이지 관측된 것이 아니다, [[ADR-098]] 결정 4와 같은 종류의 판단). 결정 — ① 측정 전용 layout effect 를 **deps 없이**(매 커밋 페인트 전 실측) 두고 **RO 는 남긴다**(대체가 아니라 담당이 다르다 — 렌더 없이 높이가 바뀌는 폰트 로드·기기 회전은 커밋이 없어 측정 effect 가 안 돈다) ② 공용 훅 `src/lib/use-measured-height.ts` 로 추출해 `PageHeader`·`BossProfitScreen` 이 함께 쓴다([[ADR-094]] 두 조건 충족 — 호출부 2곳 + 취약 구조: 실측+spacer 는 [[ADR-085]]·[[ADR-098]] 이 실기기에서 여러 번 틀린 끝에 얻었고 그 값을 [[ADR-047]] 중첩 sticky 가 자기 `top` 으로 다시 쓴다) ③ API 는 `RefObject` 가 아니라 **콜백 ref** — 요소를 state 로 잡아야 관찰 effect 가 등장·소멸을 따라 재부착되고, 그래야 `BossProfitScreen` 의 `[isEmpty]` deps 가 **사라진다**(빈 상태에서 헤더가 언마운트되는 경로를 훅이 구조적으로 처리). 기각 — **deps 목록 명시**(조건부 블록이 늘 때마다 같이 고쳐야 하고 빠뜨리면 조용히 재발, 무엇보다 `children` 이 임의인 공용 셸에는 **원리적으로 불가능**) · **헤더 높이 고정**(로딩 중 90px 빈 블록이 남는 디자인 변경이고 `PageHeader` 의 탭·필터에는 못 써 화면 전용 처방이 된다) · **RO 콜백 `setState` 를 `flushSync`**(같은 프레임에 반영되나 RO broadcast 루프 안에서 렌더+레이아웃을 다시 돌려 `ResizeObserver loop` 계열 위험을 새로 들인다) · **spacer 를 없애고 흐름으로 복귀**(**기각이 아니라 금지에 가깝다** — [[ADR-085]] 결정 1이 iOS 실기기에서 `sticky` 헤더가 `-1065px` 로 날아가는 것을 풀려고 `fixed` 로 옮긴 것이라 되돌리면 더 큰 문제가 산다). **대가**: 커밋마다 `getBoundingClientRect()` 1회 = 강제 리플로우 1회 — 지금은 안전하다(당김은 목록에 `transform` 만 걸어 레이아웃을 안 더럽혀 [[ADR-072]] 결정 4·[[ADR-073]] 의 "터치 프레임마다 리플로우 없음"이 유지된다), **다만 인디케이터·목록 이동을 흐름·크기를 바꾸는 방식으로 되돌리면 이 대가가 실제 비용이 된다**. 폐기 없음 — [[ADR-085]] 결정 1·[[ADR-098]] 결정 2의 계약(`fixed` 유지 · 실측을 `useEffect` 로 되돌리지 말 것)은 그대로 서고 갱신 경로가 하나 더해질 뿐이다. 구현하며 셋을 정정 — 훅은 요소가 사라져도 **마지막 실측값을 남긴다**(이 훅이 정하는 것은 측정 시점이지 값의 수명이 아니다 — 두 호출부의 기존 동작), 측정 effect 의 `setState` 한 줄에서만 `react-hooks/set-state-in-effect` 를 끈다(그 "동기 반영"이 훅의 존재 이유이고 같은 높이면 bailout 한다), `BossProfitScreen.tsx` 안에 `new ResizeObserver` 가 **한 건 남는다**(같은 파일 `CharacterAccordion` 의 **카드 헤더** 실측 — 접힘 66/펼침 64, deps `[isExpanded]`. 페이지 헤더 spacer 와 다른 요소·다른 관심사이고 옮기면 카드 페이드 갱신 타이밍이 바뀌는데 그것을 다루는 결정이 없어 범위 밖) (**구현 완료 · 실기기 미검증**, 2026-08-08, 이슈 #168 — 테스트 2,523개 통과/171파일·build 성공·lint 0 errors(17 warnings, baseline 동일), 이 결정으로 8케이스 추가. **자동 테스트가 담보하는 것은 측정 시점이지 픽셀이 아니다**(jsdom 은 레이아웃을 계산하지 않아 높이를 스텁으로 흉내 낸다) — 기록 없는/있는 기간 이동의 튐 소멸, 스케줄러 콜드 스타트 로딩 카드→탭 줄 전환, 펼친 카드 중첩 sticky stuck 위치([[ADR-047]])는 실기기 확인이 남았다. 이슈의 '검증' 4항목 중 스케줄러 스탈 배너 건은 **여전히 미관측** — 구조가 같아 함께 고친 것이지 증상이 있었던 것이 아니다) |
| [ADR-113](./adr/ADR-113.md) | **계정 선택은 전수 프로브가 settle 한 뒤에 목록을 그린다 + `character/basic` 5분 TTL 가드(네 호출부 공유)** — 계정 선택 화면에서 **고를 수 있어 보이는 카드가 먼저 뜨고** 잠시 뒤 "조회할 수 없습니다"가 튀어나오며 비활성으로 바뀌고 대표 캐릭터의 이름·레벨·초상화도 그 사이 한 번 바뀐다(이슈 #163). 버그가 아니라 [[ADR-086]] 결정 8 이 명시한 정책("프로브 도착 전에는 선택 가능 — 모르는 것을 단정하지 않는다")이라 이 결정은 그 **정정**이다. 목록을 기다리게 하는 것은 **대기를 늘리는** 방향이라 먼저 그 원천을 접는다 — `character/basic` 이 온보딩 한 바퀴에 **세 번** 나가고 있다(프로브 `use-account-probes.ts:60` 전 계정 · 예열 `prefetch.ts:56` · 피커 `character-roster.ts:187`, 셋 다 **완전히 같은 요청**이고 소비 필드만 다르다, 40캐릭터면 120회) + 이슈 밖의 **네 번째**([[ADR-097]] 결정 7 편승 갱신 `schedule-sync.ts:222`). 결정 — ① **5분 TTL 가드를 공유 경로 하나로** 두고 네 호출부가 전부 통과한다(호출부를 하나씩 고치면 **호출자끼리 서로를 알아야** 하고 새 호출부가 생기면 조용히 중복이 되살아난다 — 실제로 결정 7 이 그렇게 넷째를 추가했다). 5분인 이유는 온보딩 한 바퀴를 덮으면서 **[[ADR-097]] 동기화 TTL 10분보다 짧아야** 편승 갱신이 경계에서 죽지 않기 때문 ② **프로브가 결과를 캐시에 쓴다**(각 계정의 `accountId` 로) — [[ADR-068]] 결정 4 구현의 "쓰지 않는다" 폐기. 그 근거였던 전역 인덱스를 [[ADR-086]] 결정 9 가 계정별로 쪼개(`characterBasicCacheIndexKey`, stub 은 `getAllCachedCharacterBasicOcids(accountId)`) **누출이 구조적으로 불가능**해졌는데 프로브 동작과 주석만 남아 있었다(원래 ADR 본문도 "버리지 않고 쓴다"였다) ③ **settle 전에는 목록을 그리지 않는다** — 원칙("모르면 단정하지 않는다")은 유효하고 그것을 **"모르는 동안은 보여주지도 않는다"** 로 적용한다([[ADR-053]] 결정 2 가 피커 콜드 스타트에 내린 것과 같은 처방) ④ **완료 판정은 성공이 아니라 settle** — `getAuthConfig()` 가 `null` 인 조기 return(`:49-50`, `setProbes` 를 한 번도 안 부른다) · 계정 0개 · 개별 실패 전부 완료로 친다. 성공 기준이면 **무한 스피너**가 실현된다 ⑤ 대기는 **진행률 바**로 그리고 **문구는 붙이지 않는다**(시작 시점에 총량 = 전 계정 캐릭터 수를 안다), 설정 계정 변경의 직전 단계 `verifying` 도 같은 자리에 같은 바를 **0% 로** 두어 두 대기가 **하나의 연속된 로딩**으로 읽히게 한다 — [[ADR-061]] 결정 1 의 **두 번째 예외**([[ADR-074]] PTR 에 이어)이고 근거는 결정 6("진행률은 얇은 바 하나")이 **적용 가능한 자리**라는 것(**총량을 아는 대기에 불확정 스피너를 두는 것은 아는 것을 안 보여주는 것**), 결정 1 자체는 그대로 선다 ⑥ **자격 스윕은 넣지 않는다** — `basic` 은 어차피 전 계정을 불러 캐싱이 공짜지만 `scheduler/character-state` 는 선택 계정만 부르므로 당기면 **안 고를 계정 몫이 순증**해 이슈 #158 을 악화시키고, 003 판별에 스윕은 필요 없다 ⑦ **실패한 프로브는 후보 자격을 유지한다**(현행 유지·명시 — 003 만 영구, 나머지는 원인을 모르므로 초상화만 비운다). **중복 제거의 진짜 값은 호출 수가 아니다** — 자격 스윕은 실패를 원장에 기록하지 않으므로(의도된 설계) 429 가 나면 다음 화면이 같은 13일을 다시 훑고 그게 또 429 를 부른다. **429 확률을 낮춰 원장이 채워질 확률을 올리는 것**이 값이고, 원장이 한 번 차면 그 캐릭터는 그 뒤로 스윕이 없다(이슈 #158). 기각 — 호출부 개별 수정 · "온보딩 중에만 캐싱 끄기"(피커의 갱신은 **의도된 신선도**라 판정이 "어느 단계인가"가 아니라 "몇 분 이내인가"여야 한다) · TTL 10분(편승 갱신이 죽는다)·3분(모드 선택에서 머물면 피커가 TTL 을 넘긴다) · 스피너만(총량을 아는 자리다) · 캐시 없이 대기만 늘리기(체감만 늘고 총 호출은 그대로) · 프로브가 003 을 원장에 기록(피커 live 루프는 여전히 전원에게 `basic` 을 불러 절감 0 — 범위 밖). **대가**: 이 화면의 대기는 **새로 생기는 것이 맞다**(프로브는 전 계정·예열은 선택 계정이라 총합이 줄어도 이 화면은 는다 — **총합이 주는 것과 이 화면이 느는 것은 별개**고 사용자는 총합을 못 본다) · 안 고른 계정의 엔트리가 저장소에 쌓인다(엔트리는 전역 키·인덱스만 계정별 — 대신 그 계정으로 바꾸면 따뜻한 캐시를 재사용, [[ADR-086]] 결정 9 가 의도한 성질) · 5분 안에 다시 열면 개명·레벨업이 최대 5분 늦다 · [[ADR-097]] 결정 7 의 "별도 TTL 은 두지 않는다"가 **정정된다**(호출 조건 결정 1~4 는 그대로, 가드가 하나 앞에 붙을 뿐). 구현하며 여섯을 정정 — **`(완료/전체)` 숫자는 남는다**(없앤 것은 설명 **문장**이고, 총량을 아는 것이 이 결정의 근거인데 그 총량을 지우면 앞뒤가 안 맞는다 · 설정 `verifying` 은 `character/list` 가 한 번이라 총량이 없어 숫자 없이 0% 바만 — 이어지는 것은 **프리미티브와 자리**이지 내용이 아니다) · 훅 반환은 `isLoading` 이 아니라 **`isSettled`**(실패로 끝난 대기를 "아직 로딩"으로 읽게 하지 않는다) · **세대 교체를 effect 가 아니라 렌더 시점에**(effect 는 페인트 뒤라 한 프레임 동안 옛 `probes`+`isSettled:true` 가 새 목록에 얹히는데 그게 결정 3 이 없애려는 장면이다) · `now` 는 라운드 하나가 공유(헬퍼가 시계를 안 읽고 인자로 받아 오차가 TTL 이 **짧아지는** 쪽으로만 난다) · **`cachedAt` 에 계약이 생기자 픽스처가 드러났다**([[ADR-097]] 이 "읽는 곳이 없다"고 적은 필드라 테스트 13곳이 페이크 시계와 같은 값을 심고 있었다) · 뒤집힌 옛 테스트 2건은 갱신이 아니라 **삭제**(계약이 정반대라 이름을 남기면 오독한다). **미해결로 남긴 사실** — **동시성 캡이 없다**(프로브는 전 계정 × 전 캐릭터를 하나의 중첩 `Promise.all` 로 동시에 부른다 · 총 호출 수는 접었지만 **한순간의 fan-out 폭**은 그대로라, 429 가 남으면 다음 후보는 여기다) · single-flight 없음(TTL 은 끝난 요청만 접는다) · 프로브의 003 원장 기록 미도입 · 결정 6 은 테스트가 아니라 **구조**(import 0건)가 담보 · "한 바퀴 = 캐릭터 수 × 1" 은 호출부별 가드 3건 + 훅 1건으로 **나뉘어** 담보되고 한 바퀴를 통째로 도는 테스트는 없다 (**구현 완료 · 실기기 미검증**, 2026-08-08, 이슈 #163 — 테스트 **2,548개 통과/172파일**(이 phase 순증 25 = 신규 28 − 삭제 3)·build 성공·lint **0 errors/17 warnings**(baseline 동일). **미확인 4가지가 전부 이 결정의 값을 판정한다**: 73캐릭터 계정에서 대기가 견딜 만한가 · 실호출이 정말 3라운드 → 1라운드가 되는가(네트워크 계측) · **429 가 실제로 줄어 원장이 채워지는가(주 목적, 이슈 #158)** · 두 대기가 하나의 연속된 로딩으로 보이는가). **결정 7 은 [[ADR-116]] 결정 3 으로 정정된다** — 003 이 아닌 실패는 "판정 불가"이고 판정 불가면 목록을 그리지 않는다(003 만 영구·"모르는 실패를 영구로 단정하지 않는다"는 그대로, 바뀌는 것은 "모른다"와 "괜찮다"를 같은 화면으로 그리지 않는 것뿐이다 — 결정 3 과 결정 7 이 서로 반대 방향으로 적용돼 있었다) |
| [ADR-114](./adr/ADR-114.md) | **429 는 키의 단계를 판정하지 않고 문구로만 안내한다 + 스탈 배너가 원인별로 갈린다** — 테스터 일부에게 캐릭터 관리 피커가 대표 1명만 남고 그 위에 `목록이 최신이 아닙니다` 배너가 떴다(이슈 #158). 원인은 **개발 단계 키의 호출 한도 초과(429)** 였고, 그 위에 **전제가 틀려 있었다** — `nexon-api.md:10` 의 "이 앱은 사용자가 이미 승인받은 **서비스 단계 키**를 쓰므로 개발 단계 상한 검증은 불필요"는 **개발자 본인 키에만** 맞는 말이고 사용자·테스터는 각자 발급받은 개발 단계 키(초당 5건·일 1,000건)를 넣는다. [[ADR-008]] 의 "병렬 호출이 한도와 충돌하지 않는다"가 그 전제 위에 서 있었다. 이 ADR 이 고치는 것은 **문구**다 — 429 가 세 자리에서 다르게 뭉개졌다: ① `StaleBanner` 는 props 가 `message`·`onRetry` 둘뿐이라(`StaleBanner.tsx:12-15`) `loadError.kind` 를 못 보고 호출부 2곳이 한 문장을 하드코딩(`CharacterTrackingPicker.tsx:59`·`ContentCharacterStep.tsx:46`), 401·429·조회 불가·네트워크가 **같은 한 줄 + 무조건 다시 시도**로 보였다(같은 화면의 `ErrorState` 경로는 이미 원인별인데 **항목이 남은 쪽만** [[ADR-062]] 결정 3 이전 상태였고, 캐시 stub 이 먼저 방출되므로([[ADR-017]] 결정 6) **그쪽이 기본 분기**다) ② 429 문구 4곳이 전부 "잠시 후 다시 시도해주세요"인데 개발 단계 키에는 **틀린 처방**이다(일 1,000건 소진은 다음 날까지 안 풀리고, 초당 5건은 앱이 버스트를 쏘는 한 재시도해도 또 429 — 실제 해결책인 **서비스 단계 신청**을 아무도 말하지 않는다). 결정 — ① **A안(판정하지 않는다)**: 넥슨 API 가 키 단계를 알려주지 않고 429 만으로는 구분이 불가능하다, **감지가 목적이 아니라 사용자가 다음 행동을 아는 것이 목적**이라 단계 무관 문구 한 벌로 끝낸다(추가 호출 0회·새 상태 0개·오탐 없음). 수치는 **넣지 않는다** — 서비스 단계 사용자가 봐도 어색하지 않아야 하고 배너 한 줄에도 들어가야 한다 ② **429 에는 액션을 주지 않는다** — [[ADR-062]] 결정 3 의 "429의 재시도 버튼은 **비활성화하지 않는다**(사용자 결정 2026-07-30)" 폐기. 그 결정은 "초당 한도라면 잠시 뒤 통한다"를 전제하고 잠금 여부만 따졌는데, 새 처방은 재시도가 아니라 **키 단계 확인**이라 버튼이 문구와 어긋난다. 토스트는 [[ADR-063]] 결정 3 부터 이미 액션이 없었고 `error-resilience.md` 원칙 3 도 원래 그렇게 적혀 있었다 — **`ErrorState` 와 배너 두 자리만 규칙 밖**이었다 ③ **배너가 원인별로 갈린다** — `StaleBanner` 는 계층(molecule)을 지켜 `ScheduleSyncError` 를 **받지 않고**([[ADR-094]] 결정 2 의 의존 방향) 호출부가 새 함수 `formatStaleRosterError(error, place)` 로 문구·액션을 뽑아 넘긴다(액션은 옵셔널이 된다). 401=설정 열기(피커만) · 429·`characterUnavailable`=없음 · `network` 계열=현행 유지. 근거는 **"배너는 목록이 남아 있어 액션이 없어도 막다른 길이 아니다"** 이고, 같은 401 이 온보딩 `ErrorState` 에서 재시도를 유지하는 이유도 **같은 근거의 뒷면**이다(그쪽은 목록이 없어 액션을 빼면 아무 길도 없다) ④ **토스트는 원인만, 인라인은 처방까지** — `Toast.tsx:88` 본문이 `truncate` 라 한 줄이 상한이다. `truncate` 를 푸는 대신 **자리마다 담을 수 있는 만큼** 담는다([[ADR-063]] 의 "자리가 문구를 정한다" · 결정 5 가 이름을 인원 수로 줄인 것과 같은 계열) ⑤ `nexon-api.md` 전제 정정(**프리플라이트·동시성 캡 도입은 범위 밖** — 전제가 적힌 문장을 고치는 것과 그 위에 세워진 호출 구조를 다시 짜는 것은 별건). 기각 — **B(온보딩에서 단계 프로브)**: 확실하지만 온보딩에 호출·시간이 붙고 **일부러 429 를 내는 설계**다 · **C(429 발생 누적으로 플래그)**: 추가 호출은 없으나 상태·판정 규칙이 새로 생기고 **판정이 서기 전 첫 429 에는 여전히 틀린 문구**가 나간다(그 한 번이 이 이슈의 증상이다) · `truncate`→`line-clamp-2`(429 하나 때문에 전 토스트의 레이아웃 규칙이 바뀐다) · 문구에 수치 넣기 · `StaleBanner` 가 `ScheduleSyncError` 를 직접 받기. **대가** — 서비스 단계 키 사용자도 "서비스 단계 키인지 확인해주세요"를 본다(A안을 고른 값이 이것 — 오탐 대신 **무해한 잉여 안내**) · 429 에서 앱 안에서 할 수 있는 일이 없어진다(**실제로 없었던 것을 화면이 인정하는 것**이라 후퇴가 아니다) · 토스트와 인라인의 429 문구가 갈린다 · **일 한도 소진과 초당 한도를 구분하지 않는다**(429 응답에 그 구분이 없다) · **문구를 바꿔도 429 자체는 줄지 않는다**. 범위 밖(이슈가 명시적으로 분리) — 자격 스윕이 429 를 삼켜 캐릭터가 조용히 사라지는 문제(`character-eligibility.ts:90-102` — `characterUnavailable`·`periodOutOfRange` 만 원장에 기록하고 429 는 `continue`) · 로스터·예열의 프리플라이트·동시성 캡 부재([[ADR-113]] 이 남긴 fan-out 폭과 같은 자리) · `character/basic` 3라운드 중복 제거는 이미 완료([[ADR-113]] 결정 1). **개발 단계 키로 실제 429 를 재현해 배너를 확인하는 것은 실기기·실사용 몫**이다 — 자동 테스트는 `rateLimited` 를 주입해 화면을 검사할 뿐 `nexon/http` → `toScheduleSyncError` → 로스터 실패 → 배너 사슬이 **실제 429 응답에서** 이어지는지는 모른다. 구현하며 일곱을 정정 — `StaleBanner` 는 액션 유무만이 아니라 **라벨도 호출부가 넘긴다**(피커 401 이 "설정 열기"라 하드코딩 `다시 시도` 를 남기면 결정 3 의 표가 절반만 구현된다), 포맷터가 싣는 것은 콜백이 아니라 **`kind` 판별자**(`'retry' | 'openSettings'` — `features/` 가 컴포넌트 핸들러를 알 수 없고, 덕분에 두 포맷터의 호출부가 같은 모양이 된다), 온보딩은 `kind === 'retry'` 를 **확인하고** 매핑(지금은 결과가 같지만 포맷터가 바뀌면 갈 곳 없는 버튼 대신 버튼이 사라져 드러난다), 설정 카드 가드는 `props.error?.kind !== 'rateLimited'` 라 `error === null` 폴백에 버튼이 **자연히 남고**(원인을 모르는 실패는 재시도 가능), 어미 단언은 `/(습니다|아닙니다|주세요)$/`(`목록이 최신이 아닙니다` 도 같은 하십시오체 활용), 뒤집힌 옛 기대값은 갱신이 아니라 **삭제**(`ErrorState` 옛 3케이스 → 새 5케이스 · `format.test.ts` 의 `rateLimited → retry` 단언 2자리 제거), `nexon/http.ts:44` 의 `NexonRateLimitError` 메시지는 **사용자 문구가 아니라 내부 진단**이라 미변경. **이슈 #158 필수 2 는 절반만 했다** — 해결 경로는 줬으나 **한도 수치(초당 5건·일 1,000건)는 결정 1 이 명시적으로 기각**했다(A안은 서비스 단계 사용자도 같은 문구를 보는 것을 전제로 서고, 배너 한 줄에 들어가야 한다). **목록이 비는 실제 원인은 문구가 아니라 자격 스윕이라 이 phase 뒤에도 목록은 여전히 빈다** (**구현 완료 · 실기기 미검증**, 2026-08-08, 이슈 #158 — 테스트 **2,570개/172파일** 통과(순증 22 = 신규 25 − 삭제 3, 새 테스트 파일 0개)·build 성공·lint **0 errors/17 warnings**(baseline 동일). **미확인 3가지**: 개발 단계 키로 실제 429 를 내 배너가 새 문구로 뜨는가 · 새 문구를 본 사용자가 서비스 단계 신청에 도달하는가(A안이 감지 대신 안내를 택한 값이 여기서만 확인된다) · 32자 배너가 360px 폭에서 안 잘리는가. **429 자체는 한 건도 줄지 않았다** — 바뀐 것은 화면이 하는 말뿐이다). **결정 2 는 [[ADR-116]] 결정 4 로 `ErrorState` 자리에서 정정된다**(액션 없는 429 가 그 자리를 막다른 길로 만들었다 — 스탈 배너·토스트에서는 그대로 유효하다) |
| [ADR-115](./adr/ADR-115.md) | **API 키 무효화(400 `OPENAPI00005` · 401/403) → 토스트 + 키 입력 화면 자동 이동, 뒤 단계는 저장된 값으로 재개** — 401 이 안내하는 목적지(설정)에 실제로는 키를 바꿀 자리가 없었다(`ApiKeyModal` 2026-07-25 제거, "계정 변경"은 **저장된 키 그대로** 재조회라 같은 401 을 다시 맞는다) — 남은 경로는 위험 색 **연결 해제** 하나뿐이라 화면과 안내가 어긋나 있었다(이슈 #157). 실측이 이슈의 전제도 뒤집었다 — 재개 파생([[ADR-086]] 결정 1)이 **부팅 경로에만** 있어 `getAuthConfig()` 는 apiKey 없으면 나머지를 읽지도 않고 `null` 을 주는데(✅), **키 재입력 후**에는 리듀서가 무조건 `selectingAccount` 로 가고 `selectedAccountId` 를 리셋해(`state.ts:97-106`) 계정·예열·모드·캐릭터를 **전부 다시 묻는다**(❌). 결정 — ① 401/403 을 만나면 **토스트 한 줄(`API 키가 더 이상 유효하지 않습니다`) + 곧바로 키 입력 화면**, 기다리지 않고 **액션 버튼도 두지 않는다**(이동이 이미 일어나 누를 것이 없다 · 지금의 `설정 열기` 제거 · 문구는 앱 카피 규약대로 `API ` 를 붙였다) ② 이동은 **온보딩 상태를 뒤집는 것**으로 일어나고 스토어는 라우터를 모른다 — `App.tsx:295-344` 의 `isCompleted` 가드가 이미 라우터로 `/onboarding` 에 보내므로 `window.location` 이 필요 없고, 그래서 [[ADR-050]] 의 stale SQLite 커넥션 함정을 **피하는 것이 아니라 애초에 만나지 않는다**(복귀는 기존 완료 규칙대로 `/content`, 이탈 라우트를 기억하지 않는다) ③ 무효 키는 지우되 **`apiKey` 키 하나만** — `clearAuthConfig()` 는 `selectedAccountId` 까지 지워 결정 4 를 불가능하게 만드는 **연결 해제용**이라 쓰지 않는다(안 지우면 재시작 때 무효 키로 `completed` 복귀 → 또 401 → "재시작하면 될 것처럼 보이다가 안 되는" 상태 · 삭제 실패는 같은 길을 한 번 더 도는 것이라 별도 처리 없음) ④ 키를 다시 넣으면 **뒤 단계는 저장된 값으로 재개**한다(계정·모드·캐릭터를 다시 묻지 않음 · 예열([[ADR-016]])도 다시 안 돈다) — 파생 판정은 **함수 하나**로 두고 부팅과 공유한다(두 벌이면 재개 규칙의 진실이 둘이 되는데, 그것이 [[ADR-086]] 결정 1 이 진행 상태 전용 키를 거부한 이유다) ⑤ 재개에는 **계정 목록 대조 가드** — 새 키가 다른 넥슨 계정의 것일 수 있어, 검증 때 이미 받는 `character/list` 에 저장된 `selectedAccountId` 가 없으면 재개하지 않고 계정 선택부터(추가 호출 0회) ⑥ 멱등성은 **"`completed` 일 때만 무효화가 성립한다"** 로 얻는다 — 여러 화면·여러 캐릭터의 동시 401 에도 토스트 1회·이동 1회이고, **키 입력 화면에서의 401 은 이 경로가 아니라**(상태가 `completed` 가 아니다) 폼 자체의 에러 처리라 재이동 루프가 구조적으로 불가능하다 ⑦ **감지 지점 전부가 진입점 하나를 부른다**(스케줄러 3화면 토스트 · 피커 로스터 조회 `ErrorState`·스탈 배너 · 설정 계정 변경 `refreshAccounts` — 저장된 키로 재조회하다 맞는 401 도 키 무효화다) → 그 자리들의 `설정 열기` 는 전부 제거되고, [[ADR-062]] 결정 3 의 *"401 은 설정으로 보낸다"* 가 **폐기**되어 *"키 입력 화면으로 보낸다(자동)"* 로 대체된다(목적지가 바뀐 이유는 단순하다 — **설정에는 키를 바꿀 자리가 없다**). [[ADR-008]] 의 *"401/403 → 설정에서 다시 입력해주세요 배너"*(미구현 보류)도 **배너 없이 자동 이동**으로 대체 — 배너를 새로 만들지 않는다 ⑧ 설정의 **"API 키 변경" 행은 부활시키지 않는다**(이슈 #157 미정 3 → #135 와 함께) — `changeApiKey` 는 로직·테스트가 살아 있으나 **배선하지 않고 그대로 둔다**(그 경로의 401 은 "방금 나쁜 키를 입력한 것"이라 무효화와 성질이 다르다). 기각 — 전용 키 재입력 라우트(폼·검증·실패 처리가 두 벌이 되고 결국 같은 재개 규칙을 부른다) · `clearAuthConfig()` 재사용 · 저장소를 안 건드리고 화면만 이동 · 배너 방식([[ADR-008]] 원안) · 이탈 라우트 복귀. **대가** — 작업 중이던 화면을 빼앗는다(무효 키에서는 어느 화면도 제 기능을 못 해 남겨 둘 화면이 없다) · 저장소 삭제 실패 시 옛 무효 키가 되살아난다 · **401 판정이 틀리면 유효한 키를 지운다**(`NexonAuthError` 는 401/403 에서만 생성되므로 감수, 잃는 것은 키 입력 한 번이고 뒤 단계는 결정 4 로 보존) · 토스트 1회 가드가 401 아닌 실패를 삼킬 수 있어 진입점을 401/403 전용으로 좁게 유지 · 결정 5 의 가드는 "계정이 겹치면 통과"까지만 본다. 구현하며 정한 것 — 재개 파생은 상태를 바꾸지 않고 **목표를 값으로 반환**하고(`deriveResumeTarget`) 스토어가 그것을 전이로 옮겨, 판정이 zustand 밖에서 단위 테스트되고 두 호출부가 같은 판정에 서로 다른 앞뒤 처리를 붙인다(기존 온보딩 테스트가 **한 건도 수정 없이** 통과한 것이 순수 이동임의 증거) · 무효화는 새 이벤트 없이 **`RESET` 재사용**(결과가 같아 이벤트를 더하면 리듀서의 진실이 둘이 된다 — 무효화와 연결 해제의 차이는 리듀서가 아니라 **저장소에서 무엇을 지우는가**에 있다) · 감지 지점을 잇는 훅 `useApiKeyInvalidation` 은 **자체 ref 가드 없이** 결정 6 의 가드 하나에 멱등을 맡기고, 결정 7 표의 "피커 로스터"는 피커가 아니라 **그것을 띄우는 화면**이 `rosterError` 로 부른다(온보딩 `ContentCharacterStep` 은 일부러 미배선 — 그 401 은 결정 6 이 말한 "이 경로가 아닌 401") · 401 의 `설정 열기` 가 사라지자 스탈 배너 6종이 두 자리에서 전부 같아져 **`formatStaleRosterError` 의 `place` 파라미터가 사라졌다**(`formatRosterError` 는 온보딩 401 만 재시도를 유지해 계속 받는다) · 피커 401 의 **인라인 문구는 구현이 정했다**(결정 1 이 정한 것은 토스트 문구뿐 — `설정에서 키를 다시 등록해주세요` 는 설정에 그 자리가 없어 **거짓이었다** → `키 입력 화면으로 이동합니다`) · 설정 401 은 진입점 호출 뒤 **`RESET` 까지** 한다(지나간 실패가 나중에 되살아나지 않게 + `idle` 복귀가 모달 닫힘 판정) · 뒤집힌 옛 테스트 3자리는 갱신이 아니라 **삭제 후 재작성**이라 step 4 에서 테스트 수가 오히려 줄었다 · **결정 6 의 멱등 가드만으로는 재이동 루프가 안 막혀 재현하고 고쳤다**(정정 11 — 동기화 스토어의 `error` 가 화면보다 오래 살아, 재입력해 `completed` 로 돌아오면 **이미 처리한 그 401 객체**가 가드를 통과해 방금 저장한 유효 키를 지웠다. 훅에 모듈 스코프 `WeakSet` 을 두어 이미 넘긴 객체를 기억한다 — ref 는 화면과 함께 죽어 못 막는다). **결정 9 추가(2026-08-08)** — 시뮬레이터 검증에서 **전제가 반증됐다**: 넥슨은 무효 키에 401/403 이 아니라 **400 `OPENAPI00005`** 를 준다(존재한 적 없는 키도, **정상 키를 넥슨에서 삭제한 경우도** 같은 응답 — 사용자가 재현). 그 코드가 `network` 로 degrade 돼 결정 1~8 이 옳게 구현됐는데도 **자동 이동이 한 번도 발동하지 않았고** 화면엔 "네트워크 오류"만 떴다. 판정을 `nexon/errors.ts#isInvalidApiKeyError` **한 곳**에 모아 `401·403·400 00005` 를 같게 보고 세 매퍼가 첫 분기로 쓴다(새 kind 를 만들지 않는다 — 처방이 같다). 딸린 개선으로 온보딩 키 오입력도 원인을 맞게 말한다. **고친 뒤 시뮬레이터에서 실물 확인 완료** — 폐기된 키로 앱을 띄우자 토스트 + 자동 이동이 설계대로 일어났고 저장소에는 `apiKey` 만 지워지고 나머지는 남았다(결정 3 확인). **남은 미검증 셋** — ①~~실제 401 미수신~~ **해소**, 단 **유효한 키를 다시 넣어 곧장 재개되는지**는 새 키 발급이 필요해 미확인 ② 화면을 빼앗기는 **체감**(결정 1 의 트레이드오프는 눈으로만 판정된다) ③ 토스트 1회·이동 1회가 **실기기에서** 그렇게 보이는지(여러 캐릭터 동시 401) ④ 재입력 후 재개가 정말 예열 없이 빠른지 (**구현 완료 · 시뮬레이터 부분 검증**, 2026-08-08, 이슈 #157 — 테스트 **2,619개/175파일**(기준선 2,570/172 대비 +49, 신규 파일 3)·build 성공·lint **0 errors/17 warnings**(baseline 동일). 결정 8개 전부 구현, 폐기된 결정 없음) |
| [ADR-116](./adr/ADR-116.md) | **개발 단계 키의 429 — 막힌 자리마다 "서비스 단계 키로 다시 입력" 경로를 연다** — 테스터 보고 3건(이슈 #177 계정 선택 · #176 온보딩 캐릭터 선택 · #178 캐릭터 피커 3자리)이 전부 **개발 단계 키의 429** 에서 나왔고 자리만 다르다. #177 → #176 은 **인과**다(429 로 정상처럼 보인 계정을 고르면 그대로 그 잠금에 도착한다). 결정 — ① **429 는 [[ADR-115]] 결정 10 의 알림 구조를 그대로 탄다**: 새 메커니즘을 만들지 않고 `apiKeyInvalidNotice: boolean` → **`apiKeyNotice: 'invalid' \| 'rateLimited' \| null`** 로 넓혀 **닫을 수 없는 모달 → `확인` → 키 입력 화면 + 저장된 `apiKey` 삭제** 사슬을 공유하고 **문구만 갈린다**(`호출 한도를 초과했습니다` / `서비스 단계 키로 다시 입력해주세요.`). 처방이 [[ADR-114]] 결정 1 과 **같은 말**이고(단계를 판정하지 않는다), 모달은 토스트와 달리 줄바꿈이 되므로 처방을 본문에 담는다(결정 4 의 "자리가 담을 수 있는 양") ② **알림 가드를 "키 입력 화면에 있지 않을 때"로** — 지금 가드(`status !== 'completed'`)로는 **온보딩 안에서 일어나는 #176 의 잠금을 구할 수 없다**. `awaitingApiKey`·`verifyingApiKey` 에서만 no-op(그 두 상태가 곧 "이미 키 입력 화면"이라 보낼 곳이 없고, 폼의 실패는 [[ADR-065]] 결정 1 토스트가 맡는다). 재이동 루프는 여전히 불가능하고 동시 실패도 **모달 하나**로 접히며, 원인이 겹치면 **먼저 뜬 것을 유지**한다(처방이 같아 갈아끼울 실익이 없다). [[ADR-115]] 결정 6 의 *"구조적으로 불가능"* 이 **두 번째로 정정되는 것**이다 — 첫 번째(그 ADR "구현하며 정정한 것" 11, 스탈 error 객체 재마운트)는 **가드가 못 막던 경로**를 더 막았고 이번은 **과하게 막던 자리**를 연다(`WeakSet` 은 그대로) ③ **#177: 003 이 아닌 프로브 실패는 "판정 불가"이고 판정 불가면 목록을 그리지 않는다** — `use-account-probes.ts:121-125` 가 429 를 버려 `allUnavailable` 이 **항상 false**(위양성이 아니라 **위음성**)였고 조회 불가 캐릭터가 대표로 설 수 있었다([[ADR-068]] 결정 4 가 없앴다고 적은 문제의 429 판). `AccountProbe` 에 판정 실패를 담을 자리를 만들고, [[ADR-113]] 결정 3(*"모르는 동안은 보여주지도 않는다"*)을 429 에도 적용한다 — 지금은 같은 ADR 의 **결정 3 과 결정 7 이 서로 반대 방향**이라 모르는 채로 확신에 찬 목록을 그린다. **429 만이 아니라 003 이 아닌 모든 실패를 하나로 묶는다**(사용자 결정). [[ADR-113]] 결정 7 의 **정정**이되 원칙("모르는 실패를 영구로 단정하지 않는다")과 003 만 영구인 규칙은 그대로이고, **`isSettled` 의 의미도 안 바꾼다**(결정 4 는 무한 스피너를 막는 장치 — 바뀔 것은 settle **이후의 표시**다) ④ **#178: `ErrorState` 가 그려지는 자리는 원인과 무관하게 진행 경로를 하나 이상 갖는다** — [[ADR-114]] 결정 2 를 **`ErrorState` 자리에서만** 정정한다. 그 결정의 근거(*"배너는 목록이 남아 막다른 길이 아니다"*)는 배너에서는 참이지만 `ErrorState` 는 **정의상 목록이 없는 자리**라 뒤집힌다(같은 ADR 이 401 에 대해서는 그렇게 판단해 놓고 429 엔 반대로 적용했다). **스탈 배너는 그대로**이고, **"429 에 재시도를 되살린다"가 아니다** — 더하는 것은 재시도가 아닌 **다른 출구**이고 이 phase 에서 그것은 **모달**이다(그래서 규칙은 *"자리마다 버튼을 달아라"* 가 아니라 ***"그 자리에서 앞으로 갈 수 있어야 한다"***). `ErrorState.tsx:10` 의 계약(`액션 항상`)과 구현(`action?`)의 어긋남도 함께 맞춘다 ⑤ **429 를 실제로 줄이는 일은 하지 않는다** — fan-out 동시성 캡을 **사용자가 이번 범위에서 명시적으로 기각**했다. 그래서 **429 발생 건수는 한 건도 줄지 않는다**([[ADR-114]] 가 *"바뀐 것은 화면이 하는 말뿐"* 이라 적은 **같은 한계가 그대로**다 — 그때는 화면이 사실대로 말하게 했고 이번은 그 화면에서 나갈 수 있게 한다), 자격 스윕이 429 를 삼켜 **목록이 비는 실제 원인**도 그대로다. 기각 — 429 에 다시 시도 되살리기 · 스탈 배너 액션 되살리기 · 온보딩 실패 가지에 `계정 다시 선택` 만 넘기기(**같은 키라 429 는 그대로**라 진짜 출구가 아니다) · 온보딩 전체 뒤로 가기(단계가 라우트가 아니라 `status` switch) · 429 전용 알림 UI 신설(처방이 무효 키와 같아 화면이 같아야 한다) · `isSettled` 를 성공 기준으로 · 판정 불가 계정을 중립 톤으로 표시하고 선택 허용(**들어갈 수 없는 문은 잠근다** — [[ADR-086]] 결정 8). **대가** — 일시적 초당 한도에서도 사용자를 온보딩으로 내보낸다(모달에 닫기가 없고 429 응답에 일/초당 구분이 없다 — 잃는 것은 [[ADR-115]] 결정 4 의 재개 덕에 **키 한 번 다시 붙여넣기**) · **429 로 지운 키가 다음 날이면 다시 쓸 수 있는 키일 수 있다**(그래도 지우는 이유는 [[ADR-115]] 결정 3 과 같다 — 안 지우면 *"재시작하면 될 것처럼 보이다가 안 되는"* 상태) · 429 가 잦은 계정은 **계정 선택 자체가 막힌다**(그 자리의 출구가 결정 1 의 모달이고, 후퇴가 아니라 사실을 드러내는 것) · **오프라인에서도 같은 화면**이 뜬다(하나로 묶은 대가 — 문구는 429 를 말하므로 어긋난 안내가 갈 수 있다) · 모달이 뜨는 자리가 늘어 **오탐의 대가도 커진다**(온보딩 중인 사용자의 키까지 지운다). 구현하며 정한 것 열하나 — 모달의 켜짐 판정을 `=== null` 이 아니라 **falsy** 로(부분 모킹에서 `undefined` 가 와 **모든 라우트 위에 닫을 수 없는 모달이 떴다** — 차단 UI 는 "켜라고 명시했을 때만" 켜진다) · "먼저 뜬 원인 유지"가 리듀서(같은 state 반환)와 스토어 가드 **두 겹** · 아이콘만 원인을 가리키고 톤은 둘 다 `error`(`Gauge` — 타이머 계열은 "시간이 지나면 풀린다"가 읽혀 처방과 어긋난다) · 이름을 원인 하나에서 푼 개명 3건(`ApiKeyNoticeModal`·`use-api-key-notice`·`api-key-notice-overlay`) · **429 의 토스트를 뗐다**(모달이 원인+처방을 말하므로 같은 사실을 두 번 말하지 않는다) · `changeApiKey` 미배선 유지([[ADR-115]] 결정 8) · 프로브 `retry` 신설(`attempt` 세대 카운터) · **대표 후보를 "확인된 캐릭터"로 좁혀 결정 3 보다 한 겹 더**(회귀 가드) · 헬퍼를 만들었다 인라인으로 되돌림(범위 밖 테스트가 모듈을 통째로 목으로 덮었다) · **결정 4 의 src 변경은 `ErrorState.tsx` 주석 한 파일뿐이고 새 UI 0개**(#178 의 `EmptyState` ↔ 피커 루프는 결정 1·2 배선으로 **이미 끊겨 있었다**) · step 1 은 실행이 `error` 로 남아 뒷정리를 사람이 마쳤고 그 실패가 첫째 결함을 드러냈다 (**구현 완료 · 시뮬레이터 검증**(실제 429 로 결정 1·2·3 이 한 화면에서 성립하는 것을 확인 — 사용자의 개발 단계 키가 온보딩 프로브 fan-out 에서 한도를 소진해 우연히 걸렸다. 함께 밝혀진 것: **무효 키로는 429 경로를 재현할 수 없다**(400 `OPENAPI00005` → '유효하지 않습니다'). `확인` 이후 구간은 사용자의 유효한 키를 지우게 되어 미검증), 2026-08-08, 이슈 #176 · #177 · #178 — 테스트 **2,662개/176파일** 통과(기준선 2,627 대비 +35, 새 테스트 파일 0개)·build 성공·lint **0 errors/17 warnings**(baseline 동일), 결정 5개 전부 구현·폐기된 결정 없음. **미확인 넷**: ① **실제 429 를 한 번도 받아 본 적이 없다**(전 경로가 주입된 `rateLimited` — `nexon/http` → `toScheduleSyncError` → 진입점 → 모달이 실제 429 응답에서 이어지는지 모른다) ② **429 는 한 건도 줄지 않았다**(결정 5 — 상류 fan-out 은 그대로고 목록이 비는 실제 원인인 자격 스윕도 그대로) ③ 일시적 초당 한도에서도 온보딩으로 내보내는 것이 실사용에서 옳은지 ④ 429 로 지운 키가 **다음 날이면 다시 쓸 수 있는 키**인 것을 사용자가 납득하는지) |
| [ADR-117](./adr/ADR-117.md) | **OTA 적용 경로에 복구 장치를 넣는다 — 일방통행을 끊고, 커버는 앱이 죽어도 걷힌다** — 테스터가 `지금 적용 (재시작)` 을 누른 뒤 **브랜드 주황 스플래시에서 무한 로딩**에 갇혔다(이슈 #175, iPhone 16 Pro). 시뮬레이터에서는 같은 흐름이 통과하므로 원인은 상태·타이밍이고, 고칠 것은 실패 확률이 아니라 **형태**다 — `apply()` 가 `showSplashScreen()` **먼저** 화면을 덮고 실패·행(hang) 가능한 작업 둘을 catch·타임아웃 없이 실행하는데(`store.ts:160-167`, 호출부는 `void apply()`) **커버를 걷는 코드가 존재하지 않는다.** 끊어질 수 있는 고리 셋은 증상이 전부 같다 — ⓐ iOS `SplashScreen.show()` 는 `UIView.transition` 완료 콜백에서만 resolve 하는데 `splash-screen.ts:27-30` 이 그 **await 전에** DOM 커버를 붙인다 ⓑ `closeBossProfitDb()` 에 타임아웃이 없다(여는 쪽은 10초가 있는데 — 이 저장소는 iOS 실기기 SQLite 무응답을 **두 번** 기록했다: [[ADR-008]] 2026-07-17 정정·[[ADR-050]] 결정 2) ⓒ `set()` 의 reject 가 아무 데도 도달하지 않는다. 게다가 **안전망 셋이 전부 꺼져 있다** — ① `main.tsx:12` 가 번들 **첫 문장**으로 `notifyAppReady()` 를 불러 capgo 의 유일한 안전망(`appReadyTimeout` **기본 10초**, config 미설정)이 무력화돼 **렌더 한 픽셀 전에 "정상"을 선언**하므로 메인 청크만 로드되고 죽는 번들이 **영구히 박힌다**(주석은 이 호출을 *롤백을 피하려고* 앞당겼다고 적혀 있는데, 롤백은 사고가 아니라 **유일한 복구 장치**다 — 의도가 뒤집혀 있었다) ② 커버를 걷는 유일한 코드(`hideSplashScreen`)의 타이머가 `App.tsx` useEffect 클린업에서 **취소**돼 1초 안에 렌더가 던지면 커버가 영원히 남는다 ③ ErrorBoundary 폴백은 `#root` 안이라 `z-index:2147483647` 인 `#boot-cover` **밑**에 그려지고, `show()` 가 건 `isUserInteractionEnabled=false` 때문에 '다시 시작' 이 **눌리지도 않는다**. 코드를 읽다 이슈 밖의 결함도 나왔다 — **`[data-splash-cover]` 를 걷는 코드가 저장소에 아예 없다**(`hideSplashScreen` 은 `#boot-cover` 만 지운다; *"문서와 함께 사라진다"* 는 주석은 **리로드가 성공한다는 전제**였다). 결정 — ① `apply()` 순서를 **`closeBossProfitDb()` → 커버 → `set()`** 으로 뒤집고 전체에 **12초 타임아웃 + catch**, 실패하면 **커버를 걷고 `'apply-error'`**(커버가 올라가 있는 시간을 [[ADR-027]] 이 원래 말한 리로드 구간으로 좁히는 것이고, 어댑터가 `close`+`set` 을 한 덩어리로 묶어 둔 것을 가른다) ② `notifyAppReady()` 를 **`App` 마운트 `useEffect`** 로 — "정상"이 *"메인 청크가 평가됐다"* 에서 **"React 가 마운트에 성공했다"** 로 바뀌어 렌더가 던지면 10초 뒤 직전 번들로 자동 롤백된다([[ADR-022]] 결정 6 정정). **하이드레이션 뒤로는 옮기지 않는다**(사용자 결정 — `prehydrateTabStores` 가 SQLite 에 의존해 10초를 넘기면 **멀쩡한 번들까지 롤백**된다) ③ `index.html` 인라인 스크립트에 **8초 실패 안전 타이머** — **`#boot-cover` 가 아직 DOM 에 있을 때만** 걷는다(정상 부팅 1초의 8배 여유 · 롤백 10초보다 짧아 사용자가 화면을 먼저 본다 · 가드가 없으면 정상 부팅 8초 뒤에 누른 **리로드 커버까지** 걷어버리는데 그 구간은 결정 1 이 맡는다). **컴포넌트 밖이라는 것이 요점** — 트리가 죽어도 이 타이머는 산다 ④ `hideSplashScreen()` 이 `[data-splash-cover]` 도 `querySelectorAll` 로 **전부** 걷는다 — 이 하나가 결정 1 의 *"커버를 걷고"* 를 실현 가능하게 만든다 ⑤ `closeBossProfitDb()` 에 **5초 타임아웃**(여는 쪽 10초와 대칭이되 더 짧다 — 닫기는 파일 생성·마이그레이션이 없어 정상이면 수 ms 이고, 이 값이 곧 적용 경로의 무반응 상한이다). 실패는 지금처럼 **삼킨다** — 타임아웃이 바꾸는 것은 *"실패로 끝난다"* 가 아니라 ***"끝난다"*** ⑥ ErrorBoundary 폴백이 마운트 시 `hideSplashScreen()` 을 부른다(커버 제거 + 네이티브 스플래시 해제 = **터치 복구**, 안전망 ②·③ 동시 절단). **z-index 상향은 기각** — 올릴 숫자가 없고(이미 32비트 최댓값), 매직 넘버가 두 곳에 생기며, **무엇보다 보이게 해도 눌리지 않는다** ⑦ **`'applying'` 상태 신설** — 결정 1 이 순서를 뒤집으며 **새로 여는 창**(닫기가 도는 최대 5초 동안 모달이 살아 있고 화면은 여전히 "준비 완료"라고 **거짓말**한다)을 막는다. 중복 탭 차단보다 **그 구간의 정직한 피드백이 본래 이득** — 이 이슈가 지적하는 *"아무 반응 없음"* 의 축소판을 새로 만들지 않는다(**단순 재진입 가드는 기각** — 눌러도 무반응인 버튼은 같은 문제다) ⑧ `clearCacheDataAndReload` 도 **동일 결함**이라 같은 순서로(`close` → 커버 → `reload`). 다만 [[ADR-065]] 결정 3 의 *"항상 리로드한다"* · `pendingNotice` 정책은 **바꾸지 않는다** — 고치는 것은 순서 하나뿐. **대가** — 적용을 누르고 최대 5초 커버가 없다(그 자리를 `'applying'` 이 채운다) · 결정 3 이 걷는 것은 **DOM 커버뿐**이라 React 가 마운트조차 못 하는 경로에서는 네이티브 스플래시가 남고 그 자리는 결정 2 가 되살린 10초 롤백이 메운다 · 롤백은 조용해서 배포자는 *"적용이 안 된다"* 로만 관측한다 · 5초·8초·12초는 근거 있는 추정이지 측정값이 아니다 · 상태 11 → 13 · 커버를 걷는 주체가 넷이 되지만 전부 `hideSplashScreen` 을 부른다. **범위 밖** — 적용이 왜 실패했는지의 규명(시뮬레이터 미재현 — 이 phase 는 실패의 **형태**만 바꾼다) · 크래시 리포팅 · `appReadyTimeout` 값 조정 · 고리 ⓐ 자체 제거. 구현하며 셋을 정정 — **어댑터를 가르지 않았다**(결정 1 하위 항목: `applyDownloadedLiveUpdate` 한 함수가 `close` → 커버 → `set()` 셋을 순서대로 소유한다. **실행 순서는 동일**하고 바뀐 것은 *누가 보장하는가* 뿐 — 나눠 가지면 "순서가 곧 결함이었다"는 이 ADR 의 순서를 두 파일에서 읽어야 한다. store 는 그 위를 덮는 것들(12초 타임아웃·catch·커버 걷기·상태 전이)을 맡는다), 결정 2 의 자리는 `App` 이 아니라 **`AppShell`**(`App` 은 ErrorBoundary 를 *렌더하는* 쪽이라 **자식이 던져도 자기 effect 는 돌아** 죽은 번들을 "정상"으로 찍는다 — 옮긴 의미가 통째로 무너진다. 판별력 확인에서 `AppShell`→`App` 으로 옮기자 크래시 회귀 테스트 1건만 실패), 결정 3 의 8초 타이머는 **DOM 커버뿐 아니라 네이티브 스플래시도 내린다**(`window.Capacitor?.Plugins?.SplashScreen?.hide()` — 결정 6 과 같은 이유로, DOM 만 걷으면 iOS 는 화면만 돌아오고 **터치는 죽은 채**다. 트레이드오프의 "2초 간극"이 좁아지지만 브릿지 부재 시 조용히 통과하므로 **보장이 아니라 한 겹 더한 것**). 함께 정한 것 — 성공 경로엔 상태 전환 코드가 **없다**(`set()` 이 컨텍스트를 파괴해 도달 불가라 쓰면 거짓 정보) · `'apply-error'` 는 `downloadedBundleId` 를 **유지**해 `다시 시도` 가 다시 받지 않고 `apply()` 만 재호출(`download-error` 와 갈리는 지점이 정확히 이것) · `'applying'` 모달은 **버튼 0개**·진행률 바가 아니라 스윕 스피너(적용은 퍼센트가 없어 결정형 진행률이 거짓) · 결정 6 의 자리는 `componentDidCatch` · 결정 5 의 타임아웃은 **대기 구간 전체**를 감싼다(`closeConnection` 만 감싸면 여는 쪽이 매달릴 때 그 앞에서 매달려 상한이 안 선다). **남은 문서 부채**: `src/storage/pending-notice.ts` 헤더 주석이 캐시 삭제의 **옛 순서**를 적고 있다(문서 step 이라 `src/` 미변경 — 동작 영향 0). (**구현 완료 · 실기기 미검증**, 2026-08-08, 이슈 #175 — 테스트 **2,695개/177파일** 통과(기준선 2,662/176 대비 +33, 새 테스트 파일 1)·build 성공·lint **0 errors/17 warnings**(baseline 동일), 결정 8개 전부 구현·폐기된 결정 없음. **미검증 6가지**: ① **어느 고리가 실제로 끊겼는지 여전히 모른다**(고리 A·B·C 를 전부 막았을 뿐이라 원래 증상이 사라지는지도 모른다 — "안 났다"는 "고쳤다"의 증거가 아니다) ② 실기기에서 `지금 적용` → 정상 적용(자동 테스트는 전부 목 위에서 돈다) ③ `'apply-error'` 모달·`'applying'` 체감은 **강제 주입·눈으로만** 판정된다 ④ 8초 타이머가 실제 콜드 스타트에서 오탐하지 않는지(**발화하는 것을 본 적이 없다**) ⑤ **롤백 실물 관측 없음** — 확인하려면 일부러 부팅에서 던지는 번들을 OTA 로 올려야 한다 ⑥ 5초가 실기기 정상 닫기를 자르지 않는지. 테스터 확인 항목은 [trouble/2026-08-08](./trouble/2026-08-08-ota-apply-stuck-splash.md)) |
| [ADR-118](./adr/ADR-118.md) | **설정 화면을 본화면 5행 + 하위 페이지 3으로 계층화한다** — 카드 하나에 성격이 다른 네 행(계정 변경·스케줄 관리 방법·테마·연결 해제)이 그룹핑 없이 인접하고, `SettingsRow` 가 `rightContent ?? chevron` 이라 **화살표가 동작이 아니라 값의 유무**를 말하며(같은 모달을 여는 세 행 중 `계정 변경` 에만 화살표), 개인정보 처리방침 링크는 정적 고지 사이에 끼어 색·밑줄로 버틴다(이슈 #135 · #161). 결정 — ① 본화면은 **카드 둘·5행**(값을 고르는 행 = 스케줄 관리 방법·테마 / 화면이 넘어가는 행 = 개발 노트·계정 및 데이터·앱 정보), **섹션 제목 없음**(시안 C 의 「동작·표시」류 제목이 행 이름보다 덜 구체적이라 얻는 것이 없다 · 시안 A 한 카드 5행은 덩어리를 그대로 둔다) ② **하위 페이지 셋**(`/settings/release-notes`·`/settings/account-data`·`/settings/about`)은 `/boss/manage`·`/content/manage` 와 **같은 골격**(`PageHeader` + `ArrowLeft` + `useScreenNavigate`)·`/settings` 형제 라우트·`isCompleted` 가드, 푸시·팝 전환과 스와이프 백은 **범위 밖**(이슈 #166 이 네 화면에 한꺼번에) ③ **파괴적 행은 `/settings/account-data` 안에서 별도 카드로** — 본화면에서 빼는 것만으로는 분리가 아니다(옮긴 곳에서 다시 `계정 변경` 과 붙으면 같은 문제가 한 층 내려갈 뿐) ④ **행 우측 표기 5종 고정**(설정 행 = 배지+chevron / 이동 행 = 대표값+chevron / 표시 행 = 평문·탭 불가 / 외부 링크 행 = 외부 링크 아이콘 / 실행 행 = 위험 색·**chevron 없음**) — 배타를 **병기**로 바꿔 *"chevron 이 있으면 누르면 열린다, 없는 위험 색 행은 누르면 지운다"* ⑤ **카테고리 행은 대표값 하나**(계정 및 데이터 = 캐시 총 용량 · 앱 정보 = 현재 버전 · 개발 노트는 **비운다** — "최신 버전"은 아래 행과 중복이고 "n개"는 뜻이 없다, 없는 대표값을 지어내지 않는다) ⑥ **`계정 변경` 에는 현재값을 두지 않는다**(`accountId` 는 불투명 문자열·대표 캐릭터 이름은 파생·변동값 — 결정 5 와 같은 기준의 반대편) ⑦ **개인정보 처리방침이 footer → `/settings/about` 의 행**, 옛 정책(*"footer 맨 위·`text-text-muted`+`underline`"*) 폐기 — 그 색·밑줄은 *"고지 사이에 끼면 링크로 안 읽힌다"* 를 달래던 것이고 독립 행이 되면 문제 자체가 없어진다. **스토어 요건은 그대로 충족**(Play 가 요구하는 것은 *"앱 안에 링크"* 이지 *"첫 화면에"* 가 아니다), 아이콘은 chevron 이 아니라 **외부 링크 표식**(앱을 떠난다), URL·본문 정책 불변 ⑧ **footer 는 고지 4줄만** — 전부 읽고 끝나는 문구가 되어 톤이 균일해지고, **[[ADR-098]] 의 고정 헤더 재판단도 필요 없다**(행 4→5 로 하나 늘면서 섹션 둘과 링크 한 줄이 내려가 **순감**이라 그 ADR 이 단 조건에 안 걸린다) ⑨ **넣지 않는 것 둘**(사용자 결정) — 개발 노트 **안 읽음 표시**(*"마지막으로 본 버전"* 키가 늘고 [[ADR-052]] `KEEP_KEYS` 판단까지 따라오는데, 그 신호는 업데이트 모달이 이미 말한다) · **`API 키 변경` 행 부활**([[ADR-115]] 결정 8 이 *"#135 와 함께"* 로 미룬 미정을 닫는다 — 무효 키·429 는 [[ADR-115]] 결정 10·[[ADR-116]] 결정 1 의 안내 모달 사슬이 이미 해결해 남는 경우가 "오류가 아닌데 자발적으로 갈아타는" 드문 경우뿐. `changeApiKey` 는 **로직·테스트가 살아 있고 배선만 없는 상태로 계속 남는다**) ⑩ `최신입니다` → **`최신 버전입니다`**(`상태` 행에 놓이는 값이라 주어가 문장 안에 있어야 한다). 기각 — 한 카드 5행 · 섹션 제목 부착 · `연결 해제` 만 본화면 존치(값 카드가 다시 혼종) · 하위 페이지를 모달로(안에서 또 모달을 연다) · `/settings` 중첩 라우트([[ADR-077]] 이 중첩을 고른 근거인 부모 상태 보존이 여기엔 없다) · 모든 행에 chevron(위험 행이 "다음 화면"으로 읽힌다) · `showChevron` 판단을 호출부에(그것이 지금 상태다) · 개인정보 처리방침 앱 내 화면(법적 문서가 두 벌). **대가** — 캐시 삭제·업데이트 확인이 한 탭 멀어짐 · 화면 파일 3개 증가 · 개인정보 처리방침 발견성 저하 · 배지+chevron 으로 행 오른쪽이 붐빔 · 캐시 총 용량을 **설정 열 때마다** 읽는다. **폐기** — [[ADR-104]] 결정 5 의 *"테마 행 오른쪽은 이름 배지 **하나뿐**"* → 배지+chevron(색 표식을 두지 않는다는 본론은 유효) · `settings.md` 의 footer 링크 위치·`rightContent` 배타 두 줄. 구현하며 정한 것 다섯 — 외부 링크 행은 `SettingsRow` 의 변형이 아니라 **별도 컴포넌트**(`<a>` 시맨틱이 달라 합치면 `onClick` 이 선택 필드가 된다)이고 공유 골격만 `row-class.ts` 로 뺐다(컴포넌트 파일이 값을 export 하면 fast refresh 가 깨진다 — `Button/variants.ts` 와 같은 판단) · 「앱 업데이트」 제목은 `AppUpdateSection` 이 **`<section>`+`<h2>` 껍데기를 벗는 방식**으로 사라졌고(제목을 숨기는 프롭이 아니다) 같은 계열로 `CacheDataSection.tsx` 는 옮겨진 게 아니라 **삭제**돼 로직이 화면에 흡수됐다(테스트 12케이스 이관, 커버리지 손실 0) · 모달 셋은 카드 밖이자 **`ScreenScroll` 밖**의 형제(카드 안이면 `divide-y` 가 구분선을 하나 더 그리고, 셸 안이면 `fixed` 스태킹 컨텍스트에 갇혀 오버레이가 탭바 아래로 간다) · 대표값의 **조회 전 자리는 `- KB`**([[ADR-061]] 결정 7 — 빈 문자열이면 값이 툭 나타나며 행이 밀린다) · **미검증 3 이 더 나빠진 방향으로 확정**됐다(본화면과 하위 화면이 각각 마운트 시 `loadCacheDataSizes()` 를 불러 드릴다운하면 **2회** — 하위 화면이 직접 진입 가능한 라우트라 값을 물려 내리지 않았고, 체감 지연은 **여전히 미측정**). (**구현 완료 · 실기기 미검증**, 2026-08-09, 이슈 #135 · #161 — 테스트 **2,756개/181파일** 통과(기준선 2,695/177 대비 +61)·build 성공·lint **0 errors/17 warnings**(baseline 동일), 결정 10개 전부 구현·폐기된 결정 없음. **미검증 4**: 본화면이 정말 스크롤 없이 들어가는가(835pt 기준 **추정**이고 구현 후에도 재지 않았다 — jsdom 은 레이아웃을 계산하지 않는다) · 화살표 병기가 360px 에서 붐비지 않는가(눈으로만 판정) · 캐시 용량 조회 비용(이제 2회) · 두 탭 깊이의 개인정보 처리방침이 심사에서 문제되지 않는가) |
| [ADR-119](./adr/ADR-119.md) | **개발 노트는 진실 원천 한 벌에서 두 갈래로 나간다** — 두 자리가 같은 사실을 필요로 하는데 담을 곳이 없다: 개발 노트 화면(#161)은 **과거 전체**, 업데이트 모달(#164)은 **아직 안 받은 버전 하나**(지금은 버전 번호와 용량만 말해 사용자가 **무엇이 바뀌는지 모른 채** 다운로드를 결정한다). 결정 — ① 진실 원천은 **`src/data/release-notes.ts` 한 벌** — 앱 번들에 그대로 포함돼 화면이 **오프라인·네트워크 0회**로 과거 전체를 보여주고, 배포 스크립트가 같은 파일에서 그 버전만 뽑아 `latest.json` 의 `notes` 로 파생시킨다. *"내장이냐 원격이냐"* 를 고르지 않은 이유는 **두 소비자가 필요로 하는 데이터가 실제로 다르기** 때문이다(원격 하나면 화면이 오프라인에서 비고 — [[ADR-003]], 내장 하나면 모달이 **자기가 아직 받지 않은 번들 안의 노트**를 읽어야 해 원리적으로 불가능) → 답은 **원천 하나 + 소비 둘**. `.ts` 인 것은 결정 3 의 표식에 타입이 붙기 때문 ② 버전 축은 **OTA 번들 버전**(`package.json` version) — 설정 하단 표시값·매니페스트·배포 스크립트가 이미 전부 이 값을 쓴다([[ADR-024]]) ③ 네이티브 변경 항목에 **「스토어 업데이트 필요」 표식**, 버전 전체가 아니라 **항목 단위**(한 릴리스에 OTA 변경과 네이티브 변경이 섞이는 것이 정상이고, 버전 단위로 묶으면 OTA 로 이미 받을 수 있는 나머지까지 못 받는 것처럼 읽힌다 · 매니페스트 `minNativeVersion` 과는 **다른 층** — 그쪽은 적용 게이트, 이쪽은 사람에게 하는 설명) ④ **과거 버전은 GitHub 마일스톤에 닫힌 이슈가 있는 것만 기록한다**(사용자 결정, 2026-08-09 정정 — 원안은 *"기록하지 않고 1.0.3 부터"* 였다) — 막으려던 것은 *"과거를 적는 것"* 이 아니라 *"근거 없이 적는 것"* 이고, 마일스톤의 닫힌 이슈는 사후 추론이 아니라 **그때 남긴 기록**이다. 그래서 `1.0.3`·`1.0.2` 는 싣고 **마일스톤이 없는 `1.0.1` 은 건너뛴다**(빈틈을 그럴듯한 문장으로 메우지 않는 것이 원안이 지키려던 바다). `1.0.0` 은 `앱 출시` 한 줄 ⑤ **`LiveUpdateManifest.notes` 는 선택 필드**(`minNativeVersion` 과 같은 방식) — `parseLiveUpdateManifest` 의 **필수 검사에 넣지 않는다**(넣으면 옛 매니페스트가 `null` 로 떨어져 **모든 기존 설치본의 업데이트 확인이 `check-error`** 가 된다), 모달은 `notes` 가 없으면 지금과 똑같이 동작한다 ⑥ **배포 스크립트가 노트 존재를 검사하고 없으면 중단** — `publish-live-update.mjs` 가 이미 `package.json` version 형식을 검사하고 `exit(1)` 하는 **그 자리(`:63-66`)** 에서 함께. 빌드(1/5)보다 앞이라 몇 분을 버리지 않고, **경고로 두지 않는 이유**는 경고가 반드시 무시되며 노트가 빠진 버전은 결정 4 때문에 **영영 빈 채로 남기** 때문이다 ⑦ 안 읽은 노트 표시 없음([[ADR-118]] 결정 9 와 같은 결정) ⑧ **[[ADR-006]] 대상이 아니다** — 게임 레퍼런스 수치가 아니라 우리 릴리스 기록이라 *"AI 가 임의로 추정해 하드코딩하지 말 것"* 의 대상이 아니다(그 규칙의 근거는 값의 진실이 **게임 안**에 있다는 것인데 여기 진실은 저장소 안에 있다). **다만 결정 4 때문에 근거 없는 과거를 지어내는 것은 여전히 금지** ⑨ **항목에 카테고리(`기능`·`개선`·`버그`, 이슈 접두사 승계)를 달고 화면은 배지가 아니라 그것으로 묶는다**(2026-08-09 추가) — 배지는 항목 수만큼 같은 말을 반복하지만 묶음 제목은 한 번만 말하고 그 아래 전부에 적용된다. 색은 얹지 않는다(이 화면의 색은 `사용 중`·`스토어 업데이트 필요` 두 신호가 쓴다). 순서는 데이터가 아니라 상수가 정하고 빈 묶음은 제목째 감춘다. 매니페스트는 평문이라 묶을 자리가 없어 줄머리가 `• ` → **`[카테고리] `** 로 바뀐다. 기각 — 원격 전용(오프라인에서 빈다) · 내장 전용(모달이 못 읽는다) · `notes` 필수 필드(기존 설치본 전부 `check-error`) · 버전 단위 표식 · 과거 노트 재구성 · 노트 누락을 경고로만 · JSON 형식 · GitHub Release 본문 파싱(두 채널이 **고정 태그 하나**를 재사용해 버전별 본문이 아예 없다). **대가** — 노트를 쓰지 않으면 배포가 막힌다(의도) · 1.0.2 이전 사용자는 자기 버전이 목록에 없다 · 번들이 릴리스마다 단조 증가(상한 없음, 지금은 대응 안 함) · 옛 앱에서 `notes` 가 조용히 무시됨 · 항목 표식은 사람이 붙여 틀리면 틀린 채 나간다. **폐기 없음**. 구현하며 정한 것 셋 — `.mjs` 가 `.ts` 를 읽는 방법은 **Node 내장 타입 스트리핑**(22.18+/23.6+ 부터 플래그 없이, 이 저장소는 24.x 실측)으로 `src/data/release-notes.ts` 를 **static import**, 새 의존성 0개·정규식 파싱 없음(그 방식은 원천 형식이 바뀌면 조용히 틀린 값을 낸다)·노트 문구 grep 결과 원천 1곳 · 매니페스트에 실리는 **문자열 형식을 `formatReleaseNotes` 가 고정**(항목당 한 줄 `• text`, `requiresStoreUpdate` 는 줄 끝 `(스토어 업데이트 필요)`, 구분 `\n` — 화면은 같은 표식을 배지로 그리고 매니페스트는 괄호 꼬리로 싣는다, 꼬리가 사라지면 모달이 그 사실을 잃는다) · 매니페스트의 `notes` 는 **쓰는 쪽에서 조건부 전개가 아니라 평문 필드**(가드를 통과한 시점에 반드시 있어 조건부면 죽은 분기다 — 읽는 쪽의 선택 필드성은 옛 매니페스트를 읽는 기존 설치본 사정이지 스크립트가 비워 보낼 수 있어서가 아니다. 필수로 만들면 **8건 실패, 전부 "노트 없는 매니페스트" 한 가족**이라는 것이 판별력 확인으로 실측됐다). (**구현 완료 · 실배포 미검증**, 2026-08-09, 이슈 #161 — 테스트 **2,756개/181파일** 통과·build 성공·lint **0 errors/17 warnings**(baseline 동일)·`node --check` 통과, 결정 8개 전부 구현. **지금 스크립트를 돌리면 결정 6 가드에 걸려 중단되는 것이 정상**(`package.json` 1.0.2 vs 노트 1.0.3) — 다음 릴리스에서 **1.0.3 으로 올려야 맞물린다**. **미검증 3**: 노트 한 벌이 두 소비자에게 정말 충분한가(**모달 쪽은 아직 안 그려졌다** — #164) · 노트를 실은 배포를 **한 번도 해 보지 않았다**(가드가 발동하는 것만 확인) · `notes` 를 실은 매니페스트를 옛 설치본이 무사히 읽는가(테스트가 덮은 것은 **반대 방향** — 새 앱이 옛 매니페스트를 읽는 쪽이다)) — **후속 [[ADR-126]](2026-08-11)**: 범위 밖으로 남겨 둔 "모달에 그리는 형태"가 정해지면서 매니페스트가 싣는 것이 `notes`(항목 전체 평문) → **`highlights`(핵심 목록 3~4줄)** 로 바뀌었다(문장이 갈려야 했다 — 결정 1 의 "원천 한 벌"은 그대로고 필드만 늘었다). 결정 3·9 는 개발 노트 화면에서 그대로 산다 |
| [ADR-120](./adr/ADR-120.md) | **하위 페이지는 스택으로 연다** — 탭 화면 위에 열리는 하위 페이지가 일곱인데(관리 2 · 히스토리 · 설정 하위 3 · 처방침) **앱 전체에 화면 단위 전환이 한 곳도 없어** 구조가 이미 스택인 히스토리조차 스택으로 느껴지지 않는다. 일곱 전부를 규약으로 묶고 공용 오버레이 셸 `StackScreen` 하나로 처리한다 — 중첩 라우트 · 푸시/팝(340ms, 아래 화면 −30% + 스크림 0.12) · 가장자리 28px 스와이프 백(거리 35% 또는 속도 0.4px/ms). **오버레이는 포털로 탭 레이어 밖에 그린다** — 아래를 밀려면 래퍼에 `transform` 이 필요한데 그것이 containing block 을 만들어 중첩 자식인 오버레이까지 함께 밀기 때문. React 트리 위치는 중첩 그대로라 [[ADR-077]] 언마운트 금지·[[ADR-092]] 중첩 Suspense 는 그대로 성립한다. **휴지 시엔 `transform` 속성을 아예 걸지 않는다**([[ADR-073]] 결정 3 과 같은 처방 — 중첩 sticky 의 기준을 바꾸므로), `will-change` 도 미리 걸지 않는다([[ADR-079]] 실기기 반증). 탭바는 탭 레이어 **안**으로 들어가 아래 화면과 함께 밀려 나간다(iOS `hidesBottomBarWhenPushed`) — [[ADR-077]] 결정 4 를 뒤집는다. 뒤로는 `navigate(-1)` 진짜 pop(딥링크 진입만 부모로 `replace`), 오버레이 중엔 아래 화면 PTR 을 끈다([[ADR-072]] 결정 14 를 구조로 대체). 개인정보 처리방침은 `/settings/privacy` 신설 + 사이트 `iframe` — 사본을 만들지 않아 [[ADR-118]] 결정 7 의 단일 원본 원칙이 유지되고, 대가는 오프라인에서 빈 화면(안내 + 브라우저로 열기 폴백). **수치는 실기기 프레임 확인 뒤 확정**([[ADR-079]]·[[ADR-085]]·[[ADR-098]] 이력). 처방침은 `/settings/about` 의 행에서 열려 `/settings/about/privacy` — 이 앱 유일의 2단 스택이고, 그래서 스토어는 `depth` + 최상단 `progress` 로 층을 다룬다. `useScreenNavigate` 는 모듈째 삭제([[ADR-098]] 결정 1 폐기 — 모든 탭 화면이 자기 스크롤을 소유하게 되어 `window.scrollTo(0,0)` 이 무효 호출) **청크는 라우트가 아니라 탭 단위로 묶는다**(결정 14, `vite.config.ts` `manualChunks`) — 하위 페이지를 따로 쪼개 두면 밀어 넣을 때 파일을 한 번 더 읽어 실기기에서 전환이 늦게 시작됐다. 진입 청크 356→221 kB · 총 JS 1,440→1,324 kB · 청크 59→15개로 세 축 모두 개선 (**구현 완료** 2026-08-09, 실기기 1차 확인 완료) |
| [ADR-121](./adr/ADR-121.md) | **보스 카드를 탭해 파티 인원·난이도를 그 자리에서 고친다** — 스케줄러에서 "이 보스 3인이었지"를 알아채도 고치려면 헤더 → 보스 관리 → 해당 행까지 가야 했다(이슈 #156). 카드 탭은 [[ADR-019]] 결정 4가 **"카드 인터랙션 충돌 우려"** 로 폐기했던 방식인데, 그 전제는 [[ADR-035]] 결정 18이 편집을 `/boss/manage` 로 옮기며 **카드의 인터랙션을 0개로 만들면서 이미 사라졌다** — 뒤집는 것은 결론이 아니라 전제가 바뀐 사실이다. 결정 — ① 카드 전면(80px)이 버튼, **어포던스 표식 없이** 눌림 피드백만([[ADR-018]] 카드 규격 무변경과 맞바꿈) ② 모달이 **난이도 + 파티 인원**을 함께 다룬다(저장 경로·스키마 무변경) ③ **자동 모드에서도 난이도 세그먼트를 그린다** — 단 멤버십이 아니라 "어느 난이도의 파티 인원을 편집할지" 스위치다(모드 통합 대비) ④ 미선택 칩을 **풀컬러 + `opacity-40`** 으로 되돌린다(2026-07-24 고스트 칩 재정정) — 배지 안 글자 대비가 1.43~3.15로 무너지지만 **"메이플 유저는 난이도를 색과 실루엣으로 안다"** 는 사용자 판단이고, 되돌아가는 결정이라 신규 값이 0개다(대안 3종의 6테마 대비 실측은 ADR 본문 표) ⑤ **`mergeManualBossList` 에 완료 승격을 적용** — [[ADR-031]]·[[ADR-032]] 승격이 `isRegistered` 게이트 + 수동 경로의 (보스,난이도) 정확 일치 때문에 안 걸려, 난이도를 바꾸면 완료 배지가 사라지던 **누락 보완**(`ownComplete` 는 승격 안 함 — [[ADR-033]]) ⑥ 난이도 교체를 **스토어 단일 액션 `setManualBossDifficulty(ocid, boss, to)`** 로 — 지금은 `remove` → `add` 가 **커밋 2회**라 첫 커밋 직후 "보스가 목록에 없는" 상태가 저장소에 실재하고, 거기서 실패하면 보스가 사라진다. Preferences 는 키 하나에 배열을 통째 쓰므로 **필요한 건 트랜잭션이 아니라 커밋을 1회로 줄이는 것**이고, 인자에서 `from` 을 빼 낡은 값으로 인한 무음 유실도 없앤다 ⑦ 모달 규격 — `Modal.Panel maxWidth="max-w-2xs"`(288, **4난이도 칩이 안 접히는 하한**이자 전 기기 동일 폭) + 일러스트 히어로(경계선은 `media-scope` **바깥** — 검은마법사는 `media-surface === surface`) + 전폭 스테퍼 h-10(스테퍼에 `size` 변형 신설) (**구현 완료 · 실기기 검증 완료**, 2026-08-10) |
| [ADR-122](./adr/ADR-122.md) | **스크림 위 패널의 테두리는 모드마다 역할이 반대다** — [[ADR-121]] 실기기 검증에서 "다크는 괜찮은데 라이트 모달 테두리가 도드라진다"는 보고가 나왔다(2026-08-10, iPhone 17). 실측하면 역할이 뒤집힌다 — 라이트는 배경 vs 표면 대비 **18.8~19.7** 이라 가장자리가 이미 압도적이고 테두리는 **링**으로만 읽히는 반면, 다크는 **1.07~1.18** 이라 **테두리가 유일한 경계**다. 결정 — ① 테마 모드를 **`data-mode` 로 CSS 에 노출**한다(`color-scheme` 은 [[ADR-099]]로 이미 있지만 **선택자로 못 쓴다**; `data-theme` 이름으로 분기하면 [[ADR-064]] 결정 8이 폐기한 `DARK_THEMES` 수동 목록이 CSS 쪽에 되살아난다) ② **라이트에서만** 테두리를 **텍스트 쪽으로** 밀어(`color-mix(border 40%, text)`) **배경 아래로 가라앉힌다** — 여기서 방향을 한 번 틀렸다: 라이트 테마의 모달 뒤는 검정이 아니라 **중간 회색**(스크림을 페이지 bg 위에 합성하면 머쉬맘 `#757269`)이라, 표면 쪽으로 민 첫 시도(`45%, surface`)는 배경에서 오히려 **더 멀어져**(색거리 143→196) **밝은 링**이 됐다. 텍스트 쪽으로 밀면 배경에 수렴하고(55% 에서 거리 15~24), 확정값 **40%** 는 거기서 한 단계 더 눌러 배경보다 어둡게 가라앉힌다(실기기 확인 후 사용자 요청). 교훈 — 처방은 *흐리게*가 아니라 **배경색에 맞추기**이고, 그 배경은 스크림 토큰이 아니라 **합성된 색**이다 ③ 클래스를 **둘로 가른다** — `.panel-on-scrim`(테두리를 가진 패널 자신 · `Modal.Card`·자체 오버레이)과 `.panel-on-scrim-parent`(직계 자식이 패널 · `Modal.Panel`). 합쳐서 `> *` 까지 잡으면 `Modal.Card` **안쪽** 요소(테마 타일 보더)까지 바뀌고, `Modal.Panel` 쪽을 자식 선택자로 푼 이유는 그 자식(`AccountFlowStatus`)이 **온보딩 화면과 공유**돼 거기선 스크림이 없기 때문이다 — "스크림 위"라는 사실은 [[ADR-094]] 결정 1대로 **오버레이가 소유한다**. 적용 범위는 모달 전부(9곳) (**구현 완료 · 실기기 확인 완료**, 2026-08-10) |
| [ADR-123](./adr/ADR-123.md) | **경계 페이드에서 `backdrop-filter` 를 뺀다** — 당겨서 새로고침을 하면 당겨진 자리에 흐릿한 띠가 잔상으로 남았다(사용자 보고 2026-08-10, iPhone 17). 이 버그는 **관측 도구를 빼앗은 채로** 왔다 — 볼륨/전원 버튼이 하나만 눌려도 띠가 먼저 사라져 스크린샷·화면녹화에 안 찍히고(캡처 행위가 곧 강제 리페인트다), 시뮬레이터와 Safari 탭에서도 재현되지 않는다(합성 경로가 다르다). 사용자 스크린샷 2장을 픽셀로 재니 밴드 구간 50행이 **정확히 `#f2f0e2` 하나·편차 0**이었고 40배 증폭해도 균일한 단색 면뿐이었다. 그래서 추론을 멈추고 **실기기의 실제 앱 안에 변형 스위치를 넣어 하나씩 뺐다**(A 현재/B 블러 제거/C 당길 때만 숨김/D 밴드 없음). 결과 — **B 정상, C 불충분**(스크롤이 없는 화면에서 가끔 샌다). 결정 — ① `PageHeader` 페이드에서 `backdrop-blur-sm` 제거: `backdrop-filter` 는 요소를 자기 합성 레이어로 승격시키고 그 레이어가 든 배경 스냅샷이 WKWebView 에서 갱신되지 않는 창이 생겨 **이미 없어진 내용의 흐릿한 상**을 계속 그린다. **사슬의 첫 고리를 끊는다** — C 처럼 상황을 막는 것이 아니라 레이어 승격 자체를 없애 스탈할 스냅샷이 생기지 않게 한다 ② **보스 수익의 카드 헤더 페이드도 함께**([[ADR-047]] 결정 6·후속이 *"레시피는 동일하고 배경색만 `from-surface`"* 로 정한 자리라 같은 잠재 결함을 갖는다 — 한쪽만 고치면 [[ADR-094]] 가 없앤 복붙 상태로 되돌아간다. 그 화면에서 증상이 보고된 적은 없다) ③ 그라데이션과 마스크는 유지 — 경계를 부드럽게 하는 일은 그라데이션이 이미 한다. 라이트에서만 뚜렷했던 것도 같은 원인으로 설명된다(카드가 거의 검고 배경은 크림색이라 빈 자리에 얹힌 검은 잔상의 명암차가 크다 · 다크는 둘 다 어두워 묻힌다). 기각 — 당길 때만 숨김(실측으로 불충분) · `will-change`/`translateZ(0)` 로 갱신 강제(완화책이고 [[ADR-079]] 가 실기기에서 반증한 계열) · 더미 애니메이션으로 매 프레임 무효화 · iOS 만 분기(두 벌 유지 값어치 없음). **대가** — 스크롤 중 경계가 살짝 또렷해진다 · 보스 수익은 증상이 보고되지 않은 채 모습이 바뀐다. **폐기** — `design-system.md` 의 *"색과 블러를 동시에 옅어지게 해야 자연스럽다"*(그 문장이 겨눈 부자연스러운 경계는 블러가 없으면 생기지 않는다). **미해결** — 밴드는 여전히 무조건 그려진다(스크롤 최상단·스크롤 없는 화면·당기는 동안 모두 가릴 대상이 없는데 존재하고, 배경 이미지 테마에선 헤더의 그림 아래에 `from-bg` 단색 이음매가 생긴다 — 잔상과 별개 문제) · WKWebView 가 백드롭 스냅샷을 언제 갱신하는지는 밝히지 않았다(레이어를 만들지 않으면 질문이 사라진다). 진단 경위·헛다리 넷은 [trouble/2026-08-10-ios-fade-band-ghost.md](./trouble/2026-08-10-ios-fade-band-ghost.md) (**구현 완료 · 실기기 A/B 검증 완료**, 2026-08-10, 이슈 #191) |
| [ADR-124](./adr/ADR-124.md) | **드롭 가격은 기록 한 건에 붙는 실판매가다** — 이슈 #185. [[ADR-038]] 이 *"금액은 저장하지 않고 재평가 가능한 구조만"* 으로 두고 갱신 소스를 미정으로 남긴 자리를 **사용자 직접 입력**으로 확정한다(시세는 거래소 변동값이라 [[ADR-006]] 대상이면서 고정 참조표에도 담기지 않는다 — 사용자 입력이 유일하게 정합한 소스다). 결정 — ① 가격 단위는 **기록 한 건**(아이템 시세표 아님): 같은 아이템도 캐릭터마다 판 값이 다르고 그 차이가 곧 캐릭터별 수익의 차이다. 스냅샷이라 **과거가 흔들리지 않는다**([[ADR-032]] 파티원 수와 같은 성질) ② **분배 인원은 드롭마다 따로**, 기본값만 그 행의 파티원 수 — 저장하면 독립하므로 `party_size` 와 다른 수일 수 있고, 그래서 화면이 둘을 한 줄에 붙이지 않는다 ③ **고정 드롭은 대상 아님**(`boss_drop_records` 에 존재하지 않는다, [[ADR-040]] 결정 3) ④ 컬럼 셋(`price_state`·`price_meso`·`price_share`)을 **기존 테이블에 더한다** — 그러면 난이도 확정 이관([[ADR-069]] 결정 4)·prune 삭제·히스토리 단일 원천([[ADR-071]] 결정 1)이 전부 공짜로 따라온다. **상태를 금액 유무로 추론하지 않는다**(스킵과 미입력이 둘 다 "금액 없음") ⑤ 입력은 **앱 내장 키패드** — OS 키보드를 부르지 않아 뷰포트가 줄지 않으니 간섭을 **다루는 대신 발생시키지 않는다**. 주 표기는 **자릿수 전체**(접으면 한 자 칠 때마다 단위가 갈아엎여 읽히지 않았다), 단위 칩 다섯은 10배씩 오르는 자릿수 눈금 ⑥ 드롭 시트는 `기록 → 확인 → (입력 →) 복귀` — 탭 즉시 기록은 그대로 두고([[ADR-040]]) 확인 줄이 옆에 서며, **상자 드릴다운과 같은 자리**라 값을 매긴 뒤 시트가 살아서 하던 작업을 잇는다. 상태는 타일 좌상단 동전 배지가 혼자 말한다 ⑦ 합산은 **보스 행 금액에 얹으면** 캐릭터·총액이 자동으로 따라온다. `payout_meso` 에는 쓰지 않고(가격 수정이 과거 기록 재기록을 부른다) 읽는 시점에 더한다 · 월간 소계·증감 칩 기준에도 **포함**(안 넣으면 같은 주가 탭마다 다른 숫자) · 결정석 한도 `n/90` 은 무영향([[ADR-054]]) ⑧ 가격 기록 화면은 **`/profit/prices`** 하위 스택([[ADR-120]]), 보스 수익에서 **보던 주를 이어받아** 연다 ⑨ **"물욕템"을 쓰지 않는다** — 전부 "아이템"(UI 문구엔 원래 없었고 주석·문서뿐), 단 "고가"는 별개 개념이라 남긴다. 표시 자리는 네 번 갈렸다(보스 행 인라인 → 아코디언 하단 → 중첩 접기 → **금액 아래 칩 + 화면 위 팝오버**) — 앞 셋은 스테퍼 영역 침범·목록 끝이라 멀다·아코디언 중첩 금지로 반려됐고, 채택안은 열 폭을 금액이 정해 **가로가 종전과 같고** 높이만 약 18px 는다. **값이 없는 행은 DOM 이 한 글자도 안 바뀐다**([[ADR-094]] 결정 4 스냅샷이 고정 — 작업 중 두 번 잡아냈다). 팝오버는 카드 셸 `overflow-clip`([[ADR-049]])을 피해 **포털 + `fixed`**, 가로 위치는 `anchorPopover` 재사용(컨테이너만 뷰포트). 시안은 손그림 목업 2차 반려 후 `/debug/price-185`([[ADR-092]] build-use-delete)에 **제품 컴포넌트 그대로** 띄워 조정했다 (**구현 완료 · iOS 실기기 UI 확인**, 2026-08-10, 이슈 #185 · PR #195 — 마이그레이션은 기존 기록이 있는 DB 하나에서만 확인) |
| [ADR-125](./adr/ADR-125.md) | **기능 사용법 안내는 기능 카탈로그 한 벌이고, 개발 노트는 거기로 링크만 건다** — 노트는 *"바뀐 것이 있다"* 까지만 말하고 그것이 화면 어디에 있고 어떻게 쓰는지는 말하지 않는다(새 화면일수록 간극이 크고, 이 앱엔 사용법을 설명하는 자리가 한 군데도 없다). **축은 기능**이다(결정 1 은 같은 날 뒤집혔다 — 처음엔 버전 축이었고 *"카탈로그는 별개 축을 하나 더 유지해야 하고 두 축은 반드시 어긋난다"* 가 근거였는데, 사용자 지시로 **원천을 카탈로그 하나로 못박고 노트는 링크만** 걸게 되자 그 근거가 성립하지 않는다 — 어긋나는 것은 축이 둘일 때가 아니라 **본문이 둘**일 때다. 그리고 버전 축만으로는 *"지금 이 앱을 어떻게 쓰나"* 에 답할 자리가 없다는 것이 첫 결정이 스스로 적어 둔 대가였다). 그룹은 `컨텐츠·보스·수익·유틸리티·설정`(사용자 지정)이고 빈 그룹은 탭째·그룹 하나면 탭 줄째 감춘다(지금 `유틸리티` 가 비었다). **「캐릭터 관리」는 두 그룹에 선다** — 컨텐츠·보스가 같은 피커를 쓰므로 `groups` 배열이고 사본이 아니라 같은 글 한 벌이다(반대로 「인게임 데이터 연동」은 컨텐츠 편·보스 편이 다른 글). 딸림 규칙 변경 — 양방향이던 참조 무결성에서 **"고아 안내 금지"를 폐기**한다(카탈로그가 원천이므로 노트가 안 가리키는 안내가 정상이다). **가이드 본문은 `release-notes.ts` 밖**에 산다(이슈 최초안 정정) — 취향이 아니라 `scripts/publish-live-update.mjs:33` 이 그 파일을 **Node 에서 직접 import** 하기 때문이다([[ADR-119]] 결정 1), 이미지 `.webp` import 를 넣는 순간 배포가 죽는다. 항목엔 `guideId?: string` 만 남기고 본문은 `release-note-guides.ts` 로 끊되 갈라짐은 **양방향 참조 무결성 테스트**가 막는다. 부수 효과 둘 다 이득 — 매니페스트 `notes` 가 안 커지고(모달은 번들 받기 전이라 이미지를 못 그린다), 목록 화면이 `guideId` 문자열만 보므로 이미지 모듈을 아예 안 건드린다. **같은 상세가 두 부모 아래 각각 라우팅된다**(`/settings/guide/:guideId`·`/settings/release-notes/:guideId`, 결정 3 도 같은 날 정정) — 한쪽으로 몰 수 없는 이유는 `resolveStackDirection` 이 push/pop 을 **경로 접두 관계**로 판정해서 형제로 건너뛰면 `replace` 로 떨어져 **밀려 들어오는 전환이 사라지기** 때문이다. 화면·데이터는 한 벌이고 경로만 둘이라 상세는 부모를 `resolveParentPath(pathname)` 로 깎아 쓴다(어디서 왔든 그리로 돌아가야 한다). 설정은 **2카드 6행**이 되고 `기능 설명` 이 `개발 노트` 위다. 없는 id 는 **들어온 쪽** 목록으로 `replace`. **결정 7 — 노트는 안내가 아니라 그 안의 마디를 가리킨다**: 안내가 기능 한 벌 전체를 설명하게 되면서 릴리스가 바꾼 것(보통 그중 한 마디)과 단위가 어긋났다, 그래서 안내에 `sections` 를 두고 화면 위에 목차를 세우며 노트 항목은 `guideSectionId` 로 그 마디에 떨어진다. **마디는 경로가 아니라 쿼리(`?s=`)** 인 것이 요점 — 세그먼트면 `resolveStackDirection` 이 스택 한 단으로 읽어 목차를 누를 때마다 화면이 밀려 들어온다. 스크롤은 즉시(전환과 겹치면 둘 다 어그러진다)·1회만(되끌림 방지), 없는 마디는 조용히 첫머리에 서므로 데이터 테스트가 실재를 강제하고, 참조 중복 금지도 **(안내, 마디) 쌍** 단위로 완화된다. **결정 8 — 안내 하나가 파일 하나다**(`src/data/feature-guides/` 아래 그룹별 폴더 + 두 그룹에 서는 안내는 `shared/`, 이미지는 `src/assets/guide/<안내 id>/` 를 안내 파일이 직접 import): 한 파일에 열한 벌이 다 있으면 이미지 import 가 쌓이며 그 파일이 전부의 의존성이 된다(A 의 스크린샷을 바꿔도 B 를 읽는 경로가 무효화). `index.ts` 는 조립만 하고 공개 API·import 경로는 그대로이며, 쪼개며 생긴 새 실패(파일은 만들었는데 index 에 안 넣기)는 데이터 테스트가 `import.meta.glob` 으로 폴더를 훑어 막는다. 경로 조립은 `lib/guide-route.ts` 로 뺐다 — 목록이 상세 모듈에서 상수를 가져오면 안내 카탈로그 전체가 목록의 import 그래프에 딸려 와 결정 2 의 이점이 사라진다. **이미지는 앱 번들** — 원격은 오프라인에서 안내가 통째로 비어 기각(처방침이 오프라인을 감수한 것과 판단이 갈리는 이유는 그쪽은 *"어긋난 법적 문서"* 가 걸려서였다), `import.meta.glob` 대신 **명시적 import** 라 파일명이 틀리면 `undefined` 로 통과하는 대신 **빌드가 실패한다**([[ADR-119]] 결정 1 이 JSON 대신 `.ts` 를 고른 것과 같은 판단), OTA 는 dist 전체를 압축하므로 지연 로드가 용량을 깎아 주지 않아 상한을 정해 관리한다. **안내가 있는 항목만 눌린다**(버그 수정 한 줄에 붙일 사용법은 없다 — 나머지는 DOM 이 종전과 같고 비활성 버튼도 두지 않는다), 본문은 이미지만·문단만·둘 다를 허용하는 **블록 배열** + 대체 텍스트 필수 + 가이드 자기 제목(완료형 노트 문장은 화면 제목으로 안 읽힌다). **미결** — 안내를 붙일 항목·스크린샷 조달·과거 소급·용량 상한 (**결정 완료(결정 1·3 정정 · 결정 7·8 추가) · 구현 완료(본문 초안 11벌) · 검토·스크린샷 대기 · 실기기 미검증**, 2026-08-10, 이슈 #198, 마일스톤 v1.0.4) |
| [ADR-126](./adr/ADR-126.md) | **「자세히 보기」는 받기 전엔 핵심 몇 줄이고, 받은 뒤엔 개발 노트다** — 이슈 #164. [[ADR-119]] 가 매니페스트에 노트를 실어 놓고 *"모달에 그리는 형태"* 를 범위 밖으로 남긴 자리를 채운다(그래서 `notes` 는 발행되고도 **읽는 쪽이 없었다**). 갈리는 근거는 하나 — **모달이 뜨는 시점마다 앱이 가진 것이 다르다**: `update-available` 은 그 노트가 *아직 안 받은 번들 안*에 있어 원격이 유일한 출처이고, 적용·재시작 뒤에는 지금 도는 번들이 곧 그 번들이라 개발 노트 화면이 이미 전부 갖고 있다. 결정 — ① **받기 전엔 모달 안에서 펼치고**(아코디언 — 화면을 옮기면 모달을 닫아야 하고 돌아왔을 때 다시 띄우는 처리가 필요한데, 정작 그 화면엔 새 버전이 **없다**) **받은 뒤엔 `/settings/release-notes` 로 보낸다**(흐름이 끝난 자리라 끊을 것이 없고 전부를 보여줄 수 있다) ② 매니페스트가 싣는 것은 전체 노트가 아니라 **핵심 목록 `highlights: string[]`** — [[ADR-119]] 구현이 정한 `notes` 평문 형식(`[카테고리] …` 을 `\n` 으로 이은 한 덩어리)을 **대체한다**(1.0.3 은 10줄·1.0.4 는 8줄이라 `max-w-xs` 모달에 펼치면 읽지 않고 닫는다 — 이 자리는 *"받을까 말까"* 의 결정 재료지 읽을거리가 아니다). **둘 다 싣지 않는다**: 같은 릴리스를 한 파일이 두 번 설명하면 [[ADR-119]] 결정 1 이 없앤 이중관리가 산출물 쪽에서 되살아난다 ③ 핵심 목록은 **파생하지 않고 손으로 쓴다** — "핵심"은 데이터가 아니라 판단이고(1.0.4 의 첫 변경은 `improvement` 로 적힌 보스 카드 인원 수정이다), `일부 버그 및 사용성 개선` 같은 **여러 항목을 뭉친 줄**은 어떤 파생 규칙으로도 안 나온다 ④ 완료 안내는 **「마지막으로 실행된 번들 버전」** 하나로 판정한다(적용 성공 경로엔 상태 전환 코드가 없다 — [[ADR-117]] 결정 1). 저장값이 없으면 **안 띄우고**(근거 없이 "업데이트했다"고 말하지 않는다) 판정은 `isNewerVersion` 이라 **자동 롤백이 걸러진다**. `sessionStorage` 를 안 쓴 이유는 번들이 통째로 갈리는 리로드를 넘는지가 플랫폼 구현에 달려서고, 스토어 업데이트도 같은 신호로 함께 잡힌다. [[ADR-119]] 결정 7(안 읽음 표시 금지)과 어긋나지 않는다 — **지속 상태가 아니라 1회 사건**이다 ⑤ 완료 안내는 **확인 결과가 "더 새로운 것 없음"일 때만**(`up-to-date`·`check-error`) 뜬다 — 회고와 행동 요구가 겹치면 행동 쪽이 이긴다. 밀린 안내는 다시 오지 않는다(큐를 만들면 지속 상태가 생긴다) ⑥ `highlights` 가 없으면(옛 매니페스트) 버튼을 **숨긴다**(비활성 아님 — 오류가 아니라 안 실려 온 것이다) ⑦ **`store-required`·`ready-to-apply` 엔 붙이지 않는다** — 받아만 두고 아직 안 도는 번들의 노트는 개발 노트 목록에 **없고**, 스토어 분기는 노트가 있어도 OTA 로 못 받아 판단이 안 바뀐다 ⑧ 배포 가드가 `highlights` 도 검사한다([[ADR-119]] 결정 6 확장, 빌드보다 앞·어느 쪽이 비었는지 말한다) — 선택 사항으로 두면 안 쓰게 되고 모달은 다시 용량만 말하는 자리로 돌아간다. **트레이드오프**: 받기 전 전수 목록을 볼 방법이 사라지고(의도), 릴리스마다 3줄을 손으로 더 쓰며, **완료 안내는 1.0.4→1.0.5 부터 실제로 보인다**(1.0.3 은 마지막 실행 버전을 기록하지 않는다) (**구현 완료 · 실배포 미검증**, 2026-08-11, 이슈 #164, 마일스톤 v1.0.4) |
| [ADR-128](./adr/ADR-128.md) | **React Native 로 옮긴다: 뷰만 다시 쓰고, 로직과 데이터는 있던 자리에 둔다** — [[ADR-001]] 대체. 동인은 **광고 인벤토리**다(웹뷰가 하나의 거대한 네이티브 뷰라 DOM 카드 *사이*에 광고 뷰를 넣을 자리가 원리적으로 없고, 그래서 전면광고를 걷어내고 인라인으로 가는 **선택지 자체가 없다** — 수익 최적화가 아니라 제품 방향의 문제). 망설임의 근거였던 *"RN 가면 OTA를 잃는다"* 는 **사실이 아니다** — 예외 조항(Apple APLA §3.3(b) · Play Device and Network Abuse)의 조건은 **런타임 종류가 아니라 인터프리터 여부**이고 Hermes 번들이 그대로 해당한다(웹뷰는 그 예외를 쓰는 한 가지 방법이었을 뿐). 결정 — ① RN 전환, `expo-updates` 로 OTA 유지 ② **Flutter 기각** — OTA는 Shorebird 단독·유료·iOS 인터프리터 강제고, 인라인 광고는 PlatformView 경유라 **화면 진입 전 임프레션이 집계돼 AdMob 밴 위험**(`native/ads.ts` 가 테스트 ID를 강제하는 바로 그 위험을 프레임워크가 만든다), 재사용 자산 **0%**(Dart) ③ **`packages/core` 공유 병렬 운영** — 현 앱은 패리티까지 계속 OTA로 배포하고 언제 중단해도 손해는 `app-rn` 뿐(core 분리는 RN을 안 가도 남는 이득) ④ **어댑터 시그니처 한 글자도 안 바꾼다** — 117/141 파일(83%)이 무수정으로 사는 유일한 조건이고(나머지 24 = 포트 역전 15 · DOM 수정 2 · 삭제 7), *"이왕 하는 김에"* 손대면 이식이 재작성이 된다 ⑤ **데이터는 마이그레이션하지 않는다** — Preferences·SQLite가 **프레임워크가 아니라 앱 번들 ID에 귀속된** OS 표준 저장소(`CapacitorStorage` SharedPreferences / `UserDefaults` `"CapacitorStorage."` 접두사 / `'no-encryption'` SQLite)라 같은 appId 면 그냥 읽힌다 → 단발 실행 코드가 사라져 최대 위험이 *"실패 시 복구 불가"* 에서 *"실패해도 원본 보존"* 으로 내려간다 ⑥ **이식 단위는 파일이 아니라 ADR 계약** — 113개 ADR이 203개 파일에 박혀 있고 대부분이 화면에 안 보이는 엣지 케이스(시계 되돌림 [[ADR-090]] · 월드 리프 [[ADR-069]] · 롤백 [[ADR-126]] · 미입력≠0원 [[ADR-124]])라 눈으로는 못 잡는다. `BossProfitScreen.tsx` 는 **ADR 32개**로 단독 계획 대상 ⑦ OTA 프로토콜 재설계(@capgo → expo-updates)는 **별도 ADR**. 트레이드오프 — 전환 릴리스는 이 저장소 최초의 **고칠 수 없는 릴리스**(새 빌드에 @capgo 가 없어 롤백 불가), **DOM 스냅샷 3종 상실**(가장 복잡한 화면 셋이자 *"예전과 같은가"* 에 기계적으로 답하던 유일한 장치 — 대체 장치를 3단계 전에 정할 것), 순수 웹 라이브러리 OTA 자유 상실, `@keyframes` **7종**(작성 시 8종으로 어림잡음) Reanimated 재구현, 풀타임 2.5~4개월. 상세: `migration/README.md`·`migration/parity-inventory.md`·`migration/data.md` (**단계 0~4 구현 완료 · 실기기 미검증**, 2026-08-13 — 화면을 다 옮겼다: 자리표시자 라우트 0 · jest 693 → **1,382개/109파일** · vitest 3,056개(4단계 step 2~8 증감 0) · lint 0 errors · `@keyframes` 화면 몫 **0**(이식 2 · degrade 1). 이 단계의 가장 값진 결과는 **구조가 계약을 대신 지킨 것** — [[ADR-077]]·[[ADR-085]]·[[ADR-098]]·[[ADR-099]]·[[ADR-100]]·[[ADR-112]]·[[ADR-120]] 이 실기기에서 두 번 반증당하며(079·084) 얻은 처방들이 RN 에서는 **처방이 아니라 전제**가 됐다(다만 정말 필요 없는지는 실기기에서만 확인된다). **남은 것 넷**: 결정 7 의 **OTA 프로토콜 미배선**(모달·섹션은 다 그렸고 벽은 core live-update 스토어가 `import.meta.env` 를 모듈 최상위에서 읽어 **import 만으로 죽는 것** — 전환 릴리스는 OTA 로 못 고치므로 그 다음 릴리스의 수단이 이것이라 5단계보다 앞선다) · [[ADR-047]] **중첩 sticky**(`stickyHeaderIndices` 는 카드를 펴야 해 [[ADR-045]] 링을 가르고 [[ADR-049]] 클리핑 상자를 없앤다, 손수 만드는 길은 **jest 가 한 줄도 검증 못 한다** — 육안 대조 1순위) · [[ADR-088]] **테마 배경 전면 백드롭**(반쪽만 만들면 그 결정이 없애려던 이음매를 오히려 만든다) · [[ADR-048]]·[[ADR-103]] **드롭 연출 재생 엔진**(프레임 에셋은 [[ADR-129]] 로 들어왔고 배율은 눈으로 확정할 값이다). **미검증**: 실기기에서 아무것도 안 봤다 — step 2~8 이 남긴 육안 대조 **67항목**이 5단계의 첫 작업이고, 자동 테스트가 담보하는 것은 분기와 숫자이지 픽셀이 아니다) |
| [ADR-129](./adr/ADR-129.md) | **에셋 목록을 빌드 타임 글롭에서 «커밋된 생성물»로 옮긴다** — core 는 275장의 목록을 `import.meta.glob` 으로 만들었는데 그것은 Vite 전용이라 Metro 에 짝이 없고, 모듈을 **평가하는 순간** 죽었다(`__ExpoImportMetaRegistry.glob is not a function`). 3단계는 앱 수준 치환표로 다섯을 막고 URL 을 전부 `null` 로 돌려 **깨지지는 않지만 그림이 없는** 상태로 두었는데, 4단계 화면들은 그 그림이 실제로 보여야 하는 자리다. 결정 — ① `npm run assets:gen`(`scripts/generate-asset-manifest.mjs`)이 디렉터리를 훑어 `core/src/assets/generated/*.ts` 를 쓰고 **웹·RN 이 한 벌을 함께 쓴다**. step 지시는 *"웹은 URL 문자열, RN 은 `require()` 결과"* 라며 값을 가르라고 했지만 **가를 필요가 없다는 것이 요점**이다 — 생성물 안에는 평범한 ESM 에셋 import 만 있고 그 한 줄이 Vite 에선 URL, Metro 에선 에셋 id 가 된다(**번들러가 이미 가른다**). 손으로 한 번 더 가르면 목록이 두 벌이 되어 한쪽만 갱신되는 사고가 열린다(테마 토큰·라우트 표에서 이미 거부한 형태) ② 갈리는 것은 값이 아니라 **타입 한 줄** — `types/image-asset.ts`(웹 `string`) ↔ `.native.ts`(RN `number`)이고 Metro·jest 는 원래 `.native.*` 를 먼저 보므로 tsc 만 `moduleSuffixes: [".native", ""]` 로 맞췄다(없으면 타입만 웹 것을 믿는다). **함수 시그니처는 그대로**([[ADR-128]] 결정 4 — 웹에서는 반환 타입까지 글자 그대로 같아 `app-capacitor` 에 보이는 변화가 0이다) ③ 생성물을 **커밋한다**(저장소 밖 빌드에서 생성 단계가 빠지면 조용히 빈 목록이 된다) — 대가인 «낡음»은 디렉터리를 직접 읽어 키 집합을 대조하는 테스트가 갚고, 훑는 규칙은 생성기와 **같은 표**(`assets/asset-groups.ts`)를 읽는다(사본을 두면 *"검사는 통과하는데 목록은 틀린"* 상태가 된다). 판별력은 파일을 실제로 넣어 확인 ④ 치환 표는 **비우되 지우지 않는다** — `import.meta.env` 벽이 남아 있어([[ADR-024]] 라이브 업데이트 스토어) OTA 를 이을 때 그 자리를 맡을 가능성이 크다. 「glob 모듈 여덟 개」 고정 테스트는 **「0이어야 한다」로 뒤집었고**(감시 대상이 사라진 게 아니라 *"없는 상태가 유지되는가"* 로 바뀐 것), `ImportMeta.glob` 타입 선언도 지워 이제 글롭을 쓰면 **tsc 가 먼저 막는다** ⑤ 여덟 번째 모듈(`data/feature-guides/index.ts`)은 **오탐이었다** — 글롭을 *"쓰지 않는 이유"* 를 적은 주석이 문자열 검사에 걸린 것. 다만 에셋을 나르는 것은 맞아 `FeatureGuideImage.src` 타입만 `ImageAssetRef` 로 바꿨고, `assets/guide/` 는 슬러그로 찾는 곳이 아니라 생성 목록에 넣지 않는다([[ADR-125]] 결정 4 유지). 기각 — 에셋 해석을 **포트로 뒤집기**(core 인터페이스가 늘어 [[ADR-128]] 결정 4 가 흔들리는데, 값 가르기는 이미 번들러가 한다) · **app-rn 전용 맵**(목록 두 벌) · **`require.context`**(Metro 엔 있어도 **jest 에 없다** — *"앱은 도는데 테스트만 죽는"* 형태) · **RN 도 문자열로 맞추기**(`resolveAssetSource().uri` — core 에 `react-native` import 가 들어오고 [[ADR-048]] 이 필요로 하는 프레임 고유 크기를 버린다). **대가** — `assets:gen` 을 잊으면 목록이 낡는다(테스트가 잡는 시점은 *"넣는 순간"* 이 아니라 CI) · RN 반환값을 `{ uri }` 로 감싸면 **조용히 안 뜬다**(같은 컴포넌트에서 얼굴=원격 URI, 엠블럼=번들 에셋으로 두 형태가 공존) · jest 값은 숫자가 아니라 `{ testUri }` 대역이다. **폐기 없음** — [[ADR-088]] 결정 3·[[ADR-093]] 결정 1·[[ADR-125]] 결정 4 전부 그대로 선다(바뀐 것은 목록을 **만드는** 방법이지 **묻는** 방법이 아니다). (**구현 완료 · 실기기 미검증**, 2026-08-12 — 웹 `dist` 에셋 126개가 **이름·해시까지 완전 동일**하고 JS 는 오히려 줄었다(`screen-profit` 438.4→433.3 kB), vitest 199파일/3046개 → **200파일/3056개**·jest 693 → **705개**·lint 0 errors(baseline 동일)·`expo export` 성공(임시 진입점으로 250장 전량 번들 확인)·글롭 사용처 **0건**. **미검증**: 실기기에서 그림이 실제로 뜨는지 · 월드 엠블럼 말고는 아직 아무것도 안 그린다(보스 초상·드롭 프레임·테마 배경은 값만 왔고 CSS 기하 → RN 변환이 화면 작업으로 남았다) · `assets:gen` 을 잊는 사고를 실제로 겪어 보지 않았다) |
| [ADR-130](./adr/ADR-130.md) | **당겨서 새로고침은 RN 의 `RefreshControl` 로 간다 — [[ADR-074]] 의 마크는 그 자리에서만 폐기한다** — [[ADR-128]] 3단계가 두 번 미뤄 둔 갈래를 첫 PTR 화면(컨텐츠 스케줄러)에서 닫는다. 갈래표에 없던 사실 하나가 판정을 결정했다 — **안드로이드에는 당김 거리 신호 자체가 없다**(iOS 는 `bounces` 로 `contentOffset.y` 가 음수가 되지만 안드로이드 `ScrollView` 는 콘텐츠를 움직이지 않고 글로우만 그려 `onScroll` 이 0 아래로 안 내려간다). 즉 커스텀을 고르면 `PanGestureHandler` 를 겹쳐 최상단 판정·목록 이동을 다시 만드는 **새 제스처 계층**이 필요하고, 그것은 옮기기가 아니라 처음부터 다시 만들기다([[ADR-073]] 「남은 검증」의 *60fps* 질문도 그 길에서만 우리 문제로 남는다). 결정 — ① `ScreenScroll` 에 `refreshControl` 프롭을 더해 세 화면이 웹과 **같은 두 값**(`refreshing = status === 'loading'` · `onRefresh = refresh(trackedOcids ?? [])`)을 넘긴다 → [[ADR-072]] 결정 2·10 은 글자 그대로 유지 ② [[ADR-074]] 마크 결정 **넷만 폐기**(2 외곽선 링 · 3 진행률 드로잉 · 4 두 구간 연속성 · 6 28px — 두 플랫폼 다 인디케이터에 커스텀 뷰를 못 넣는다), **결정 1(문구 없음)·7(`aria-hidden`)은 살아남는다**(플랫폼이 같은 답을 낸다). [[ADR-061]] 결정 1 의 **PTR 예외([[ADR-074]] 결정 5)도 함께 폐기** — 마크를 안 그리면 예외를 둘 대상이 없다(남는 예외는 [[ADR-113]] 결정 5 하나) ③ 잃는 것은 마크이지 브랜드가 아니다 — `tintColor`·`colors` 에 `primaryInk`, 안드로이드 `progressBackgroundColor` 에 `surface` 를 넘긴다 ④ [[ADR-072]] 결정 14·[[ADR-120]] 결정 10 은 **코드가 아니라 구조가 지킨다**(제스처를 스크롤 컨테이너가 소유 · 하위 페이지는 루트 스택에 덮여 올라와 아래 화면에 손가락이 안 닿는다 → `useScreenStackStore` 배선이 통째로 사라진다) ⑤ `PullToRefreshIndicator` 는 **지금 지우지 않는다** — 고아 확정은 step 5·7 이 같은 선택을 물려받은 뒤이고, 남겨 두면 이 결정을 뒤집는 비용이 「화면 배선 한 줄」로 유지된다. **대가** — 당김 인디케이터가 두 플랫폼에서 다르게 보인다([[ADR-120]] 결정 5 가 전환에서 *"두 플랫폼에 같은 그림"* 을 고른 것과 **반대 방향**이고, 갈리는 이유는 그쪽은 값을 고를 수 있었고 이쪽은 그릴 자리가 없다는 것) · 되돌리려면 안드로이드 제스처 계층이 선행 조건 · **실기기에서 한 번도 안 봤다**. 기각 — 커스텀 유지 · 플랫폼별 이원화 · 시스템 인디케이터 위에 마크 겹치기(마크가 둘 보인다) · 당김 제거 |
| [ADR-131](./adr/ADR-131.md) | **화면에 고정되는 영역을 두지 않는다 — 헤더까지 함께 스크롤한다** — [[ADR-128]] 4단계가 «판정 대기» 로 남긴 [[ADR-047]] 중첩 sticky 에 사용자가 판정을 내렸고(2026-08-13), 판정이 **두 번에 걸쳐 좁혀졌다**: ① *"sticky 를 더 짧게 — 최상단 헤더만 남기고 나머지 제거, 보스 수익 카드 sticky 포함"* → ② *"지금 페이지에 고정된 영역을 풀라는 거야. 스크롤 가능하도록"*. **최종은 «고정 영역 없음»**. 결정 — ① RN 의 `PageHeader` 는 스크롤 뷰의 **형제**여서 sticky 보다 강한 고정이었고(sticky 는 스크롤 범위 안에서만 붙지만 형제는 **영원히** 붙어 있다), 그것을 **첫 자식**으로 옮겨 함께 흐르게 한다 — 바뀌는 곳은 `ScreenScroll` 한 곳이고 화면 열하나가 전부 그 `header` 프롭을 지난다 ② [[ADR-047]] 과 딸린 셋(배지 레일·stuck 페이드·`stickyTop` 실측) RN 폐기 ③ `PageHeader` 경계 페이드 확정 폐기 — 헤더가 함께 스크롤되면 «콘텐츠가 지나가는 경계» 자체가 없다 ④ **하단 액션 바는 대상 아님**(사용자 판정). **적용 범위는 RN 만** — 웹은 교체될 앱이라 건드리지 않는다. **대가 둘**: 긴 카드에서 캐릭터 합계가 화면 밖으로 나가고([[ADR-047]] 후속 3 이 소계 footer 를 지운 근거가 sticky 였다) · **콘텐츠가 상태바 아래로 지나간다**(실기기 확인 — 고정을 없앤 직접적 결과라 버그가 아니라 대가다). 둘 다 **지금 되메우지 않는다** — 처방 중 하나(스크롤 위치에 따라 띠 띄우기)는 «고정 영역» 을 되살리는 것이라 이 결정과 정면으로 부딪힌다. 기각 — `Animated.ScrollView` 손수 고정·`stickyHeaderIndices`·웹까지 걷어내기. (**구현 완료 · 실기기 확인**, 2026-08-13. 가드 둘: `sticky-policy.test.ts` 가 sticky 유입을 막고, `ScreenScroll` 테스트가 헤더가 스크롤 뷰의 **자식**인지 본다 — 이 차이는 스크롤 0 에서 두 배치가 똑같이 생겨 화면으로는 확인할 수 없다) |
