# 앱 진입 (App Entry)

> **범위**: 앱을 켜면 어느 화면이 서는가. 그 판정과, 앱이 열리기 전 마지막 화면인 캐릭터 설정.
> **여기 없는 것**: 로그인은 [auth.md](./auth.md), 설정의 캐릭터 관리 하위 페이지는
> [settings.md](./settings.md), 후보 캐릭터 자격 판정은 [content-scheduler.md](./content-scheduler.md).
> **관련 문서**: [../foundation/nexon-api.md](../foundation/nexon-api.md) ·
> [../foundation/architecture.md](../foundation/architecture.md) ·
> [../trouble/2026-08-12-onboarding-empty-account-crash.md](../trouble/2026-08-12-onboarding-empty-account-crash.md)

## 목차

| 절 | 무엇을 정하나 |
|---|---|
| [진입 단계 셋](#진입-단계-셋) | 앱을 켜면 무엇이 서나 |
| [단계 파생](#단계-파생) | 끝내지 않은 설정이 어디서부터 이어지나 |
| [캐릭터 설정 화면](#캐릭터-설정-화면) | 두 층 구조, 로딩과 실패 |
| [시드](#시드) | 언제 거치고 무엇을 보여주나 |
| [`character/basic` 5분 TTL](#characterbasic-5분-ttl) | 네 호출부를 한 경로로 접는다 |
| [열린 질문](#열린-질문) · [폐기된 정책](#폐기된-정책-history) | |

## 관련 소스

| 구분 | 파일 | 하는 일 |
|---|---|---|
| 상태 | `features/app-entry/stage.ts` | `EntryStage` · `deriveEntryStage` |
| 상태 | `features/app-entry/store.ts` | `resolveFromStorage` · `resolveAfterSignIn` · `completeCharacterSetup` |
| 화면 | `app/character-setup/CharacterSetupScreen.tsx` | 캐릭터 고르기. 본문은 공용 컴포넌트다 |
| 셸 | `components/templates/EntryScroll/EntryScroll.tsx` | 진입 화면의 스크롤 셸. 하단 액션 바를 갖는다 |
| 배선 | `navigation/RootNavigator.tsx` | `EntryStage` 로 화면 목록을 가른다 |
| 배선 | `app/AppShell.tsx` | 부팅에서 단계를 파생한다 |
| 상태 | `features/schedule-sync/character-basic-fetch` | 네 호출부가 공유하는 `character/basic` 통과 지점 |
| 본문 | `components/organisms/CharacterManage/` | `CharacterManageBody` · `useCharacterManage`. 설정과 같은 본문 |
| 정렬 | `lib/character-order.ts` | `compareByName` · `pickRepresentativeCharacter` |
| 저장 | `storage/character-selection.ts` · `storage/character-basic-cache.ts` · `storage/scheduler-cache.ts` | |

**관련 ADR** (유효): [[ADR-006]] [[ADR-012]] [[ADR-015]] [[ADR-035]] [[ADR-053]] [[ADR-062]]
[[ADR-065]] [[ADR-086]] [[ADR-127]] [[ADR-143]] [[ADR-144]] [[ADR-149]] [[ADR-211]] [[ADR-215]]

**폐기됐지만 이 화면이 그 결정 일부를 아직 따르는 것**: 🟡 [[ADR-113]] · ⛔ ADR-016 · ADR-051.
**각 파일 배너의 🔗 줄에 적힌 것만 살아 있다.**

## 진입 단계 셋

[[ADR-215]] 결정 2·3 이다.

```
signIn → characterSetup → ready
```

`EntryStage` 하나가 `RootNavigator` 의 화면 목록을 가른다. `ready` 만 탭과 하위 페이지 전체를 열고,
앞의 둘은 각각 **화면 하나**다. 로그인과 캐릭터 설정을 스택에 나란히 등록하지 않는데, 그러면 둘
사이에 뒤로 가기가 생기고 되돌아갈 곳이 없기 때문이다.

**로그인 여부와 축이 다르다.** 그쪽은 `features/auth` 의 `AuthStatus` 가 들고([auth.md](./auth.md)),
이 축은 그 위에 캐릭터를 골랐는가를 더해 화면을 정한다. 한 열거형에 합치면 로그인 방식이 바뀔 때
캐릭터 설정 단계까지 함께 흔들린다.

**앱이 묻는 것은 둘뿐이다**([[ADR-143]]·[[ADR-144]]). 키와 캐릭터다. 스케줄 관리 방법은 설정이
갖는다([settings.md](./settings.md)). 앱을 처음 켠 자리에서는 그 선택의 판단 재료가 아직 없고,
**동작 기본값이 자동**이라 안 물어도 앱이 성립한다([[ADR-035]] 결정 2 의 `?? 'auto'` 폴백).

**메이플 ID를 고르는 단계가 없다**([[ADR-143]] 결정 1). 캐릭터 설정 화면이 계정 드롭다운을 갖고
**여러 계정의 캐릭터를 한 목록으로** 고른다([[ADR-144]]).

그래서 웹뷰 시절의 세 단계가 통째로 없어졌고, 그 자리는 이렇게 대체됐다.

| 웹뷰 시절의 단계 | 지금 |
|---|---|
| 계정 선택(`AccountSelectionList`) | **없다.** 캐릭터 설정 화면의 드롭다운이 대신한다 |
| 예열(⛔ ADR-016. 계정 전체 캐릭터 선행 조회) | **드롭다운으로 그 계정을 열 때** 그 계정 몫만 판정한다([[ADR-143]] 결정 5) |
| 계정 선택 프로브(`use-account-probes`) | 같은 이유로 없다. 판정은 로스터 조회가 한다 |

**코드에서도 전부 없앴다.** `use-account-probes.ts` 파일과 `accountScope` 플래그가 삭제됐고,
옛 `OnboardingStatus` 의 `selectingAccount`·`prefetching` 은 물론 그 열거형 자체가 없다.

## 단계 파생

[[ADR-086]] 결정 1·2(2026-08-03)다. 어느 단계에서 앱을 껐든 **다음 실행에서 그 단계부터 이어간다.**
전에는 저장된 계정 id 하나만 보고 곧바로 완료로 전이해, 모드와 캐릭터를 고르지 않은 채 빈 메인으로
떨어졌다.

**재개 지점은 저장된 값에서 파생한다. 진행 상태 전용 키를 두지 않는다.** 각 단계의 산출물이 이미
영속화돼 있어서, 진행 상태를 따로 쓰면 진실이 둘이 되고 한쪽만 써진 채 앱이 죽는 순간 어긋난다.
고치려는 결함과 같은 종류의 결함이다.

| 저장 상태 | 재개 단계 |
|---|---|
| `apiKey` 없음 | `signIn` |
| `trackedCharacters` 가 `null` 또는 `[]` | `characterSetup` |
| 그 외 | `ready` |

**이 표는 두 경로에 함께 적용된다. 부팅과 키 재입력 성공이다**([[ADR-115]] 결정 4, 2026-08-08). 파생
판정은 **함수 하나**(`features/app-entry/stage.ts` 의 `deriveEntryStage`)이고 두 경로가 그것을 공유한다. 두 벌이 되면 재개
규칙의 진실이 둘이 되는데, 그것이 [[ADR-086]] 결정 1이 진행 상태 전용 키를 거부한 바로 그 이유다.

- **캐릭터 선택은 네트워크 없이 재개된다.** `getCharacterPickerRoster` 가 자체 조회한다.
- **`trackedCharacters === []` 는 완료가 아니다.** 최소 1명 규칙이 있으므로 빈 배열은 미완료와
  같다. 저장 레이어의 `null` 과 `[]` 구분([[ADR-012]])은 그대로 두고, 진입 게이트에서만 둘을 같게
  읽는다.
- **`trackingMode` 는 읽지도 쓰지도 않는다.** 읽는 곳 둘(`features/tracking-mode/store` ·
  `features/boss-profit/store`)이 `?? 'auto'` 로 흡수하므로 이 게이트가 그 값을 볼 이유가 없다.
  앱이 `'auto'` 를 써 넣지도 않는다. 그러면 **사용자가 고른 값과 한 번도 안 고른 값이
  저장소에서 같아져**, 설정 화면이 「자동」을 그리는 근거가 폴백에서 기록으로 바뀐다.


## 캐릭터 설정 화면

`CharacterSetupScreen` 이다([[ADR-035]] 결정 13). **본문은 이 파일에 없다.** 설정 하위 화면
(`SettingsCharactersScreen`)과 **같은** `CharacterManageBody` 와 `useCharacterManage` 를 쓰고, 갈리는
것은 머리와 CTA 뿐이다([[ADR-144]] 결정 1). 이 파일에 있는 것은 제목 블록 · ‘계속하기’ · 최소 1개
게이트 셋이다.

### 두 층이다

[[ADR-144]] 결정 2(구현 완료 2026-08-17)다.

| 층 | 내용 | 좌우 슬롯 |
|---|---|---|
| 위 | 계정을 넘는 ‘선택된 캐릭터’ 목록. 순서와 대표가 여기 산다 | 끌기 핸들과 `★ ✕` |
| 아래 | 계정 드롭다운 + 그 계정의 후보 목록 | `＋` |

**두 층은 같은 행 카드를 쓴다.** 갈리는 것은 좌우 슬롯뿐이다.

아래 로딩·실패 규칙은 **아래 층에만** 적용된다. 계정을 바꾸면 아래만 다시 돌고, 위는 로컬 캐시로 그려
네트워크와 무관하다.

- **후보 0건일 때의 ‘계정 다시 선택’ 액션이 없다.** 드롭다운을 되돌리는 것이 그 자리의
  출구다([[ADR-143]] 결정 10).
- **고를 수 있는 계정이 하나도 없으면** 화면 전체가 빈 상태에 키 재입력이다.
- **같은 계정을 5분 안에 다시 열면 조회 자체를 하지 않는다**([[ADR-144]] 결정 6의 계정 전환 TTL).

### ‘계속하기’는 하단에 고정된다

[[ADR-144]] 정정 2(2026-08-18 사용자 지정)다. 설정 하위 화면의 ‘저장’과 **같은 액션 바**다(`bg-bg` +
위쪽 테두리 · 폭을 다 쓰는 버튼 · 안전영역은 바가 먹고, 잰 바 높이만큼 콘텐츠 아래를 비운다).

처음에는 이 단계만 ‘본문 끝에 서는 CTA’로 뒀는데, 본문이 설정과 같은 두 층이라 캐릭터가 많으면 유일한
전진 버튼이 화면 밖에 있게 된다.

그 바를 갖는 것은 화면 파일이 아니라 **진입 셸**(`EntryScroll`)이고, 그래서 `CharacterSetupScreen` 이 그
셸을 직접 두르는 유일한 단계다. 활성 조건이 `useCharacterManage` 안에 있어 스크롤과 바의 조상이 그
훅을 부르는 쪽이어야 한다.

### 후보 목록 로딩

`getCharacterPickerRoster` 를 캐릭터 관리 피커와 공유하므로 같은 규칙을 따른다. 원문은
[content-scheduler.md](./content-scheduler.md) 의 "후보 캐릭터 자격"에 있다. 요약하면 캐시가 있으면
즉시 표시하고, 확인된 캐릭터부터 흘리고([[ADR-149]]), 조회가 끝났는데 목록이 비면 "활성 캐릭터 없음"과
"조회 실패"를 구분해 안내한다.

판정 분기는 `CharacterSetupScreen` 안에서 직접 그린다(피커와 같은 우선순위와 마크업이다). 실패도
피커와 같은 공용 `ErrorState` 를 쓰고([[ADR-062]]) 스탈 배너 분기도 같다.

**다른 것은 401 뿐이다.** 이 화면의 `invalidApiKey` 는 **다시 시도** + "API 키를 다시
확인해주세요"이고, 피커는 **액션 없음** + "키 입력 화면으로 이동합니다"다([[ADR-115]] 결정 7,
2026-08-08).

갈리는 근거는 "설정 화면이 있는가"가 **아니다.** 이 화면의 401은 방금 넣은 키에 대한 **폼 자체의
실패**라 재시도가 실제 처방이고, 그래서 이 자리는 무효화 진입점에 **배선하지 않는다.** 피커의 401은
반대로 곧 화면을 키 입력으로 옮기므로 누를 것이 없다.

**스탈 배너는 두 자리가 같다**(401 포함 전부). `formatStaleRosterError` 가 `place` 를 받지 않는
이유다.

**근거가 구조에서 배선으로 내려왔다**([[ADR-116]] 결정 2, 2026-08-08). 전에는 *"무효화 경로가
앱이 열린 뒤에만 성립하므로 이 화면의 401은 애초에 그 경로가 아니다"* 였는데, 가드가 **"키 입력
화면에 있지 않을 때"** 로 넓어져 구조가 더는 막아 주지 않는다. 이 자리의 401을 폼 실패로 남기는 것은
이제 **배선하지 않는다는 선택**이다.

### 429로 이 화면이 잠기던 것이 모달 경로로 풀린다

[[ADR-116]] 결정 1·2(2026-08-08, 이슈 #176)다. 로스터 조회가 429로 실패하면 출구 다섯 방향이 전부
막혀 있었다.

1. `계속하기` 는 고를 캐릭터가 없어 영구 비활성
2. `다시 시도` 는 같은 요청을 다시 쏴 또 429
3. `계정 다시 선택` 은 **확정된 빈 상태 가지에만** 붙어 실패 화면에 도달하지 못함
4. 단계는 라우트가 아니라 상태 switch 라 되돌릴 UI 없음
5. 재시작하면 재개 파생이 **같은 단계로 되돌린다**

이 화면에서는 설정으로 갈 길이 없어 연결 해제조차 못 했다. 이제 그 위에 **닫을 수 없는 안내 모달**이 덮이고
확인을 누르면 키 입력 화면으로 가서 **서비스 단계 키로 다시 넣을 수 있다.**

- **‘계정 다시 선택’을 실패 가지에 넘기는 것은 답이 아니었다.** 계정을 바꿔도 **같은 키라 429는
  그대로**다. 실제로 통하는 처방은 키를 바꾸는 것 하나뿐이다.
- **429 발생 자체는 줄지 않는다**([[ADR-116]] 결정 5). 로스터와 예열의 fan-out 동시성 캡은 이번 범위
  밖이다(아래 [열린 질문](#열린-질문)).

### 최소 1명

[[ADR-086]] 결정 7이다. 0명은 화면을 빈 상태로 만들 뿐 어떤 의도도 표현하지 않는다. 피커도 같은
규칙이다.


### 캐릭터가 0명인 메이플 ID는 목록에 오지 않는다

[[ADR-127]](2026-08-12)이다. `normalizeCharacterList` 가 걸러낸다. `MapleAccount` 의 뜻이 *"응답에
있던 계정"* 이 아니라 **"고를 수 있는 계정"** 이고, 소비하는 자리가 전부 캐릭터가 있다는 전제로 쓴다.

이 불변식이 깨졌을 때 무슨 일이 나는지는
[../trouble/2026-08-12-onboarding-empty-account-crash.md](../trouble/2026-08-12-onboarding-empty-account-crash.md)
에 있다.


## 시드

캐릭터 선택을 마쳤을 때 **시드가 필요한 모드면** 거치는 단계다. 그 모드로 이 자리에 오는 길은
캐시 데이터(general) 삭제다.

```
설정에서 캐시 데이터(general)를 지운다
  → trackedCharacters 는 지워지고 trackingMode 는 보존된다(storage/cache-data.ts 의 KEEP_KEYS)
  → 다음 부팅에서 characterSetup 으로 들어온다. 이때 모드가 그대로 남아 있다
```

거기서 캐릭터를 고르면 시드가 필요하고, 끝날 때까지 스피너를 유지해야 한다. 분기를 지우면 그
사용자의 체크리스트가 빈 채로 완료된다.

**진행률 숫자가 없어 스피너만 쓴다.** `MapleSweepSpinner size={32}
text-primary` 와 "체크리스트를 준비하고 있어요"다. 화면 전체 대기라 셸 승계 카드를 씌우지 않는다
([[ADR-061]] 결정 2).


## `character/basic` 5분 TTL

[[ADR-113]] 결정 1이다. `character/basic` 호출을 **공유 경로 하나**
(`features/schedule-sync/character-basic-fetch` 의
`fetchCharacterBasicCached(apiKey, accountId, ocid, now)`)로 모으고, "이 ocid의 `basic` 이 5분 안에
캐시됐으면 네트워크를 건너뛰고 캐시 값을 돌려준다"를 그 자리에서 판정한다. TTL 값은 같은 모듈의
`CHARACTER_BASIC_TTL_MS`(`5 * 60 * 1000`) 하나뿐이다.

- **호출부가 전부 이 경로를 통과한다.** 캐릭터 피커 · 동기화 편승 갱신([[ADR-097]] 결정 7) 등이다.
  전부 **완전히 같은 요청**이고 소비하는 필드만 달라, 첫 설정 한 바퀴에 같은 캐릭터로 여러 번이
  나가고 있었다. 원형 `fetchCharacterBasic`(`nexon/character`)과 캐시 쓰기를 부르는 제품 코드는 이제
  이 모듈뿐이라, 새 호출부가 원형을 직접 부르면 눈에 띈다.
- **`now` 는 호출부가 넘긴다.** 헬퍼가 시계를 스스로 읽지 않는다. 라운드 하나가 같은 기준 시각을
  공유해야 같은 한 바퀴 안에서 판정이 흔들리지 않고, 오차는 항상 TTL이 **짧아지는** 보수적인
  방향으로만 난다. 파싱 불가와 미래 `cachedAt` 은 만료로 취급하고(손상된 값이나 기기 시계 되감기가
  캐시를 영구히 신선하게 만들면 안 된다) 경계는 배타적이다(정확히 5분이면 만료).
- **실패는 캐시로 폴백하지 않고 그대로 던진다.** 호출부들이 그 예외에 판정을 걸고 있다(400
  `OPENAPI00003` → `characterUnavailable`, 401·429 → 전역 실패). 여기서 삼키면 그 판정이 통째로
  죽는다.
- **호출부를 하나씩 고치지 않는 이유**: 그러면 호출자끼리 서로의 존재를 알아야 하고, 새 호출부가
  생기면 조용히 중복이 되살아난다(실제로 [[ADR-097]] 결정 7이 그렇게 추가됐다). 공유 경로 하나면
  **호출자끼리 서로를 몰라도 접힌다.**
- **5분인 이유**: ① 첫 설정 한 바퀴를 여유 있게 덮는다 ② **[[ADR-097]] 동기화 TTL 10분보다 짧아야
  한다.** 같거나 길면 편승 갱신이 경계에서 거의 항상 건너뛰어져 사실상 죽는다. 대가는 5분 안에 피커를
  다시 열면 개명과 레벨업이 그만큼 늦게 반영되는 것이다.
- **절감의 값은 호출 수가 아니다.** 자격 스윕은 실패를 조회 원장에 기록하지 않으므로(의도된 설계,
  [[ADR-086]] 결정 4) 429가 나면 다음 화면이 같은 13일을 처음부터 다시 훑고 그게 또 429를 부른다.
  중복을 접는 값은 **429 확률을 낮춰 원장이 채워질 확률을 올리는 것**이고, 원장이 한 번 차면 그
  캐릭터는 그 뒤로 스윕이 없다(이슈 #158).
- **접은 것은 총 호출 수이지 한순간의 fan-out 폭이 아니다.** 대상 전체를 `Promise.all` 로 **동시에**
  부르고 동시성 캡이 없다. single-flight도 없어 같은 ocid를 두 경로가 **동시에** 부르면 둘 다
  나간다. TTL은 이미 끝난 요청만 접는다. 429가 남으면 다음 후보는 여기다.


## 열린 질문

- **다계정 동시 추적을 아무도 해 본 적이 없다**([[ADR-143]] 미검증, 2026-08-16 신설). 계정이 2개
  이상인 `character/list` 응답은 [[ADR-007]] 시절 한 번 실측됐지만, 그 캐릭터들을 **함께** 추적한 적은
  없다. 특히 계정 공유 진행도 원장(`accountSharedProgress:{accountId}`, [[ADR-030]])이 캐릭터마다 자기
  계정으로 갈리는지는 실제 다계정 키로만 확인된다([[ADR-143]] 결정 6).
- **계정을 처음 열 때의 대기가 견딜 만한지**([[ADR-143]] 결정 5, 2026-08-16 신설). 예열을 ‘열 때’로
  미룬 대가라, 캐릭터가 많은 계정을 드롭다운으로 처음 열면 그 계정 몫의 자격 판정을 기다린다. 두
  번째부터는 조회 원장과 5분 TTL이 받는다. **눈으로만 판정된다.**
- **넥슨이 `character_list: []` 를 내는 조건**([[ADR-127]] 대가, 2026-08-12 신설). 그런 응답을 직접
  받아 본 적이 없다. 테스터 재현 조건에서 역산한 것이라 방어는 하되 조건을 안다고 적지 않는다. 테스터
  키로 `character/list` 를 한 번 호출해 보면 닫힌다.
- **안 고른 계정의 캐시 엔트리를 정리할지**([[ADR-113]] 결정 2의 대가, 2026-08-08 신설). 엔트리
  (`characterBasicCache:{ocid}`)는 전역 키이고 인덱스만 계정별이라 훑은 모든 계정 몫이 저장소에
  남는다. 지금은 **의도한 성질**로 둔다(그 계정으로 바꾸면 따뜻한 캐시로 재사용, [[ADR-086]] 결정 9).
  계정이 많은 사용자에게 실제로 부담이 되는지 관측된 바 없으므로 관측 전에 정리 정책을 만들지 않는다.
- 캐릭터 관리 피커 개선([[ADR-015]]) 잔여: Nexon `character/look` 이미지 얼굴 크롭 쿼리를 공식
  지원하는지(미지원이면 CSS 근사), 개선을 다른 캐릭터 선택 UI로 확장할지.
## 폐기된 정책 (history)

### 다섯 단계에서 두 단계로

- ~~온보딩은 `API 키 → 계정 선택 → 예열 → 관리 캐릭터 선택` 처럼 여러 단계다~~ →
  **두 단계**이고 계정을 고르지 않는다([[ADR-143]] 결정 1, 2026-08-16). 함께 내려간 것 셋: 계정 선택
  화면 · 예열(⛔ ADR-016) · 계정 선택 프로브(🟡 [[ADR-113]]).

  코드에서도 전부 없앴다. [[ADR-155]] 가 캐패시터 소스를 지우면서 정리 조건이 충족됐다.
  `use-account-probes.ts` 파일과 `accountScope` 플래그가 삭제됐고,
  `OnboardingStatus` 에서 `selectingAccount` 와 `prefetching` 도 사라졌다.
- ~~계정 수와 무관하게 항상 계정 선택 화면을 보여주고 사용자가 ‘계속하기’로 확정해야 예열이
  시작된다~~(⛔ ADR-051, 2026-07-29. 그 전에는 계정이 1개면 자동 확정이었다) → 계정을 고르는 단계
  자체가 없다.
- ~~예열은 계정 확정 즉시 그 계정 **전체 캐릭터** 데이터를 진행률 바와 함께 미리 받는다~~
  (⛔ ADR-016) → **드롭다운으로 그 계정을 열 때 그 계정 몫만 판정한다**([[ADR-143]] 결정 5). 예열은
  "이번 실행의 동기화"로 쳐서 온보딩 직후 첫 화면 진입이 방금 받은 것을 다시 받지 않게 하는 장치도
  ([[ADR-097]] 결정 3) 함께 내려갔다.
- ~~예열 진행률을 `MapleWaveProgress`(단풍잎 물 채우기 + %)로 표시~~ → 얇은 바 프리미티브로 통일하고
  컴포넌트를 폐기했다([[ADR-061]], 2026-07-30). 그 바도 예열과 함께 없어졌다.
- ~~전원 조회 불가 계정은 고를 수 없다~~([[ADR-086]] 결정 8, 2026-08-03. 그 전에는 경고만 띄우고 선택은
  허용했다) → **잠글 문이 없다**([[ADR-143]] 결정 10, 2026-08-16). 고르는 대상이 계정이 아니라
  캐릭터라, 그 계정을 열면 후보가 0건이고 드롭다운을 되돌리는 것이 출구다.
- ~~후보가 0건이면 ‘계정 다시 선택’을 준다~~([[ADR-086]] 결정 8) → 같은 이유로 없어졌다.
### 계정 선택 화면과 프로브

- ~~각 계정 = 대표 캐릭터 얼굴 아바타 36px 원형 + 텍스트 2줄(월드 엠블럼 · 월드 · 이름 · 레벨 /
  캐릭터 N개). 직업은 표시하지 않는다~~ → 화면이 없어졌다([[ADR-143]] 결정 1). 계정을 고르는 자리는
  캐릭터 선택 화면의 드롭다운이고, 거기서는 **대표 캐릭터 얼굴과 이름을 쓰지 않는다.** 판정이 ‘계정을
  열 때’로 미뤄져 열기 전에는 확인된 캐릭터가 없기 때문이다([[ADR-144]] 결정 2).
- ~~계정 선택 화면은 각 계정의 **전체 캐릭터**에 `character/basic` 을 프로브하고, 전수 프로브가 settle
  할 때까지 목록을 그리지 않는다~~([[ADR-113]] 결정 3, 2026-08-08, 이슈 #163) → 프로브 자체가
  없어졌다. **판정을 로스터 조회가 한다**([[ADR-143]] 결정 5). 그래서 [[ADR-116]] 결정 3(‘판정 못 한
  계정이 있으면 목록을 안 그린다’)이 **계정 하나 단위**로 좁아졌다.

  그 프로브가 정한 규칙 넷도 함께 내려갔다. 완료 판정이 성공이 아니라 settle 인 것(결정 4) · 대표는
  "확인된 캐릭터 중 최고 레벨"인 것 · 전원 조회 불가면 계정 카드에 경고를 다는 것 · 조회 불가
  (`OPENAPI00003`)만 영구로 다루고 나머지는 "판정 불가"로 두는 것(결정 7)이다.
- ~~003이 아닌 프로브 실패는 후보 자격을 유지하고 초상화만 비운 채 목록에 그대로 그린다~~
  ([[ADR-113]] 결정 7) → **판정 불가로 다루고, 판정 못 한 계정이 있으면 목록 자체를 그리지 않는다**
  ([[ADR-116]] 결정 3, 2026-08-08, 이슈 #177). 옛 동작은 429를 `catch` 에서 버려 ‘전원 조회 불가’
  판정을 **항상 거짓**으로 만들었고, 조회 불가 계정이 정상으로 보인 채 선택돼 이슈 #176의 잠금으로
  이어졌다.
- ~~프로브 결과를 캐시에 쓰지 않는다(다른 계정 캐릭터 누출 방지)~~ → 계정별 인덱스([[ADR-086]] 결정
  9)로 누출이 구조적으로 불가능해져 각 계정의 `accountId` 로 캐시에 쓴다([[ADR-113]] 결정 2).
- ~~계정 선택 실패를 목록 상단 인라인 문구로 알린다~~ → **토스트**([[ADR-083]] 결정 4, 2026-08-02).
  계정 목록과 ‘계속하기’가 그 자리에 남으므로 인라인일 이유가 없었다.
### 그 밖

- ~~모드 선택 단계의 옵션과 CTA는 기존 선택 카드 클래스를 그대로 재사용한다~~ → 카드 **안쪽**에
  아이콘과 주의 박스를 둔다([[ADR-035]] 결정 22, 2026-08-03, 이슈 #59). 바깥 카드 클래스 공유와 페이지
  레이아웃(카드 박스 없음), 스텝 인디케이터 미도입은 그대로다.
- ~~예열은 `access_flag: true` 인 캐릭터만 스케줄을 받는다~~ → 게이트 폐기, 전원 예열 + 후보 자격 판정
  병행([[ADR-086]] 결정 3, 2026-08-03). [[ADR-067]] 계측이 "false 여도 세 엔드포인트 모두 200"을
  확인했다.
- ~~`trackingMode` 저장값이 없으면 `'auto'`~~ → 저장 레이어는 `null`(미선택)을 돌려주고 소비처가
  `?? 'auto'` 로 흡수한다([[ADR-086]] 결정 2, 2026-08-03). 동작 기본값은 그대로 자동이다.
- ~~`selectedAccountId` 가 있으면 온보딩 완료로 본다~~ → 저장된 값에서 재개 단계를 파생한다
  ([[ADR-086]] 결정 1, 2026-08-03).
- ~~선택 계정의 `character_list` 를 캐싱한다~~ → 캐싱하지 않고 매번 재조회한다(2026-07-11).
- ~~캐시가 없으면 `character/list` 응답으로 `access_flag` 미상 캐릭터까지 먼저 표시한다~~ → 활성이
  확인된 캐릭터만 표시하고 콜드 스타트는 스피너([[ADR-053]], 2026-07-29).