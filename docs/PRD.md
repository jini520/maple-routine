# PRD: 메이플 루틴 (Maple Routine)

> 이 문서는 초안입니다. 하이브리드 프레임워크(Capacitor), 웹 프레임워크(Vite+React), 백그라운드 상시 알림 요구사항은 확정되었습니다. 기록 데이터는 로컬 저장 전용이나 일간/주간 진행 상태는 Nexon Open API 연동으로 조회합니다([[ADR-007]], 2026-07-09). 별도 공개 웹 서비스 제공은 보류, 게임 레퍼런스 데이터는 `src/data/`에 반영 완료.

## 목표
메이플스토리 플레이어가 일간/주간 콘텐츠 소진, 사냥 중 스킬 재사용 타이밍, 주간 보스 수익, 물욕템 드랍 기록을 캐릭터/계정 단위로 한 곳에서 관리하도록 돕는다.

## 사용자
- 메이플스토리를 플레이하며 일간·주간 콘텐츠를 여러 캐릭터(본캐+부캐)에 걸쳐 반복 수행해야 하는 유저
- 주간 보스 파티 수익을 캐릭터별/계정별로 파악하고 싶은 유저
- 장시간 사냥 중 솔 야누스 등 주기적 스킬 재사용을 놓치지 않아야 하는 유저

## 플랫폼
웹앱으로 개발 후 Android/iOS에 WebView 하이브리드 앱으로 패키징. 하이브리드 프레임워크는 Capacitor로 확정 ([[ADR-001]] 참고). 백그라운드 알림/상시 타이머 표시가 핵심 기능이므로 순수 웹(브라우저 PWA)만으로는 요구사항을 충족하지 못한다고 보고 네이티브 래핑을 전제로 함.

**전제 조건 (2026-07-09 추가, [[ADR-007]])**: 일간/주간 스케줄러 기능을 쓰려면 사용자가 (1) 게임 클라이언트 자체의 "스케줄러" 기능에 추적하고 싶은 퀘스트·보스를 먼저 등록해두어야 하고, (2) 앱 설정 화면에 `openapi.nexon.com`에서 본인이 직접 발급받은 개인 API 키를 입력해야 한다. NEXON Open ID(OAuth 로그인)는 "메이플스토리 파트너스" 승인이 필요한 제한된 기능이라 사용하지 않는다 — 앱 자체의 회원가입/계정 시스템도 두지 않는다.

**온보딩 (확정, 2026-07-09)**: API 키를 입력하면 `character/list`로 캐릭터 목록을 조회한다. 하나의 Nexon 계정에 여러 "메이플 ID"가 있을 수 있어 응답에 계정이 2개 이상 나올 수 있는데, 이 경우 **어느 메이플 ID를 쓸지 선택하는 화면**을 보여준다. 선택은 이후 설정 화면에서 언제든 바꿀 수 있다. API 키 발급 안내는 앱 안에서 단계별 위저드를 만들지 않고, openapi.nexon.com 링크 + 간단한 샘플 이미지 + 설명 문구로 가볍게 안내한다.

## 핵심 기능

### 1. 일간 스케줄러 ([[ADR-007]]로 데이터 소스 전면 변경, 2026-07-09)
- 캐릭터별로 일간 콘텐츠 진행 상태를 Nexon Open API(스케줄러 Open API)로 동기화해 읽기 전용으로 표시. 앱 안에서 수동 체크는 하지 않음(확정, 2026-07-09)
- 표시되는 항목은 사용자가 게임 클라이언트의 "스케줄러" 기능에 실제로 등록해둔 것만 해당(`registration_flag: "true"`). 앱 자체에서 항목을 추가/등록할 수 없음
- 진행률은 API의 `now_count`/`max_count`를 그대로 표시(예: 몬스터파크 7/14)
- 지정 시각까지 미완료 항목이 있으면 로컬 알림 발송. 판단 기준은 마지막 캐시가 아니라 **알림 시각에 Nexon API를 실시간 재호출**해 정확히 확인(확정, 2026-07-09 — [[ADR-004]]). 배터리/네트워크 사용이 늘고, iOS는 정확히 그 시각에 재확인이 안 될 수 있음(베스트 에포트)
- 알림 발송 시각은 사용자가 설정 가능
- **확정(2026-07-09)**: "사용자 커스텀 항목 추가"는 완전히 제거. 게임에 등록되지 않은 항목은 API로 완료 여부를 추적할 수 없어 이 모델과 맞지 않음 — 표시되는 항목은 항상 게임 내 스케줄러 등록 목록과 정확히 일치
- **엣지 케이스 — 게임 내 스케줄러 미등록**: 신규 캐릭터 또는 스케줄러를 아직 써본 적 없는 캐릭터는 모든 항목이 `registration_flag: "false"`로 옴. 이건 에러가 아니라 정상 상태이므로, "표시할 항목이 없습니다 — 게임에서 먼저 스케줄러에 등록해주세요" 같은 빈 상태 화면으로 처리 (일간·주간 공통, [[ADR-008]])
- **에러 상태**: 동기화 실패 시 마지막으로 성공한 데이터를 유지하고 "마지막 동기화 n분 전" 표시 + 새로고침 버튼. API 키가 무효화된 경우 설정 화면 재입력 유도 배너 ([[ADR-008]])

### 2. 주간 스케줄러 ([[ADR-007]]로 데이터 소스 전면 변경, 2026-07-09)
- 캐릭터별로 주간 퀘스트·주간 보스 진행 상태를 Nexon Open API로 동기화해 읽기 전용으로 표시(일간 스케줄러와 동일한 모델, 엣지 케이스·에러 상태도 동일)
- "주간 퀘스트"와 "주간 보스" 두 섹션으로 구성
- 주간 보스 섹션: 기존에 검토했던 "앱 내에서 최대 12마리 선택" UI는 두지 않음. `boss_contents` 중 `cycle: "bossWeekly"`인 항목만 표시(`bossDaily`는 무시, `bossMonthly`인 검은마법사는 이 섹션이 아니라 별도 취급 — 확정, 2026-07-09)하고 그 완료 여부(`complete_flag`)를 그대로 표시. `src/data/weekly-bosses.json`은 API 응답의 보스명·난이도 표기(영문/공백 차이, "시즌 보스 메이린" 같은 별칭 포함)를 우리 데이터와 매핑하는 참조 테이블로만 쓰임. 매핑 안 되는 보스명은 원문 그대로 "알 수 없는 콘텐츠"로 표시(크래시 금지, [[ADR-008]])
- **게임 규칙 — 주간 보스 12마리 제한 (확정, 2026-07-09)**: 캐릭터당 주간 보스는 최대 12마리(난이도 조합 단위)까지만 처치 가능한 게 게임 자체 규칙이며, API의 `weekly_boss_clear_count`/`weekly_boss_clear_limit_count`가 이 카운트를 그대로 반영한다. "시즌보스"(현재는 메이린만 해당, 카이는 시즌 종료로 이미 제외)는 이 제한에서 예외라 진행률 표시(예: "n/12")에서 별도로 다뤄야 함
- 지정 시각까지 미완료 항목이 있으면 로컬 알림 발송(일간과 동일하게 알림 시각 실시간 재확인 기준, 확정)
- ~~엣지 케이스 — 리셋 경계~~ — 확인 완료(2026-07-09): Nexon 서버는 KST 목요일 00:00에 초기화되어 로컬 계산 리셋 시각과 정확히 일치. 리셋 경계 불일치 리스크 없음

### 3. 사냥 타이머
- 30분 카운트다운 타이머(1소재 단위)
- 타이머 진행 중 지정 주기(기본 1분 또는 2분)마다 알림음을 반복 재생해 솔 야누스 재설치 타이밍을 알림
- 5자석펫 등으로 더 긴 주기가 필요한 경우를 위해 사용자가 직접 분 단위 주기를 입력 가능
- **확정**: 앱이 백그라운드/종료 상태여도 동작해야 함. 네이티브 타이머 앱처럼 알림창에 경과 시간이 항시 표시되는 상시 알림 형태로 구현 ([[ADR-005]] 참고). iOS는 Live Activity(iOS 16.1+) 기반이라 구버전 기기는 폴백(로컬 알림+사운드만) 필요
- **확정**: 한 번에 하나의 타이머만 실행(여러 캐릭터 동시 사냥 미지원)
- **확정(2026-07-09)**: 알림음은 기본 제공 사운드만 지원. 사용자 업로드/커스텀 사운드 선택은 지원하지 않음
- **에러 상태**: OS가 알림 권한을 거부했거나 Foreground Service/Live Activity 시작에 실패하면, 상시 알림 UI 없이도 최소한 주기 사운드 알림은 로컬 알림으로 폴백 ([[ADR-008]])

### 4. 주간 보스 수익 계산기
- 처치한 보스 목록은 주간 스케줄러와 동일하게 Nexon API 동기화 데이터를 그대로 사용(수동 입력 없음, [[ADR-007]])
- 파티원 수는 Nexon API가 알 수 없는 정보이므로 캐릭터별로 계속 수동 입력
- 보스 종류 × 파티원 수에 따른 "강력한 힘의 결정" 판매 가격을 조회해 수익 계산 (정가는 `src/data/boss-crystal-prices.json`. 파티원 1인당 실수령액 = `floor(정가 / 파티원 수)`. 벨로나는 미출시라 가격 없음)
- 선택된 메이플 ID(계정)의 전체 캐릭터를 합산해 주간/월간 수익 확인 (하나의 Nexon 계정에 여러 메이플 ID가 있을 수 있음 — 온보딩 참고)
- 주차별 기록을 누적 저장해 월간 추이 확인 가능(Nexon API는 최근 14일치만 조회 가능하므로 장기 추이는 로컬에 누적 저장)
- **확정(2026-07-09) — 물욕템 수익 통합·주간/월간/히스토리 ([[ADR-010]])**: 물욕 아이템 드랍(핵심 기능 5) 획득 기록도 아이템별 고정 시세로 환산해 이 화면에 합산한다. 결정 판매 수익과 물욕템 환산 가치는 항목별로 구분 표시하되 합계는 하나로 보여준다. 새 화면을 만들지 않고 이 화면 안에 "이번 주 / 월간 추이" 탭과 히스토리 차트를 추가하는 방식으로 확장(화면 수 유지)
- **확정(2026-07-09) — 입력 트리거 시점**: 별도 유도(알림/배지) 없이, 사용자가 보스 수익 계산기 화면에 직접 들어와 파티원 수를 입력한다
- **엣지 케이스 — 중복 동기화**: 같은 주에 앱을 여러 번 열어 같은 보스의 완료를 여러 번 감지해도 캐릭터+보스+난이도+주차 조합당 기록은 1건만 유지(멱등성, [[ADR-008]])
- **엣지 케이스 — 참조 데이터 제거**: `weekly-bosses.json`에서 보스가 제거돼도(카이 사례) 과거에 이미 기록된 수익 데이터는 삭제하지 않고 "(더 이상 지원하지 않는 콘텐츠)" 등으로 표시 ([[ADR-008]])
- **입력값 검증 (확정, 2026-07-09)**: 파티원 수는 1 이상의 정수, 상한은 보스별로 다름 — 기본 6인, 스우 익스트림은 2인, 최초의 대적자·찬란한 흉성·림보·발드릭스·유피테르는 3인, 메이린은 1인. `src/data/boss-crystal-prices.json`의 `partySizeScaling.defaultMaxPartySize`/`maxPartySize`로 관리

### 5. 물욕 아이템 드랍 현황
- **화면 구조(확정, 2026-07-09, 와이어프레임 작업 중 발견)**: 이 기능은 단일 화면이 아니라 2단계다 — 드랍 탭 진입 시 먼저 **보스 목록**이 뜨고, 하나를 선택하면 그 보스의 **물욕템 그리드 + 획득 히스토리** 상세 화면으로 들어간다
- **보스 목록 출처 (확정, 2026-07-09, [[ADR-011]])**: 독립 목록이 아니라 주간 스케줄러(핵심 기능 2)와 동일한 Nexon 동기화 캐시에서 가져온다 — 게임 내 스케줄러에 실제로 등록한 주간 보스만 표시(수동 등록 불가, [[ADR-007]] 원칙과 동일). 월간 보스(검은마법사)는 현재 이 목록에서 제외 — 의도된 것인지 확인 필요
- **난이도 무관 아이템 노출 (확정, 2026-07-09, [[ADR-011]])**: 보스 목록·아이템 그리드 어디에도 난이도를 표기하지 않는다. 보스 하나를 선택하면 `item-drop-table.json`에서 그 보스명과 일치하는 모든 난이도(노멀/하드/익스트림/카오스 등) 엔트리의 아이템을 이름 기준으로 합쳐서 보여준다 — 유저가 실제로 처치하는 난이도를 게임 내에 정확히 등록해두지 않았을 수 있어, 난이도로 거르면 실제로 얻은 아이템이 목록에 없을 위험이 있기 때문
- **아이템 이미지 (확정, 2026-07-09, [[ADR-011]])**: 물욕템 버튼은 텍스트가 아니라 이미지로 표시한다. 이미지는 사용자가 직접 준비해 `src/assets/items/`에 추가(파일명 = 아이템명, 공백은 언더스코어로 치환). 아직 이미지가 없는 항목은 플레이스홀더로 표시
- 보스별로 정해진 물욕템 목록을 버튼 형태로 노출 (`src/data/item-drop-table.json`. 사용자가 제공한 보상 정보 전체를 카테고리별(고정 보상/장비/소비 아이템/주문서/기타/최초 격파)로 보유하며, 이 중 실제로 버튼화할 항목은 사용자가 선별 예정)
- 버튼 클릭/터치 시 해당 아이템을 "획득"으로 기록 (획득 일자 포함). 컨테이너형이 아닌 일반 아이템은 확인창 없이 탭 즉시 기록되고 토스트로 피드백한다(제안, `docs/UI_GUIDE.md` 미반영 — 문구·노출 시간 미정)
- 캐릭터별/보스별 드랍 히스토리 조회 가능
- **확정**: 잘못 등록한 획득 기록은 히스토리에서 개별 삭제/취소 가능. 삭제 시 확인 다이얼로그를 넣을지는 미정(즉시 삭제 vs 확인 후 삭제)
- **확정(2026-07-09) — 랜덤 컨테이너 아이템 결과 기록 ([[ADR-010]])**: "OO옥의 보스 반지 상자"·"혼돈의 칠흑 장신구 상자"처럼 개봉 결과가 확률로 정해지는 아이템은 "획득" 기록 시 실제로 나온 결과(레벨·반지 종류, 또는 장신구 종류)를 후보 목록에서 선택해 함께 기록한다(확률로 자동 추정 금지). 후보 목록은 `src/data/boss-ring-boxes.json`(녹옥/홍옥/흑옥/백옥/생명의 5종, 확률표 반영 완료) + `src/data/accessory-boxes.json`(혼돈의 칠흑 장신구 상자·메이린의 칠흑 장신구 상자 — 이름만 다르고 내용물 동일, 후보 7종 반영 완료. 개별 확률은 게임 내 비공개라 목록만)
- 물욕템 획득 기록은 아이템별 고정 시세로 환산해 핵심 기능 4(보스 수익 계산기)의 수익 합계에 포함된다([[ADR-010]])

## MVP 제외 사항
- 앱 자체 회원가입/계정 시스템 및 클라우드 동기화 — 기록 데이터는 기기 로컬 저장 전용으로 확정 ([[ADR-003]] 참고). Nexon 개인 API 키도 기기에만 저장하며 별도 서버를 두지 않음
- NEXON Open ID(OAuth) 로그인 — "메이플스토리 파트너스" 승인이 필요한 제한된 기능이라 MVP에서 사용하지 않음 ([[ADR-007]], 2026-07-09)
- 별도 공개 웹 서비스(브라우저에서 접근 가능한 랜딩/정보 사이트) — 보류. Capacitor 앱(WebView) 제공에 먼저 집중하고, 추후 필요 시 별도 검토
- 메소/결정 시세 자동 연동 — 가격표는 수동 입력 또는 정적 데이터 기반
- 파티원 매칭, 랭킹 등 소셜/커뮤니티 기능
- 다국어 지원 (한국어 우선)
- 데미지 계산기, 스타포스 계산기 등 스케줄링/기록 이외의 다른 유틸리티

## 에러/엣지 케이스 처리 원칙 ([[ADR-008]])
세부 정책은 ADR-008·ARCHITECTURE.md 참고. 제품 차원의 원칙만 요약:
1. **실패는 숨기지 않는다**: 동기화/저장이 실패하면 조용히 이전 상태를 유지하는 대신, 무엇이 실패했고 마지막으로 성공한 게 언제인지 사용자에게 보여준다.
2. **빈 상태는 에러가 아니다**: 게임에 아직 아무것도 등록 안 한 신규 캐릭터의 "표시할 항목 없음"은 에러 화면이 아니라 다음 행동을 안내하는 빈 상태 화면으로 다룬다.
3. **과거 기록은 지우지 않는다**: 게임 데이터(보스 목록 등)가 바뀌어도 이미 쌓인 사용자의 기록(수익, 드랍)은 보존한다. 다만 실수로 잘못 남긴 기록(물욕템 획득 등)은 사용자가 직접 지울 수 있어야 한다.
4. **자동 재시도는 최소화한다**: 네트워크 실패 시 백그라운드에서 무한 재시도하지 않고, 사용자가 명시적으로 새로고침하도록 한다(배터리 소모 방지). 단, 알림 발송 판단만은 예외로 실시간 재확인한다([[ADR-004]]).
5. **크래시 리포팅은 opt-in (확정, 2026-07-09)**: 서버가 없는 로컬 전용 앱이라 실제 사용자에게 발생하는 문제를 개발자가 알 방법이 없다는 현실적 문제를 해소하기 위해, 설정 화면에 "익명 오류 보고" 토글(기본값 OFF)을 두고 켠 사용자에 한해서만 Sentry 등으로 스택트레이스를 전송한다. 캐릭터명·API 키 등 개인 식별 정보는 전송하지 않는다 ([[ADR-008]]).

## 디자인
- 매일 켜서 체크하는 "도구"로서의 대시보드. 마케팅 랜딩 페이지 느낌 배제
- **정정(2026-07-09)**: ~~다크모드 기준~~ → 게임 관련 앱 성격에 맞춰 캐주얼하고 친근한 톤으로 방향 전환, **라이트 테마가 기본**이며 다크는 폐기하지 않고 보조 팔레트로 유지(설정에서 전환 여부는 [[ADR-009]]와 함께 추후 결정). 정보 밀도 높은 리스트/카드 UI 방향 자체는 유지
- 세부 룰은 `docs/UI_GUIDE.md`에서 별도 정의
- 직업(전직) 고유 컬러 기반 다중 테마 지원 예정([[ADR-009]]) — 뼈대만 확정, 컬러 값은 추후 확정

## 확인이 필요한 사항
1. ~~하이브리드 앱 프레임워크~~ — 확정: Capacitor
2. ~~백그라운드 알림/사운드 요구 수준~~ — 확정: 백그라운드/종료 상태에서도 상시 알림(경과 시간 표시) + 주기적 사운드 필요
3. ~~데이터 저장 방식~~ — 확정: 기기 로컬 저장 전용, 서버 없음 ([[ADR-003]], Nexon 개인 API 키도 기기에만 저장)
4. ~~웹 프레임워크~~ — 확정: Vite + React ([[ADR-002]])
5. ~~게임 레퍼런스 데이터~~ — 확정 및 반영 완료 (`src/data/`). 힐라(하드)·핑크빈(카오스)는 일간 보스로 격하되어 목록에서 제거, 벨로나는 미출시로 보류(가격·아이템 데이터 없음), 파티원 가격 계산은 `floor(정가 / 파티원 수)`. 벨로나 출시 시 가격·아이템 데이터 추가 필요
6. ~~일간/주간 진행 상태 데이터 출처~~ — 확정: 수동 체크 아님, Nexon Open API(스케줄러 Open API)로 동기화 ([[ADR-007]], 2026-07-09)
7. ~~Nexon API 인증 방식~~ — 확정: OAuth/NEXON Open ID 아님(파트너스 승인 필요해 미사용). 사용자 개인 API 키를 앱 설정에 직접 입력, 캐릭터 조회→ocid→스케줄러 API 2단계 호출로 실측 확인 완료 ([[ADR-007]], 2026-07-09)
8. ~~개인 API 키의 호출 빈도 제한(rate limit)~~ — 확인 완료(2026-07-09, 공식 가이드): 개발 단계 초당 5건/일 1,000건, 서비스 단계 초당 500건/일 20,000,000건. 개인 사용 기준 예상 호출량(하루 수십 건)은 개발 단계로도 충분 — 단 초당 5건 제한 때문에 여러 캐릭터 동기화 시 순차 호출 필요 ([[ADR-007]])
9. ~~일간/주간 화면에 수동 체크 병용 여부~~ — 확정: 완전 읽기 전용, 수동 체크 없음 (2026-07-09)
10. ~~미완료 알림 판단 기준~~ — 확정: 마지막 캐시가 아니라 알림 시각에 Nexon API 실시간 재호출로 판단. 배터리/네트워크 사용 증가, iOS는 정확한 시각 보장 안 됨(베스트 에포트) (2026-07-09, [[ADR-004]])
11. ~~일간 스케줄러 "사용자 커스텀 항목 추가"~~ — 확정: 완전히 제거 (2026-07-09)
12. ~~온보딩 흐름~~ — 확정(2026-07-09): API 키 발급은 앱 내 단계별 위저드가 아니라 openapi.nexon.com 링크 + 샘플 이미지 + 설명으로 안내. API 키 입력 후 캐릭터/계정 조회는 아래 #16 참고
13. ~~계정당 캐릭터 수 제한 / 캐릭터 목록 조회 메커니즘~~ — 확정(2026-07-09): 제한 없음. 캐릭터명+월드 수동 입력 폼은 없고, API 키 입력 직후 `GET /maplestory/v1/character/list`로 캐릭터 목록을 자동 조회한다
14. ~~사냥 타이머 사운드~~ — 확정: 기본 제공 사운드만 지원, 사용자 업로드/선택 없음 (2026-07-09)
15. ~~파티원 수 입력 범위~~ — 확정(2026-07-09): 기본 1~6인, 예외로 스우 익스트림 2인, 최초의 대적자·찬란한 흉성·림보·발드릭스·유피테르 3인, 메이린 1인. `boss-crystal-prices.json`에 반영 완료
16. **정정 — 멀티 계정은 v1 범위 밖이 아니라 실제로 설계해야 하는 흐름이었다** (2026-07-09): #13에서 실측한 `character/list` 응답에 실제로 `account_id`가 2개 나옴 — "하나의 Nexon 계정에 여러 메이플 ID가 존재할 수 있다"는 게 정상적인 게임 시스템이라고 확인됨(사용자 확인). 그래서 온보딩에 "계정(메이플 ID) 선택 화면"이 확정으로 추가됐고, 설정에서 계정을 나중에 바꿀 수도 있어야 한다(확정). 이전에 "본인은 단일 계정이라 문제없다"고 적었던 건 정정함 — 여러 메이플 ID를 갖는 게 오히려 흔한 케이스로 다뤄야 함
17. ~~`weekly_boss_clear_limit_count` 필드 의미~~ — 확인 완료(2026-07-09): 게임 자체의 "캐릭터당 주간 보스 12마리 제한" 카운트. 시즌보스(메이린)는 예외. 샘플에서 0으로 보였던 건 그 응답의 완료 보스가 월간 보스라 카운트 대상이 아니었기 때문 ([[ADR-007]] 참고)
18. ~~에러 핸들링/복원력 정책~~ — 확정: [[ADR-008]] 참고 (동기화 실패 시 마지막 캐시 유지, 빈 상태와 에러 상태 구분, 참조 무결성 보존, 멱등성 보장 등)
19. ~~크래시 리포팅~~ — 확정: opt-in Sentry(또는 유사 서비스), 기본 OFF, 개인 식별 정보 제외 (2026-07-09, [[ADR-008]])
20. ~~Nexon API 호출 도메인·인증 헤더·호출 제한~~ — 확인 완료(2026-07-09, 공식 가이드): 도메인 `https://open.api.nexon.com/`, 헤더 `x-nxopen-api-key`, 개발 단계 초당 5건/일 1,000건 ([[ADR-007]] 참고)
21. ~~NEXON Open API 출처 표기 위치~~ — 확정(2026-07-09): 앱 footer
22. ~~스케줄러 API 엔드포인트~~ — 확정(2026-07-09): `GET /maplestory/v1/scheduler/character-state`
23. **참고 — 상업적 이용 제한**: 넥슨 승낙 없이 API 데이터를 영리 목적으로 이용 불가(이용약관 제6조⑥). 현재 무료 개인용 MVP라 문제 없으나, 유료화/광고를 고려하게 되면 별도 확인 필요
24. ~~계정(메이플 ID) 선택 UI 표시 방식~~ — 확정(2026-07-09): 각 `account_id`는 그 계정 내 **가장 레벨이 높은 캐릭터의 닉네임 + 직업 + 레벨**로 표기(예: "낟낟 · 렌 Lv.293"). `account_id` 해시는 화면에 노출하지 않음
25. ~~`cycle` 필드 전체 값~~ — 확인 완료(2026-07-09, 실측): `bossDaily`/`bossWeekly`/`bossMonthly` 세 값. 앱은 `bossWeekly`·`bossMonthly`만 사용, `bossDaily`는 무시 ([[ADR-007]] 참고)
26. ~~보스명 정규화 방향~~ — 확인 완료(2026-07-09, 실측): 공백 유무가 보스마다 다른 방향으로 나타남(API에 더 있는 경우/데이터에 더 있는 경우 둘 다 확인) — 양쪽 다 공백 제거 후 비교해야 함. "시즌 보스 메이린"처럼 공백 제거로 안 잡히는 예외는 `weekly-bosses.json`의 `apiAlias` 필드로 명시 반영
27. ~~파티원 수 입력 트리거 시점~~ — 확정(2026-07-09): 별도 유도(알림/배지) 없이 사용자가 보스 수익 계산기 화면에서 직접 파티원 수를 입력한다 (핵심 기능 4 참고)
28. **확인 필요 — `item-drop-table.json` 미확정 수치([[ADR-006]])**: 찬란한 흉성(하드)·유피테르(노멀/하드)의 솔 에르다의 기운 수량이 `(추정)` 표기로 남아있고, 유피테르(하드) "오만의 원죄" 장비는 슬롯 정보가 "미상"이다. 실제 게임 수치로 확정 필요 — 확정 전까지 UI에도 동일하게 추정/미상 표시를 유지한다(2026-07-09, 우선 추정 상태 유지로 확정 — 추후 실제 수치 확인 시 갱신)
29. **확인 필요 — 파풀라투스(카오스) 드랍 데이터 완전성**: 다른 보스 항목은 모두 fixed(주문의 흔적 등)+equipment+consumable 구성인데 파풀라투스만 equipment 1건뿐이다. 실제로 이 보상만 존재하는 게 맞는지, 데이터 제공 시 누락된 것인지 확인 필요
30. ~~상태 관리 라이브러리~~ — 확정(2026-07-09): Zustand 사용
31. **확인 필요 — `docs/UI_GUIDE.md` 미작성**: 디자인 원칙·색상·컴포넌트 규칙이 전부 `{...}` 플레이스홀더 상태 — UI 구현 착수 전 채워야 함
32. **확인 필요 — 직업 기반 테마 시스템**: 직업(전직) 고유 컬러로 다중 테마를 지원할 예정([[ADR-009]], 2026-07-09 방향 확정). 뼈대(데이터 출처 `src/data/job-themes.json`, CSS 변수 기반 런타임 적용, Zustand 상태 관리)만 확정했고, 직업별 실제 컬러 값·테마 단위(대분류/세부 전직)·전환 UX(수동/자동)는 모두 추후 확정 필요
33. **확인 필요 — 물욕템 시세·랜덤 컨테이너 아이템 데이터([[ADR-010]])**: 보스 반지 상자 5종(녹옥/홍옥/흑옥/백옥/생명의)의 레벨·아이템 확률표는 반영 완료(2026-07-09, `src/data/boss-ring-boxes.json`). "혼돈의 칠흑 장신구 상자" 후보 7종도 반영 완료(2026-07-09, `src/data/accessory-boxes.json`, 개별 확률은 게임 내 비공개라 목록만) — "메이린의 칠흑 장신구 상자"는 이름만 다르고 내용물은 동일함을 사용자가 확인해 같은 후보 목록 적용. 아직 확인 필요한 것: (1) 물욕템(반지·장신구 포함) 개별 시세 — 전혀 없음, (2) 개봉해서 나온 개별 반지/장신구의 거래 가능 여부(박스 자체는 교환 불가로 확인됨)
34. **확인 필요 — 물욕 아이템 화면 보스 목록·이미지 에셋([[ADR-011]])**: 보스 목록을 주간 스케줄러 동기화 캐시로 통일하고, 보스 선택 시 난이도 무관 전체 아이템 노출, 이미지는 파일명 컨벤션(`assets/items/아이템명.png`)으로 조회하는 것까지 확정(2026-07-09). 아직 확인 필요한 것: (1) 월간 보스(검은마법사)를 이 목록에 포함할지, (2) 이미지 파일 포맷·권장 해상도, (3) 같은 아이템이 난이도별로 수량만 다를 때(예: "주문의 흔적" 160개 vs 240개) 합쳐서 보여줄 때 수량 표시 방식
