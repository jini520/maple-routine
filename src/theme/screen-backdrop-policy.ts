/**
 * 화면이 자기 벽지를 들고 다니는지 정하는 상수. 안드로이드만 그렇다.
 *
 * 벽지를 셸에 한 장으로 깔면 그 위 화면들이 투명해야 하는데, 안드로이드는 전환 중 두 화면이 겹치는
 * 동안 **위 화면 너머로 아래 화면 글자가 비친다**(실기기 확인). iOS 에는 그 증상이 없어서 한쪽에서만
 * 나는 문제로 양쪽 모습을 바꾸지 않는다.
 *
 * 대가는 안드로이드에서 벽지가 화면과 함께 미끄러지는 것이다. 겹쳐 읽히는 글자를 없애는 값으로 받는다.
 *
 * **값이 하나여야 한다.** 화면 배경을 불투명하게 칠하는 쪽(`navigation-theme.ts`)과 벽지를 화면마다
 * 그리는 쪽(`ScreenBackdrop`)이 함께 지켜야 성립한다. 한쪽만 바뀌면 벽지가 통째로 사라지거나 겹침이
 * 그대로 남는다.
 */

import { Platform } from 'react-native'

export const SCREENS_CARRY_BACKDROP = Platform.OS === 'android'
