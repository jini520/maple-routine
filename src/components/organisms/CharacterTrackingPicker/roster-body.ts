// 그리드가 들어가는 자리의 최소 높이 — **카드 3줄이 렌더링될 때와 같은 높이**(실측 385px =
// 카드 123px × 3 + gap 8px × 2). 카드 123px 안쪽은 아바타 56 + gap 4 + 이름행 17(월드 엠블럼
// h-[17px] 가 정하는 값, 엠블럼이 없으면 16) + gap 4 + 레벨 16 + py-3 24 + 테두리 2다.
//
// 이 자리는 상태에 따라 그리드/스피너/실패/빈 상태로 갈리는데, 각 상태의 높이가 다르면 그 아래
// CTA(온보딩 "계속하기", 모달 "닫기·저장")가 상태마다 위아래로 움직이고 실패 문구의 액션 버튼과
// CTA 가 붙어 보인다(사용자 보고 2026-07-30). 높이를 3줄로 못 박아 그 의존을 끊는다 —
// [[ADR-054]] 정정 4에서 라벨행 높이를 h-6 으로 명시 고정한 것과 같은 처방이다.
//
// ── RN 에서 갈린 것 둘 ─────────────────────────────────────────────────────────────
//
// ① **클래스(`min-h-[385px]`)가 아니라 값(px)이다.** 모달은 이 값을 짧은 기기에서 클램프해야 하는데
//    ([[ADR-107]] 결정 2) 그 식이 `min(385px, calc(100dvh - …))` 이라 클래스로는 표현할 수 없다 —
//    두 호출부가 같은 수를 쓰도록 숫자를 내보내고, 계산은 각자 자기 자리에서 한다.
// ② **컴포넌트 파일이 아니라 별도 모듈이다.** 컴포넌트 파일이 컴포넌트 아닌 값을 함께 export 하면
//    fast refresh 가 깨진다(`atoms/Button/variants.ts` 와 같은 판단 — 웹에서는 그 값이
//    `CharacterTrackingGrid.tsx` 안에 있었다).
export const ROSTER_BODY_MIN_H_PX = 385
