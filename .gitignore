node_modules
dist
dist-ssr
# mapleroutine.store 정적 사이트 산출물 (npm run build:site)
dist-site
*.local
tsconfig.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

memo

# ── Expo / React Native (모노레포 해체로 packages/app-rn/.gitignore 가 여기로 왔다, [[ADR-155]])
# Expo
.expo
dist/
web-build/
expo-env.d.ts

# 네이티브 프로젝트(android/ · ios/)는 **커밋한다** — bare 로 가기로 했다([[ADR-127]]).
# 서명 설정과 커스텀 네이티브 모듈(Preferences 어댑터)이 거기 들어간다.
# 빌드 산출물만 제외하고, 그 목록은 `expo prebuild` 가 android/.gitignore · ios/.gitignore 로 만든다.

# 로컬 네이티브 모듈(modules/*)은 소스만 커밋한다 — `expo prebuild` 가 만들어 주는 자리가 아니라
# 우리가 직접 쓰는 소스라 위 두 .gitignore 가 안 덮는다.
modules/*/android/build/
modules/*/android/.cxx/

# Kotlin 컴파일러 세션 파일. `expo prebuild` 가 만드는 android/.gitignore 에 없는 자리라 여기서 덮는다.
android/.kotlin/

# Xcode 파생 데이터. `xcodebuild -derivedDataPath build` 로 구우면 저장소 뿌리에 쌓인다(수 GB).
# 앞에 `/` 를 붙여 **뿌리의 것만** 가린다. 그냥 `build/` 로 두면 어느 깊이의 build 든 걸린다.
/build/

# 릴리스 서명(ADR-091) — 저장소 밖 키스토어를 가리키고 **비밀번호가 들어간다. 절대 커밋 금지.**
# 이 규칙이 android/.gitignore 가 아니라 여기 있는 이유: `expo prebuild` 가 android/.gitignore 를
# 다시 만들면서 손으로 넣은 줄을 지운다(실제로 한 번 지워졌다). 이 파일은 prebuild 가 안 건드린다.
android/keystore.properties

# FCM 발송 자격증명. **이 파일 하나로 전 사용자에게 푸시를 쏠 수 있다.**
# 이 저장소는 public 이라 한 번 커밋되면 히스토리에서 지워도 이미 나간 것이다.
# 발송은 서버(별도 저장소)가 하고 키는 그 기기에만 둔다. 여기 받아 두지 말 것.
*-firebase-adminsdk-*.json
firebase-service-account*.json
service-account*.json

# APNs 인증 키. Firebase 콘솔에 올리는 물건이고 저장소에 둘 이유가 없다.
# 애플이 **한 번만 내려받게** 해 주므로 잃으면 키를 새로 만들어야 한다.
*.p8

# macOS
.DS_Store

# 로컬 빌드 설정. 값은 .env.example 참고
.env
.env.local
