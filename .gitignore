node_modules
dist
dist-ssr
# mapleroutine.store 정적 사이트 산출물 (npm run build:site)
dist-site
*.local
tsconfig.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

memo

# 드롭 이펙트 원본 프레임(29MB PNG). 최적화본(packages/core/src/assets/drop-effect/*.jpg)만
# 커밋한다(ADR-038).
packages/app-capacitor/src/assets/ScreenEff/

# 스토어 리스팅용 스크린샷 원본·산출물(26MB PNG). _raw 는 로컬 전처리 입력이고 나머지는
# 앱에서 다시 찍어 만들 수 있는 산출물이라, 위와 같은 원칙으로 저장소에서 제외한다(ADR-038).
# 스토어에 올라간 최종본의 출처는 Play Console·App Store Connect 쪽이다.
packages/app-capacitor/resources/screenshots/