{
  /* 이 패키지의 `npm run build` 가 `tsc -b` 로 부르는 솔루션 파일.

     앱 프로젝트 하나만 가리키지 않는 이유는 **검사 범위를 옮기기 전과 똑같이 유지**하기 위해서다 —
     예전엔 저장소 루트의 `tsc -b` 가 셋(앱 · core · 루트 scripts)을 함께 검사했고, 루트 `npm run build`
     가 이 패키지로 위임된 지금도 같은 셋이 검사돼야 한다. 빠뜨리면 `packages/core` 의 테스트 파일과
     `scripts/*.ts` 가 아무 타입 검사도 받지 않게 되는데(vitest·eslint 둘 다 타입을 안 본다) 그건
     조용한 후퇴다. */
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "../core" },
    { "path": "../../tsconfig.node.json" }
  ]
}
