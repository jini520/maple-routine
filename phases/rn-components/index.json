{
  "project": "maple-routine",
  "phase": "rn-components",
  "steps": [
    {
      "step": 0,
      "name": "nativewind-setup",
      "status": "completed",
      "started_at": "2026-08-11T23:04:55+0900",
      "summary": "NativeWind 4.2.6(안정판) 도입 — v5 preview 는 var() 컴파일이 lightningcss 방문자 API에서 죽어(Node 24, 1.27~1.32 전부) 탈락. 안정판이 Tailwind v4 를 거부하고 nativewind 는 루트로 호이스팅되므로 배치를 뒤집었다: 루트 tailwindcss 3.4.19(nativewind 용) / packages/app-capacitor 4.3.x(웹 빌드용). 웹 산출물 CSS 는 md5 바이트 동일로 확인. 토큰 공유는 베끼지 않고 파생 — tailwind-v4-axes.cjs 가 웹이 쓰는 tailwindcss/theme.css 를 읽어 실제로 갈리는 세 축(spacing 배수·container·borderRadius)만 v3 theme 으로 바꾸고 tailwind.config.js 가 extend 가 아니라 교체로 얹는다(h-13·h-22·max-w-2xs 가 v3 엔 없는 클래스라 조용히 사라지던 것을 해소). rem 은 웹 루트 폰트와 맞춰 16 으로 못박음(기본 14 면 전 유틸리티 12.5% 축소). babel.config.js 는 NativeWind 프리셋이 필요해 되살렸고 이유를 파일 주석에 기록. metro 는 watchFolders·nodeModulesPaths 를 마친 객체에 마지막으로 withNativeWind 를 씌워 모노레포 해석 보존. jest 는 globalSetup 이 실행당 1회 global.css 를 앱 tailwind.config 로 컴파일 → setup 이 registerCSS 로 주입(nativewind/test 는 JSX 미트랜스파일 + 넘긴 JSX 의 className 만 훑어 미채택). 스냅샷 관례 확정: 테스트 옆 __snapshots__/<파일명>.snap · `expect((await render(<X/>)).toJSON()).toMatchSnapshot()`(RNTL 14 render 는 비동기) · 트리 전체 · 이 스냅샷은 '예전과 같은가'가 아니라 '앞으로 안 바뀌는가'만 답한다고 주석·문서에 명시. 컴포넌트 이동 0. vitest 199파일/3044개 증감 0, jest 209→222",
      "completed_at": "2026-08-11T23:48:51+0900"
    },
    {
      "step": 1,
      "name": "theme-system",
      "status": "completed",
      "started_at": "2026-08-11T23:48:51+0900",
      "summary": "ThemeAppearancePort 해소 — 웹뷰의 <style> 주입 대신 NativeWind vars() 로 38토큰을 렌더 트리에 내린다(buildThemeCss 는 웹 전용으로 유지). 값이 흐르는 방향이 반대라: 포트(rn-theme-appearance)가 구독 가능한 한 칸(src/theme/appearance-store)에 (theme, definition)을 놓고 ThemeProvider 가 useSyncExternalStore 로 읽어 vars 를 얹은 View 로 감싼다 — core 의 features/theme/store 는 무수정. 초기값은 DEFAULT_THEME(웹의 index.css @theme 기본 블록과 같은 역할 — 변수를 못 찾으면 색이 조용히 사라진다). className 을 그대로 쓰는 쪽을 골랐다: tailwind.config 의 색을 값이 아니라 var(--color-*) 로 두고 이름은 job-themes.json 키에서 파생(38 + panel-border + transparent/white/black, v3 기본 팔레트는 교체로 제거, current/inherit 는 RN 무의미해 제외). vars 는 트리를 따라 상속되고 하위 재선언이 서브트리만 덮으므로 .media-scope 가 <MediaScope> 로 그대로 옮겨졌다. ADR-122 는 선택자가 없어 파생 토큰 --color-panel-border 로 — definition.mode 로만 분기(라이트=color-mix(in srgb, border 40%, text), 다크=border 그대로)하고 ADR 본문의 확정값 3개를 테스트가 지킨다; useThemeMode·useScrollIndicatorStyle(ADR-099 자리)도 mode 파생. 막힌 것: @core/lib/theme-registry 가 import.meta.glob 모듈을 물어 Metro/jest/tsc 에서 죽어(__ExpoImportMetaRegistry.glob is not a function) core 수정 없이 앱 수준 치환을 넣었다 — core-shims.js 한 표를 Metro resolveRequest 와 jest moduleNameMapper 가 공유, 대체 구현은 null(ADR-088 정상 경로)이라 색은 흐르고 배경만 없다(혼테일·검은마법사 단색). tsc 용 ImportMeta.glob 앰비언트 선언의 부작용(치환 안 된 glob 모듈은 타입 초록·런타임 사망)은 core 의 glob 모듈 8개 목록을 고정하는 테스트로 막았다. 남는 함정: v3 는 var() 색에 /60 같은 투명도 접미사를 생성하지 않는다(웹 2곳). 컴포넌트 이동 0. vitest 199파일/3044개 증감 0, jest 222→283",
      "completed_at": "2026-08-12T00:20:47+0900"
    },
    {
      "step": 2,
      "name": "navigation",
      "status": "completed",
      "started_at": "2026-08-12T00:20:47+0900",
      "summary": "react-navigation 골격 — 루트 네이티브 스택(탭 레이어 1 + 하위 페이지 11) + bottom-tabs 4, 화면은 전부 자리표시자(4단계 몫). 계획서 §1 17경로를 `routes.ts` 에 **데이터로** 두고 내비게이터가 그 목록에서 파생 — 손으로 적으면 계획서와 두 벌이 되어 빠뜨려도 안 드러난다. 하위 페이지를 탭 **안**이 아니라 **위**에 쌓아 ADR-120 결정 4(탭바가 아래 화면과 한 덩어리로 밀려 나감)를 구조로 성립시켰다(탭 안 스택 + tabBarStyle:none 이면 탭바가 함께 안 밀리고 제자리에서 사라진다). 온보딩 분기는 리다이렉트가 아니라 **화면 목록 자체를 갈아 끼움** — 딥링크가 없어 도달 경로가 없으므로 계약(도달 불가·히스토리 없음)을 더 강하게 만족. 광고 게이트는 탭 `listeners.tabPress` 로: `preventDefault()` 없이 `void maybeShowTabSwitchAd()`(이동을 지연시키지 않음, ADR-090 결정 3) + `isFocused()` 로 같은 탭 연타 제외. `tab-switch-ad.ts` 무수정. **BackGesturePort 는 삭제가 아니라 1/3 구현**: 계획서 예상(전부 삭제)과 달리 `moveToBackground` 는 남는다 — ADR-120 결정 18(종료가 아니라 백그라운드)을 내비게이션 라이브러리가 대신해 주지 않고 RN 기본 폴백이 정확히 그 결정이 거부한 finish 다. Expo 모듈 `app-background`(Kotlin, `moveTaskToBack(true)`·`Queues.MAIN`) 신설. 나머지 둘(setEnabled·addListeners)은 계속 던지되 사유가 *'아직 안 했다'* 가 아니라 *'이제 네이티브 스택이 소유한다'* 라 메시지를 `rn-back-gesture.ts` 가 갖는다. `use-root-back` 은 판정을 `canGoBack()` **하나**로 둬 BackHandler 등록 순서와 무관하게 같은 결과를 낸다. ADR-120 결정 5·6 은 기본값이 아니라 ADR 을 따름 — `animation: 'ios_from_right'`(안드로이드도 iOS 식 슬라이드: 웹뷰 앱이 두 플랫폼에 같은 전환을 그렸다), `gestureResponseDistance` 는 **주지 않음**(28px·35% 가 UIKit 가장자리 인식기를 흉내 낸 값이라 숫자를 얹으면 흉내가 원본을 덮는다). screen-stack·stack-transition 미이식(주석 참조만). **계획서 §1 오류 1건 정정**: `/settings/privacy` → `/settings/about/privacy`(실제 라우트가 about 의 자식, ADR-120 결정 11) — 이 앱 유일의 2단 스택. jest.config 에 `testMatch: ['**/*.test.[jt]s?(x)']` 추가(jest-expo 기본값이 `__tests__` 의 보조 파일까지 스위트로 집어 빨개짐). **육안 확인 대기(코드로 확인 불가)**: 탭바 동반 이동·아래 화면 시차·3버튼과 제스처 수렴·전환 340ms 체감 — 값이 이제 OS/react-native-screens 쪽이라 결정 12 의 실기기 확인은 값 확정이 아니라 채택 판정이 된다. 모달 뒤로가기(결정 18 후반)는 organisms 단계 몫. vitest 199파일/3044개 증감 0, jest 283→318",
      "completed_at": "2026-08-12T01:12:00+0900"
    },
    {
      "step": 3,
      "name": "atoms",
      "status": "completed",
      "started_at": "2026-08-12T01:06:13+0900",
      "summary": "atoms 9개 이식(app-capacitor 원본 무수정 유지) + RN 쪽 계층 의존 테스트. **ADR 확인**: AnimatedMeso — 046 '숫자만 + 실제 공백 문자' 유지, 087 결정 6·7·8·정정 1 은 core use-count-up 과 호출부 identity 키에 살아 훅을 그대로 부름(키는 step 4). Badge/Button/Card/ProgressBar — 094 결정 3(design-system 규정만)·좁은 범위 유지, 단 **결정 4(DOM 1:1 보존)는 Button 에서 성립 불가**(상자/글자를 갈라야 함) 라 그 자리를 새 스냅샷 기준선이 대신함. MapleSweepSpinner/ProgressBar — 061 결정 1 크기 규칙(16px 트레일 링 / 24px+ 스윕)·아래→위 방향·결정 6 h-1.5 단일 프리미티브 유지. ProfitIcon — 066 결정 3 lucide 규격 6항목·결정 4 좌표로 겹침(clipPath·mask 0개) 유지. DifficultyBadge·MapleSpinner 는 ADR 계약 없음. **대체한 className**: ①글자 유틸을 상자에서 분리(RN 은 View→Text 글자 상속이 없어 Pressable 에 남은 text-*/font-* 가 죽는다 — BUTTON_VARIANT_CLASS/BUTTON_VARIANT_TEXT_CLASS 두 벌, primary 라벨은 웹이 상속으로 받던 16px 라 text-base 명시, 호출부의 text-sm 은 textClassName 으로 옮겨야 함) ②hover:* 제거(네이티브에서 NativeWind 가 버림) ③ProgressBar 의 `bg-${tone}` 조립 → 정적 표(Tailwind 스캔에 안 잡혀 색이 조용히 사라짐, 웹은 다른 파일 덕에 우연히 살아 있었음) ④transition-[width] 제거(NativeWind transition 은 Reanimated 워클릿을 타는데 배선이 없어 렌더가 즉시 죽음 — step 7) ⑤space-y-*/divide-y 는 NativeWind 에 없어 호출부가 gap-* 로(step 4~6). **SVG**: className 이 그냥 무시되므로 src/lib/nativewind-interop.ts 에서 cssInterop 등록(style.color→color 프롭 → 자식 currentColor, width/height 도 함께) — 그래서 호출부 API 는 웹과 같다. react-native-svg 가 없어서 갈린 값 셋 — pathLength 미지원이라 실측 둘레 601.3157 에 70/30 비율(mapleLeafPath.ts), 그라디언트 정지점이 currentColor 를 못 받아(경고 후 gradient 가 빈다) MapleSweepSpinner 의 띠를 'fill=currentColor + 흰색 알파 램프 마스크'로 분리, clipPathUnits 는 RN 의 유일한 동작이라 삭제. **모션 미구현 2개**: MapleSpinner(maple-trail)·MapleSweepSpinner(maple-sweep) 는 정적 0프레임이고 step 7 몫(주석·문서·테스트에 명시). 계획서는 AnimatedMeso 를 골격 대상으로 적었으나 **전제가 틀렸다** — 카운트업은 CSS 가 아니라 rAF 훅이라 모션이 그대로 돈다. 반대로 MapleSpinner 가 @keyframes 대상이다. motion-reduce 의 RN 짝(AccessibilityInfo)은 아직 없고 step 7 이 함께 처리해야 한다. 신규 의존성 2개(react-native-svg 15.15.4 · expo-linear-gradient 57 — 둘 다 expo install). 계층 테스트는 '네 계층 전부' 대신 '있는 계층은 아래에서부터 끊기지 않는다'(prefix)로 적어 step 4~6 이 도착해도 손댈 것이 없게 했고, 위반·계층 건너뛰기 양쪽으로 판별력을 확인했다. RN 트리 스냅샷 12장 신규(전부 ThemeProvider 안에서 — 밖이면 색이 빠진 트리를 기준선으로 굳힌다). core·app-capacitor 무수정. vitest 199파일/3044개 증감 0, jest 318→379. **미검증**: 화면이 없어 눈으로 본 것이 없다 — Text 의 borderRadius/padding(Badge)·마스크 페이드·그라디언트 뱃지·Hermes toLocaleString 천 단위 구분자는 실기기 확인이 남았고, expo export 는 기본적으로 atoms 를 그래프에 안 넣으므로 임시 import 로 Metro 해석만 따로 확인했다(1475→1602 모듈, 에러 0).",
      "completed_at": "2026-08-12T01:55:00+0900"
    },
    {
      "step": 4,
      "name": "molecules",
      "status": "pending"
    },
    {
      "step": 5,
      "name": "organisms",
      "status": "pending"
    },
    {
      "step": 6,
      "name": "templates",
      "status": "pending"
    },
    {
      "step": 7,
      "name": "animations",
      "status": "pending"
    }
  ],
  "created_at": "2026-08-11T23:04:55+0900"
}