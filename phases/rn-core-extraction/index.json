{
  "project": "maple-routine",
  "phase": "rn-core-extraction",
  "steps": [
    {
      "step": 0,
      "name": "workspace-setup",
      "status": "completed",
      "started_at": "2026-08-11T15:37:24+0900",
      "summary": "npm workspaces(`packages/*`) 도입 + `packages/core` 뼈대 3파일(package.json `@maple-routine/core`(main/exports 가 TS 소스 직행, 빌드 단계 없음) · src/index.ts(빈 진입점 — 배럴 아님) · tsconfig.json(tsconfig.app.json 상속, buildinfo 충돌 회피용 override)) 생성. `@core/*` → `packages/core/src/*` alias 를 tsconfig.app.json `paths` · vite.config.ts `resolve.alias`(정규식 find) · tsconfig.json `references` 세 곳에 배선하고 tsc·vitest 양쪽에서 실제 해석되는 것을 임시 프로브로 확인 후 삭제. eslint(`eslint .`)·vitest 는 설정 변경 없이 packages/ 를 이미 커버함을 실측. 파일 이동 0건 — 테스트 199파일/3044개로 이전과 동일(동작 변화 0).",
      "completed_at": "2026-08-11T15:42:34+0900"
    },
    {
      "step": 1,
      "name": "core-pure",
      "status": "completed",
      "started_at": "2026-08-11T15:42:35+0900",
      "summary": "`git mv` 로 `src/{data,types,nexon}` → `packages/core/src/` 이동(47파일 = data 30 + types 9 + nexon 8, 각 `__tests__` 포함 · 전부 R 로 기록돼 `git log --follow` 유지). **JSON 게임 데이터 17개는 rename 유사도 100%로 내용 변경 0**([[ADR-006]]). 딸려 옮긴 것 하나 — `src/assets/guide/`(21 webp + README): `data/feature-guides/**` 만이 유일한 소비자이고, 함께 옮기면 상대 경로(`../../../assets/guide/…`)가 그대로 맞아 안내 파일 import 를 한 줄도 안 고치면서 core→app 의존이 생기지 않는다. src/ 127파일의 상대 import 를 `@core/*` 로 일괄 치환(`vi.mock`·`importActual` 의 문자열 지정자 12건 포함), 옮긴 파일 내부 상호 참조는 디렉터리를 통째로 옮겨 그대로 해석됨(무변경). scripts 3개 경로 수정 — `theme-gen.ts`(`../packages/core/src/data/job-themes.json`·`../packages/core/src/types/theme`, `src/lib/theme-derive` 는 step 2 대상이라 유지) · `publish-live-update.mjs`(`release-notes.ts` import + 실패 메시지 경로) · `scripts/__tests__/publish-live-update.test.mjs`(단계 지시에 없던 세 번째 참조 — 안 고치면 테스트가 깨진다). `tsconfig.node.json` 에 `@core/*` paths 추가(그 프로젝트가 `scripts/theme-gen.ts` → `src/lib/theme-derive.ts` 를 끌어오는데 그 파일이 `@core/types/theme` 를 쓴다). **남긴 한시적 core→app 참조 4건**(전부 테스트, 주석으로 표시): `data/__tests__/job-themes.test.ts` → `src/lib/{color,theme-derive}`(step 2 에서 해소) · `boss-portraits`·`item-icons`·`boss-ring-boxes` 테스트의 `src/assets/{bosses,items}` 경로(그 에셋을 읽는 `lib/*-icons` 가 core 로 올 때 정리). 옮긴 파일 안의 낡은 경로 문구도 함께 갱신(`src/assets/guide`·`src/data/feature-guides`·`src/data/release-notes` → `packages/core/…`, 19파일 주석·README). AC — build 성공 · 테스트 **199파일/3044개** 통과(step 0 과 동일, 증감 0) · lint 0 errors/17 warnings(baseline 동일) · `vite-node scripts/theme-gen.ts` 사용법 출력 + `--existing 머쉬맘` 실제 토큰 출력 · `publish-live-update.mjs` Node 타입 스트리핑 import 무에러 · `grep -rE \"document\\.|window\\.|@capacitor\" packages/core/src` 빈 출력 · 청크 구성 무변화(`screenChunk` 는 `/src/app/` 만 보고 옮긴 파일은 이전에도 매칭되지 않았다).",
      "completed_at": "2026-08-11T15:51:30+0900"
    },
    {
      "step": 2,
      "name": "core-lib",
      "status": "completed",
      "started_at": "2026-08-11T15:51:30+0900",
      "completed_at": "2026-08-11T16:07:00+0900",
      "summary": "`git mv` 로 `src/lib` 41개(+테스트) → `packages/core/src/lib/` 이동, 8개 잔류(stack-transition · use-stack-location · use-swipe-back · use-pull-to-refresh · use-body-scroll-lock · use-measured-height · use-delayed = DOM 의존 [[ADR-120]] 삭제 대상 / use-system-back = Capacitor 직접 import, step 4 대상). 41+8=49 로 회계 일치. **에셋도 함께 이동** — `src/assets/{bosses,items,maps,themes,worlds,drop-effect}` → `packages/core/src/assets/`: `lib/{boss-icons,item-icons,theme-backgrounds}` 가 유일한 소비자라 함께 옮겨야 상대 경로가 유지되고, 덤으로 step 1 이 남긴 임시 core→app 참조 4건이 전부 해소됐다(현재 core→app 역참조 0). src/ 92파일의 import 를 `@core/lib/*` 로 갱신. `scripts/theme-gen.ts` 의 마지막 잔여 경로도 `@core/lib/theme-derive` 로 수정(step 1 에서 미뤄둔 것). AC — build 성공 · 테스트 **199파일/3044개 통과**(step 0·1 과 동일, 증감 0) · lint 0 errors/17 warnings(baseline 동일) · `grep -rE \"document\\.|window\\.|@capacitor\" packages/core/src` 빈 출력 · core→app 역참조 빈 출력. **주의(다음 step 참고)**: 이 step 은 harness 실행이 외부 종료되어 커밋 직전에 멈췄고, AC·아키텍처 체크리스트를 수동 재검증한 뒤 완료 처리했다. 작업 내용 자체는 step 파일 지시대로다."
    },
    {
      "step": 3,
      "name": "storage-ports",
      "status": "completed",
      "started_at": "2026-08-11T16:17:30+0900",
      "completed_at": "2026-08-11T16:36:14+0900",
      "summary": "의존 방향만 뒤집었다(파일 이동 0건 — 이동은 step 5). **포트** `src/storage/ports.ts`: `PreferencesPort`(get/set/remove/**keys** — keys 는 cache-data 의 [[ADR-052]]·[[ADR-058]] 삭제 범위·용량 계산이 필수로 쓴다) · `SqlitePort`(isWebPlatform/initWebStore/isConnection/closeConnection/createConnection) + `SqliteDbConnection`(open/execute/query/run — db.ts·boss-*.ts 가 실제로 쓰는 4연산뿐). 주입은 모듈 수준 setter(`setPreferencesPort`/`setSqlitePort`, `__resetStoragePortsForTest`)이고 **미주입 접근은 조용한 no-op 이 아니라 throw**. 호출부 편의를 위해 `preferences` 파사드(매 호출 포트 재조회 — 주입이 모듈 평가 순서에 안 묶인다). **어댑터** `src/storage/adapters/capacitor-preferences.ts`·`capacitor-sqlite.ts`(`SQLiteConnection` 싱글턴·`readonly:false` 플래그·웹 플랫폼 판정만 여기 남고, `'no-encryption'`·버전 1 은 db.ts 가 넘긴다 — migration/data.md 결정 2 의 계약이 core 쪽에 남아야 해서다). **주입 지점** `src/main.tsx` 최상단(저장소를 처음 만지는 `checkOnBoot()` 보다 앞). 14개 파일이 포트를 경유하게 바뀌었고 **함수 본문 로직은 무변경** — ensureColumn([[ADR-069]] 결정 1)·메이린 키 이관 UPDATE 2건·타임아웃 10초/5초([[ADR-117]] 결정 5)·stale 커넥션 복구([[ADR-050]] 결정 2)·KEEP_KEYS([[ADR-052]])·읽기-수정-쓰기 락 3종 그대로. **외부 시그니처 변화 1건뿐**: `getBossProfitDb(): Promise<SQLiteDBConnection>` → `Promise<SqliteDbConnection>`(타입 **이름**만 — 구조(open/execute/query/run)는 동일하고, `features/` 는 이 심볼을 import 하지 않는다. `features/settings/cache-data`·`native/live-update` 는 `closeBossProfitDb` 만 쓴다). 나머지 export 는 `git diff | grep '^-export'` 로 무변화 확인. **테스트는 mock 지점만 이동**(검증 내용 불변) — 공용 인메모리 헬퍼 `src/storage/__tests__/fake-preferences.ts`(vi.fn 4종이라 인자 단언·mockRejectedValueOnce 그대로) 로 storage 12파일 + 이 mock 에 얹혀 있던 feature 테스트 4파일 전환, db.test.ts 는 플러그인 목 → 가짜 SqlitePort(단언은 `('boss_profit','no-encryption',1)` 로 옮겨 `'no-encryption'` 검증 유지). 지운 단언은 `sqliteConnectionCtorMock` 싱글턴 1줄뿐(어댑터 소유가 됐고 새 테스트 파일은 test-count AC 때문에 추가하지 않음)이고, `retrieveConnection` 미사용은 포트 표면에 그 연산이 없어 구조적으로 보장된다. `vitest.setup.ts` 에 인메모리 포트 기본 주입 — 예전엔 `@capacitor/preferences` 가 어디서나 그냥 동작했고 그 자리를 메운다(없으면 앱을 렌더만 하는 테스트 56건이 부팅 시 drop-effect 복원에서 미주입 에러를 던졌다). 실제로 포트가 없어 깨진 테스트 2건(issue78-repro·BossDropSheet)은 각자 beforeEach 에서 설치. AC — build 성공 · 테스트 **199파일/3044개 통과 + Errors 0**(baseline 과 완전 동일) · lint 0 errors/17 warnings(baseline 동일) · `grep -rn '@capacitor' src/storage`(테스트·adapters 제외) 빈 출력 · `features/` **소스 수정 0건**(수정된 features 파일은 전부 `__tests__`)."
    },
    {
      "step": 4,
      "name": "native-ports",
      "status": "completed",
      "started_at": "2026-08-11T16:36:14+0900",
      "completed_at": "2026-08-11T16:55:40+0900",
      "summary": "step 3 과 같은 방식으로 의존 방향만 뒤집었다(파일 이동 0건). **포트** `src/native/ports.ts` — 어댑터별 9개(`AdsPort` · `SplashScreenPort` · `StatusBarPort` · `SystemBarsPort` · `KeyboardPort` · `NotificationsPort` · `BackGesturePort` · `HuntingTimerPort` · `LiveUpdatePort`) + 딸린 타입 6개(`BackProgressEvent`·`BackGestureHandlers`·`LocalNotificationRequest`·`HuntingTimerState`·`NetworkType`·live-update HTTP 3종). **import 0줄짜리 자립 파일**이고 `Capacitor`·`getPlatform`·`web`/`android`/`ios` 가 산문 주석 말고는 한 글자도 없다. 주입은 step 3 과 같은 모듈 수준 setter/getter + **미주입 접근은 throw**(`__resetNativePortsForTest`), 다만 포트가 아홉이라 그 계약을 손으로 아홉 번 베끼는 대신 내부 `createPortSlot()` 하나로 모았다 — 밖으로 나가는 표면(`setXPort`/`getXPort`)은 step 3 과 동일하다. **웹 no-op 가드는 전부 어댑터로** — `src/native/adapters/` 9파일 + `index.ts`(`installCapacitorNativePorts()`). 포트가 플랫폼을 묻지 않게 하려고 **결과로 능력을 말하게** 했다: `prepareInterstitial()/showInterstitial(): Promise<boolean>`(옛 `adId() === null` 게이트 자리) · `LiveUpdatePort.isSupported(): boolean`(**동기** — 매니페스트를 받기 *전에* 판정해야 `checkForLiveUpdate` 의 web→`unsupported` 가 네트워크 0회로 유지된다. 비동기 `getCurrent()` 로 갈음하면 http↔current 순서가 뒤집힌다). **DOM 커버 두 장(`#boot-cover`·`[data-splash-cover]`)도 스플래시 어댑터로 옮겼다** — `showSplashScreen` 의 웹 가드가 네이티브 호출만이 아니라 **커버 생성까지** 막고 있어서(테스트가 그 계약을 단언한다) 가드만 떼어낼 수 없었고, 애초에 그 두 장은 웹뷰 구현이다(RN 에는 없다). 그래서 `native/splash-screen.ts`·`status-bar`·`system-bars`·`keyboard`·`notifications`·`back-gesture`·`hunting-timer` 는 얇은 위임이 되고, **정책을 쥔 둘만 로직이 남았다** — `ads.ts`(`isLoaded` 상태·에러 삼킴·[[ADR-090]] ID 표와 `shouldUseTestAds`/`resolveInterstitialAdId` **무변경**, 어댑터가 그 순수 함수를 불러 쓴다 → 실 ID 게이트가 플랫폼 구현마다 복제되지 않는다) · `live-update.ts`(매니페스트 파싱·`isNewerVersion`·6상태 판정·캐시 우회 파라미터([[ADR-026]])·**닫기 → 커버 → set 순서([[ADR-117]] 결정 1)** 전부 그대로, 어댑터는 @capgo 호출과 `APP_ID`/`APP_STORE_ID` 만 가져갔다 — [[ADR-127]] 결정 7 대로 프로토콜은 손대지 않았다). `src/lib/use-system-back.ts` 는 `Capacitor.getPlatform() !== 'android'` 조기 return 을 지웠다(어댑터가 이미 no-op 해제 함수를 돌려줘 동작 동일). **외부 시그니처 변화 0건** — `git show HEAD:<f> | grep '^export'` 대조로 9파일 전수 확인했고, 함수는 전부 바이트 동일하며 다른 것은 타입 6개가 `ports.ts` 로 옮겨가 **같은 이름으로 재수출**되는 것뿐(모양 동일). **테스트는 mock 지점이 아니라 주입만 추가**(단언 한 줄도 안 고쳤다) — 8개 native 테스트가 각자 실제 Capacitor 어댑터를 주입해 「어댑터 함수 → 어댑터 → 플러그인」을 여전히 한 단위로 검사한다(`ads.test.ts` 는 순수 함수·설정 파일만 봐서 무변경). `vitest.setup.ts` 에 `installNoopNativePorts()` 기본 주입 — 옛 어댑터들이 테스트 환경(플랫폼 `web`)에서 no-op 이던 그 동작을 재현한다(없으면 App 을 렌더하는 테스트가 스플래시 해제·인셋·시스템 뒤로가기에서 미주입 에러를 던진다). 주입 지점은 `src/main.tsx` 의 저장소 포트 **바로 다음**, `checkOnBoot()` 앞([[ADR-117]] 실패 경로가 스플래시를 건드린다). AC — build 성공 · 테스트 **199파일/3044개 통과 + Errors 0**(baseline 과 동일) · lint 0 errors/17 warnings(baseline 동일) · `grep -rn '@capacitor' src/native`(테스트·adapters 제외) 빈 출력 · `grep -rn '@capacitor' src/lib`(테스트 제외) 빈 출력 · `git diff --stat src/features` **완전히 빈 출력**(테스트조차 안 건드렸다). **웹 부팅 확인**: 브라우저 자동화 도구가 없어 임시 프로브(`web-boot-probe.test.ts`, 목 없이 실제 Capacitor 패키지 + 플랫폼 `web`)로 부팅 호출 전부 무예외 · 리로드 커버 미생성 · 광고 미준비 · 라이브 업데이트 `unsupported`(네트워크 0회)를 실측하고 삭제했으며, `npm run dev` 서버가 `main.tsx`·`adapters/index.ts`·`capacitor-ads.ts`·`ports.ts` 를 전부 200 으로 변환·서빙하고 로그에 에러 0건인 것을 확인했다. **미검증**: 실제 브라우저 런타임 실행과 실기기 동작(step 0 게이트의 '실기기 동작 확인'은 이 phase 끝에 남는다)."
    },
    {
      "step": 5,
      "name": "core-storage",
      "status": "completed",
      "started_at": "2026-08-11T16:55:40+0900",
      "completed_at": "2026-08-11T17:02:00+0900",
      "summary": "`git mv` 로 **41파일**을 `packages/core/src/storage/` 로 옮겼다 — 로직 21(`ports.ts`·`index.ts` 포함) + `sqlite/db.ts` + 테스트 19(`__tests__` 18 + `sqlite/__tests__/db.test.ts`). git 이 전부 rename 으로 기록(R 38 · RM 3)해 `--follow` 가 유지되고, **RM 3건의 실제 변경은 7+/2- · 1+/1- · 3+/3- 이 전부**(나머지 38 파일은 0줄 변경). **경계는 step 지시대로 갈렸다** — `src/storage/adapters/`(capacitor-preferences·capacitor-sqlite) 는 `src/` 에 남고, 그 둘의 `'../ports'` 만 `'@core/storage/ports'` 로 바뀌었다. 즉 **인터페이스는 core, 구현은 앱**이고 `src/main.tsx` 의 주입 순서(포트 → `checkOnBoot()`)는 손대지 않았다. import 갱신은 `src/` **63파일**(+`vitest.setup.ts`) 의 `'../../storage/*'`·`'./storage/*'`·`'./src/storage/*'` → `'@core/storage/*'` 일괄 치환이고 `adapters/` 경로는 정규식에서 제외해 `main.tsx` 의 어댑터 import 2줄이 그대로 남았다. **core 내부는 `@core/*` 유지** — 옮긴 파일의 `@core/{types,lib}` 참조 16건은 기존 core 35파일이 이미 쓰는 방식이라 그대로 뒀다(상대 경로로 되돌리면 step 1·2 가 세운 관례와 갈린다). **남긴 한시적 core→app 참조 1건**(주석으로 표시): `storage/__tests__/cache-data.test.ts` 가 `clearCacheDataAndReload`(`src/features/settings/cache-data`) 와 그 짝인 `vi.mock('src/native/splash-screen')` 을 `../../../../../src/` 로 가리킨다 — 이 파일은 캐시 삭제 **범위**([[ADR-052]]·[[ADR-058]], core 쪽)와 **순서**(닫기 → 커버 → 리로드, [[ADR-117]] 결정 8, 앱 쪽)를 한 벌로 검사하는데 쪼개면 테스트 파일이 200개가 되어 AC(199파일 동일)에 걸린다. step 6 에서 `features/` 가 core 로 오면 상대 경로로 복귀한다. 그 밖에 **주석 4줄의 문구만** 고쳤다 — core 오염 grep 이 산문까지 잡으므로 `vi.mock('@capacitor/preferences')`·`vi.mock('@capacitor-community/sqlite')`·`어댑터(adapters/capacitor-sqlite)`·`window.location.reload` 를 뜻이 같은 표현으로 바꿨다(코드 무관). AC — build 성공 · 테스트 **199파일/3044개 통과**(step 0~4 와 동일, 증감 0) · lint **0 errors/17 warnings**(baseline 동일) · `grep -rn '@capacitor' packages/core/src` · `grep -rn 'adapters/' packages/core/src/storage` · `grep -rE 'document\\.|window\\.|matchMedia' packages/core/src/storage` **셋 다 빈 출력**(step 3 의 역전이 완전했다는 뜻 — 남겨 둔 파일 0개). 부수 확인: core storage 의 런타임 외부 import 0건(`node:fs`·`vitest` 는 db.test.ts 전용), `docs/` 는 미변경(ADR 본문의 `src/storage/...` 는 그 결정 시점의 기록이라 step 1~4 와 같이 그대로 뒀다). **미검증**: 실기기 동작(step 0 게이트 항목으로 이 phase 끝에 남는다)."
    },
    {
      "step": 6,
      "name": "core-features",
      "status": "completed",
      "started_at": "2026-08-11T17:02:00+0900",
      "completed_at": "2026-08-11T17:35:00+0900",
      "summary": "`git mv` 로 **features 14모듈 75파일**(소스 39 + 테스트 36)을 `packages/core/src/features/` 로 옮겼다. `screen-stack`(2파일)은 지시대로 `src/` 잔류. **src 에 남긴 테스트 2개** — `theme/__tests__/store.test.ts`(문서에 실제로 칠해지는지가 검사 대상이라 DOM 단언 덩어리) · `boss-profit/__tests__/issue78-repro.test.tsx`(`app/boss-profit/BossProfitScreen` 을 렌더하고 `document.body` 를 읽는다): 둘 다 core 오염 grep 에 걸리고, 애초에 검사 대상이 앱 쪽이다. **계획 밖 결정 하나 — `src/native/` 델리게이트도 core 로 옮겼다**(ports.ts + 9개 + `hunting-timer/`, adapters·__tests__ 는 `src/` 잔류). step 지시의 '순수 39개' 판정식이 통과시킨 파일 중 **4개가 `src/native/*` 를 import 한다**(`ads/tab-switch-ad` · `live-update/store` · `settings/cache-data` · `theme/store`) — 그대로 옮기면 **제품 코드에 core→app 참조**가 생기고(step 7 에서 `src/`가 `packages/app-capacitor` 가 되는 순간 패키지 순환), 그 4개를 남기면 features 를 쪼개게 된다. 경계는 step 5 와 같다 — **인터페이스·플랫폼 무관 로직은 core, 구현은 app**(migration/README 0단계의 \"core 는 storage/·native/ 의 인터페이스만\"). 네이티브 테스트가 `src/` 에 남는 이유는 그것들이 `vi.mock('@capacitor/*')` 로 「델리게이트 → 어댑터 → 플러그인」을 한 단위로 검사하기 때문(core 로 옮기면 AC grep 위반). **포트 2개 신설** — ① `ColorSchemePort` 는 지시대로 추가하되 **`get()` 만**: `subscribe()` 는 부를 곳이 없고([[ADR-009]] 2026-07-14 — 실행 중 OS 변경 반영은 범위 밖) 구현마다 죽은 코드가 된다 ② **`ThemeAppearancePort` 는 지시에 없던 추가**다 — 지시가 짚은 `matchMedia` 는 `theme/store.ts` DOM 의 일부일 뿐이고 나머지(스타일 태그 주입·`data-theme`/`data-mode`·`color-scheme`·`scrollbar-color`)를 그대로 두면 core 오염 AC 를 원리적으로 통과할 수 없다. 그 DOM 뭉치는 `src/native/adapters/capacitor-theme-appearance.ts` 로 나갔고(웹뷰 구현이라는 점에서 `capacitor-splash-screen` 의 DOM 커버와 같은 자리), **상태바·내비바 명암은 store 에 남겼다**(이미 자기 포트가 있고 '다크면 밝은 글리프'는 플랫폼 무관). 두 어댑터는 `installCapacitorNativePorts()` 에 편입돼 **주입 지점은 늘지 않는다**. **`onboarding/store.ts` 는 DOM 을 쓰지 않는다** — 판정식이 잡은 건 주석 한 줄(`window.location` 언급)이라 뜻이 같은 문구로 바꾸고 그대로 core 로 옮겼다(step 5 가 쓴 처리와 동일). 억지 추상화 0건. **테스트는 단언을 한 줄도 안 고쳤다** — theme 테스트는 `beforeEach` 에 진짜 웹뷰 어댑터 2개를 주입하는 2줄만 늘었고(전역 no-op 기본값을 덮는다), `vitest.setup.ts` 의 no-op 기본값에 두 포트가 추가됐다(색 구성표는 `light`, 테마 적용은 무동작 — 옛 `matchMedia` 부재 폴백과 같은 값). step 5 가 남긴 한시적 core→app 참조 1건(`storage/__tests__/cache-data.test.ts`)은 `features/settings/cache-data` 가 core 로 오면서 **상대 경로로 복귀해 해소**됐다 — 현재 core→app 참조 **0**. 부수 수정: `scripts/__tests__/publish-live-update.test.mjs` 의 `src/native/live-update.ts` 직접 import(단계 지시 밖 — 안 고치면 테스트가 깨진다)·`capacitor-hunting-timer` 의 웹 폴백 동적 import 경로·옮긴 파일 주석의 낡은 경로 2건. **AC** — build 성공 · 테스트 **199파일/3044개 통과**(step 0~5 와 동일, 증감 0) · lint **0 errors/17 warnings**(baseline 동일) · `npm run dev` 부팅 + 옮긴 모듈 6종 200 응답·에러 0 · core 오염 grep 3종(`@capacitor` · DOM · `screen-stack`) **전부 빈 출력**. 부수 확인: 청크 **이름·개수 동일**(index 246.32→246.96 kB, 테마 어댑터 배선분) · core features 의 외부 import 는 `@core/*` 와 react/zustand/lucide-react 뿐. **관측 2건** — ① `StackScreen` 스와이프백 타이밍 테스트가 전체 실행 중 간헐 실패하는데, **HEAD 워크트리에서도 8회 중 2회 실패**해 이 step 과 무관한 **기존 플레이키**임을 확인했다(내 트리 12회 중 2회로 같은 비율) ② `features/theme/store.ts` 만 git 이 rename 으로 못 잡았다(내용 절반이 어댑터로 빠져 유사도 미달 → A+D 기록). **미검증**: 실기기 동작 · 브라우저 런타임 실행(자동화 도구가 없어 dev 서버 변환·서빙까지만 확인 — step 4 와 같은 한계). **남은 부채**: `features/toast/store.ts` 가 `lucide-react` 를 import 한다(RN 에서 `lucide-react-native` 로 갈아끼울 자리 — migration/README 의존성 표에 이미 있고 DOM·Capacitor 가 아니라 AC 는 통과)."
    },
    {
      "step": 7,
      "name": "app-capacitor",
      "status": "pending"
    },
    {
      "step": 8,
      "name": "rn-scaffold",
      "status": "pending"
    }
  ],
  "created_at": "2026-08-11T15:37:24+0900"
}