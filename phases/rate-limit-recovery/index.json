{
  "project": "메이플 루틴 (Maple Routine)",
  "phase": "rate-limit-recovery",
  "steps": [
    {
      "step": 0,
      "name": "docs-adr",
      "status": "completed",
      "started_at": "2026-08-08T19:23:54+0900",
      "summary": "ADR-116 신설(설계 확정·구현 전) — 결정 1: 429는 ADR-115 결정 10의 알림 구조(닫을 수 없는 모달→확인→키 입력 화면+apiKey만 삭제)를 그대로 타고 문구만 갈린다(apiKeyInvalidNotice:boolean → apiKeyNotice:'invalid'|'rateLimited'|null / '호출 한도를 초과했습니다'+'서비스 단계 키로 다시 입력해주세요.') · 결정 2: 알림 가드를 status!=='completed'에서 'awaitingApiKey·verifyingApiKey면 알리지 않는다'로(ADR-115 결정 6 두 번째 정정 — #176 잠금이 온보딩 안에서 나기 때문, 동시 실패 1모달·먼저 뜬 원인 유지·WeakSet 유지, 배선은 429만 추가하고 온보딩 401은 폼 실패로 유지) · 결정 3: 003이 아닌 모든 프로브 실패는 '판정 불가'이고 판정 불가면 계정 목록을 그리지 않는다(ADR-113 결정 7 정정, 원칙·003 영구·isSettled 의미는 유지) · 결정 4: ErrorState가 그려지는 자리는 원인과 무관하게 진행 경로를 하나 이상 가진다(ADR-114 결정 2를 ErrorState 자리에서만 정정, 스탈 배너·토스트는 유지, 경로가 버튼일 필요는 없다 — 여기선 모달) · 결정 5: fan-out 동시성 캡 기각으로 429 발생은 한 건도 줄지 않는다. ADR.md에 116행 append + 113·114행에 정정 구절 덧붙임. error-resilience.md(원칙 3 규칙·429/401 표 행·history 2줄) · onboarding.md(프로브 판정 불가·#176 모달 경로·원인 둘·history 1줄) · content-scheduler.md·settings.md의 429 서술 갱신. src 변경 0줄, 테스트 2,627개/176파일 기준선 유지, build 성공",
      "completed_at": "2026-08-08T19:34:50+0900"
    },
    {
      "step": 1,
      "name": "notice-generalize",
      "status": "completed",
      "started_at": "2026-08-08T19:34:50+0900",
      "failed_at": "2026-08-08T19:40:19+0900",
      "summary": "알림 상태를 원인별로 넓히고 가드를 교체했다. state.ts: ApiKeyNoticeKind='invalid'|'rateLimited' 신설, apiKeyInvalidNotice:boolean → apiKeyNotice:ApiKeyNoticeKind|null, 이벤트 API_KEY_INVALID_NOTICED → API_KEY_NOTICED{kind}, 리듀서는 status를 안 바꾸고 **이미 알림이 있으면 같은 state 객체를 그대로 반환**(먼저 뜬 원인 유지). store.ts: noticeApiKeyInvalid→noticeApiKeyIssue(kind)(동기), confirmApiKeyInvalid→confirmApiKeyNotice(RESET+removeApiKey, 원인 무관 동일). **가드가 핵심** — 옛 `status !== 'completed'`를 버리고 `apiKeyNotice !== null` + `status가 awaitingApiKey·verifyingApiKey면 no-op`으로 바꿨다(ADR-116 결정 2 — 옛 조건으로는 selectingContentCharacters에서 나는 #176 잠금을 구할 수 없었다). 호출부는 컴파일 유지용 최소 치환(use-api-key-invalidation·settings/store가 noticeApiKeyIssue('invalid')). **실행 중 step이 status 갱신에 실패해 error로 남았고, 남은 뒷정리를 사람이 마쳤다**: 테스트 3파일의 스탈 mock 이름(noticeApiKeyInvalid/confirmApiKeyInvalid/apiKeyInvalidNotice) 교체 + route-splitting 테스트의 onboarding store mock에 apiKeyNotice:null 추가. 그 실패가 **실제 결함 하나를 드러냈다** — 모달의 `apiKeyNotice === null` 검사가 부분 모킹에서 undefined를 받아 **모든 라우트 위에 닫을 수 없는 모달이 떴다**(route-splitting 5건 실패). 차단 UI는 '켜라고 명시했을 때만' 켜지도록 falsy 검사로 바꾸고 근거를 주석에 남겼다. 테스트 2,636개/176파일 통과(baseline 2,627 대비 +9)·build 성공·lint 0 errors/17 warnings."
    },
    {
      "step": 2,
      "name": "modal-copy",
      "status": "completed",
      "started_at": "2026-08-08T21:05:49+0900",
      "summary": "ApiKeyInvalidModal → **ApiKeyNoticeModal**(파일·테스트·App.tsx import/렌더 함께 개명, git mv 로 이력 보존). 문구를 ADR-116 결정 1 표대로 `NOTICE_COPY: Record<ApiKeyNoticeKind, {icon,title,body}>` 로 갈랐다 — invalid: '**API 키가 더 이상 유효하지 않습니다**' / '**키 입력 화면으로 이동합니다.**', rateLimited: '**호출 한도를 초과했습니다**' / '**서비스 단계 키로 다시 입력해주세요.**'. 429 본문이 토스트 한 줄과 갈리는 이유(모달은 truncate 가 아니라 줄바꿈이 되므로 처방까지 담는다 — ADR-114 결정 4)를 주석에 남겼다. 아이콘만 원인을 가리키고 톤은 둘 다 error 유지 — 429 도 어미 규칙상 실패(`~습니다`)이고 error-resilience.md 실패 표에 함께 서 있어서다; `Gauge`(계기판)를 골랐고 TimerOff 계열은 '시간이 지나면 풀린다'가 읽혀 처방(키 단계 확인)과 어긋나 배제했다(근거 주석). 버튼은 두 원인 모두 `확인` 하나, onClose no-op 유지(닫기·취소 없음). testId 는 api-key-invalid-overlay → **api-key-notice-overlay**. 테스트는 describe.each 로 두 kind × 3(문구·확인 1회 호출·오버레이 클릭해도 안 닫히고 버튼 1개) + null 케이스 = 7개. **판별력 확인**: rateLimited 분기를 invalid 문구로 바꾸자 rateLimited 테스트 2건이 실제로 실패했고 되돌렸다. 감지 배선은 하지 않았다(step 3) — 이 step 에서 429 는 아직 모달을 띄우지 않는다. 테스트 2,639개/176파일 통과(step 1 기준선 2,636 대비 +3)·build 성공·lint 0 errors/17 warnings(baseline 동일). src 변경은 src/app/ 2파일 + src/App.tsx 2줄(개명 필수분)뿐이다.",
      "completed_at": "2026-08-08T21:09:38+0900"
    },
    {
      "step": 3,
      "name": "rate-limit-wiring",
      "status": "completed",
      "started_at": "2026-08-08T21:09:38+0900",
      "summary": "429를 감지 지점에 배선했다. 훅을 `use-api-key-invalidation.ts` → **`use-api-key-notice.ts`**(`useApiKeyNotice`)로 git mv 하고 원인 둘을 `NOTICE_KIND` 매핑 한 곳으로 넓혔다(invalidApiKey→'invalid' · rateLimited→'rateLimited', 나머지는 무시). **`routedErrors` WeakSet 유지** — 429도 확인하면 키를 지우므로 스탈 error 객체 재마운트 함정이 똑같이 있다(주석에 429를 포함해 갱신). `use-sync-error-toast`: 429를 401과 같이 조기 반환시켜 **토스트를 뗐다**(모달이 원인+처방을 말하므로 같은 사실을 두 번 말하지 않는다) · `characterUnavailable`·`network`는 무변경 · `format.ts`의 rateLimited case는 그대로(assertNever 소진 가드). 화면: ContentScreen·BossScreen은 이름 교체만으로 로스터 429가 따라왔고(전역 동기화는 토스트 훅 경유), BossProfitScreen은 로스터가 없어 전역 경로 하나뿐이다. **`ContentCharacterStep`에 신규 배선 — #176의 실제 해제**. 넘기는 것은 `rosterError?.kind === 'rateLimited' ? rosterError : null`로 **429뿐**이다(ADR-116 결정 2 — 이 자리의 401은 '방금 입력한 키가 나쁘다'라 성질이 달라 계속 폼/ErrorState 재시도로 남는다). ADR-115 시절 미배선 근거('온보딩 중엔 status가 completed가 아니라 어차피 no-op')가 step 1 가드 변경으로 무효가 된 이력을 주석에 남겼다(되돌림 방지). settings `refreshAccounts`: 429도 `noticeApiKeyIssue('rateLimited')` + `RESET`(인라인 카드는 '다시 시도'밖에 못 주는데 눌러도 또 429였다), `network`·`storageWriteFailed`는 인라인 유지, **`changeApiKey`는 미배선 유지**(ADR-115 결정 8 — 기존 401 가드 테스트 유지 + 429 미호출 단언 추가). **판별력 확인**: 훅에서 rateLimited 분기를 지우자 **정확히 새 케이스 10건만** 실패하고(6파일) network·characterUnavailable 회귀는 전부 통과 — 되돌렸다. **#176 사슬 수동 추적: 끊긴 고리 없음** — 로스터 429 → toScheduleSyncError → useApiKeyNotice → 가드 통과(selectingContentCharacters는 키 입력 화면이 아니다, step 1 테스트가 고정) → apiKeyNotice='rateLimited' → ApiKeyNoticeModal(App.tsx의 라우트 **밖**이라 온보딩 위에도 뜬다) → 확인 → RESET+removeApiKey → awaitingApiKey → 키 입력 폼. **설정 계정 변경 모달(AccountFlowStatus)도 같은 배선이 함께 동작한다** — 그 화면이 `ContentCharacterStep`을 재사용하고 온보딩 status는 completed라 가드를 통과하므로, 계정 변경 중 로스터 429도 같은 모달로 나간다(그 흐름의 `refreshAccounts` 429와 목적지가 일치). 아키텍처: 훅은 `ScheduleSyncError`·`ApiKeyNoticeKind`를 **타입으로만** import, onboarding/store는 schedule-sync를 import하지 않아 순환 없음. 테스트 **2,649개/176파일** 통과(step 2 기준선 2,639 대비 +10)·build 성공·lint 0 errors/17 warnings(baseline 동일). 금지사항 준수: use-account-probes(step 4)·ErrorState/format의 429 문구·액션(step 5) 무변경, 429에 재시도 액션 되살리지 않음.",
      "completed_at": "2026-08-08T21:20:38+0900"
    },
    {
      "step": 4,
      "name": "account-probe-verdict",
      "status": "completed",
      "started_at": "2026-08-08T21:20:38+0900",
      "summary": "이슈 #177을 닫았다 — 429가 삼켜지던 자리를 판정 세 갈래로 갈랐다. **새 판정 타입**(step 5·6이 읽는다): `AccountProbe.allUnavailable: boolean` → **`verdict: AccountProbeVerdict`** = `{kind:'queryable'}` | `{kind:'allUnavailable'}` | `{kind:'undetermined', error: ScheduleSyncError}` (판별 유니온, 겹침 없음). catch는 `failure.kind === 'characterUnavailable'`이면 003 집합에 넣고(영구·판정을 막지 않음) **else면 첫 원인을 `undetermined`에 담는다** — 전에는 그냥 버려서 `allUnavailable`이 항상 false였다(위양성이 아니라 위음성). 판정 순서는 undetermined → 전원 003 → queryable. **대표 후보를 `!unavailableOcids.has(ocid)`(003이 아닌 캐릭터)에서 `confirmedOcids.has(ocid)`(성공적으로 확인된 캐릭터)로 좁혔다** — ADR-068 결정 4가 없앤 '조회 불가 캐릭터가 계정 표기가 된다'의 429판 회귀 가드. `isSettled`는 손대지 않았다(ADR-113 결정 4 — try/finally 그대로). 훅에 `retry: () => void` 신설(`attempt` 카운터를 effect deps에 넣고 `ProbeRun`이 `source`+`attempt`로 세대를 가른다 — 계정 참조가 바뀔 때와 같은 방식으로 이전 회차를 렌더 시점에 버린다). 화면: settle 뒤 `undetermined`가 하나라도 있으면 **목록·CTA·안내 문구를 그리지 않고**(부분 판정 금지 — 확인된 계정이 섞여 있어도 안 그린다) `ErrorState`를 그린다. 모달로 보내는 것은 **429뿐**(`useApiKeyNotice(undetermined.find(e => e.kind === 'rateLimited') ?? null)`) — 그 원인만 처방이 키 교체다. 나머지는 `formatRosterError(_, 'onboarding')` 문구 + **다시 시도**(`retry`)이고, 원인이 섞이면 429를 앞세운다(출구를 쥔 쪽이라 화면과 모달이 같은 말을 해야 한다). format.ts·ErrorState는 무변경(step 5). **판별력 확인**: catch의 else 분기를 지우고 후보를 옛 방식으로 되돌리자 새 테스트 3건이 실제로 실패했다 — '429는 판정 불가이고 allUnavailable이 false로 위장되지 않는다' · '429로 확인하지 못한 캐릭터는 대표로 뽑히지 않는다' · '네트워크 실패도 판정 불가다'. 되돌렸다. **#177 → #176 인과 차단 확인(수동 추적, 끊긴 고리 없음)**: 429 → verdict `undetermined` → 목록 자체가 안 그려져 **그 계정을 고를 수단이 없다**(→ ContentCharacterStep 잠금에 도달 불가) + `noticeApiKeyIssue('rateLimited')` → step 1 가드가 `selectingAccount`를 통과(키 입력 화면 두 상태만 no-op) → 라우트 밖 `ApiKeyNoticeModal` → 확인 → RESET+`apiKey` 삭제. 설정 계정 변경도 같은 컴포넌트라 함께 덮인다(status=completed로 가드 통과). **설계 정정 1건**: 처음에 feature 층에 `undeterminedProbeErrors()` 헬퍼를 export했으나 **범위 밖 테스트 2파일**(OnboardingScreen·AccountFlowStatus)이 이 모듈을 부분 모킹 없이 목으로 덮고 있어 깨졌다 — 호출부가 1곳뿐이라 화면 안 한 줄로 인라인해 해소(단일 사용 추상 금지 규칙과도 맞다). docs: onboarding.md 판정 불가 절에 '모달로 보내는 것은 429뿐, 나머지는 ErrorState+재시도' 한 줄 추가. 테스트 **2,657개/176파일** 통과(step 3 기준선 2,649 대비 +8 = 신규 9 − 삭제 1[뒤집힌 '네트워크 실패는 후보 자격을 유지한다'])·build 성공·lint 0 errors/17 warnings(baseline 동일). AC 전부 통과: `kind === 'characterUnavailable'` 여전히 1건 · 범위 밖 src 변경 0건. **미검증**: 실제 개발 단계 키의 429로 이 화면이 어떻게 보이는지(자동 테스트는 `rateLimited`를 주입할 뿐 nexon/http→toScheduleSyncError→프로브 사슬이 실제 429 응답에서 이어지는지는 모른다) · 429가 잦은 계정에서 계정 선택 자체가 막히는 체감.",
      "completed_at": "2026-08-08T21:33:11+0900"
    },
    {
      "step": 5,
      "name": "errorstate-contract",
      "status": "completed",
      "started_at": "2026-08-08T21:33:11+0900",
      "summary": "이슈 #178을 닫았다 — **버튼을 더한 것이 아니라 규칙을 세우고 세 자리가 그 규칙을 지키는지 확인했다**. src 변경은 `ErrorState.tsx` 주석·JSDoc **한 파일뿐**이고 나머지는 전부 테스트다. ① **계약 정정**: `ErrorState … 액션 항상`(구현은 `action?`)을 **'이 컴포넌트가 그려지는 자리는 원인과 무관하게 진행 경로를 하나 이상 갖는다'**로 바꾸고, **그 경로가 이 컴포넌트의 `action` 일 필요는 없다**(껍데기의 닫기·취소, 위에 덮이는 안내 모달)와 **액션 없이 쓸 수 있는 유일한 조건**('그 자리에서 앞으로 갈 다른 수단이 실제로 있을 때')을 명시했다. `action?` JSDoc에도 401·429(ADR-114 결정 2)를 넣고 생략 조건을 걸었다. ② **세 자리 확인 테스트**(새 UI 0개) — 온보딩 `ContentCharacterStep`: ErrorState 버튼 0개 + `계속하기` disabled + **껍데기 없음**이라 유일한 길이 진입점 호출 하나임을 단언 / 피커 `CharacterTrackingPicker`: 429 문구·처방 + ErrorState 버튼 0개 + 껍데기 `닫기` 잔존(모달 배선은 화면 쪽이라 여기선 껍데기만 본다) / 설정 `AccountFlowStatus`: **`ErrorState`가 뜨는 자리는 error 카드가 아니라 `selectingCharacters`**(온보딩과 같은 컴포넌트 재사용)임을 짚고 429에서 `취소` + 진입점 호출을 단언 — 이 파일에 `getCharacterPickerRoster`·onboarding store 모킹을 새로 넣었다. ③ **EmptyState ↔ 피커 루프는 step 1~3으로 이미 끊겨 있었다 — 코드를 더 고치지 않았다**. 피커를 여는 순간 로스터 429가 진입점을 불러 닫을 수 없는 모달이 덮이므로 닫아도 EmptyState로 되돌아올 일이 없다. ContentScreen·BossScreen 양쪽에 확인 테스트를 넣고 'EmptyState에 두 번째 길을 더하지 않는 근거가 이 호출'이라고 주석에 적었다. ④ **`formatRosterError`의 `rateLimited`는 고칠 것이 없었다** — 문구는 ADR-114 결정 1이 정한 그대로고 액션도 그대로 없다(진행 경로를 모달이 준다). **판별력 확인**: 온보딩 배선 한 줄(`useApiKeyNotice(…)` → `useApiKeyNotice(null)`)을 무력화하자 **정확히 3건만** 실패했다 — 새 '429의 진행 경로는 ErrorState 버튼이 아니라 모달이다' + step 3의 '429로 실패하면 키 재입력 경로로 넘긴다' + 설정 `selectingCharacters`(같은 컴포넌트를 재사용하므로 같은 배선에 걸린다). 되돌렸다. **세 자리 잠금 점검(손으로, 429일 때 누를 수 있는 것)**: 온보딩=ErrorState 0 · 계속하기 disabled · 계정 다시 선택 없음(확정 빈 상태 가지 전용) · **모달 확인 1** → 경로 1 / 피커=ErrorState 0 · 저장 disabled · **닫기 1** + **모달 확인 1** → 경로 2 / 설정=ErrorState 0 · **취소 1** + **모달 확인 1** → 경로 2(그 흐름의 `refreshAccounts` 429는 RESET으로 모달이 닫히고 안내 모달만 남아 경로 1). **잠긴 자리 0개**. 범위 밖이지만 나머지 ErrorState 자리 셋도 규칙을 지킨다 — `AccountSelectionList`(step 4, 429는 모달·그 외는 재시도) · `BossProfitScreen`·`DropHistoryScreen`(둘 다 `다시 시도` 보유). 아키텍처: `ErrorState`는 여전히 lucide만 import(layer-dependencies 통과), 렌더 결과·design-system 실패 상태 규격 무변경. 테스트 **2,662개/176파일** 통과(step 4 기준선 2,657 대비 +5, 새 테스트 파일 0개)·build 성공·lint **0 errors/17 warnings**(baseline 동일). AC 전부 통과(`액션 항상` 0건 · `진행 경로` 존재 · layer-dependencies 3건 통과). **정직하게 적는 것 둘**: (1) 전체 스위트 8회 중 **1회** `src/__tests__/App.test.tsx`의 탭 이동 스크롤 테스트 3건이 실패했다 — 내가 건드리지 않은 파일이고 `React.lazy` 라우트 청크 해석 타이밍에 의존하는 동기 단언이라 부하에 따라 흔들린다(재현 시도 7회 연속 통과, 격리 실행 3회 통과, 기준선 3회 통과). 이 step의 변경과 무관한 **기존 flaky**로 판단하지만 관측된 사실이므로 남긴다. (2) **429는 여전히 한 건도 줄지 않는다**(ADR-116 결정 5) — 이 step이 바꾼 것은 막혔을 때 나갈 길이 있다는 사실을 규칙과 테스트로 고정한 것뿐이고, **실제 개발 단계 키의 429에서 세 자리가 어떻게 보이는지는 실기기·실사용 몫**이다.",
      "completed_at": "2026-08-08T21:45:33+0900"
    },
    {
      "step": 6,
      "name": "docs-verify",
      "status": "completed",
      "started_at": "2026-08-08T21:45:33+0900",
      "summary": "문서를 구현과 대조해 마감했다. **src 변경 0줄**(AC 확인) — 문서만 고쳤다.\n\n**AC 실측**: build 성공 · 테스트 **2,662개/176파일** 통과(step 0 기준선 2,627 대비 **+35**, 새 테스트 파일 0개) · lint **0 errors/17 warnings**(baseline 동일) · `git status --porcelain -- src/` **0** · `grep '구현 완료' docs/adr/ADR-116.md` 통과 · `grep 'ADR-116' docs/ADR.md` 3건.\n\n**① ADR-116 마감** — 상태를 `(설계 확정 · 구현 전)` → **`(구현 완료 · 실기기 미검증, 2026-08-08, 이슈 #176·#177·#178)`** 로 바꾸고 `docs/ADR.md` 인덱스 행도 동기화했다. 검증 절을 실측표(step별 순증 +9/+3/+10/+8/+5) · **테스트 파일 × 담보 결정 대조표 13행** · **판별력 확인 4건**(step 2 문구 2건 / step 3 정확히 10건 / step 4 3건 / step 5 정확히 3건) · **결정별 구현 상태표**로 채우고, `구현하며 정정한 것` 11항목과 `자동 테스트가 담보하지 못하는 것` 7항목을 새로 썼다.\n\n**② 결정 5개 전부 구현됐다(하나씩 코드로 확인) · 폐기된 결정 없음**\n\n| 결정 | 상태 | 확인한 자리 |\n|---|---|---|\n| 1 — 429가 무효 키의 알림 사슬을 그대로 탄다 | 구현 | `state.ts`의 `apiKeyNotice`(`'invalid' | 'rateLimited' | null`) · `store.ts`의 `noticeApiKeyIssue`/`confirmApiKeyNotice`(`RESET`+`removeApiKey`, `clearAuthConfig` 아님) · `ApiKeyNoticeModal.tsx`의 `NOTICE_COPY`가 ADR 표와 **문자 단위로 일치** · `onClose` no-op + 버튼 `확인` 하나 |\n| 2 — 가드를 \"키 입력 화면에 있지 않을 때\"로 | 구현 | `store.ts:267-270`(`awaitingApiKey`·`verifyingApiKey`만 no-op) + 배선 5곳 · `ApiKeyNoticeModal`은 `App.tsx:356`의 **`<Routes>` 밖**이라 온보딩 위에도 뜬다(직접 확인) |\n| 3 — 003 아닌 프로브 실패는 판정 불가, 판정 불가면 목록 안 그림 | 구현 | `use-account-probes.ts`의 `AccountProbeVerdict` 3갈래(판정 순서: undetermined → 전원 003 → queryable) · `AccountSelectionList.tsx:95` |\n| 4 — `ErrorState` 자리는 진행 경로 1개 이상 | 구현 | `ErrorState.tsx:11-20` 계약 + `action?` JSDoc, 세 자리 확인 테스트 |\n| 5 — 429를 실제로 줄이는 일은 하지 않는다 | **의도대로 미구현** | 동시성 캡·프리플라이트·single-flight 코드 0줄. 프로브는 여전히 중첩 `Promise.all` |\n\n**③ 문서-코드 대조 — 정정 5건(건너뛰지 않았다)**. 각 서술이 가리키는 코드를 열어 확인했고, **결정도 구현도 옳은데 문서 서술만 부정확한** 경우만 고쳤다.\n\n1. `onboarding.md` \"대표는 **조회 가능한** 캐릭터 중 최고 레벨\" → **\"성공적으로 확인된\"**. step 4가 후보를 `!unavailableOcids.has()` → `confirmedOcids.has()`로 좁혔는데 문서는 옛 `queryable` 어휘였다.\n2. `onboarding.md` \"그 외 실패는 판정 불가이고 **후보 자격을 잃지도 얻지도 않는다**\" → 실제로는 **잃는다**(대표 후보에서 빠지고 그 계정은 목록에 안 선다). *\"영구로 단정하지 않는다\"* 가 *\"확인된 것과 똑같이 다룬다\"* 는 뜻이 아님을 명시하고 `AccountProbeVerdict` 3갈래를 적었다.\n3. `error-resilience.md` 개별 `character/basic` 실패 행 \"429는 **액션 없는 에러 상태로 남는다**\" → 실제로는 `ContentScreen`·`BossScreen`이 `useApiKeyNotice(rosterError)`로 429를 그대로 넘겨 **같은 안내 모달로 귀결**한다(액션 없음은 그대로, 막다른 길이 아닌 것이 달라졌다).\n4. `content-scheduler.md` \"`rateLimited` → 액션 **없음**\" + \"**토스트의 429 문구는** 원인만 말한다\" → step 3이 429 토스트를 **아예 뗐다**(모달이 원인+처방을 함께 말하므로 두 번 말하지 않는다). `formatScheduleSyncError`의 `rateLimited` case는 `assertNever` 소진 가드로만 남아 도달하지 않는다는 사실을 적었다.\n5. `settings.md` \"카드의 `rateLimited` 문구·버튼 없음은 그대로 남는다 — **모달이 덮이기 직전 한 프레임이자 안전망**\" → `refreshAccounts`는 진입점 호출 뒤 **`RESET`까지 하므로 그 카드에 도달하지 않는다**(계정 모달이 `idle`로 닫힌다). 코드에는 남지만 지금 갈 수 있는 경로는 미배선 `changeApiKey` 뿐임을 적었다.\n\n**ADR-115 결정 6**에 **정정 2**를 옛 서술을 지우지 않고 인용 블록으로 덧붙였다(가드 조건 변경 · 막는 대상은 그대로 · `WeakSet` 유지 · **두 정정의 성격이 반대**: 정정 11은 못 막던 경로를 더 막았고 이번은 과하게 막던 자리를 연다).\n\n**④ 이슈 3건 대조표 — \"결정 필요\" 한 행씩**\n\n| 이슈 | 항목 | 판정 |\n|---|---|---|\n| #176 | 다섯 출구 중 무엇이 열렸나 | **다섯 중 0개** — `계속하기`(로스터가 비어 영구 비활성) · `다시 시도`(같은 키라 또 429) · `계정 다시 선택`(확정 빈 상태 가지 전용) · 뒤로 가기(단계가 `status` switch) · 재시작(`deriveResumeTarget`이 같은 단계로) 전부 **그대로**다. 열린 것은 **여섯 번째 출구 = 안내 모달**이다 |\n| #176 | 잠금이 실제로 끝났나 | **끝났다**(코드 추적, 끊긴 고리 없음): 로스터 429 → `toScheduleSyncError` → `ContentCharacterStep.tsx:178`(**429만** 넘긴다) → `useApiKeyNotice` → `NOTICE_KIND` → `noticeApiKeyIssue('rateLimited')` → 가드 통과(`selectingContentCharacters`는 키 입력 화면 아님) → `apiKeyNotice='rateLimited'` → `App.tsx:356` **라우트 밖** 모달 → `확인` → `RESET`+`removeApiKey` → `awaitingApiKey`. 이 자리의 **401은 계속 미배선**(폼 실패로 남는다 — 근거가 \"가드가 막는다\"에서 \"배선하지 않는다\"로 내려왔다) |\n| #176 | 수정 방향(실패 가지에 `emptyAction`) | **안 했다** — 계정을 바꿔도 같은 키라 429는 그대로라 진짜 출구가 아니다(ADR-116 기각안) |\n| #176 | **결정 필요 3 — 온보딩 전체 뒤로 가기** | **안 했다.** 단계가 라우트가 아니라 `status` switch라 구조 변경이 크고 되돌아가도 429는 그대로다. **별건으로 남는다** |\n| #177 | 위음성이 사라졌나 | **사라졌다** — `catch`의 `else`가 첫 비-003 실패를 `undetermined`에 담는다. `allUnavailable`이 항상 false이던 것이 3갈래 판별 유니온으로 대체됐고, 판정 불가가 하나라도 있으면 목록·CTA·안내 문구를 **통째로** 안 그린다(확인된 계정이 섞여 있어도 — 부분 판정 금지) |\n| #177 | 대표 캐릭터 오염이 막혔나 | **막혔다, 두 겹으로** — ① 판정 불가 계정은 목록에 안 선다(ADR 본문의 근거) ② 후보 집합 자체를 `confirmedOcids`로 좁혔다(**ADR이 말한 것보다 한 겹 더** — 다른 경로로 되살아나지 않게) |\n| #177 | 결정 필요 1 — ①/②/③ 중 | **③ 채택**(목록 자체를 안 그린다). ②(중립 톤 + 선택 허용)는 *\"들어갈 수 없는 문은 잠근다\"* 로 기각 |\n| #177 | 결정 필요 2 — 429만인가 003 아닌 전부인가 | **003 아닌 전부**(사용자 결정). 대가: **오프라인에서도 같은 화면**이 뜨는데 문구는 429를 말하므로 어긋난 안내가 갈 수 있다 |\n| #177 | ADR-113 결정 7 정정이 반영됐나 | **반영**(ADR-116 폐기 절 + `onboarding.md` history 1줄). 원칙(\"모르는 실패를 영구로 단정하지 않는다\")·003만 영구·`isSettled`의 의미는 **그대로** |\n| #178 | 세 자리에 진행 경로가 있나 | **셋 다 있다, 잠긴 자리 0개** — 온보딩=모달 `확인` 1(껍데기 없음) / 피커=껍데기 `닫기` + 모달 2 / 설정=`취소` + 모달 2. `ErrorState` 본문 버튼은 세 자리 모두 **여전히 0개**다(429에 재시도를 되살리지 않았다) |\n| #178 | `ErrorState` 계약이 정정됐나 | **정정** — `액션 항상` → **\"이 컴포넌트가 그려지는 자리는 원인과 무관하게 진행 경로를 하나 이상 갖는다\"** + 그 경로가 이 컴포넌트의 `action`일 필요는 없다 + **액션 없이 쓸 수 있는 유일한 조건** 명시 |\n| #178 | EmptyState ↔ 피커 루프가 끊겼나 | **끊겼다 — 코드를 더 고치지 않고**(step 1~3으로 이미 끊겨 있었다). 피커를 여는 순간 로스터 429가 진입점을 불러 모달이 덮으므로 닫아도 EmptyState로 되돌아올 일이 없다. 확인 테스트만 넣었다 |\n| #178 | 결정 필요 4 — ADR-114 결정 2를 어디까지 정정 | **`ErrorState` 자리에서만**. 스탈 배너·토스트의 액션 없음은 **그대로 유효**하고 `error-resilience.md` history로 이동 |\n| 공통 | **fan-out 동시성 캡** | **안 했다(사용자 명시 기각, ADR-116 결정 5).** **429 발생 건수는 한 건도 줄지 않았다** — 상류(프로브 전 계정×전 캐릭터 중첩 `Promise.all`, single-flight 없음)와 **목록이 비는 실제 원인인 자격 스윕의 429 삼킴**(`character-eligibility.ts`)이 그대로다. **별도 이슈가 필요하다** — 지금 그 사실은 ADR-113·114·116 세 곳의 \"미해결/범위 밖\"에 흩어져 있을 뿐 추적 가능한 이슈가 없다 |\n\n**⑤ 열린 질문 정리** — **이 phase 로 닫힌 열린 질문은 없다**(닫히지 않은 것을 지우지 않았다). 새로 추가·갱신 3건: ① **신설** `429로 키를 지우고 온보딩으로 내보내는 것이 실사용에서 옳은지`(실제 429 미수신 · 초당 한도에도 내보냄 · 지운 키가 내일이면 유효할 수 있음) ② `무효 키 안내 모달 체감` 질문에 *\"ADR-116 이후 429도 같은 모달이라 두 원인 모두에 걸린다\"* 를 덧붙임 ③ `계정 선택 대기` 질문의 \"동시성 캡을 둘지\"에 **\"한 번 제기됐다 기각된 것이지 닫힌 것이 아니다\"** 와 이제 429면 목록 자체를 안 그린다는 새 대가를 덧붙임.\n\n**⑥ 코드 결함 보고(고치지 않았다 — 사용자 판단 몫)**\n\n- **결함 아님, 관측 1건**: `use-account-probes.ts`의 `undetermined ??= failure` 주석이 *\"응답 도착 순서에 따라 뒤바뀌면 안 된다\"* 를 근거로 드는데, 동시 `Promise.all` 안이라 실제로 **먼저 도착한 실패가 이긴다**(도착 순서 의존은 남는다). 화면이 `undetermined.find(kind === 'rateLimited')`로 429를 명시적으로 앞세우므로 **모달 판정은 결정적**이고, 흔들릴 수 있는 것은 비-429 원인이 섞였을 때의 `ErrorState` 문구뿐이다. 실사용 영향이 작아 보고만 한다.\n- **알려진 대가(설계대로)**: 컨텐츠/보스 피커의 429는 이미 온보딩을 마친 사용자의 키를 지운다(`useApiKeyNotice(rosterError)`가 전체 원인을 넘긴다). ADR-116 대가 1·2에 적힌 그대로다.\n- **그 외 결함 없음.**\n\n**⑦ 이슈 코멘트 초안 (발행하지 않았다 — `gh` 미사용, 사용자 몫)**\n\n▸ **#176**\n\n> 온보딩 캐릭터 선택의 429 잠금을 해소했습니다(ADR-116). **다섯 출구를 여는 방식이 아니라 여섯 번째 출구를 만드는 방식**입니다 — 429를 만나면 무효 키와 **같은 사슬**(닫을 수 없는 안내 모달 → `확인` → 키 입력 화면 + 저장된 `apiKey`만 삭제)이 돌고 문구만 갈립니다(`호출 한도를 초과했습니다` / `서비스 단계 키로 다시 입력해주세요.`). 다섯 출구가 전부 \"같은 키로 다시\"라 진짜 처방이 아니었기 때문입니다. 이걸 가능하게 한 것은 알림 가드를 `status !== 'completed'` → **\"키 입력 화면(`awaitingApiKey`·`verifyingApiKey`)에 있지 않을 때\"** 로 바꾼 것입니다(ADR-115 결정 6 두 번째 정정) — 옛 가드로는 **온보딩 안에서 잠긴 사용자를 구할 수 없었습니다**. 뒤 단계는 ADR-115 결정 4의 재개로 보존돼 잃는 건 키를 한 번 다시 붙여넣는 것뿐입니다. 이 자리의 **401은 그대로 폼 실패**로 둡니다(방금 입력한 키가 나쁜 것이라 성질이 다릅니다). **안 한 것 둘**: `계정 다시 선택`을 실패 가지에 넘기는 수정(계정을 바꿔도 같은 키라 429는 그대로) · **결정 필요 3의 온보딩 전체 뒤로 가기**(단계가 라우트가 아니라 `status` switch라 구조 변경이 크고, 되돌아가도 429는 그대로 — 별건으로 남깁니다). **429 발생 자체는 한 건도 줄지 않았습니다** — fan-out 동시성 캡은 이번 범위에서 기각됐습니다. **실기기 미검증**: 실제 개발 단계 키의 429로 이 사슬이 이어지는지는 자동 테스트가 담보하지 못합니다(전 경로가 주입된 `rateLimited`입니다).\n\n▸ **#177**\n\n> 프로브가 429를 삼키던 위음성을 없앴습니다(ADR-116 결정 3). `AccountProbe.allUnavailable: boolean` → **`verdict`(`queryable` / `allUnavailable` / `undetermined`) 3갈래 판별 유니온**이고, `catch`의 else가 003이 아닌 첫 실패를 `undetermined`에 담습니다(전에는 버려서 `allUnavailable`이 항상 false였습니다). 결정 필요 1은 **③(판정 불가면 목록 자체를 그리지 않는다)**, 결정 필요 2는 **003이 아닌 모든 실패를 하나로** 묶는 쪽입니다 — ADR-113 결정 3(*\"모르는 동안은 보여주지도 않는다\"*)을 429에도 적용한 것이고, 같은 ADR의 결정 3과 결정 7이 서로 반대 방향으로 적용돼 있던 것을 맞춘 정정입니다(원칙과 003만 영구인 규칙, `isSettled`의 의미는 그대로입니다). **확인된 계정이 섞여 있어도 부분 판정은 하지 않습니다.** 그 자리에서 429는 안내 모달로, 그 외 원인은 `ErrorState` + **다시 시도**(프로브 재실행)로 갈립니다. 대표 오염은 **두 겹**으로 막았습니다 — 목록에 안 서는 것에 더해 대표 후보를 `!003` → **\"성공적으로 확인된 캐릭터\"** 로 좁혔습니다. **#176 인과도 끊겼습니다** — 429면 그 계정을 고를 수단 자체가 없습니다. **대가**: 429가 잦은 계정은 계정 선택 단계에서 멈추고 출구가 모달 하나입니다(후퇴가 아니라 사실을 드러내는 것) · 오프라인에서도 같은 화면이 뜨는데 문구는 429를 말합니다. **실기기 미검증**입니다.\n\n▸ **#178**\n\n> 규칙을 세웠습니다 — **\"`ErrorState`가 그려지는 자리는 원인과 무관하게 진행 경로를 하나 이상 갖는다. 그 경로가 이 컴포넌트의 `action`일 필요는 없다\"**(결정 필요 2 채택, ADR-116 결정 4). ADR-114 결정 2는 **`ErrorState` 자리에서만** 정정되고 **스탈 배너·토스트의 액션 없음은 그대로 유효**합니다(결정 필요 4) — 배너는 아래 목록이 남아 막다른 길이 아니지만 `ErrorState`는 정의상 목록이 없는 자리라 그 근거가 뒤집힙니다. **\"429에 재시도를 되살린다\"가 아닙니다**(눌러도 또 429라는 판단은 유효합니다) — 더한 것은 **재시도가 아닌 다른 출구**이고 지금 그것은 결정 1의 안내 모달입니다. 결정 필요 3의 계약 정정도 했습니다(`action?`이 옵셔널인 이유 + **액션 없이 쓸 수 있는 유일한 조건**). 세 자리 잠금 점검 결과 **잠긴 자리 0개**입니다(온보딩=모달 1 / 피커=`닫기`+모달 2 / 설정=`취소`+모달 2). **`EmptyState` ↔ 피커 루프는 코드를 더 고치지 않고 끊겼습니다** — 피커를 여는 순간 로스터 429가 모달을 부르므로 닫아도 되돌아올 일이 없습니다(확인 테스트만 추가). 그래서 이 이슈의 **src 변경은 `ErrorState.tsx` 주석 한 파일뿐이고 새 UI는 0개**입니다. **429 발생 자체는 줄지 않았고**(fan-out 캡 기각), 실제 429에서 세 자리가 어떻게 보이는지는 **실기기·실사용 몫**입니다.",
      "completed_at": "2026-08-08T22:05:00+0900"
    }
  ],
  "created_at": "2026-08-08T19:23:54+0900"
}
