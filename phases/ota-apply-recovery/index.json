{
  "project": "메이플 루틴 (Maple Routine)",
  "phase": "ota-apply-recovery",
  "steps": [
    {
      "step": 0,
      "name": "docs-adr",
      "status": "completed",
      "started_at": "2026-08-08T23:28:03+0900",
      "summary": "ADR-117 신규 작성(이슈 #175 — OTA 적용 경로에 복구 장치를 넣는다: 일방통행을 끊고, 커버는 앱이 죽어도 걷힌다) + ADR.md 인덱스 1줄 append + features/live-update.md·features/splash.md 갱신(옛 정책은 지우지 않고 history 로 이동). 소스 변경 0. 이후 step 1~8 의 계약 = 결정 8개: (1) apply() 순서를 close → 커버 → set() 으로 뒤집고 전체에 12초 타임아웃+catch, 실패 시 커버 걷고 'apply-error' — 어댑터 applyDownloadedLiveUpdate 의 close+set 덩어리를 갈라 set() 만 남긴다 / (2) notifyAppReady() 를 main.tsx 첫 문장에서 App 마운트 useEffect 로(하이드레이션 뒤로는 안 옮긴다 — prehydrate 가 10초를 넘기면 멀쩡한 번들까지 롤백) / (3) index.html 인라인 스크립트에 #boot-cover 8초 실패 안전 타이머, '아직 DOM 에 있을 때만' 동작 / (4) hideSplashScreen() 이 [data-splash-cover] 도 querySelectorAll 로 전부 걷는다(지금은 걷는 코드가 저장소에 없다) / (5) closeBossProfitDb() 에 5초 타임아웃, 실패는 지금처럼 삼킨다 / (6) ErrorBoundary 폴백이 마운트 시 hideSplashScreen() 호출(z-index 상향은 기각 — 보이게 해도 안 눌린다) / (7) 'applying' 상태 신설(단순 재진입 가드는 기각), 'applying'·'apply-error' 둘 다 MODAL_STATUSES / (8) clearCacheDataAndReload 도 close → 커버 → reload 로, ADR-065 결정 3 의 '항상 리로드'·pendingNotice 정책은 불변. 검증: build 성공 · 테스트 2,662개/176파일 통과(기준선 동일) · git status src/ index.html 0건.",
      "completed_at": "2026-08-08T23:37:14+0900"
    },
    {
      "step": 1,
      "name": "boot-cover-failsafe",
      "status": "completed",
      "started_at": "2026-08-08T23:37:14+0900",
      "summary": "ADR-117 결정 3 구현 — index.html 의 #boot-cover div 바로 다음에 고전(type=module 아님) 인라인 스크립트로 실패 안전 타이머를 넣었다. setTimeout 8000ms, 콜백은 `var cover = document.getElementById('boot-cover'); if (!cover) return` 가드로 **커버가 아직 DOM 에 있을 때만** 동작하고(정상 부팅이면 아무것도 안 함 — 8초 시점에 사용자가 `지금 적용` 을 눌러 올라간 리로드 커버를 걷지 않기 위해서, 그 구간은 결정 1 의 12초 타임아웃 몫), 있을 때는 cover.remove() + `window.Capacitor?.Plugins?.SplashScreen?.hide()` 를 try/catch 로(네이티브 hide 까지 불러야 iOS isUserInteractionEnabled=false 가 풀려 터치가 산다 — DOM 커버만 걷으면 화면만 돌아오고 먹통). 컴포넌트가 아니라 문서 셸에 둔 이유(App.tsx useEffect 클린업이 커버 제거 타이머를 취소해 앱이 죽으면 걷을 주체가 사라진다)를 주석으로 남겼다. 새 테스트 src/__tests__/index-html-boot-cover.test.ts (5케이스, jsdom) — 정적 검사에 그치지 않고 인라인 스크립트 본문을 정규식으로 뽑아 new Function 으로 실행하고 가짜 타이머로 전진시킨다: A 부팅 실패(커버 제거 + hide 1회) · B 정상 부팅(hide 미호출 = 가드 회귀 테스트) · C 7,900ms 에는 미제거(상수 방어) · D window.Capacitor 부재에도 미throw. **판별력 확인**: 가드를 `if (cover) cover.remove()` 로 바꿔 '있을 때만' 조건을 지우자 **케이스 B 만** 실패(4 passed/1 failed)함을 실행으로 확인 후 되돌렸다. dist/index.html 을 열어 스크립트가 변형·최소화 없이 그대로 들어간 것도 확인(Vite 는 고전 인라인 스크립트를 건드리지 않는다). 검증: build 성공 · 테스트 2,667개/177파일 통과(기준선 2,662/176 대비 +5, 새 파일 1) · lint 0 errors/17 warnings(baseline 동일) · git status src/ 에서 새 테스트 파일 외 변경 0건.",
      "completed_at": "2026-08-08T23:42:25+0900"
    },
    {
      "step": 2,
      "name": "sqlite-close-timeout",
      "status": "completed",
      "started_at": "2026-08-08T23:42:25+0900",
      "summary": "ADR-117 결정 5 구현 — closeBossProfitDb() 에 CLOSE_TIMEOUT_MS = 5_000 상한을 줬다. **계약: 여전히 던지지 않는다**(타임아웃이든 네이티브 에러든 항상 resolve — 이 함수 뒤에 오는 리로드가 실행되지 못하면 그게 곧 고치려는 증상이라, step 3(native-apply-path)·step 8(cache-data-order)이 이 성질에 기대어 close → 커버 → set()/reload() 로 짜인다). 타임아웃이 바꾸는 것은 '실패로 끝난다'가 아니라 '끝난다'. 구현은 기존 withOpenTimeout 의 race+clearTimeout 본체를 제네릭 withTimeout(promise, ms, message) 로 뽑아 두 상수가 한 몸으로 읽히게 하고(withOpenTimeout 은 그 얇은 래퍼로 남아 10초·에러 문구·동작 전부 불변), 닫기는 **대기 구간 전체**(dbPromise 대기 + closeConnection)를 async IIFE 로 묶어 감쌌다 — 여는 쪽이 매달리면 닫기도 그 앞에서 매달리므로 closeConnection 만 감싸면 상한이 안 선다. 기존 성질 넷 보존: 던지지 않음 · dbPromise 는 닫기가 끝난 뒤(성공·실패·타임아웃)에만 null(먼저 비우면 동시 getBossProfitDb 가 새 createConnection 을 시작해 'Connection boss_profit already exists') · dbPromise === null 이면 조기 return 이라 타이머조차 안 걺 · .finally 로 clearTimeout. 5초인 이유(여는 쪽 10초보다 짧음 — 열기는 파일 생성·마이그레이션 포함이나 닫기는 정상이면 수 ms 이고 이 값이 곧 'applying' 구간 = 사용자가 무반응을 견디는 상한)를 상수 주석에 남겼다. 새 테스트 4케이스(db.test.ts, 케이스별로만 fake timer — 파일 전역이면 기존 레이스 케이스의 마이크로태스크 순서가 흔들린다): 무응답 닫기가 5초에 resolve(reject 아님) · 타임아웃 뒤 dbPromise 가 비워져 다음 getBossProfitDb 가 createConnection 2회 · 4.9초엔 미완료/5.1초엔 완료(상수 방어) · 한 번도 연 적 없으면 vi.getTimerCount() === 0. **판별력 확인**: withTimeout race 를 걷어내자 새 3케이스가 전부 실패(2건은 vitest 5s 타임아웃=행, 1건은 assertion) 후 되돌렸다. 기존 레이스 케이스('종료 중 동시 호출')를 포함해 기존 4케이스 전부 무수정 통과. 검증: build 성공 · 테스트 2,671개/177파일 통과(기준선 2,667/177 대비 +4, 새 파일 0) · lint 0 errors/17 warnings(baseline 동일) · git status src/ 는 storage/sqlite 2파일뿐(밖 0건) · storage/ → features/·native/ import 0건(레이어 방향 유지).",
      "completed_at": "2026-08-08T23:47:28+0900"
    },
    {
      "step": 3,
      "name": "native-apply-path",
      "status": "completed",
      "started_at": "2026-08-08T23:47:28+0900",
      "completed_at": "2026-08-08T23:51:25+0900",
      "summary": "ADR-117 결정 1(순서 부분)·결정 4 구현 — src/native 두 파일만. ① applyDownloadedLiveUpdate 의 새 호출 순서는 closeBossProfitDb() → showSplashScreen() → CapacitorUpdater.set({id}) 다(커버를 이 함수 안으로 들여왔다 — 스토어와 나눠 가지면 순서 보장이 두 파일로 흩어진다. 시그니처·분할 없음, showSplashScreen().catch(()=>{}) 로 커버 실패가 적용을 막지 않고, closeBossProfitDb 는 step 2 가 '던지지 않는다'를 계약으로 만들어 재감싸지 않았다. 타임아웃은 넣지 않았다 — 전체 12초 상한과 실패 시 화면 복구는 step 4 의 스토어 몫). ② hideSplashScreen() 이 이제 커버 **둘 다** 걷는다 — 기존 #boot-cover 에 더해 [data-splash-cover] 를 querySelectorAll 로 전부 제거하고, 둘 다 플랫폼 가드(web 조기 반환)와 모든 await 보다 앞이라 웹 개발 서버에서도 걷히고 네이티브 hide() 가 매달려도 DOM 커버는 이미 사라진 뒤다. step 4(apply catch)·step 7(ErrorBoundary 폴백)이 이 성질에 기댄다. showSplashScreen 쪽 '문서와 함께 사라지므로 별도 정리 불필요' 주석은 지우지 않고 '리로드가 성공하면 그렇지만 실패하면 남는다 → hideSplashScreen 이 걷는다'로 정정. 테스트 +6(새 파일 0): splash 4(리로드 커버 제거 · 여러 장 전부 · 웹에서도 둘 다+native hide 미호출 · hide 무응답에도 DOM 먼저) + live-update 2(close→cover→set 호출 순서 배열 단언 · 커버 reject 해도 set 호출). 판별력 확인 2건: 옛 순서(커버→닫기→set)로 되돌리면 **순서 단언 1건만** 실패(기존 'set 전에 close' 케이스는 통과 유지), [data-splash-cover] 제거를 빼면 **새 splash 4건만** 실패 — 둘 다 확인 후 되돌렸다. 아키텍처: native/ → features|app import 0건, live-update→splash-screen 은 동일 레이어·순환 없음. **ADR-117 결정 1 의 하위 항목 '어댑터를 가른다(set 만 남기고 close 는 store 가 부른다)'와는 소유 주체가 다르다** — step 3 지시가 명시적으로 '커버를 이 함수 안으로, 쪼개지 마라'라 그쪽을 따랐고, 실행 순서 자체는 ADR 과 동일하다. docs/features/live-update.md 40행의 같은 문장은 step 9(docs-finalize)에서 정정 필요. 검증: build 성공 · 테스트 2,677개/177파일 통과(기준선 2,671/177 대비 +6) · lint 0 errors/17 warnings(baseline 동일) · git status src/ 에서 src/native 밖 변경 0건."
    },
    {
      "step": 4,
      "name": "apply-error-store",
      "status": "pending"
    },
    {
      "step": 5,
      "name": "apply-error-modal",
      "status": "pending"
    },
    {
      "step": 6,
      "name": "notify-ready-move",
      "status": "pending"
    },
    {
      "step": 7,
      "name": "error-boundary-cover",
      "status": "pending"
    },
    {
      "step": 8,
      "name": "cache-data-order",
      "status": "pending"
    },
    {
      "step": 9,
      "name": "docs-finalize",
      "status": "pending"
    }
  ],
  "created_at": "2026-08-08T23:28:03+0900"
}
