# 이 Worker 는 상태가 없다 — KV·R2·D1 바인딩이 하나도 없는 것이 [[ADR-137]] 결정 2 의 결과다.
# 바인딩을 더하고 싶어지면 먼저 그 문서를 읽어라(배포가 두 갈래가 되는 순간의 대가를 적어 두었다).
name = "maple-routine-ota"
main = "src/index.js"
compatibility_date = "2026-08-14"

# 관측용. 무료 플랜에서도 켤 수 있고, "업데이트 확인 실패"가 앱 문제인지 여기 문제인지 가른다.
[observability]
enabled = true
