// 배포 노트 가드 ([[ADR-119]] 결정 6 + [[ADR-126]] 결정 8).
//
// **원래 `publish-live-update.mjs`(캐패시터) 에 살았고 RN 스크립트가 그것을 import 했다.**
// [[ADR-155]] 결정 1 이 그 패키지를 지우면서 import 가 고아가 됐고, 그때부터 `publish-rn-ota.mjs`
// 는 **첫 줄에서 죽었다**(`ERR_MODULE_NOT_FOUND`). 2026-08-30 에 1.0.7 을 발행하려다 드러났다 —
// 그 사이 유일한 발행(08-25 안드로이드)이 ADR-155 **이전** 커밋의 워크트리에서 돌아서, 아홉 날
// 동안 아무도 안 밟았다.
//
// 이제 소비자가 하나뿐이라 «두 스크립트가 나눠 가지면 한쪽만 고쳐진다» 는 옛 근거는 없어졌다.
// 그래도 파일로 갈라 두는 이유는 **테스트**다 — `publish-rn-ota.mjs` 는 최상위 `await` 를 쓰는
// ESM 이라 jest 가 import 하지 못한다(`ota-runtime-version.mjs` 를 가른 이유와 같다).

/**
 * 이 노트로 배포하면 안 되는 이유를 돌려준다. **통과하면 `null`** 이고, 그때 `items` 와
 * `highlights` 가 둘 다 있다.
 *
 * 판정이 아니라 **문구**를 돌려주는 이유는 둘이 갈리기 때문이다 — 무엇을 쓰라는 것인지 모르면
 * 가드가 그냥 장애물이 된다.
 *
 * - 노트가 아예 없거나(`undefined`) **항목이 비어 있으면** 안 된다 — 빈 노트는 노트가 아니다.
 *   버전만 적어 두고 내용을 안 쓴 것을 통과시키면, [[ADR-119]] 결정 6 이 막으려는 "영영 빈 채로
 *   남는 버전"이 그대로 나간다(결정 4 가 사후 재구성을 금지하므로 나중에 채울 방법이 없다).
 * - **핵심 목록이 비어 있어도** 안 된다([[ADR-126]] 결정 8) — 그것이 업데이트 모달이 받기 전에
 *   보여줄 유일한 재료다. 선택 사항으로 두면 안 쓰게 되고, 안 쓰면 모달이 다시 "버전 + 용량"만
 *   말하는 자리로 돌아간다.
 *
 * 내용의 품질은 검사하지 않는다 — 검사할 수 없다.
 */
export function describeReleaseNoteGap(note) {
  if (note === undefined || note.items.length === 0) {
    return '노트가 없습니다. 릴리스 노트를 먼저 작성해주세요.'
  }
  if (note.highlights === undefined || note.highlights.length === 0) {
    return '노트에 highlights 가 없습니다. 업데이트 모달이 받기 전에 보여줄 핵심 목록 3~4줄을 먼저 작성해주세요.'
  }
  return null
}
